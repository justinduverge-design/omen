# Omen Sprints Completed

Done-item evidence with run IDs, commit SHAs, PR numbers, and test counts. Pulled out of `current_sprint.md` on 2026-06-27 so the active queue stays light. `decision_log.md` holds the why-and-doctrine; this file holds the per-item ack.

Vendor-agnostic lane labels (lanes are work areas, not agent assignments): Backend, Frontend, Ops/Justin, Verify, Decisions.

> **How to read this file:** Each section mirrors `current_sprint.md`. New rows append to the bottom of their section as items close. When a phase fully closes, rotate the whole completed section into `Archive/sprint-completed-<YYYY-MM-DD>.md` and start a fresh section here.

---

## Reconciled completion ledger — 2026-07-28

This section is the internal source-of-record repair after the GitHub Actions billing hold exposed documentation drift. “Merged” means the verified commit is on `main`; it does **not** mean deployed, live, store-verified, or real-provider-verified unless the linked evidence says so.

| Area | Completed work | Verified evidence | Verification posture |
| --- | --- | --- | --- |
| M0-BE | Safe provider-state API, Sleeper request-id replay, and verified Yahoo native OAuth return | PRs #189, #190, #191; `src/routes/platforms.js`, `src/routes/yahoo.js`, and focused route tests on `main` | Merged; real-device OAuth remains a separate human/device gate. |
| M0-BE-3 record repair | Yahoo mobile-return source, research, security evidence, and living-contract reconciliation | `Blueprints/handoffs/2026-07-25-m0-be-3-yahoo-return-reconciliation.md`; focused 9/9 and full backend 422/422 on 2026-07-28 | Local evidence reconciled; provider-console and real-device proof remain deliberately unclaimed. |
| M4 native | Omen decision destination | PR #210; `Blueprints/handoffs/2026-07-26-m4-omen-screen.md` | Android evidence recorded; iOS unsigned CI deferred while Actions is unavailable. |
| B2-D | Sleeper projection mapping correctness fix | PR #214; local `npm test` 417/417 at the reconciliation baseline | Merged; no deploy claim. |
| Trade | Honest live-or-unavailable `GET /api/trade/pulse` contract | PR #197; `test/tradeRoute.test.js` | Merged; unavailable stays empty and explicitly non-mock until live ADP exists. |
| AI cost control | D2 `AI_PROVIDER=local|cloud` with a $0 cloud cap | PRs #225/#226; `Blueprints/handoffs/2026-07-27-d2-ai-provider-zero-cap.md` | Merged; no cloud credential, spend, deploy, or live-provider claim. |
| Native support | Help + Support contract, founder approval record, and SwiftUI/Compose implementation | PRs #227/#228/#229; `Blueprints/handoffs/2026-07-27-m4-help-support-implementation.md` | Android compile/scanner evidence passed; iOS/device accessibility evidence is deferred, not claimed. |
| Operating record | Inbox/Done-ledger reconciliation and degraded-verification doctrine | PR #221; `Blueprints/definition-of-done.md` | Merged; local equivalents must be recorded, and unavailable CI is marked `DEFERRED-CI`. |
| This sprint-history repair | D2 completion status and the missing Done/decision records | PR #230 | Merged; documentation-only, no runtime or deployment change. |
| Security hardening landing | OAuth telemetry redaction, deny-by-default browser permissions, and review-only waitlist RLS source | PR #232 merged as `5fdb2f3`; `Blueprints/handoffs/2026-07-28-security-hardening-landing.md`; local `npm test` 422/422, frontend build, moderate audit 0 | Merged; SQL was not applied, and no deploy/live claim is made. |
| B2-D-2 Yahoo waiver fallback | Selected-context, Yahoo-only availability fallback; no OUT/IR-like starter means no Yahoo pool request; unavailable/empty data returns no advice | PR #236 merged as `ffa8999`; PR #211 closed as superseded; handoff + security evidence; RED/green, focused service/route 20/20, local `npm test` 426/426, frontend build, moderate audit 0, and `git diff --check` | Merged; Actions is substituted during the billing hold. Fixture-verified only: Yahoo API reapproval is still required for real-account proof. No deploy/live claim. |
| B2-D Sleeper waiver + selector recovery | Sleeper available-pool/three-state waiver behavior and deterministic choice among scoreable candidates; preserves Yahoo’s availability-only fallback when no scoreable candidate exists | PR #215 plus recoveries #238/#239/#240; focused selector/service/route 55/55, local `npm test` 469/469, frontend build, moderate audit 0, and `git diff --check`. On 2026-07-29, public read-only S3 script against founder-provided drafted league `1387633793615036416` found 120 rostered players, a 3,174-player pool, 415 projected players, nulls last, and 0 rostered-player leaks. | Merged; GitHub Actions unavailable, so local evidence is substituted. Sleeper roster subtraction is now real-provider proven. No deployment claim. |
| Security / store / ESPN evidence recovery | Restored security handoff/review, store-review preparation notes, and ESPN E0 feasibility verdict without replaying stale branch code | PRs #241/#242/#243; original #222/#223/#224 closed as superseded; `git diff --check` on each recovery | Merged documentation/evidence. No Supabase application, store submission, credential handling, provider request, deploy, or real-provider claim. |

**Still active / not completed:** ESPN E1 implementation and its drafted-league roster-subtraction proof, Discord/passkey auth work (#198, iOS verification deferred), real-device/provider QA, and the Actions restoration sweep. These remain distinct from the completed local merge/evidence work above.

---

## Planning-pipeline cutover — migrated dispositions (2026-07-30)

The 2026-07-30 planning-pipeline cutover reconciled 35 task dispositions against `main` and the GitHub PR record. The 22 `CLOSED` records below moved out of `Direction/current_sprint.md`, which now carries only active work. The remaining 13 stayed active as `READY`.

Evidence is per-task and exact. Broad ranges are not used: the prior `#125–#139` range was wrong — it swept in #126 (an unrelated Jules docs gate), #132 (closed, and itself task A2), and #137 (closed, superseded by #138).

### CLOSED / COMPLETED (18)

| Task key | Title | Closure | Closure date | Exact evidence | Successor |
| :--- | :--- | :--- | :--- | :--- | :--- |
| M1P-Next-1 | PlatformBadge foundation | COMPLETED | 2026-07-21 | PR #174 `79fea1d`; PR #176 `a9376ff` (platform legibility tokens + fill-on-platform) | — |
| M1P-Next-2 | ConfirmationDialog foundation | COMPLETED | 2026-07-21 | PR #175 `604c108` | — |
| M1P-P4 | Dual-platform gallery and primitive enforcement | COMPLETED | 2026-07-21 | PR #179 `8ee8f68`; `Blueprints/handoffs/2026-07-21-m1p-p4-gallery-enforcement.md` | **M4-Auth** — discharged the `OmenAndroidApp.kt` allowlist-retirement residual |
| M1P-P3 | Product compositions (3 batches) | COMPLETED | 2026-07-23 | PR #182 `47da861` (Batch 1 metric primitives); PR #183 `9a497b8` (Batch 2 connection primitives); PR #184 `6831d20` (Batch 3 DecisionBrief shell, titled "closes P3"); handoffs `2026-07-22-m1p-p3-connection-primitives.md`, `2026-07-22-m1p-p3-decision-brief-shell.md` | — |
| M0-BE-0 | Native backend shared contract and acceptance matrix | COMPLETED | 2026-07-23 | `63980b5` authored `Blueprints/specs/mobile/omen-native-backend-state-contract-v1.md` including its §Acceptance Matrix covering F2 and M0-BE-1/2/3; dependents PR #189 `c9009e1`, PR #190 `9f8a7c9`, PR #191 `66e39c3` | — |
| M4-Auth | Omen-primitive-native auth surfaces (allowlist retirement) | COMPLETED | 2026-07-23 | PR #193; `Blueprints/handoffs/2026-07-23-m4-auth-primitive-retirement.md`; `PrimitiveEnforcementTest.ALLOWLISTED_FILES` now empty | — |
| M4-Omen-Screen | Omen destination owning the full DecisionBrief | COMPLETED | 2026-07-26 | PR #210 `6c2f9ae`; `Blueprints/handoffs/2026-07-26-m4-omen-screen.md` | — |
| M4-Help-Support-Contract | Define native contextual Help + Support | COMPLETED | 2026-07-27 | PR #227; PR #228; `Blueprints/specs/mobile/m4-help-support-v1.md`; Figma nodes `61:2`, `63:2`, `63:26` approved by Justin 2026-07-27 | **M4-Help-Support-Implementation** (active) |
| D2 | `AI_PROVIDER=local\|cloud` control with $0 cap | COMPLETED | 2026-07-27 | PR #225 `3019597`; PR #226 `e6f455f`; `Blueprints/handoffs/2026-07-27-d2-ai-provider-zero-cap.md`. Done-when met in full; the unclaimed deployment/live-provider proof falls outside this task's scope and outside its "do not touch: paid API credentials or production env" boundary | — |
| F2 | Resolve `ready` vs `pending_live_engine` documentation conflict | COMPLETED | 2026-07-22 | PR #181 `95b2606`; `Blueprints/handoffs/2026-07-22-f2-status-truth.md`; runtime authority `src/services/omenReadiness.js`; contract §F2 in `omen-native-backend-state-contract-v1.md` | — |
| GROOM-1 | Operational repo/deploy rename approval | COMPLETED | 2026-06 | Deploy runs `27994773332` and `27995488318`; `Blueprints/specs/omen-operational-rename-cutover.md`; repo and KVM1 deploy path are Omen | — |
| GROOM-2 | Orphaned `src/omen_gdpr.js` cleanup | COMPLETED | 2026-07-14 | PR #119 `449e9ff` | — |
| GROOM-3 | Account subscription-card removal and Stripe integration removal | COMPLETED | 2026-07-12 | PR #117 `6cb72ec`; PR #118 `c1de0c9` | — |
| GROOM-4 | Phase 4.20b and 4.20d | COMPLETED | 2026-07-12 → 2026-07-14 | PR #121 `44703a3` (4.20b public legal/support pages); PR #115 `28d4a9d` (4.20d store metadata / gambling-copy audit) | — |
| GROOM-5 | ESPN mobile onboarding, public setup guide, and walkthrough | COMPLETED | 2026-07-15 | PR #122 `cc15d15`; PR #123 `1f462a6` | **F4** — production verification remains active |
| GROOM-6 | Phase 4.21 transparent lockup swap | COMPLETED | 2026-07-26 | `2c48bbf` (wordmark refit + light-surface lockup) and `ca96559` (shield → approved Omen B2 mark), both landed via PR #199 `9ecd562`. **Evidence corrected:** the prior record cited PR #120, which is `28d1b16` "remove duplicate logo link class" — one file, one deletion — and never carried this change | — |
| GROOM-7a | Completed web UI primitive work | COMPLETED | 2026-07-16 → 2026-07-17 | PR #125 `2b3897f` Button; #127 `14a3ab7` Input+Textarea; #128 `5ead941` Badge+Chip; #129 `9afa46a` PlatformBadge; #130 `dc4788b` PlatformConnectionCard; #131 `0eaaa09` Empty/Error/Loading states; #133 `0ac9a9c` Tooltip; #134 `3002394` SegmentedControl+TabNav+RadioCardGroup; #135 `a7be1f0` PageHero; #136 `08245bd` MetricStrip; #138 `333baa6` PlayerRow+PlayerChip (PR #137 closed, superseded by #138) | — |
| GROOM-7b | Trade Analyzer migration | COMPLETED | 2026-07-18 | PR #139 `d9beda1` | — |

### CLOSED / DESCOPED (4)

| Task key | Title | Closure | Closure date | Exact evidence | Reason |
| :--- | :--- | :--- | :--- | :--- | :--- |
| A1 | Review and disposition PR #140: SVG logo masters | DESCOPED | 2026-07-30 | PR #140 **CLOSED, never merged** | The PR it existed to review was closed without merging, so the review task has no subject. |
| A2 | Decide PR #132: Master Design System Blueprint v1 | DESCOPED | 2026-07-30 | PR #132 **CLOSED, never merged** | The PR it existed to decide was closed without merging, so the decision task has no subject. |
| GROOM-8 | Per-team design documents, team-colorway stubs, and chant implementation | DESCOPED | 2026-07-12 | PR #114 `616a840` removed team-based runtime theming | Team-based runtime theming was removed; the active North Star treats team skins as future-only. |
| GROOM-9 | Paid-launch and Omen Pro wording | DESCOPED | 2026-07-12 | Stripe removal PR #117 `6cb72ec`, PR #118 `c1de0c9` | Omen is free indefinitely; paid-launch wording has no referent. |

**Reconciliation:** 13 active (`READY`) + 22 `CLOSED` = **35 dispositions**. `IN_PROGRESS` 0 and `VERIFIED` 0 — no task held a valid active `Claim:`, and no merged-but-incomplete task met its own `Done when:`. The two items surfaced during reconciliation (ESPN waiver-pool implementation, Actions-restoration sweep) are **excluded** from these totals and are held in `Direction/agent_inbox.md` under "Planning intake — pending planning-pass".

---

## Completed Since Last Sprint (legacy bucket, pre-2026-06-12)

- Authenticated smoke 13/13 (2026-06-04): Stripe prices, 401 guards, favorite-team hydration, preference patch+restore, Omen feedback, Move History, Sleeper standings. (Closed old Next #2–#4.)
- Full `/ui-ux-pro-max` audit: all 15 routed pages + shared components. (Closed old Next #6.)
- The Ledger naming finalized (`/ledger`); `Standings.jsx` vs `LeagueStandings.jsx` separation; `Omen.jsx` dev-only at `/dev/omen`.
- Backend polish: `GET /api/version`, `OMEN_TIER2_CLEANUP=1`, `Blueprints/api-routes.md`, standardized League Standings error envelopes.
- SPA cache headers fixed with regression test (`test/spaCache.test.js`).
- Stripe webhook hardening + recovery follow-up written locally (260/260). Code complete; **not deployed** at the time of writing — see Ops/Justin row below for the deployed close-out.
- Tuesday scoring provider decision closed: first launch scoring should use `nflverse-data` weekly player stats, not the current unvalidated Sportradar cron path. Cron stays disabled until the nflverse adapter, scoreable move shape, tests, and dry-run validation are complete.
- Tuesday scoring nflverse adapter completed locally: `fetchNFLScores()` now uses nflverse weekly stats, `getPlayerActualPoints()` exists, `SPORTRADAR_API_KEY` is no longer required by `REQUIRED_SCORING_ENV`, and `OMEN_CRON_DRY_RUN=true` avoids Supabase writes. Fixture tests passed in `npm test` 281/281.

## Ops / Justin (gated — agent prepares, Justin executes)

- [x] **OP2 — Seed Omen GHCR images without changing live deploy identity.** Completed in deploy run `27994773332`; Omen images seeded while the old path still served production.
- [x] **OP3 — Prepare KVM1 `/opt/omen` target and `omen-deploy` runner label.** Completed in cutover run `27995488318`; env file copied without printing secrets and `omen-deploy` label added while retaining `corvus-deploy` for rollback.
- [x] **OP4 — Cut live Docker Compose identity from Corvus to Omen.** Completed in cutover run `27995488318`; `omen_api` and `omen_cron` are active, public health/ready pass, and rollback to `/opt/corvus` remains available.
- [x] **OP5 — Decide and execute the repo/folder rename after deploy identity is stable.** GitHub repo renamed to `justinduverge-design/omen`; local target path is `slops-saloon/omen`; active L0/L1/L2 routing docs and git remotes point to Omen targets. Spec: `Blueprints/specs/omen-operational-rename-cutover.md`.
- [x] Deploy the Stripe webhook recovery follow-up; resend the failed `customer.subscription.created` event; confirm `200`.
- [x] Hostinger KVM1 public hostname confirmed: `slopssaloon.com` with `www.slopssaloon.com`.
- [x] Hostinger KVM1 secrets backup target handled by Justin; real `.env.production` stays off git.
- [x] Hostinger KVM1 launch scoring setting decided: `OMEN_CRON_SCORING_ENABLED=false`.
- [x] Hostinger KVM1 GHCR pull access confirmed; images pulled from GHCR.
- [x] Hostinger KVM1 private LLM link confirmed over Tailscale to KVM2 Ollama.
- [x] Hostinger KVM1 DNS/HTTPS cutover complete; root and `www` health/ready pass over HTTPS.
- [x] Browser smoke passed in Chrome and Firefox.
- [x] Load testing for `POST /api/omen/mvp-move`, `POST /api/trade/compare`, `GET /api/dashboard/summary` via `scripts/load-omen-routes.js`. Local authenticated 10-iteration run passed 2026-06-05: Trade Analyzer p95 34ms ✓, dashboard summary p95 633ms ✓, Omen p95 4999ms ⚠ (functional, not blocked — latency concern logged; optimization deferred to post-launch tech debt).
- [x] Stripe/payment surfaces decided (2026-06-08): keep the plumbing + guard the server via `OMEN_BILLING_ENABLED` kill-switch (gate, don't delete). Logged in `decision_log.md`. Justin canceled his own (only) subscription in Stripe 2026-06-08; ungated webhook reconciles `is_subscribed`.

## Backend — Phase 1 (Spines)

- [x] **Phase 1.1 — CI/CD retarget to KVM1 (2026-06-12; operating mode updated 2026-06-17; Omen path updated 2026-06-23).** `.github/workflows/deploy.yml` is the primary path: `quality`/`build` run on GitHub-hosted runners, then `deploy` runs locally on KVM1's self-hosted runner (`corvus-kvm1-deploy`, label `omen-deploy`) and uses Docker Compose from `/opt/omen/deploy/hostinger`. `.github/workflows/deploy-kvm1-tailscale-fallback.yml` is the manual backup path: a GitHub-hosted runner joins Tailscale with `tag:github`, SSHes to KVM1 over MagicDNS/private networking, and runs the same Omen pull/restart commands. Do not reintroduce Infisical, `git pull`, or public SSH-from-GitHub as the default without a new decision-log entry.
- [x] **Phase 1.2 — Sentry SaaS free tier wired (backend half) (2026-06-13).** Shipped via PR #33 (commit `cc14e79`). `@sentry/node@^8.55.2` added; `Sentry.init()` runs as first require in `src/server.js` + `src/omen_tuesday_cron.js` via shared `src/middleware/sentry.js`. `Sentry.setupExpressErrorHandler(app)` wired before existing error handler. Cron has `uncaughtException` + `unhandledRejection` capture + `flushSentry()` on exit. `beforeSend`/`beforeBreadcrumb` scrubbers enforce `Blueprints/security-privacy.md`: ESPN-credential routes (`/api/platforms/espn/connect`, `/api/auth/espn/connect`, `/api/espn/roster`) drop event entirely; sensitive headers (`cookie`/`set-cookie`/`authorization`/`x-api-key`/`token`/`secret`) stripped; body keys (`password`/`cookie`/`token`/`secret`/`swid`/`espn_s2`/`vault`) replaced with `[scrubbed]`; URL query string scrubbed same way; stack traces truncated to 20 frames; `__skipBodyLog` honored. `SENTRY_DSN` blank in `.env.example` (no-op locally); row added to `deploy/hostinger/ENV-INVENTORY.md`. Smoke test at `test/sentryBoot.test.js` (3 tests). `npm test` 291/291. No new audit advisories (pre-existing `hono` only).
- [x] **Phase 1.4 — ADP schema + per-league scoring config tables (2026-06-12).** Review-only SQL written at `sql/2026-06-12_phase1_adp_scoring_schema_review.sql` with focused schema tests at `test/phase1SchemaReviewSql.test.js`. ADP tables = service-role only; league scoring config = service-role writable + authenticated self-readable via RLS. No Supabase apply, no production action.

## Backend — Phase 2

- [x] **Phase 2.5 — Proprietary ADP weighting service (2026-06-18).** Added a deterministic consensus board to `src/services/adp.js`: normalized cross-provider player matching, weighted-average ADP score with contribution evidence, equal `1:1:1` defaults, and per-league overrides at `default_scoring_rules.adp_source_weights`. Missing sources reweight safely; invalid rows are excluded; all-zero overrides restore defaults. `GET /api/draft-assistant/adp` exposes additive `weighting` + `weighted_players` fields while preserving raw sources and mock labels. No DB/service-role path changed. Focused tests 10/10; full backend tests 297/297; `slops-code-review` merge verdict with no P0/P1. Implementation commit `d04c535`. Done docs: feature + recommendation.
- [x] **Phase 2.6 — Math engine parameterized (2026-06-19).** `src/services/optimizer.js` and `src/services/tradeValue.js` now accept the Phase 2.5 whole-row `scoringConfig` convention without changing existing callers or default outputs. Optimizer config supports lineup/waiver thresholds and waiver limit; trade config supports scoring format, position baselines/weights, neutral band, and scarcity-signal weight. Explicit legacy options win over config. Focused engine/caller tests 30/30; full backend 307/307; `slops-code-review` merge verdict with no P0/P1. Implementation `798ad4e`; squash-merged PR #49 as `93e1a71`; KVM1 deploy run `27834697621` green; production health/ready/homepage smoke passed. Done docs: feature + recommendation + release.
- [x] **Phase 2.7 — Demo Mode backend (2026-06-19).** Added public, rate-limited `GET /api/demo` (`omen-demo.v1`) with a deterministic populated normalized roster and optimizer-backed Omen envelope. Demo identity is explicit (`mode:"demo"`, `is_demo:true`, `is_live:false`, `is_mock:false`); fixture telemetry is marked ineligible for analytics and LLM training; no auth, Supabase, provider, or LLM path runs. Focused tests 5/5; full backend 312/312; audit 0 vulnerabilities; `slops-code-review` merge verdict with no P0/P1. Implementation `e966a0a`; squash-merged PR #51 as `cccc857`; KVM1 deploy run `27842680052` green; independent production canary 24/24 with p95 242 ms. Done docs: feature + recommendation + release.
- [x] **Phase 2.8 — Sleeper live draft tracking (2026-06-19, ahead of Aug 15 deadline).** Canonical authenticated routes remain `GET /api/sleeper/draft?leagueId=`, `GET /api/sleeper/draft/:draftId`, and `GET /api/sleeper/draft/:draftId/state?since=`. Post-deploy audit hardening adds the approved two-second active mode and 30-second low mode, validates every league/draft against the user's active Sleeper connection, replaces raw `draft_order`/`picked_by` user IDs with `user_draft_slot`/`is_user_pick`, uses safe error envelopes, and adds a 900-call/minute process budget alongside Redis caching and single-flight dedupe. Snake/linear drafts retain on-the-clock math; auction remains explicit with `on_the_clock:null`. Focused tests 40/40; full backend 352/352; audit 0; `slops-code-review` merge verdict with no P0/P1. Original release: PR #53 / `8c6c3bd`; hardening: PR #56 / `b1ec05d`; KVM1 run `27853988393` passed quality, builds, restart, health smoke, and log tail. Independent production health/readiness/`www` smoke passed and all three routes returned `401 Missing bearer token` without auth. Justin explicitly accepted the unresolved Sleeper terms risk; this is not legal clearance. Done docs: feature + recommendation + security + release.
- [x] **Phase 2.10 — Trade share hash routes (2026-06-25, PR #68, merge `bfefbf1`, deploy run `28192331330`).** Added public `POST /api/trade/share` + `GET /api/trade/share/:hash` (`trade-share.v1`) using `crypto.randomUUID()` hashes, bounded Trade Analyzer input, server-recomputed trade snapshots, Redis-backed 30-day TTL in production, memory-only test/dev fallback, and fail-closed `503 trade_share_storage_unavailable` when production Redis is absent. No auth, Supabase, provider, ESPN cookie, LLM, package, SQL, env, or frontend route change. Focused share tests 4/4; existing trade+share route tests 10/10; full backend `npm test` 378/378. KVM1 workflow passed quality, image build/push, restart, health smoke, and read-only canary. Done docs: feature + recommendation + security + release.
- [x] **Phase 2.17 — Platform `lastResult` fields for post-win pulse (2026-06-27).** Additive `lastResult: 'W'|'L'|null`, `lastGameId`, `lastGameKickoff` on `/api/dashboard/summary.platforms.{sleeper,yahoo,espn}`; fail-closed nulls on ties, errors, or unavailable data; ESPN cookies stay server-only (no values, vault ids, or auth headers returned). `lastGameKickoff` contract-stable but null in v1 — provider fantasy-matchup resources don't expose real kickoff timestamps. Unblocks Frontend Phase 1.5d (post-win pulse animation). Receipt: `Blueprints/done/LEDGER.md` row `Phase 2.17 — Platform lastResult fields`. PR #69, feat `8331b72`, docs `c0dc5e7`, merge `d86de57`, deploy auto-triggered on push-to-main (KVM1 self-hosted runner; fill run # from GitHub Actions). Done docs: feature + security.

## Backend — Phase 3

- [x] **Phase 3.12 — Tailscale -> KVM2 Gemma 4-E4B bridge (2026-06-27; deployed 2026-06-28 UTC).** Optional local LLM bridge now accepts only private/Tailscale-style `LLM_BASE_URL` hosts and fails closed for public/invalid values without leaking URL, hostname, or port. `/api/ready` and `/api/platform-status` expose additive safe bridge status only. Default local model target is `gemma4:e2b-q4_0`; `LLM_MODEL` still overrides, and production currently reports `gemma3:4b` through env. No secrets, env file edits, package files, Supabase, Stripe, or production config changed. Evidence: implementation `fbbf0c5`; PR #70; merge `a13160b`; deploy run `28306784898`; independent canary green; focused tests 51/51; full `npm test` 388/388; root audits 0; primary frontend build clean with existing Vite chunk warning; legacy client build clean; research/security/AI review in `Direction/reviews/`; handoff `Blueprints/handoffs/2026-06-27-phase3-12-kvm2-gemma-bridge-handoff.md`. Done docs: feature + security + release.
- [x] **Phase 3.13 — Token-constrained prompts (2026-06-28).** `src/services/llm.js` now applies a shared narration instruction of <=50 words and <=2 sentences to trade, start/sit, and Omen MVP Move LLM explanation calls, caps narration generation at 90 tokens, and rejects over-limit Omen strict-JSON explanations so deterministic copy remains. No route, package, env, SQL, provider, auth, payment, frontend, or deploy behavior changed. Evidence: RED `node --test test/llmService.test.js` failed on missing prompt/token/parser limits; GREEN focused LLM 2/2; focused Omen/agent 58/58; full `npm test` 387/387; `npm audit --audit-level=moderate` 0; AI review `Direction/reviews/2026-06-28-phase3-13-token-constrained-prompts-ai-integration.md`; security evidence `Direction/reviews/2026-06-28-phase3-13-token-constrained-prompts-security-evidence.md`; code review `Blueprints/audits/2026-06-28-phase3-13-token-constrained-prompts-code-review.md`; handoff `Blueprints/handoffs/2026-06-28-phase3-13-token-constrained-prompts-handoff.md`. Done docs: feature + recommendation.

## Backend — Pre-pivot completions (evidence)

- [x] Billing kill-switch implemented 2026-06-08: `OMEN_BILLING_ENABLED` (default false) gates `/prices` + `/checkout` + `/portal` (`403 billing_disabled`); `requireSubscription` is a pass-through when off (auth still required); webhook untouched. `npm test` 290/290.
- [x] WS1 Hostinger KVM1 prep — draft compose, Nginx, env inventory, deploy notes, and local deploy ignore file under `deploy/hostinger/`.
- [x] Trade Analyzer Phase 2 — backend player-search endpoint (Request 20).
- [x] Webhook persists `trial_ends_at` / `current_period_end` — confirmed working 2026-06-08.

## Frontend — Phase 1

- [x] **Phase 1.2 — Sentry SaaS free tier wired (frontend half) (2026-06-13).** Shipped via `fa35c76` on `claude/wonderful-jepsen-afb4fd`. `@sentry/react@^8.55.2` added to `frontend/package.json` (version parity with backend). `initSentry()` runs in `frontend/src/main.jsx` before `createRoot` via new `frontend/src/lib/sentry.js`. `<App />` wrapped in `Sentry.ErrorBoundary` with Omen-themed `CrashFallback` (≥44px reload button — meets Phase 1.3 touch-target discipline preemptively). `beforeSend`/`beforeBreadcrumb` scrubbers mirror the backend (`src/middleware/sentry.js`): ESPN-credential URL patterns (`/api/platforms/espn/connect`, `/api/auth/espn/connect`, `/api/espn/roster`) drop event entirely (also checked against `event.transaction`); sensitive headers, body keys, and URL query strings scrubbed with identical patterns to backend; stack traces truncated to last 20 frames; `__skipBodyLog` honored; fetch/xhr breadcrumbs for ESPN URLs dropped. `VITE_SENTRY_DSN` blank in `.env.example` (no-op locally); `VITE_COMMIT_SHA` added for release tagging; both rows in `deploy/hostinger/ENV-INVENTORY.md`. Source-map upload deferred to post-launch per spec. `npm --prefix frontend run build` clean (456 kB bundle, 1.67s). No frontend test runner configured; scrubber coverage is indirect via backend smoke tests.
- [x] **Phase 1.3 — Page-system spec (2026-06-15).** Authored `Blueprints/specs/page-system.md` v1 from QA Part 2 markdown + 17 annotated screenshots (light + dark mobile Safari). Per-route typography role, accent role, palette, copy anchor, approved/disapproved patterns, light/dark parity rules. Verified against `SKILL_ROUTING.md@2026-06-15`. Guardrails used: `design:design-system` + `ui-ux-pro-max`. Every Phase 1.4–1.12 item is "Blocked by Phase 1.3" and must reference a Page System Table row + Component Rule before shipping.
- [x] **Phase 1.4 — Font system propagation fix (2026-06-15 source; 2026-06-16 actually-in-prod).** Replaced the old Cormorant direction with Alegreya Sans for headings/card titles/UI and Alegreya for body text per Justin's brand decision. Updated the brand/design/page specs and applied the font propagation sweep across flagged pages/components. Fixed TE1 "1"-vs-"TE" parity by moving strategy titles to the heading/UI font. Fixed light-mode olive-gold "Continue with Email" token misuse. Source PR: #38 (`Codex/phase1 4 font system`, merged 2026-06-16 20:25 UTC with `ui-quality` red). Lockfile hotfix PR: #39 (`fix(ci): resync root package-lock.json with package.json`, merged 2026-06-16 ~21:07 UTC, `quality` ✅ `build` ✅). Production live after manual `Deploy to Hostinger KVM1` workflow_dispatch ~21:30 UTC (stale `KVM1_HOST` secret re-set by Justin). Two carry-forward rules added to `decision_log.md`: clean-clone verification required for any Phase touching package files; never merge with the gating CI check red. Full incident: `Blueprints/handoffs/2026-06-16-phase1-4-lockfile-resync-handoff.md`. Done docs: page + design.
- [x] **Phase 1.5 PR1 — Team-theming system core (merged 2026-06-17 via PR #41).** Token model (`--color-team-primary/secondary/accent/surface/surface-card` in `frontend/src/index.css`) + runtime template resolver (`frontend/src/lib/teamTemplate.js` — 6 templates: Deep & Brand / Two-Tone Royal / Hot Brand / Aqua & Cool / Earth / Bred) + `TeamTheme.jsx` → `Appearance.jsx` rename with 3-button Mode picker (System / Team / Omen, exclusive; Team always dark; System default for new users) + page sweep on the 8 "Accent active" pages (Account, Football, OmenPage, Ledger, Standings, TradeAnalyzer, DraftAssistant, Appearance) + voice extension to 6 surfaces (TeamTheme picker, NavDrawer label, Omen loading, Omen page subhead cry, Standings page subhead wardRoom, Dashboard header pill cultureTag) sourced from `frontend/src/data/nflTeams.js`. Saints get a primary↔secondary flip with extra gold weight. Falcons get Template 6 "Bred" (pure black canvas, varsity-red CTA, white hairline — Jordan 1 homage). 32-team contrast audit doc at `Blueprints/audits/2026-06-16-phase1-5-team-template-assignment.md`. Theme persists across signed-in sessions via localStorage. Merged to `main` by Justin in PR #41 (`claude/phase1-5-pr1-team-theming-core` → `main`, merge commit `209408e`), followed by a same-day fix-up `6970a3b` ("apply team palettes to app shell").
- [x] **Phase 1.5b — Onboarding "Pick your look" first step (2026-06-17; gaps closed 2026-06-18).** New step 0 in `Onboarding.jsx` (Welcome/Connect/Complete shifted to steps 1/2/3; `StepDots` now renders 3 dots, hidden on Complete). Extracted `ModePicker` + `TeamTile` (+ `MODE_OPTIONS`) out of `Appearance.jsx` into a new shared module `frontend/src/components/theme/AppearancePicker.jsx`; `Appearance.jsx` now imports from there with no behavior change. New `PickLookStep` reuses those exact components: 3-button Mode picker (System / Team / Omen) + marquee-8/expand-to-32 team grid, identical interaction pattern to `/account/appearance` (`setThemeMode`/`setThemeTeam`, silent PATCH to `/api/account/preferences`). "Continue →" advances to Welcome; "Skip — use Omen default" explicitly calls `setThemeMode('corvus')` (the named Omen-default mode) before advancing. `Blueprints/specs/page-system.md` `/onboarding` row updated: accent role is now "Accent active" on this one step only. `npm --prefix frontend run build` clean (461 kB bundle). **Gap closure 2026-06-18:** light+dark screenshots captured against production via Claude-in-Chrome MCP (AppearancePicker component verified at `/account/appearance` — onboarding redirects authenticated users to completion step, PickLookStep documented by component equivalence). `slops-ui-ux-audit` run inline — no P0 found (P1 noted: Onboarding CTA buttons use `color: '#0A0A0B'`, no `--color-text-on-accent` token). `Onboarding.jsx:256` error color `#B66A6A` → `var(--color-risk-high)` fixed in commit 9e3a58c. LEDGER row added.
- [x] **Phase 1.5c — Team voice extension into Ledger / Draft / Trade (2026-06-17; gaps closed 2026-06-18).** Applied the existing `useTheme()` + `useMemo` placement-guide pattern (already live in `Football.jsx`/`OmenPage.jsx`/`Standings.jsx`) to three more surfaces: `MoveHistory.jsx`'s `EmptyHistory()` → `wardRoom` line under "No moves yet."; `DraftAssistant.jsx` header → `cry` line under the intro paragraph; `TradeAnalyzer.jsx`'s `ResultPanel()` → `cultureTag` pill next to the "Result" eyebrow. No new copy — all three reuse existing `nflTeams.js` fields. Renders only when mode = Team and a team is selected. Build clean. **Gap closure 2026-06-18:** light+dark screenshots captured against production (ss_35349udgx Ledger dark, ss_78836a3gv Ledger light, ss_5406mmemw Trade dark, ss_52456s6z0 Trade light, ss_0514so4h0 Draft dark, ss_4918b8aov Draft light). `slops-ui-ux-audit` run inline — P0 found and fixed: `MoveHistory.jsx` off-palette hex values `#4ade80`/`#f87171` → `var(--color-risk-low)`/`var(--color-risk-high)` with `color-mix()` alpha variants (commit 9e3a58c). No remaining P0. LEDGER row added.
- [x] **Phase 1.5e — 32-team identity audit (full scope, 2026-06-20).** Scope corrected mid-day per Justin: original ask was about per-team fan-perceived identity (some teams' identity is inherently light) and cultural-anchor accents (Jordan colorways, Vice, Toro Bravo, etc.), not user toggle. **Full-32 audit shipped** at `Blueprints/audits/2026-06-20-phase1-5e-32-team-identity-audit.md` (first-pass `2026-06-20-phase1-5e-32-team-visual-qa.md` marked superseded). Each of 32 teams has a fan-identity sentence, cultural anchor where applicable, axis recommendation, accent recommendation. **6 teams flip to LIGHT axis** (MIA Vice, IND white-helmet, LAC beach, DAL silver, CAR Carolina-blue, ARI Toro Bravo). **NYG flipped to primary royal-blue accent** (contra first-pass), resolving the HOU/NYG/ATL hue-collision and matching "Big Blue" identity. Other 8 secondary-scheme teams confirmed on secondary with cultural-anchor justifications. **7 defects filed** (1.5e-defect-1 LV silver; -2 Bred Falcons; -3 hue-collision resolves-by-design; -4 PHI cyan; -5 TB pewter surface; -6 SF coral-lift; -7 escalated text-on-accent token). **Phase 1.5f re-scoped** to 11-point spike (per-team `surfaceAxis` field, two-axis `SURFACE_RECIPES`, theme-aware `getTeamTemplate()`, sat-aware `textSafe()`, Bred bypass, per-team accent overrides, TB surface re-derivation, `--color-text-on-accent` token, two-axis WCAG re-sweep, Mode picker copy, spec update). **New reusable methodology doc** at `Brand/entity-identity-theming.md` (6 principles + 10-step audit process + 6 anti-patterns) so the next SLOPS product inherits this as a starting point.
- [x] **Phase 1.5f — Theme-aware team palettes + cultural-anchor citations (shipped 2026-06-21, single-PR spike).** All 12 audit items landed: per-team `surfaceAxis` in `nflTeams.js` (6 light: MIA / IND / LAC / DAL / CAR / ARI); two-axis `SURFACE_RECIPES` in `teamTemplate.js`; theme-aware `getTeamTemplate()` returning `{axis, accentBg, textOnAccent, ...}`; sat-aware `textSafe()` with hue-clamp + S-decay (defect 1.5e-defect-1 LV silver fix); Bred template-6 bypass of `textSafe` (defect 1.5e-defect-2 ATL varsity-red preserved); NYG accent flipped to primary royal-blue `#0B2265` (defect 1.5e-defect-3 hue-collision resolved); per-team `accentLifted` overrides for HOU `#D04250` / PHI `#5DAB9F` / SF `#D72020` (defects 1.5e-defect-2 carryover, -4, -6); TB `surfaceFrom: 'secondary'` for pewter not blood (defect 1.5e-defect-5); `--color-text-on-accent` token added to `index.css` (defect 1.5e-defect-7 from Phase 1.5b) + per-axis runtime override in `themeMode.js` + sweep of 7 hardcoded `#0A0A0B` literals in CTAs; `culturalAnchor: { name, year?, kind }` field on all 32 teams + `CulturalAnchorAttribution` component surfaced on `/account/appearance` tile detail; Mode picker copy "Light or dark to match your team's true colors."; `page-system.md` updated. **Bonus:** `readableOn()` switched from luminance heuristic to actual contrast comparison (fixed 8 of 11 initial sweep failures without other changes). New programmatic audit at `Blueprints/audits/2026-06-21-phase1-5f-two-axis-wcag-sweep.md` — 32 teams × 2 surfaces × 4 contrast cells; 0 unexpected failures; 3 baked-in known marginals (KC/DET CTA pass AA-large; ATL by-design). Build: `npm run build` clean (674.30 kB / 186.31 kB gzip; +5.89 kB / +1.85 kB gzip vs parent). Runtime spot-check via Chrome MCP confirmed all 12 spot-check teams render expected axis + accent + overrides.
- [x] **Phase 1.5h — Multi-color official palettes + special cultural variants (shipped 2026-06-21, single-PR; on top of 1.5f branch).** Justin doctrine 2026-06-21 retired the 1.5f single-accent model: no team page may be in stylistic dark mode, every team must surface its full official palette on every themed page. New `palettes` array shape on all 32 teams (Official + 30 Special variants), each with explicit `surfaceRole` + role-keyed colors (`primary/secondary/tertiary/neutral/mute/accent-pop`). Render pipeline rewrite: `teamTemplate.js` returns role-keyed bundle with derived `accent` (falls through to secondary when surface == primary so GB green-on-green CTAs stay visible), `themeMode.js applyTeamTokens` writes per-role CSS vars driving core Omen tokens. `/account/appearance` gets Official/Special toggle, N-swatch row with role labels, role-aware identity copy hierarchy. Cultural anchor extends from /appearance attribution line to all themed pages via `Footer`. Programmatic sweep rewritten for multi-palette WCAG. Specials use region/music/food/history names (NO Nike/Jordan trademarks): Stankonia, Calle Ocho, Paisley Park, 8 Mile, Wing Sauce, Lambeau Tundra, Gasparilla, etc. Audit + spec rewrite + LEDGER + decision_log all updated. Build clean (688.73 kB / 188.53 kB gzip). Runtime verified on authenticated `/account/appearance` via Chrome MCP.
- [x] **Phase 1.5g.0 — Per-team motif grammar spec (shipped 2026-06-22, doc-only).** Path A from kickoff plan-approval: author a richer template grammar before any code. `Blueprints/specs/team-motif-grammar.md` v1 landed with category-axis shape — three independent optional arrays on team root (`motifs`, `typeFlourishes`, `culturalMoments`), each with its own resolver, CSS-var namespace, and AAA gate. `Blueprints/specs/page-system.md` extended with a Motif / Moment posture column. v1 scope bound to 6 teams across three follow-up phases. Winner selected via 3-proposal × 3-lens synthesis (category-axis avg 5.67 vs Maximum-flexibility / Tier-Based 4.67 each; brand-voice 7 weighted strongest); critic verdict patch-then-ship, 10 patches applied. Decision log: `Direction/decision_log.md` 2026-06-22 entry.
- [x] **Phase 1.5g.1 — Motif schema + 4-team hairline ship (2026-06-23).** Main commits `4efa338` + `8e5d56b` add motif arrays for PIT, MIA, NO, and GB; `frontend/src/lib/motifs.js`; `assertCategoryShape()`; motif CSS vars/clear path in `themeMode.js`; `[data-motif-target]` selectors in `index.css`; and the motif-aware contrast sweep. All four motifs are static hairlines with `trademarkReview: 'self-assessed'` and `excludesOmenCard: true`; no logo-adjacent shapes ship. NO uses neutral cream for the hairline because the sweep caught the Mardi Gras purple special-surface collision. Deployed via Hostinger KVM1 run `28062382244` and independent canary passed health/ready/homepage/Demo/auth-boundary checks. Evidence: `test/teamMotifs.test.mjs` 3/3, `npm test` 356/356, frontend build clean, audit 0, contrast sweep 62 palettes / 0 unexpected failures, screenshots in `Solutions/reports/_screenshots/phase1-5g1/`, guardrails at `Blueprints/audits/2026-06-23-phase1-5g-code-review.md`, `Blueprints/audits/2026-06-23-phase1-5g-ui-ux-audit.md`, and `Blueprints/audits/2026-06-23-phase1-5g-rbac-risk-review.md`.
- [x] **Phase 1.5g.2 — TypeFlourish schema + NE small-caps eyebrow (2026-06-25, commit `440be1f`).** Mirrored the Motif system file-for-file. `typeFlourishes: []` array added to NE (`frontend/src/data/nflTeams.js`) — `ne-engraved-eyebrow`, Alegreya Sans 600 small-caps tracking 0.12em `font-feature-settings: "smcp" 1`, enabled in both Official and Special. New `frontend/src/lib/typeFlourishes.js` (`resolveTypeFlourishes(team, variant)` — filters by `enabledInVariant`, no trademark gate; typography isn't IP-sensitive the way motifs are). `teamTemplate.js` returns `typeFlourishes` sibling to `motifs`. `themeMode.js` adds `TYPE_FLOURISH_VARS` (6 vars), `applyTypeFlourishTokens()`, `clearTypeFlourishTokens()` wired into `clearTeamTokens()`. `index.css` adds `--type-flourish-*` defaults + `.type-flourish-eyebrow` utility (`font-family/weight/style/variant-caps/letter-spacing/font-feature-settings`). `Appearance.jsx`'s `cultureTag` pill ("Pats Nation") conditionally drops fake-caps `uppercase` for `type-flourish-eyebrow` true small-caps when `template.typeFlourishes.active` has an `eyebrow`-scope entry — NE only in v1, all other teams unchanged. **Pre-merge font-feature spike passed:** Playwright/Chromium screenshot against the served Google Fonts CSS2 Alegreya Sans confirms `font-feature-settings: "smcp" 1` renders visibly distinct glyphs from `text-transform: uppercase` (initial letter full-height, subsequent letters reduced-height caps vs. uniform full-height) — no self-host fallback needed. Grep gate added to `frontend/scripts/contrast-sweep.mjs` (`assertFontImportBaseline()`, runs pre-`runSweep()`): fails if `index.css` `@import`/`@font-face` line counts exceed baseline (2/0); re-run confirmed no false positive against the unchanged imports. `assertCategoryShape()` (already implemented ahead-of-need in 1.5g.1) needed no changes — its existing `assertTypeFlourishes()` already rejects italic-700 and non-Alegreya families. `test/teamTypeFlourishes.test.mjs` (`node:test`, matching repo convention) pins: NE shape validity, italic-700 rejection (typography lock regression), `index.css:1` import-line drift, themeMode exports, CSS utility presence — 4/4 pass; full suite 145/208 pass (unchanged 63 pre-existing failures from missing `node_modules` in this sandbox, +4 from this PR, 0 regressions). `npm --prefix frontend run build` blocked by the same missing `node_modules` (pre-existing sandbox condition, not installed per the install-approval gate); substituted `node --check` on every edited plain-JS file and a TypeScript-transpiler JSX parse check on `Appearance.jsx`, both clean.
- [x] **Phase 1.5g.3 — CulturalMoment schema + DET / NO / GB moments (2026-06-25, branch `claude/corvus-kickoff-2gz8am`).** Landed `culturalMoments: []` for DET (Thanksgiving Classic, `date-list` 2026-11-26 + 2027-11-25, scope app/account/ledger/standings/football), NO (Mardi Gras Week, `date-range` 02-08 → 02-25, scope `['app','account']`, `tertiary` tint @0.12 — resolves on the Mardi Gras Special palette where the roles exist), GB (Lambeau Tundra, `manual-flag` `localStorage['omen.theme.moments']['gb-lambeau-tundra']`, no weather claim). New `frontend/src/lib/culturalMoments.js` (fail-closed resolver, three activation rules, scope+tint policy, `?moment=` DEV override), `frontend/src/data/nflCalendar.js` (Justin-owned date-list), `frontend/src/lib/dataMode.js` (the route-level mock/live indicator — `window.__omenDataMode` with `__corvusDataMode` read-alias, per Justin), `frontend/src/lib/useActiveMoment.js`, `frontend/src/components/layout/MomentChrome.jsx` (sibling of `<main>`, eyebrow + mandatory mock badge). `themeMode.js` `MOMENT_VARS` + `applyMomentOverlay()`/`clearMomentTokens()`; `index.css` moment defaults + `[data-moment-active] [data-moment-target='page-surface']` color-mix tint + `.moment-eyebrow`. Footer appends moment citation (never replaces the anchor). `setDataMode` wired into Football/Standings/Ledger(via MoveHistory)/Account/Appearance only; every other route stays `null` → moment suppressed. /omen + /trade resolve to empty scope → zero moment chrome. Eyebrow-only routes (/account/appearance, /standings) drop the tint per page-system posture. `assertCulturalMoments` (shipped ahead in 1.5g.1) needed no change. Evidence: `test/teamCulturalMoments.test.mjs` 11/11; `test/teamMotifs` + `test/teamTypeFlourishes` 7/7 (no regression); sweep `Blueprints/audits/2026-06-25-phase1-5g3-moment-contrast-sweep.md` 62 palettes / **0 unexpected failures** (DET eyebrow 4.34/4.11 AA-large decorative; GB 7.23/8.16 AAA; NO eyebrow 10.34 AAA + post-tint body 15.13 AAA). Build substituted by `node --check` on all edited plain-JS (frontend `node_modules` absent — same sandbox condition as 1.5g.1/.2); Safari `color-mix` smoke + `slops-ux-copy`/`slops-code-review`/`slops-ui-ux-audit` deferred (see LEDGER + handoff). PR opened as draft for Justin.
- [x] **Phase 1.6 — Position chip palette + selected-state styling (shipped 2026-06-21 via PR #61, squash-merged `787ea53`).** Six new `--color-pos-*` tokens (RB/WR/QB/TE/DEF/K) added to all three CSS scope blocks in `frontend/src/index.css`; light + dark variants are deuteranopia + protanopia validated (token table + CB rationale in `Blueprints/specs/page-system.md` §"Position Chip Palette (Phase 1.6)"). New shared helper `frontend/src/lib/positionChip.js` returns `{borderColor, background, color}` via `color-mix()`; FLEX falls through to neutral surface. `DraftAssistant.jsx` `PositionBadge` rewritten on the helper (drops `emerald-400 / sky-400 / purple-400` inline Tailwind hex classes). Selected-state on Scoring Format chips ([DraftAssistant.jsx:380](frontend/src/pages/DraftAssistant.jsx:380)) and Position Needs chips ([DraftAssistant.jsx:431](frontend/src/pages/DraftAssistant.jsx:431)) now renders filled `--color-team-accent` + `--color-text-on-accent` (Phase 1.5h per-team override carries the contrast) with `aria-pressed` added; the "broken yellow-with-X" QA finding is resolved. `TradeAnalyzer.jsx` `BuyLowCard` position pill propagated to the same helper. Build clean (`npm --prefix frontend run build` → 689.04 kB / 188.68 kB gzip; +0.31 kB raw vs Phase 1.5h baseline). 8-shot Chrome-MCP QA sweep across `/draft` + `/trade` × {Omen dark, Omen light, Team KC, Team MIA} confirmed no P0/P1 — evidence IDs in the LEDGER row. `slops-code-review` verdict: **merge** with three P2 follow-ups (radio semantics → folded into Phase 1.13; Waiver Wire route activation → new Phase 2.18; sprint-board flip → this row).
- [x] **Phase 1.10A — UX copy options packet (2026-06-25).** Invoked `slops-ux-copy`; 3 options each for (a) `/omen` offseason empty state, (b) `/onboarding` "You're ready" success copy, and (c) the banned hero headline "Know the move before you make it." — found live in both `Landing.jsx` and `OmenLanding.jsx`, not just the one file this item named. Note: "Less guessing. Better moves." (floated above as a candidate) is retired per `Brand/brand-system.md` 2026-06-22 — not used. No source changes; Justin selects. Packet: `Blueprints/handoffs/2026-06-25-phase1-10a-ux-copy-options.md`.
- [x] **Phase 1.10B (partial) — Approved copy applied and deployed (2026-06-25).** Justin picked A1/B1/C1+C2 from the 1.10A packet. Shipped: `Landing.jsx` hero → "See the result before it happens."; `OmenLanding.jsx` hero → "See the move before the league does." (both files had the banned line, not just the one originally named); `Onboarding.jsx` success step → "You're set." / "League connected. Omen reads the matchup the moment your roster locks — your first call lands Tuesday." Commits `e21426c` + `5fe20c1` pushed to `main`; push auto-triggered `Deploy to Hostinger KVM1` run `28169065015` (quality/build/deploy all green). Independent post-deploy check: `https://slopssaloon.com/api/health` ok, `/api/ready` ready, homepage 200, and the live production JS bundle (`assets/index-DqpaXHbG.js`) confirmed to contain all three new strings with zero remaining "Know the move" anywhere. `/onboarding` success step not independently browser-rendered pre-deploy (gated behind auth + platform-connect) but its string is confirmed live in the production bundle. **Still open:** `/omen` offseason empty state copy — no backend status exists to wire it to yet; request filed in `Blueprints/handoffs/frontend-to-backend.md` (2026-06-25). **Resolved 2026-06-25:** `/draft` "Preview Mode" banner — checked whether the page is actually live (it isn't: frontend never sends `adp_players`, so the backend always returns fictional mock player names); decision is to **keep** the banner. See `decision_log.md`.
- [x] **Phase 1.11A — Demo Mode frontend fixtures (2026-06-25).** Added private dev-only fixtures for Omen visual testing (`?fixture=omen-roster`), Ledger previous results (`?fixture=previous-results`), and Draft Assistant mock draft (`?fixture=mock-draft`). All are `mode: "mock"`, `is_mock: true`, `is_demo: false`, `is_live: false`, labeled with `MockBanner`, and gated by `import.meta.env.DEV`; routed pages dynamically import the fixture payload so the production bundle does not ship the sample data. No public `/demo` or mock-draft route was added. Evidence: RED `node --test test/phase1_11aPrivateFixtures.test.mjs` failed on missing fixture module; GREEN focused fixture/nearby-contract tests 15/15; full `npm test` 374/374; `npm --prefix frontend run build` clean; `npm audit --audit-level=moderate` 0; `git diff --check` clean; production bundle grep found no private fixture labels/sample names; Playwright runtime smoke confirmed dev `/draft?fixture=mock-draft` renders fixture, production preview ignores it, and protected `/omen` + `/ledger` fixture URLs remain auth-gated. Release: PR #67 squash-merged to `main` as `44f99d2`; `Deploy to Hostinger KVM1` run `28172865389` passed quality/build/deploy; post-deploy canary confirmed apex/www homepage 200, `/api/health` ok, `/api/ready` ready, `/api/demo` still `omen-demo.v1`, HTTP->HTTPS 301, HSTS present, and live bundle `assets/index-CoI_lqSx.js` contains no private fixture labels or sample player names.
- [x] **Phase 1.7 — Platform brand color emphasis + button-style consistency (2026-06-30, local — not pushed/merged/deployed).** New `--color-platform-{sleeper,yahoo,espn}` tokens (+ dark-only `-chip` legibility overrides for Yahoo/ESPN + per-theme on-color tokens) in `frontend/src/index.css`, all three CSS scope blocks; new shared helper `frontend/src/lib/platformChip.js` (`platformChipStyle()` for tinted badges/icons, `platformButtonStyle()` for solid CTA fills — mirrors `lib/positionChip.js`). Applied to `ConnectLeague.jsx` (`/account/connect`) platform icons + primary Connect buttons, `PlatformConnections.jsx` (`/account` connect-row) Connect buttons via a new `platform` prop on `AccentButton`/`CTAButton`, and the `Standings.jsx` + `LeagueStandings.jsx` (`/football` embedded widget) `PlatformBadge`. Button shape/size were already unified pre-Phase-1.7 via the shared button components — this phase changed color only. Secondary actions (Disconnect/Reconnect/Cancel) intentionally stay neutral; the green "Connected" success badge intentionally stays universal green rather than platform-colored, matching the existing data-live/success color convention. Sourced Yahoo `#410093` and ESPN-brand-reference `#E52534` (Pantone Red 032C) from multiple independent public brand-color databases (high confidence); deepened ESPN to `#C81E2C` for both themes because the lighter sourced value only cleared white-text AA contrast by ~0.02 against this app's actual off-white text token — too thin a margin. Sleeper has no confirmed official hex anywhere found; kept the pre-existing approximation (`#1FA3E8` dark / `#0E6FB3` light) and flagged it for Justin. Two `page-system.md` Approved-Patterns sub-items ("Hall of Records badge", "step-counter cyan→platform") resolved as N/A (page retired / step covers all 3 platforms at once, not one at a time). Evidence: hand-verified WCAG contrast math for every button-fill/on-color and badge-text/surface pairing across both themes (documented in `page-system.md`); browser computed-style check (`getComputedStyle` against `:root[data-theme]`) confirmed all 8 new custom properties resolve to the intended hex in both themes; `npm --prefix frontend run build` clean; full `npm test` 398/399 (the 1 failure is `test/deployHardening.test.js` — a pre-existing, pre-session CRLF line-ending mismatch against `.github/workflows/deploy.yml`, reproduced in isolation with no changes of mine touching that file; spun off as its own follow-up rather than fixed under this item); `npm audit --audit-level=moderate` 0; `git diff --check` clean. Self-administered `slops-code-review` verdict: merge, no P0/P1 (P2s: pre-existing 36px `AccentButton` touch target — out of scope, shape unchanged this phase; two parallel hover mechanisms now coexist in `PlatformConnections.jsx` — minor, scoped-minimal). Self-administered `slops-ui-ux-audit`: ready to ship pending the Sleeper-hex confidence gap. Could not capture a live authenticated screenshot in this sandbox — Supabase `getSession()` hangs with no reachable backend, same root cause as the Phase 1.5d handoff; substituted the computed-style check above. `slops-mobile-smoke` is proposal-only (v0.1.0, driver not yet implemented per its own SKILL.md) — substituted a manual `/login` mobile-viewport (375×812) overflow + console-error check (clean) since all four touched routes are auth-gated.

- [x] **Phase 1.15 — Post-deploy visual smoke on prod (2026-07-05, local — not pushed/merged/deployed).** Added a read-only public-route logo smoke to `.github/workflows/deploy.yml` after the existing production bundle logo check: it fetches `/`, `/about`, and `/login`, follows each route's hashed Vite bundle, rejects `[C]`, requires a horizontal lockup PNG and the transparent lockup, and rejects the baked-black fallback. Browser QA found current prod `/login` was still text-only; `Login.jsx` now uses `/omen-horizontal-lockup-transparent.png` with `alt="Omen"`. Evidence: `test/deployHardening.test.js`; `test/loginBranding.test.js`; code review and UI audit in `Blueprints/audits/2026-07-05-phase1-15-post-deploy-visual-smoke-*.md`; handoff `Blueprints/handoffs/2026-07-05-phase1-15-post-deploy-visual-smoke.md`; browser evidence under `output/playwright/phase1-15-post-deploy-visual-smoke/`; focused 5/5; full `npm test` 416/416; frontend build passed with existing warnings; legacy client build passed; root audit 0; `git diff --check` clean. Done docs: release + page + design.

## Frontend — Phase 2

- [x] **Phase 2.7 — Demo Mode UI (2026-06-26).** New top-level public `/demo` route in `frontend/src/pages/Demo.jsx` fetches `GET /api/demo` and renders via a new shared `OmenRecommendationView` extracted from `OmenOfTheWeek.jsx` (both `/omen` and `/demo` now go through one render path; private fixture mode customization preserved). Persistent non-dismissible Demo Mode banner above the data state per `Blueprints/demo-mode.md` §Visible labeling; sky-blue "Demo" pill suppresses the green "Live · platform" pill when `mode === 'demo'`; HITL feedback gated off on the demo path; conversion footer = primary "Connect a league" → `/account/connect`, secondary "Back to home" → `/`. `dataMode.js` `VALID_MODES` extended `['mock','live']` → `['mock','demo','live']`. No backend code touched, no new runtime deps. Build clean: 503.64 kB / 141.52 kB gzip. Local Vite preview confirmed: route registers, banner persistent, zero console errors.
- [x] **Phase 2.9 — Account delete UI (2026-07-04; branch `codex/phase2-9-account-delete-ui`).** `/account` now has a Privacy subsection and accessible confirmation dialog for the mounted `DELETE /api/user/delete` route. The exact phrase is `DELETE MY OMEN DATA`; success signs out and redirects to `/login?deleted=true` with a completion notice. Copy says Omen data only and excludes fantasy-platform/sign-in-provider deletion. Evidence: focused 4/4; full `npm test` 403/403; frontend build passed with existing warnings; audit 0; `git diff --check` clean; security evidence, code review, UI audit, contract updates, Done ledger, and handoff are linked from `Blueprints/handoffs/2026-07-04-phase2-9-account-delete-ui.md`. Done docs: feature + page + design + security.
- [x] **Phase 2.10 — Trade share card (2026-07-04; branch `frontend/phase2-10-trade-share-card`; implementation `4db9db0`).** `/trade` now exposes a Share result panel after successful comparison, creating public hashes through `POST /api/trade/share` from the last submitted analyzer payload. Added public `/trade/share/:hash` with recommendation-first summary, confidence/risk/source/expiry chips, send/receive cards, loading/error states, and privacy copy that excludes connected-platform context, ESPN cookies, tokens, and private league data. Server now injects share-specific OG/Twitter meta for valid `/trade/share/:hash` SPA requests and serves `GET /api/trade/share/:hash/og.svg` from the public snapshot. Evidence: focused share/meta/frontend tests 9/9; full `npm test` 414/414; frontend build passed with existing warnings; audit 0; `git diff --check` clean; browser QA screenshots and `qa-summary.json` under `output/playwright/phase2-10-trade-share-card/`; code review, UI/UX audit, security/privacy evidence, contract updates, Done ledger, and handoff are linked from `Blueprints/handoffs/2026-07-04-phase2-10-trade-share-card.md`. Done docs: feature + page + design + recommendation + security.
- [x] **Phase 2.11 — FP1 signal-honesty labels (2026-07-05; branch `codex/phase2-11-signal-honesty-labels`).** Omen recommendation signals now render in the shared recommendation view as visible input-honesty labels: Live, Stub, Mock, or Unavailable, with data-source token colors plus text cues for Used / Limited / Preview / Not used. Backend/public demo `status: "demo"` display-normalizes to Mock / preview. Evidence: `frontend/src/lib/omenSignalLabels.js`; `frontend/src/pages/OmenOfTheWeek.jsx`; `test/omenSignalLabels.test.mjs`; focused 2/2; full `npm test` 418/418; frontend build passed with existing warnings; root audit 0; `git diff --check` clean; browser QA screenshot/body/summary under `output/playwright/phase2-11-signal-honesty-labels/`; code review, UI/UX audit, Done ledger, skill ledger, and handoff are linked from `Blueprints/handoffs/2026-07-05-phase2-11-signal-honesty-labels.md`. Done docs: feature + page + design + recommendation.
- [x] **Phase 2.12 — Trade Analyzer form redesign (2026-07-05; branch `codex/phase2-12-trade-form-redesign`).** `/trade` now uses position buttons instead of dropdowns, sends explicit `scoring_format`, exposes an honest Multi-team net-side mode, adds a desktop swap cue between Send/Receive, preserves autocomplete team metadata into payloads, adds VORP help text, and replaces the plain buy-low mock footer with `MockBanner`. Evidence: `frontend/src/lib/tradeForm.js`; `frontend/src/pages/TradeAnalyzer.jsx`; `test/tradeForm.test.mjs`; browser QA under `output/playwright/phase2-12-trade-form-redesign/`; RED focused test failed on missing helper; GREEN focused/helper/share tests 5/5; focused trade contract tests 16/16; full `npm test` 419/419; frontend build passed with existing warnings; root audit 0; `git diff --check` clean; code review, UI/UX audit, Done ledger, skill ledger, page-system update, and handoff are linked from `Blueprints/handoffs/2026-07-05-phase2-12-trade-form-redesign.md`. Done docs: page + design + recommendation.

## Frontend — Pre-pivot completions (evidence)

- [x] Paid surfaces hidden behind `VITE_BILLING_ENABLED` (default false) 2026-06-08.

## Verify (confirm done vs open — restate findings, do not assume)

- [x] **V2 — Verify Omen operational rename cutover.** Post-cutover checks passed on 2026-06-23: public health/ready green, `omen_api` and `omen_cron` healthy, primary deploy run `27995567629` passed from `/opt/omen`, Tailscale fallback run `27996194346` passed from `/opt/omen`, and residual Corvus operational references are retained only for compatibility/rollback/history.
- [x] Real-account QA of Yahoo / Sleeper / ESPN Omen + League Standings, especially ESPN reconnect, without logging cookie values. **ESPN QA completed 2026-06-05 (off-season mode).** Four fixes shipped: (1) `seasonCandidates()` retry logic for off-season year; (2) `isSeasonRetryable` handles ESPN 202 redirect; (3) CORS localhost default in `docker-compose.yml`; (4) off-season connect mode — when ESPN API returns 202 for all seasons, credentials are stored and a gold "saved" banner shown. Full in-season QA (live API returning 200) deferred until 2026 season opens (~August). All code changes in source, 13/13 tests pass, frontend build clean.

## Decisions (Justin / Agents — not builds)

- [x] Final user-facing naming: Omen of the Week vs Omen vs MVP Move. **Confirmed: Omen of the Week.**
- [x] Monetization posture changed: Omen should be free/non-monetized because Yahoo and ESPN cannot be monetized safely under current platform constraints.
- [x] **B1 — Unified Omen recommendation contract (2026-07-19).** `POST /api/omen/mvp-move` is the only canonical Omen recommendation route; `POST /api/optimizer/mvp-move` remains retired with a `410 legacy_route_retired` canonical hint. Live UI calls dashboard first, then posts `{}` only when `tools.omen_of_the_week.status === "ready"`. Omen is free/auth-gated, explicit mock only, dashboard off-season pre-call gate, no-data `empty`, recovery analytics deferred until after B2/B4. Evidence: `Direction/reviews/2026-07-19-b1-unified-omen-recommendation-contract.md`, `Direction/reviews/2026-07-19-ai-integration-omen-recommendation-contract.md`, `Direction/reviews/2026-07-19-b1-security-privacy-evidence.md`, `Blueprints/handoffs/2026-07-19-b1-unified-omen-recommendation-contract.md`. Focused 55/55; full `npm test` 391/391; production audit 0; root dev audit has pre-existing promptfoo/adm-zip advisory.
- [x] **B2 — Unified Omen recommendation layer (2026-07-19).** `POST /api/omen/mvp-move` now authenticates live callers before returning a direct `state: "off_season"` no-advice envelope when the shared NFL calendar is outside weeks 1-18; the service-level guard prevents provider adapter/Vault calls in that state. Omen MVP mock/demo/live envelopes now include `contract_version`, and tests pin success/empty/recovery/error/off-season field completeness for `DecisionBrief`. No new recommendation builder file, AI/cloud spend, analytics, SQL, packages, UI migration, push, merge, or deploy. Evidence: `Blueprints/handoffs/2026-07-19-b2-unified-omen-recommendation-layer.md`, `Direction/reviews/2026-07-19-b2-ai-integration-review.md`, `Direction/reviews/2026-07-19-b2-security-privacy-evidence.md`, `Blueprints/audits/2026-07-19-b2-unified-omen-recommendation-layer-code-review.md`.

---

## Sprint-queue reconciliation — 2026-08-16

23 items were carrying `Status: VERIFIED` (or, for `M7`, `CLOSED · DESCOPED`) in `Direction/current_sprint.md` while their `Done when:` criteria were already met. They are closed here and removed from the active queue, which is now active work only. `CLOSED` is terminal — a regression creates a new linked task rather than reopening one of these.

**Held back deliberately — still in `current_sprint.md`, not closed.** Each of the four now carries an explicit **✅ DONE — do not rebuild** / **🔨 REMAINING** split at the top of its record, so whoever pulls one can see at a glance what already shipped and what is actually owed. Do not re-derive the finished half:

- `M5-Native-API-Client` — slices A+B+C verified; **D–G unstarted**. Partial work is not a closure.
- `M4-CC-WaiverWatch` — merged (#271), but the six-state iOS render/accessibility evidence its `Done when:` requires is still outstanding.
- `R3-BUILD-Android` — config and guard merged (`231c9d2`), but `Done when:` requires a **signed** AAB and the upload keystore is founder-generated and not yet created.
- `M4-CC-PlatformsCompact` — merged as `6466a4c`; flipped `READY → VERIFIED` in this pass. Not closed: the Pixel-6a-class Android above-the-fold clause has no committed evidence and PR #304 shipped no handoff.

### CLOSED / COMPLETED (22)

| Task key | Title | Closure date | Exact evidence |
| :--- | :--- | :--- | :--- |
| A3 | Production security and Supabase review | 2026-08-01 | `Direction/reviews/2026-07-31-a3-production-security-supabase-review.md`; TLS handshake (Let's Encrypt, valid through 2026-09-06); RLS confirmed enabled on all 11 public tables. Residual WARN (leaked-password protection disabled) carried into `S1` — not a reopen. |
| A5-NflversePath | Correct the retired nflverse release path | 2026-08-15 | PR #309 `02857e7`; `test/omenTuesdayCronNflverse.test.js` (URL-shape + negative `player_stats` guard + PRE/REG collision + schema-drift fail-closed); backend **537/537**; `stats_player_week_2025.csv` downloads from the corrected URL. Handoff: `Blueprints/handoffs/2026-08-15-native-api-scope-and-scoring-source.md`. |
| R1 | App Store Connect operable during the Valor Ventures transfer | 2026-08-05 | ASC record live — `Omen — Fantasy Football Tool`, iOS 1.0, "Prepare for Submission", bundle `com.slopssaloon.omen`, SKU `omen-ios`. Root cause was agreements/tax setup under the Valor Ventures entity, not the transfer. |
| R2-iOS | Create the App Store Connect app record | 2026-08-05 | Same ASC record; bundle matches `PRODUCT_BUNDLE_IDENTIFIER` (Debug + Release) and the App ID in `Blueprints/specs/mobile/m3a-ios-auth-parity-spec.md` under team `6RWR5G9894`. `com.slopssaloon.omen.web` correctly not used. |
| R2-NAME | App Store display name of record | 2026-08-05 | Listing name `Omen — Fantasy Football Tool`; `PRODUCT_NAME = Omen` unchanged. Listing title only — feeds `R7` and `K1`. |
| M5-NativeConnect | Native connect flow, onboarding steps 3–6 | 2026-08-15 | PR #310 `e18b896`; iOS **174/0** (Xcode 26.6 `17F113`, iPhone 17 Pro sim, 158/0 baseline); Android **42/0** connected on `medium_phone` API 36 (26/0 baseline) + `:app:assembleDebug` + `:app:assembleDebugAndroidTest` + `:core:*` unit tests; backend unchanged 537/537. Handoff: `Blueprints/handoffs/2026-08-15-native-connect-flow.md`. Sleeper path only — Yahoo `.onHold` and ESPN `.useWeb` ship as honest non-dead-end states by contract. |
| M6-ContextualHelp | Contextual help on every shipped native destination | 2026-08-15 | PR #312 `e2e5088`; `Blueprints/handoffs/2026-08-15-m6-contextual-help.md`; iOS **183** unit + **5** UI (188 total), Android **50/50** instrumented + `:core:designsystem:test` + `:app:assembleDebug`. TalkBack genuinely driven on API 36; iOS VoiceOver **SUBSTITUTED** with `performAccessibilityAudit()` because `com.apple.VoiceOverTouch` never acquires a PID on Simulator. The audit caught a real AA defect (`text-secondary` on `surface-2`, 4.43:1 light) — fixed on both platforms. Residuals (real-device VoiceOver, pre-existing Command Center contrast, `OmenTypography` Dynamic Type, missing visual-evidence CI matrix rows) are logged in `Direction/known_issues.md` and fall outside this item. |
| M4-CC-LedgerPreview | Ledger preview composition + wiring | 2026-08-13 | `Blueprints/handoffs/2026-08-13-native-ui-parity-command-center.md`; Xcode 26.6 **123/0**; Android API 36 Command Center **4/4**; primitive scanner + `:app:assembleDebug` green; signed build installed and launched on the paired iPhone. |
| M4-CC-LeaguePulse | League Pulse composition + wiring | 2026-08-13 | Same handoff and runs; honest empty state ships until real events flow in, which is what the `Done when:` allows. |
| B2-D | Canonical Omen engine — live Waiver + Trade intelligence | 2026-08-02 | `c021b52`, `ffa8999`, `a0dea67`, `521268b`, `171508f`, `623068a`; focused B2-D **84/84**, full `npm test` **506/506**; capability matrix in `Blueprints/specs/b2d-canonical-omen-context-and-capability-contract-v1.md`; sanitized provider proofs in the 2026-07-29 Sleeper and 2026-08-02 ESPN handoffs. GitHub issue #162 acceptance. |
| B2-D-E1 | Normalize the ESPN waiver pool | 2026-08-02 | PR #265 `171508f`; fixture tests cover pagination, position/status filter, `onTeamId === 0` exclusion, requested-week projected stats, and no-cookie logging. No deployment or production-route claim. |
| B2-D-E2 | Wire ESPN waiver candidates into canonical Omen | 2026-08-02 | PR #266 `623068a`; canonical service/route tests prove selected-context ownership, live candidate selection, unavailable/empty behavior, and no mock fallback; Yahoo/Sleeper unchanged. |
| B2-D-E3 | Prove ESPN roster subtraction in a drafted league | 2026-08-02 | Founder-authorized read-only proof: 10 populated teams, 160 distinct rostered players, 0 leaks in a 500-entry filtered pool. `Blueprints/handoffs/2026-08-02-b2d-e3-espn-live-proof.md`. Aggregate evidence only — no cookies, league ID, team name, username, or player lists recorded. |
| B2-D3-S | Live trade capability: Sleeper | 2026-08-02 | PR #259 `521268b`; `test/sleeperAdapter.test.js`, `test/tradeLineup.test.js`, `test/omenMvpLiveService.test.js`; credential-free drafted-league aggregate proof in `Blueprints/handoffs/2026-08-02-b2d3-sleeper-live-trade-capability.md`. |
| B3 | Replace Sportradar with nflverse for Tuesday scoring | 2026-08-02 | PRs #260, #261, #262; KVM1 deploy run `30754635716`; production process-only dry run `archived=0`, `scored=0`, no Supabase or Redis writes. Pre-season deferral behavior was tracked as issue #263 and delivered by `A5-NflversePath`. |
| D1 | Real `GET /api/trade/pulse` | 2026-08-01 | Live hit 2026-08-01T03:10:53Z returned `"status":"live","is_mock":false,"source_status":"live_adp"` with 5 real current players; `Direction/reviews/2026-08-01-d1-adp-source-research.md`; `test/adpService.test.js` (9), `test/tradeRoute.test.js` live/unavailable cases. |
| O1 | Infrastructure observability | 2026-08-08 | Raspberry Pi Command Center stack, Layer 2 COMPLETE. Uptime Kuma (Tailscale-bound `100.98.81.0:3001`) with three content-aware Omen monitors — public HTTPS, `/api/health`, `/api/ready` — proven for content-failure detection, DOWN→UP recovery, restart persistence, and full-host reboot survival. Beszel hub (`100.98.81.0:8090`) collects from Command Center, KVM1 (`omen_api` + `omen_cron` via a loopback-only read-only Docker socket proxy), KVM2, Steward, Sentinel; agent-loss detection and reconnect proven on KVM1 without disturbing production. Error tracking and native crash reporting are a different signal class — `O1b` and `O6`, both still open. |
| O5 | Supabase backup and restore verification | 2026-08-11 (doc-reconciled 2026-08-17) | Encrypted Restic pipeline KVM1 → KVM2 with explicit Supabase Auth export (`auth.users`/`auth.identities`/`auth.mfa_factors`), scheduled every 6 hours. Full disaster-recovery drill proven: snapshot restored into an isolated network-disconnected disposable Postgres container, every recovered row count matched source exactly, zero orphaned Auth identities. Steward monitors freshness via a credential-free forced-command channel. `Blueprints/specs/infrastructure/slops-os-raspberry-pi-fleet-v1.md`. This item sat as "never verified" in this repo for a week after the work was actually done — found during Drive-doc reconciliation, not sprint work. Residual gap: KVM1/KVM2 are same-provider, so this is off-host, not off-provider, disaster separation. |
| O1b | Application error tracking (Sentry-class) | 2026-08-17 | GlitchTip (`postgres:18` + `valkey:9` + `glitchtip:6`, `SERVER_ROLE: all_in_one`) live on Command Center at `/opt/command-center/glitchtip/compose.yml`, Tailscale-bound to `100.98.81.0:8000` only. Resource estimate corrected from the sprint doc's "~1-2 GB" to the primary source's 256-512 MB, which is what made Command Center (not KVM2) the right host. A deliberate synthetic `ESPNMalformedResponseError` sent from an external host was confirmed stored, grouped, and queryable with full stack frames in the Postgres `issue_events_issueevent` table in under 1 second; ~300 MB RAM / ~2.9 GB disk measured. Two real defects found and fixed during deploy: a `smtp://…:465` URL that opened plaintext into a TLS-only port and hung until timeout (fixed: STARTTLS on 587), and `mem_limit` caps silently discarded by a kernel with no cgroup memory accounting (fixed: `cgroup_enable=memory` in `/boot/firmware/cmdline.txt` + reboot + `--force-recreate`, since a plain restart reuses each container's pre-fix config). All seven containers on the host survived the reboot via `restart: unless-stopped`. `Blueprints/handoffs/2026-08-17-o1b-glitchtip-error-tracking.md`. Unblocks the Android half of `O6`; iOS half stays gated on `R3-BUILD-iOS`. |
| P1-YahooLeagueBinding | Yahoo can never reach `ready` — no league is ever bound | 2026-08-14 | PR #293; `GET /api/yahoo/leagues` + `POST /api/yahoo/league` + the `ConnectLeague.jsx` picker; `npm test` **530/530** on Node 24.19.0 including the bind, not-your-league, and no-connection cases. The end-to-end live round-trip waits on Yahoo's Fantasy Sports entitlement (`P1-YahooReauth`) — a separate, external item, not this one's scope. |
| P1-YahooConnectButtons | No working UI path to connect or reconnect Yahoo | 2026-08-14 | PR #292; `/api/yahoo/auth` in `frontend/src/` now returns exactly one hit — `frontend/src/lib/yahooAuth.js:5`, the correct `apiFetch` POST; both `window.location.href` navigations gone; `providerError` logged before the state row is deleted (`src/routes/yahoo.js:118-119`). Accepted residual: `GET /api/yahoo/auth` still exists behind `requireAuth`, unreachable from any UI surface — fold into the next `src/routes/yahoo.js` touch. |
| P1-WaiverGateMultiProvider | Waiver readiness hardcoded to Yahoo | 2026-08-14 | `buildWaiverTool()` in `src/routes/dashboard.js` derives readiness from `activeRows.some(isOmenReadyConnection)` with no Yahoo-specific branch; `test/dashboardSummary.test.js` asserts `waiver_wire: ready` for a Sleeper-only and an ESPN-only user; `npm test` **530/530**. |
| F1 | Service-key Supabase route-scoping audit | 2026-07-31 | `Direction/reviews/2026-07-31-f1-service-key-route-scoping-audit.md`; `test/userPrivacyIsolation.test.js` (4), `test/espnRouteIsolation.test.js` (3); `npm test` 476/476 at closure. |
| F4 | ESPN public handoff production verification | 2026-07-31 | `Direction/reviews/2026-07-31-f4-espn-public-handoff-verification.md`; `test/espnConnectGuideRegression.test.js` (5); `npm test` 481/481 at closure. |

### CLOSED / DESCOPED (1)

| Task key | Title | Closure date | Reason |
| :--- | :--- | :--- | :--- |
| M7-EspnSafariExtension | Bundle the ESPN connect helper into the Omen iOS app | 2026-08-15 | Founder decision. **Safari Web Extensions cannot read HttpOnly cookies** — the extension compiled, embedded, and installed, and could never have worked. Cut before merge; feasibility memo and the corrected connect page landed in PR #311 `e3038d2`. Android path is deferred separately as `M8-EspnAndroidHelper`. |

---

## Post-reconciliation closure — 2026-08-16 (later session)

One item, closed in the session immediately after the reconciliation above. It is filed separately rather than appended to that table because it was **not** part of that pass: it merged after it, and the reconciliation's own inbox refresh went on to describe it as unmerged.

### CLOSED / COMPLETED (1)

| Task key | Title | Closure date | Exact evidence |
| :--- | :--- | :--- | :--- |
| P1-ConnectContinueRoute | "Continue" after connecting lands on the wrong page | 2026-08-16 | Merged PR [#314](https://github.com/justinduverge-design/omen/pull/314) / `107ed66` (2026-08-16T14:55Z). New `frontend/src/lib/onboarding.js`; `consumeConnectDestination()` in `nextUrl.js` defaults to `/football`; `ProtectedRoute.jsx` treats the local flag as a cache in front of `/api/platforms` and fails closed. RED first (`test/connectContinueRoute.test.mjs`); GREEN 12/12 focused, full `npm test` **549/549** (537 baseline), frontend build clean. Handoff: `Blueprints/handoffs/2026-08-16-p1-connect-continue-route.md`. Ledger row: `Blueprints/done/LEDGER.md`, 2026-08-16. **Merged, not deployed.** |

**Why this needed a correction pass at all.** Both `current_sprint.md` and `agent_inbox.md` still read "Not pushed or merged" after `107ed66` landed — the **sixth** recorded instance of this queue advertising shipped work as open, and the second inside one day. The mechanism is now specific enough to name: the handoff is written before the PR merges and nothing revisits it afterward, so every downstream file inherits a pre-merge sentence. The reconciliation pass itself proposed the fix — *a check that flags any sprint item whose key appears in a merged PR title while its `Status:` is not `CLOSED`* — and this recurrence is the argument for building it rather than repeating the manual sweep a seventh time.

---

## Draft goes dark for 1.0 — 2026-08-16

Two items, merged together as PR [#315](https://github.com/justinduverge-design/omen/pull/315) / `08aa73f` with all four checks green. **Merged, not deployed.**

### CLOSED / COMPLETED (2)

| Task key | Title | Closure date | Exact evidence |
| :--- | :--- | :--- | :--- |
| P1-DraftAssistantSideline | Remove Draft Assistant from the 1.0 surface | 2026-08-16 | Founder decision of 2026-08-11 had been logged in three places and never executed; the feature still shipped to every visitor. Removed from nav, `/draft`, the hardcoded `dashboard.js` tool entry, the Football tab, landing mini-card, about pill, four help entries, the `nextUrl.js` redirect allowlist, and the Privacy/Terms clauses. **Nothing deleted** — backend behind `DRAFT_ASSISTANT_ENABLED` (failing closed to a 404), tripwire test on every preserved file, five-step re-activation path in `Direction/decision_log.md` with both flag directions proven. **Founder extended the scope mid-session:** `/api/sleeper/draft*` went dark too, and the Privacy "drafts" collection line moved with it. Backend **563/563** (549 baseline, +14); frontend build clean; production bundle contains zero occurrences of `Draft Assistant` / `draft-assistant` / `Draft Position`. Handoff: `Blueprints/handoffs/2026-08-16-p1-draft-assistant-sideline.md`. |
| R7 | Scrub store metadata of Draft Assistant claims | 2026-08-16 | Store metadata was already clean — all three specs name it only as a prohibition, listing still unsubmitted. The live defects were in **native** copy on both platforms: "…plus seasonal **Draft entry**…" and "will surface relevant **draft** and roster opportunities". `M6-ContextualHelp` banned the product name in help copy and could not have caught either; the new tests ban the *word* in user-facing literals. Internal sprint keys (`M4-League-Screen`, `M4-Trade-Screen`) removed from five sites and banned by shape. `omen-native-app-shell-auth-api-contract-v1.md` amended at §1.4 per governance §3, `draft` row preserved for 2027. **iOS 192/192** (Xcode 26.6 `17F113`, iPhone 17 Pro sim; baseline 188); **Android 51/51** connected on `medium_phone` API 36 (baseline 50) + `:app:assembleDebug`. Recorded grep returns one residual, `"Leave draft?"` in the dev-only gallery. |

**The closeout gap this session set out to fix, tested on itself.** The handoff was written pre-merge saying "Not pushed, merged, or deployed", carrying an explicit note that the line would go stale the moment the PR landed — which is exactly how the previous item became the sixth instance of this queue advertising shipped work as open. It was re-read and corrected immediately after the merge, in the same session, along with both sprint records. That is the behaviour the proposed automated check (*flag any sprint item whose key appears in a merged PR title while its `Status:` is not `CLOSED`*) is meant to make unnecessary — the check is still worth building, because doing it by memory only works when someone remembers.

---

## M5 slice E — the Ledger goes live — 2026-08-17

| Task key | Title | Closure date | Exact evidence |
| :--- | :--- | :--- | :--- |
| M5-Slice-E-Ledger | Wire the Ledger to `GET /api/moves` | 2026-08-17 | Merged PR [#320](https://github.com/justinduverge-design/omen/pull/320) / `ee4387f` (2026-08-17T00:41Z). New `mobile/ios/OmenIOS/OmenIOS/App/Api/MovesHistory.swift` and `mobile/android/app/src/main/kotlin/com/slopssaloon/omen/app/feature/api/MovesHistory.kt`; `MovesRepository` on both platforms; `OmenLedgerPreviewState` gains `loading` and `error`, both rendered through the existing `OmenStateSurface` with the approved Figma node `72:2` composition unchanged. Evidence: **iOS 221/221** unit + **5/5** UI (Xcode 26.6 `17F113`, iPhone 16 simulator; baseline 208), **Android `:app:testDebugUnitTest` 27/27** (baseline 13) + `:app:assembleDebug` + `:core:designsystem` 22/22 with `--rerun-tasks`. No backend, frontend, or SQL surface touched. Handoff: `Blueprints/handoffs/2026-08-16-m5-slice-e-ledger.md`. Ledger row: `Blueprints/done/LEDGER.md`, 2026-08-16. **Merged, not deployed.** |

**The closeout worked this time.** The handoff was written pre-merge, said "local only", and was corrected in the same session the PR landed — the first time that loop closed without a later reconciliation pass. `scripts/check-sprint-staleness.js` was run before and after and stayed clean.

**One thing the stacked-PR experiment cost, recorded so it is not repeated blindly.** The design track (`M1-Screen-Trade` / `M1-Screen-League`, PR [#321](https://github.com/justinduverge-design/omen/pull/321) / `38771f3`) was deliberately opened against the code branch so the dependency was enforced by GitHub rather than by a note. It merged **into that base branch, not into `main`** — GitHub retargets a stacked PR only when its base branch is deleted, and it was not. `main` carried the code track alone until the design commit was re-landed by cherry-pick as PR #322. The separation was correct and worth keeping for audit; the mechanism needs one of two fixes: delete the base branch on merge, or open the second PR against `main` once the first lands.

---

## O7 — the forced-update gate lands inert — 2026-08-19

| Task key | Title | Closure date | Exact evidence |
| :--- | :--- | :--- | :--- |
| O7 | Forced-update / minimum-version gate | 2026-08-19 | PR [#337](https://github.com/justinduverge-design/omen/pull/337) / `2e10337` on `feat/o7-forced-update-gate`. Backend `GET /api/system/min-version` (`system-min-version.v1`) in `src/routes/system.js` + `getMinVersionStatus()` in `src/services/systemContracts.js`; `config.minAppVersion` from `MIN_APP_VERSION_IOS`/`MIN_APP_VERSION_ANDROID`. iOS `MinVersionGateClient`/`UpdateGateViewModel`/`ForcedUpdateView` gating in `AppShellView`; Android mirrors gating in `OmenAndroidApp`. Evidence: **backend 570/570** + `npm audit` clean, **iOS 234/234 unit + 12/12 UI** (Xcode 26.6 `17F113`, iPhone 16 simulator), **Android `:app:testDebugUnitTest` 43/43** + `:app:assembleDebug`. Live-route smoke against a booted server. Screenshots + review patch: `Direction/reviews/evidence/2026-08-19-o7/`. Handoff: `Blueprints/handoffs/2026-08-19-o7-forced-update-gate.md`. **Merged, not deployed. Not armed** — minimums stay at `0.1.0` and store URLs blank. |

**The gate ships deliberately inert, and that is the finished state.** Both minimums equal the currently-shipped version, so nothing is blocked. O7's own **Do not touch** forbids forcing an update with no live store listing, and `R3`/`R4` are open. The lever has to be in users' builds *before* it can help — a gate added after a bad ship is worthless. Arming it is a founder action with an ordering constraint: fill `OMEN_IOS_APP_STORE_URL`, confirm the Play URL resolves, **then** raise the minimums. The reverse order produces a correct prompt attached to a dead end.

**Rendering and reviewing both caught defects that a green suite could not.** Rendering the screen exposed `ForcedUpdateView` drawing the system default background instead of `OmenColor.bg` — invisible to 276 passing tests, iOS-only because Android already hosted it inside a themed `Surface`. The review gate then found three more: Android's fail-open was narrower than iOS's *on a launch crash path*; a suffixed version like `1.0.0-rc1` could never be blocked because unparseable fails open; and the iOS update button was a silent no-op in exactly the configuration that ships today. All four fixed, each with a test that fails without the fix.

**Two findings deliberately carried forward rather than absorbed:** the app-wide Android light-mode status bar is washed out (pre-existing, confirmed against the shipped Command Center), and `AppShellView.init` reads the bundle environment rather than the injected one (latent, no current misbehavior).

---

## S8 — the Dependabot queue was already empty — 2026-08-19

| Task key | Title | Closure date | Exact evidence |
| :--- | :--- | :--- | :--- |
| S8 | Triage the standing Dependabot queue | 2026-08-19 | **The work was finished 2026-08-11; only the status never advanced.** Verified against GitHub 2026-08-19: **zero open Dependabot PRs.** All six named in the item resolved exactly as its own recorded verdicts prescribed — [#273](https://github.com/justinduverge-design/omen/pull/273), [#274](https://github.com/justinduverge-design/omen/pull/274), [#277](https://github.com/justinduverge-design/omen/pull/277), [#281](https://github.com/justinduverge-design/omen/pull/281), [#282](https://github.com/justinduverge-design/omen/pull/282) **merged**; [#280](https://github.com/justinduverge-design/omen/pull/280) **closed** (correctly — it carried `tailwindcss` 3→4 and `vite` 7→8, which would have broken the build) and now covered by an `ignore` rule so Dependabot stops reopening it. `.github/dependabot.yml` carries the ignore rules and the phantom-label defect is fixed. Every `Done when:` clause met: written verdicts for all six, #281 resolved by merge, each red diagnosed to its real cause (a single `nanoid` frontend dev-advisory on `main`, not the PRs), and the config amended rather than PRs manually closed. Closed in PR [#342](https://github.com/justinduverge-design/omen/pull/342). |

**It was advertised as an available P1 pull for eight days, and was listed as a candidate in the re-derived queue on the morning it was found.** `check-sprint-staleness.js` was structurally blind to it: the script matched sprint keys against merged PR *titles*, and Dependabot titles are `build(deps): …` — they never carry a sprint key. An entire category of work was invisible to the one mechanism watching for exactly this drift.

**That blindness is now closed.** The `sprint-cited-prs-resolved` checker added the same day flags any `READY`/`IN_PROGRESS` item whose every cited PR is resolved, which is the shape `S8` had all along. See `scripts/checks/README.md`.

## Founder-gate clearing — 2026-08-19

Two items closed the same day, both surfaced by the staleness sweep rather than by anyone noticing. Neither needed new implementation.

| Task key | Title | Closure date | Exact evidence |
| :--- | :--- | :--- | :--- |
| R3-BUILD-iOS | Establish an iOS build-and-signing path | 2026-08-19 | Every clause but `cost recorded` was evidenced 2026-08-18 (signed build on TestFlight, Version 0.1.0 Build 1, confirmed in App Store Connect). The founder supplied the cost 2026-08-19: **a MacBook Neo at $699**, not the Mac mini the item's Options list assumed. Hardware read from the machine rather than taken from the verbal report — `MacBook Neo`, `Mac17,5`, Apple A18 Pro, 8 GB, macOS 26.5.1 (`25F80`), Xcode 26.6 (`17F113`). Recorded as the actual rather than mapped onto the nearest bullet. |
| M4-Auth-Providers-v1 | Discord OAuth (iOS passkeys promoted separately) | 2026-08-19 | Implementation landed in PR [#198](https://github.com/justinduverge-design/omen/pull/198); local verification passed 2026-08-13. **Only the recording step was ever missing.** All clauses re-verified against `main` 2026-08-19: provider-conditional rendering read at `OmenAuthFlow.kt:118` and `SignInView.swift:64`; callback exchange read on both platforms; scanner 1/1 each side; Android 22/22 + 43/43 + **connected 51/51** + `assembleDebug`; iOS `xcodebuild test` **234/234** on Xcode 26.6 (`17F113`). The live Discord round-trip evidence remains the 2026-08-13 founder-observed run — today's pass did not repeat it, and does not claim to. |

**Both were found by `check-sprint-staleness.js`, not by a person.** `M4-Auth-Providers-v1` is the `S8` shape exactly: every PR it cited was merged, its own text said "verification only, not implementation", and it sat at `READY` for six days as available P1 work. The widened checker flags that class now.

**The cost line is worth keeping honest.** `R3-BUILD-iOS` listed three options with estimates; the machine actually bought matches none of them on price and is a different form factor entirely. Writing "$699 MacBook Neo" rather than quietly ticking "Option 1: Mac mini (~$599 new M4)" is the difference between a record and a rationalisation — and the next hardware decision will be read off this line.

## F9 — mock / live labeling sweep — 2026-08-20

| Task key | Title | Closure date | Exact evidence |
| :--- | :--- | :--- | :--- |
| F9 | Mock / live labeling sweep | 2026-08-20 | All three `Done when:` clauses met. **Written inventory:** `Direction/reviews/2026-08-20-f9-mock-live-labeling-inventory.md` maps all thirteen shipped recommendation surfaces to a truth source and a visible label. **Implementation:** `4c4a0fe` — explicit `mode` required on web, iOS, and Android; missing/unknown/contradictory modes fail closed to `Unverified` or an error; backend per-signal status preserved instead of a minted `Live league data`; Mock and Demo given distinct copy; waiver and Trade Analyzer provenance labeled. **Cross-platform evidence, run on macOS:** signed iPhone 16 simulator suite **246/246** on Xcode 26.6 (`17F113`) with no `CODE_SIGNING` overrides, focused `OmenDecisionTests` **14/14**; Android `:core:designsystem:connectedDebugAndroidTest` **60/60, 0 skipped** on a booted `medium_phone` AVD (Android 16), with `mockStateLabelsFixtureDataAndRendersPayload` and `demoStateLabelsSampleDataAndRendersPayload` confirmed present in the results XML. Backend **575/575**, frontend build PASS, both audits **0 vulnerabilities**, `git diff --check` clean, Valor Brain **2/2 valid**. |

**The deferred verification found a defect, which is the argument for not deferring it.** `4c4a0fe`'s iOS test target **did not compile.** Four Swift triple-quote literals were written inline, which the compiler rejects outright — so *no iOS assertion in that commit had ever executed*, including the four that are the whole point of F9. The commit message's "Android instrumented-test compilation" was true and was silently read as cross-platform. Fixed in `4e67c82`, formatting only. **A DEFERRED-CI row records that a run has not happened; it does not record that the code would survive one**, and those are different claims. The Windows session did nothing wrong by deferring — it recorded the deferral honestly — but the gap between "deferred" and "would have passed" was a full compile failure.

**Two red iOS UI tests are pre-existing and were proved so rather than assumed.** `ContextualHelpAccessibilityUITests` fails two contrast assertions. Rather than reason from the diff's shape, the class was run at pre-F9 base `33fcfe6` in a clean worktree and failed identically. It belongs to F11.

**What F9 does not prove.** No human VoiceOver or TalkBack pass over the new Mock/Demo/Unverified states, and no rendered screenshot of them — the Compose assertions prove the label text is in the tree, not that it is legible. O7 already demonstrated a design-token defect surviving 276 green tests. Those remain F10/F11's.

## O8 — error tracking reaches production, and finds it was never on — 2026-08-21

**Closure: COMPLETED.** PR [#353](https://github.com/justinduverge-design/omen/pull/353), merged and deployed (run `32530387393`). Handoff: `Blueprints/handoffs/2026-08-21-o8-glitchtip-error-paths.md`.

**What O8 was:** `O1b` stood GlitchTip up and proved it accepts an error. Nothing in `src/` sent one. O8 wired provider adapter failures — Yahoo, Sleeper, ESPN — at each provider's single lowest-level HTTP chokepoint, added the API's missing `uncaughtException`/`unhandledRejection` handlers, and enforced demo isolation with two independent guards.

**`Done when:` — all three clauses met.** A real provoked ESPN adapter failure (live request, genuine HTTP 404, raised inside `doEspnRequest()`) arrived in GlitchTip with a 10-frame stack trace; tests prove no credential, cookie, or PII fragment reaches the payload; tests prove demo-mode errors never reach the real project. `npm test` 591/591.

**The finding that outgrew the task.** O8's Scope was written on a false premise — it claimed nothing in `src/` sent anything and told the agent to hand-roll an HTTP integration to dodge the dependency gate. `@sentry/node` had been wired since Phase 1.2. Chasing that discrepancy led to reading the deployed config, which showed **`SENTRY_DSN` set to the literal placeholder `paste_the_value_here` with a real DSN glued on**, byte-identical in `omen_api` and `omen_cron`. Not a legal URL, so the SDK built **no transport** and dropped every event while reporting `enabled: true`. **Omen had never reported a single error from production, to any tool.** Tracked and closed as [#354](https://github.com/justinduverge-design/omen/issues/354).

**The before/after worth remembering:**

| | Before | After |
|---|---|---|
| `enabled` | `true` | `true` |
| **`transport`** | **`false`** | **`true`** |

`enabled` never changed. **The field anyone would have checked was never the broken one** — which is why an "is it set?" health check would have passed forever.

**Verified in production**, not inferred: `GET /api/ready` → `checks.error_tracking` reports `valid: true`, `host: 100.98.81.0:8000`; a real ESPN 404 provoked inside the running production container grouped into GlitchTip issue 2 by fingerprint, which now carries two events — one `development`, one **`production`**. The stored payload was searched for the exact canary credentials the adapter was handed: absent.

**Three guards shipped so this class cannot recur silently:** the DSN is validated rather than truthiness-checked and a set-but-invalid value disables the client loudly; the validator matches the SDK's own key grammar (a validator looser than what it validates is a second bug wearing the guard's clothes — found when it passed GlitchTip's dashed-UUID key that `@sentry/node` rejected); and `GET /api/ready` surfaces error-tracking state, host and project id only, never the key.

**Also recorded for the next session:** GlitchTip mints project keys as dashed UUIDs and `@sentry/node` refuses them — strip the dashes.

**Not done, and deliberately out of scope:** GlitchTip issues do not reach the Discord `#slops-alerts` dispatcher. The Layer 5 alerting that has been messaging the founder polls Steward, Sentinel, and Kuma — **infrastructure health, not application errors** — and predates GlitchTip. That integration is `O9`.

## O9 — GlitchTip reaches the phone — 2026-08-21

**Closure: COMPLETED.** The Layer 5 Discord dispatcher on Command Center now carries unresolved GlitchTip issues alongside Steward, Sentinel, Kuma and Pi-hole. Artifact vendored at `ops/command-center/slops-alert-dispatcher`, verified byte-identical to what is deployed.

**Chose (a) extend the dispatcher over (b) GlitchTip's native webhook.** (b) would have put a second alert path with different semantics into one channel, and O9's `Done when:` requires the same dedup/recovery behavior as every other Layer 5 signal — (b) could only get there by reimplementing noise control that already existed. Reading GlitchTip's Postgres directly mirrors how Kuma's SQLite is already read, so **no new secret and no new auth surface**.

**Proven live, in sequence, with the founder confirming each Discord message:** CRITICAL alert listing three unresolved issues → silence on the unchanged repeat → founder resolved all three in the UI → exactly one `SLOPS RECOVERY` → silence again. Other signals verified unaffected by running each collector directly.

**The finding that outgrew the task — again.** Adding a fourth signal source produced the dispatcher's first genuinely multi-line signature, and it failed with Discord `400`. The payload had always been built by interpolating the signature into JSON, and raw newlines are invalid inside a JSON string. **Every alert this system had ever proven carried exactly one failing signal.** A simultaneous two-signal failure would have produced no notification at all — the alerting layer failing precisely when it mattered most, and silently, because a second defect persisted the signature *before* confirming delivery, so the failed send was never retried and the state claimed it had already been reported.

Both fixed: a real JSON encoder builds the payload, and delivery now precedes persistence so a failure retries on the next run.

**Generalisable, and it is the same lesson as `O8` one layer down:** monitoring and alerting fail in the shape of good news. `O8` found production reporting errors nowhere while reporting itself enabled; `O9` found the alerting path unable to deliver the multi-signal alert it exists for. **Neither was visible to any "is it configured?" check, and both were found only by exercising the real path with real content.** Every proof this fleet has recorded for Layer 5 used a single simulated failure — the noise-control semantics were genuinely proven, and the delivery mechanism was accidentally only ever tested on its easiest input.

**Constitution honored:** notification-only, no remediation added. Every new data path is read-only by construction — the GlitchTip read runs in a forced read-only transaction, verified by confirming an `UPDATE` is refused.

## F9 — mock / live labeling sweep — closed 2026-08-21 (work completed 2026-08-20)

**Closure: COMPLETED.** PR [#350](https://github.com/justinduverge-design/omen/pull/350). Evidence: `Direction/reviews/2026-08-20-f9-mock-live-labeling-inventory.md`; `Blueprints/handoffs/2026-08-20-f9-mock-live-labeling-verification.md`.

All three `Done when:` clauses were met on 2026-08-20 — every recommendation surface across web, iOS and Android either shows verifiably live data or is explicitly labeled, a written inventory maps each surface to its labeling, and no path presents fallback output as live. Signed iOS simulator 246/246 (`OmenDecisionTests` 14/14, Xcode 26.6, iPhone 16) and Android `:core:designsystem:connectedDebugAndroidTest` 60/60, both new Mock/Demo label assertions executing.

**Explicitly not claimed, and assigned elsewhere by their own records:** human VoiceOver/TalkBack on the labeled states, and rendered screenshot evidence of the new Mock/Demo/Unverified treatments. Those belong to `F10`/`F11` and are not F9 clauses.

**Why this needed a separate closing pass:** the work was finished and evidenced, and the item simply never advanced from `VERIFIED` to `CLOSED`. `check-sprint-staleness.js` flagged it for two days. **The status model's terminal step is a human act by design** — the checker can prove an item's PR merged but cannot judge whether every `Done when:` clause was met — so an item that is genuinely done still sits stale until someone reads it and decides. Re-checked rather than rubber-stamped: the inventory file was confirmed to exist, and the deferred items were confirmed to sit on other tasks.

## O6 — native crash reporting, and the IP-address question that held it open — closed 2026-08-21

**Closure: COMPLETED.** PRs [#332](https://github.com/justinduverge-design/omen/pull/332) (Android) and [#343](https://github.com/justinduverge-design/omen/pull/343) (iOS). Evidence: `Direction/reviews/2026-08-18-o6-android-crash-reporting.md`, `Blueprints/handoffs/2026-08-18-o6-android-crash-reporting.md`.

Both halves were proven live on 2026-08-19: a deliberate Android crash (`adb shell am crash`) and a deliberate iOS `NSException` each reached their Sentry project — iOS in **~2 seconds against a 60-second requirement**, with symbolicated Swift frames, not bare addresses.

**This item then sat at `VERIFIED` for two days on purpose, and the reason was a good one.** The dashboard showed the iOS issue with **Users: 1** and the Android issue with **Users: 0**, while neither payload emits a `user` field — so Sentry was attributing a user at ingestion, almost certainly by storing the client IP. An IP is personal data under GDPR, and the Android half's evidence had been written as "0 users (confirming no PII attached)" — a sentence that could not honestly be written for iOS. **Identical code, different per-project settings.** Holding the task open rather than closing on the strength of the payload-level proof was correct.

**Resolved 2026-08-21:** the founder enabled **Prevent Storing of IP Addresses** on the Sentry iOS, Android and React projects.

**Evidence boundary, recorded rather than blurred:** the setting is founder-confirmed in the dashboard; it was **not** re-proven by a fresh crash. "The toggle is on" and "no IP is stored on the next crash" are different claims, and only the first is evidenced. Folding the empirical re-check into `F10`'s real-device matrix is cheaper than minting a task for it.

**Two findings from this task worth keeping.**

**1. A silent truncation that would have shipped.** The first iOS DSN write truncated to `https:` in the built `Info.plist`, because xcconfig treats `//` as a comment — and `Base.xcconfig`'s own header already warned about exactly this. **The warning existed and was walked into anyway**, because the DSN was pasted as a plain value rather than treated as a URL. Any future `Local.xcconfig` URL needs the same `$(OMEN_SLASH)` indirection, and must be verified by extracting the key from the **built** `Info.plist`, never by reading the xcconfig.

**2. iOS crash coverage is partial, and the record says so.** `NSSetUncaughtExceptionHandler` fires for Objective-C exceptions only. Pure Swift runtime traps — `fatalError()`, force-unwrapped nil, index out of range — raise `SIGTRAP`/`SIGILL` and never reach it. Catching those needs signal handlers with real async-signal-safety hazards, deliberately out of scope. Android has no equivalent gap. **The deliberate-crash proof therefore had to use an `NSException` for the claim to be honest about what it demonstrates** — which is a limit on the proof, not a defect in it.


## S3 + S4 + O4 — the three hot routes get limits, containment, and their first load number — 2026-08-22

**Closure: COMPLETED for all three.** PR [#355](https://github.com/justinduverge-design/omen/pull/355) — **open, unmerged, and deliberately left that way** for founder review. Commits `0c1f85e` (S3), `0f546e8` (S4), `1d3f97d` (O4). Nothing is on `main` and nothing is deployed; this section records the work as done, not as released.

`npm test` **618/618** (baseline 593 at `f4a6ae1`, +14 S3, +11 S4). `npm audit` 0 vulnerabilities. `git diff --check` clean. `npm run evals:validate` passed. `node scripts/check-sprint-staleness.js` → no findings in the checks that ran. **No dependency added; `package.json` and `package-lock.json` are byte-identical to `main`.**

**Baseline correction.** The package was briefed against a 591/591 baseline. `main` at `f4a6ae1` actually runs **593/593** — higher, not lower, so nothing had regressed. Recorded as the actual rather than mapped onto the expected number.

### Why the three shipped together, and in this order

S3 and O4 act on the same three routes, and the order was load-bearing rather than cosmetic. Load-testing before the limits existed would have measured a system that no longer exists; load-testing after them without accounting for them measures `express-rate-limit` and reports the p95 of a 429. S4 extends a proof `O8` started the day before rather than starting fresh.

### S3 — rate limits on the three hot routes

`POST /api/omen/mvp-move`, `POST /api/trade/compare`, and `GET /api/dashboard/summary` shared the app-wide 100/min/IP budget with every other API call the SPA makes. A client could spend most of that budget on the single most expensive route in the product — provider fan-out plus an LLM call — without tripping anything. Each route now carries an enforced per-IP **and** per-credential limit per 60-second window (20/10, 20/20, 60/30), documented with its reasoning in `Blueprints/api-routes.md` § Rate Limits.

**The decision worth re-reading later.** These limiters run *before* authentication — `/api/omen/mvp-move` authenticates inside its own handler — so no verified user id exists when the decision is made. Keying on the JWT's unverified `sub` is the obvious shortcut and is an availability hole: anyone can mint a token carrying a victim's `sub` and lock that victim out. The user bucket is keyed on a SHA-256 digest of the presented token instead. **The cost is written into the contract rather than hidden: this is per-credential, not per-account.** Two devices get two buckets and a token refresh mints a fresh one; the per-IP limit is what keeps refresh-to-reset bounded, which is why both are enforced rather than either alone.

Proven by driving real requests through the shipped middleware instances until they 429, and against a booted server (20 × 401, then 429 with `Retry-After: 60` and `RateLimit-Policy: 20;w=60`). Mutation-checked — removing the mount turns 8 of the 14 tests red.

### S4 — no provider credential reachable in logs or error envelopes

O8 proved this for GlitchTip payloads; this extends it to stdout and the HTTP error envelope. **Two real gaps were found, and both were found by provoking failures rather than by reading the adapters:**

1. **`authorization` was missing from the shared scrubber's sensitive-key pattern.** An axios failure carries `error.config.headers`, which is where an `Authorization` header sits. Anything that logged or reported that object whole published the header verbatim. Every other key on it was covered.
2. **Even with the key added, `authorization: Bearer ya29.x` still leaked.** The key/value rule stops a value at the first space, so it redacted the word "Bearer" and published the token after it. `Bearer`/`Basic` now have their own rule, applied first. Deliberately not `OAuth` — that challenge is scheme plus `key=value` parameters, already covered, and blanket redaction would destroy the `oauth_problem` diagnostic that made the Yahoo 403 tractable.

Containment moved from per-call-site convention to structural: every winston line leaves through the scrubber, and the terminal error handler moved to `src/middleware/errorEnvelope.js` so the message it echoes back is scrubbed and the shipped envelope is the one under test. Call sites still pass only what they need — this is the backstop for when one of them stops doing that.

**One boundary recorded rather than left implicit:** a credential logged as a bare value with no key beside it is invisible to the text scrubber. No code path does that, and facts-of-record #6 keeps ESPN cookie values out of every emission site by construction, but the limit is now written down in the test instead of assumed away.

### O4 — the load rehearsal

`scripts/load-omen-routes.js` existed and had never been run. Extended rather than replaced, per `scripts/README.md`.

| Run | p95 `trade/compare` | p95 `mvp-move` | p95 `dashboard/summary` | error rate | 429s |
|---|---|---|---|---|---|
| Beta — 20 concurrent, 160/route | 20 ms | 5 ms | 23 ms | 0 % | 0 |
| 10× — 200 concurrent, 1600/route | 107 ms | 20 ms | 101 ms | 0 % | 0 |

**The 0 rate-limited count is the number that makes the latency numbers meaningful** — it confirms no request in either run was answered by the limiter instead of by the route. Worst p95 is ~7× inside the tighter investor-demo threshold.

Concurrency was chosen off the repo: 8 requests per client sits under the tightest per-credential budget (`mvp-move`, 10/min); 20 is the top of the K2 beta cohort; 200 is R6's internal-track ceiling (100 iOS + 100 Android), which lands 10× on a real boundary rather than an arbitrary multiplier. Every simulated client carries its own `X-Forwarded-For` and bearer token so the limits distribute as they will in production instead of collapsing the whole run into one bucket.

Two saturation runs then did the opposite on purpose — one IP, one credential — and the limiters bound at exactly their documented numbers, with the documented envelope, correct `scope`, and no 5xx. **That surfaced something worth knowing: for a client using the whole app rather than hammering one endpoint, the app-wide 100/min/IP limiter binds before `dashboard/summary`'s own budget does.** The `api-routes.md` numbers stack rather than replace.

`scripts/local-load-stack.js` is new. It boots the real `src/server.js` against a loopback Supabase stub, because without one `GET /api/dashboard/summary` 401s before doing any work — and worse, `requireAuth` would reach the configured Supabase host on every request, so the numbers would be DNS and TLS timings against a third party. The stub returns 404 loudly on any unstubbed path, so a future route change surfaces as a failure rather than as a suspiciously fast measurement.

**Not claimed, and listed beside what is:** no provider fan-out to Yahoo, Sleeper, or ESPN; no real LLM call; `mvp-move` ran in explicit mock mode; the stub user has no platform connections; loopback on one host with no TLS, Nginx hop, container limit, or cold start. Per O4's own Scope, **Week 1 Sunday morning is the real load test — this is the rehearsal.** The right reading is that nothing in Omen's own code is a bottleneck at beta scale, so if Sunday goes badly the cause is upstream of this measurement.

Full record: `Direction/reviews/2026-08-22-o4-hot-route-load-rehearsal.md`.

## Decision closeouts — 2026-08-22

| Area | Completed work | Evidence | Posture |
| --- | --- | --- | --- |
| M8 Android ESPN path | Founder selected the existing desktop/web ESPN connection through beta and Microsoft Edge mobile-extension feasibility as the first post-beta experiment. | `Direction/decision_log.md` (2026-08-22 Android ESPN decision); `Direction/reviews/2026-08-15-espn-mobile-feasibility-memo.md` § 2026-08-22 Android decision brief | Decision complete only. No extension packaged, submitted, published, deployed, or given credential access. |
| R3 signing and provisioning | iOS build 1 reached TestFlight; Android version code 1 was accepted into Google Play's internal-testing release draft and Play App Signing is active. | `Direction/current_sprint.md` R3 platform evidence; `Direction/decision_log.md` 2026-08-22 Android first-upload entry | Signing and first-upload acceptance complete. Android internal release remains unpublished with no testers; R4/R5 still gate rollout. |
