# Corvus Agent Inbox

## Current Request

Context markdown is being normalized so Justin can rewrite `AGENT.md` and `CLAUDE.md` with the correct route.

## Current Route

```text
SLOPS/
  slops-saloon/
    corvus/
```

## Active Notes

- This repo is the Corvus product repo.
- The old nested `Corvus/` folder is retired.
- Product handoffs live in `Blueprints/handoffs/`.
- Product context lives in `Direction/`.
- Division context lives one layer up in `..\`.
- OS context lives two layers up in `..\..`.

## Do Not Touch Unless Explicitly Asked

- `AGENT.md`
- `CLAUDE.md`
- `.env`
- Secrets or credentials
- Deploy config
- Package files
- SQL or migrations
- Production infrastructure
