# Omen Current Sprint

Last updated: 2026-07-07 (Added "iOS Native App (ESPN Relay Sync)" lane — Phase 5.1-5.4 — per `planning-pass` against `Blueprints/specs/ios-espn-relay-app-plan-v1.md` and `Direction/reviews/2026-07-07-espn-ios-cookie-sync-research.md`. Desktop `EXT1` friction fix was built this same session (`extension/`, not yet committed) and needs no sprint-item change since it reuses the existing cookie-connect endpoint as originally scoped. A `platform_connections.connection_mode` schema addition is drafted on branch `claude/platform-connections-relay-mode`, not applied — Phase 5.3 is blocked on Justin's explicit apply-approval for that.)

Prior: 2026-07-05 (Phase 2.11 signal-honesty labels and Phase 2.12 Trade Analyzer form redesign completed locally; Phase 1.15 post-deploy visual smoke completed locally; Phase 2.10 trade share card completed locally; canonical off-season signal merged via PR #78; Phase 2.9 account delete UI conflict resolution prepared; Phase 1.13 marked complete per Justin correction. Added Mobile / App Store Readiness lane, ESPN/Stripe posture decisions, two security bug-fix items, and two Verify items per `Direction/reviews/2026-07-05-app-store-mobile-readiness-sprint-proposal.md` and `Direction/reviews/2026-07-05-espn-community-api-and-extension-research.md`. Prior phase evidence remains in the relevant completed items below. Added Decisions-lane item on Trade Analyzer Buy-Low mock-data refresh process gap, found during the Phase 2.13 copy-pass session.)

## How this feeds the loop

`Next` below is the queue. Each item lives under a **lane** (work area, not an agent assignment) and is a checkbox.
- Agents are lane-agnostic — any agent may pull any agent-buildable item from any lane. No frontend/backend lean, no Claude/Codex split. Pick by readiness, blockers, and token-cost (see `Blueprints/playbooks/sprint-synergy.md` once written).
- When `Direction/agent_inbox.md` has no pinned Active Task, agents organize the next unchecked agent-buildable items across Backend + Frontend lanes and surface blockers.
- `agent_inbox.md` overrides this — pin a specific or custom task there and it wins.
- When an item is done, the agent moves it to `Direction/sprints_completed.md` with full evidence (run IDs, commit SHAs, test counts) and logs the decision in `Direction/decision_log.md`.
- Agents do not auto-pull from **Ops / Justin**, **Verify**, or **Decisions** unless Justin explicitly pins one.

> **Where to look for history:** done-item evidence with run/commit IDs lives in `Direction/sprints_completed.md`. Doctrine and decisions live in `Direction/decision_log.md`. Per-phase deliverable receipts live in `Blueprints/done/`. In-app retro signal lives at `/ledger` (Move History). Don't pull these into context unless the task actually needs them.

## Current State

- Live on the renamed route; PR #22 deployed (run `26833528435`); `/api/health` ok, `/api/ready` ready.
- Backend tests 378/378 local (2026-06-25); frontend build clean at the prior baseline; full `npm audit --audit-level=moderate` reports 0 vulnerabilities. The last fresh-clone verification at `2acb663` passed `npm ci` and the then-current 297/297 tests.
- Tier 2 authenticated production smoke: 13/13 passed 2026-06-04. Local authenticated load smoke 2026-06-05: Trade Analyzer p95 34ms, dashboard summary p95 633ms, Omen p95 4999ms (latency follow-up, not blocked).
- ESPN real-account QA complete — Sleeper / Yahoo / ESPN all connected.
- Tuesday scoring nflverse adapter implemented locally with fixture tests and dry-run no-write mode. Cron stays disabled (`OMEN_CRON_SCORING_ENABLED=false`) until approved real-data dry-run validates production Supabase data without writes.
- Posture: **KVM1-live / post-cutover QA**. Remaining gates: final production secrets/Supabase review, and Tuesday scoring enablement after backend provider validation.
- Phase 1.4 (font system) actually-in-prod 2026-06-16. Two carry-forward operating rules in `decision_log.md`: clean-clone (`rm -rf node_modules && npm ci` or `/tmp` clone) verification required for any Phase touching `package.json`/`package-lock.json`, and never merge with the gating CI check red. Full incident: `Blueprints/handoffs/2026-06-16-phase1-4-lockfile-resync-handoff.md`.
- Deploy operating mode: `.github/workflows/deploy.yml` runs deploy locally on KVM1's self-hosted runner (`corvus-kvm1-deploy`, label `omen-deploy`). `.github/workflows/deploy-kvm1-tailscale-fallback.yml` is manual-only backup over Tailscale (last green run `27693807505`).

## Now

- **Phase 1.5 PR1 (team-theming system core) merged 2026-06-17 via PR #41.** Phase 1.5b / 1.5c / 1.5d are unblocked on the frontend side (1.5d still needs Backend Phase 2.17).
- Hold all API contracts stable; keep docs aligned to the 2026-06-04 state.
- Keep Tuesday scoring disabled until scoreable move metadata is confirmed against real rows and an approved nflverse dry-run validates production Supabase data without writes.
- Treat Omen local p95 around 5s under repeated load as a performance follow-up before broad paid-user traffic.

## Next

### Ops / Justin (gated — agent prepares, Justin executes)
- [ ] **OP1 — Approve Omen operational rename boundaries.** Decide whether the cutover includes only deploy identity or also the GitHub repo/local folder rename; confirm maintenance window, KVM1 access, runner-label approach, and old Corvus artifact retention. Spec: `Blueprints/specs/omen-operational-rename-cutover.md`. Cost: small (agent prep only; Justin executes). Done-when: Justin explicitly approves the scope and rollback owner; no production mutation happens in this item.
- [ ] Final review of production secrets + Supabase settings before paid-launch confidence. Cost: small (audit-only).
- [ ] Decide on enabling `OMEN_CRON_SCORING_ENABLED=true` only after the nflverse scoring adapter and dry-run validation are complete. Cost: small (decision + env flip + post-flip verify).

### Backend — Phase 3
- [x] **Phase 3.12 — Tailscale → KVM2 Gemma 4-E4B bridge (2026-06-27; deployed 2026-06-28 UTC).** Hardened the optional local narration bridge so `LLM_BASE_URL` is used only when it resolves to a private/Tailscale-style host (`localhost`, private RFC1918/link-local ranges, Tailscale CGNAT `100.64.0.0/10`, or private DNS suffixes such as `.internal`, `.local`, `.tailnet`, `.ts.net`). Public or invalid URLs fail closed to deterministic/template narration and are reported as `misconfigured_public`/`invalid_url` without leaking the URL. Default model updated to `gemma4:e2b-q4_0` while `LLM_MODEL` still overrides it; production currently reports `gemma3:4b` via env override. `/api/ready` and `/api/platform-status` now report the bridge status safely; no secrets, env values, package files, production env edit, or KVM state changed. Evidence: implementation `fbbf0c5`; PR #70; merge `a13160b`; deploy run `28306784898`; workflow quality/build/deploy green; independent canary health/ready/platform-status/home/demo/www health/HTTP redirect/HSTS green; live ready reports `llm_private: true` and `configured_private`; RED `node --test test/llmService.test.js` failed on missing bridge status; GREEN focused LLM/system/Omen route tests 51/51; full `npm test` 388/388; root audits 0; primary frontend build clean with existing Vite chunk warning; legacy client build clean; research `Direction/reviews/2026-06-27-phase3-12-kvm2-gemma-bridge-research.md`; security evidence `Direction/reviews/2026-06-27-phase3-12-kvm2-gemma-bridge-security-evidence.md`; AI review `Direction/reviews/2026-06-27-phase3-12-kvm2-gemma-bridge-ai-integration-review.md`; code review `Blueprints/audits/2026-06-27-phase3-12-kvm2-gemma-bridge-code-review.md`; contract in `Blueprints/handoffs/backend-to-frontend.md`. Done docs: feature + security + release.
- [x] **Phase 3.13 — Token-constrained prompts (2026-06-28).** LLM narration prompts now enforce <=50 words / <=2 sentences and use a 90-token generation cap for trade, start/sit, and Omen MVP Move narration. Omen strict-JSON narration is rejected when over limit and falls back to deterministic copy. Evidence: RED `test/llmService.test.js` failed on missing prompt/token/parser limits; GREEN focused LLM 2/2; focused Omen/agent 58/58; full `npm test` 387/387; `npm audit --audit-level=moderate` 0. Done docs: feature + recommendation.
- [ ] **Phase 3.15 — `AI_PROVIDER=local|cloud` toggle** with hard budget cap. Unblocked 2026-07-12 — Justin approved a **$0 cap** (local model only; see decision log). Build constraint: the `cloud` provider path must hard-fail closed or stay disabled entirely, since $0 authorizes no cloud spend at all. Cost: medium. Done docs: feature + security + release when deployed.

### Backend — Phase 4
- [x] **Phase 4.16 — Open-agreements ToS + Privacy Policy custom paragraphs (2026-06-28).** Justin confirmed open-agreements is preferred over Termly. Drafted review-only provider clauses for ESPN cookie handling, Yahoo attribution, and Sleeper attribution; added provider terms research, security/privacy evidence, and legal spot-check. No pages were published and no endpoint, package, SQL, env, provider credential, Supabase, Stripe, or deploy behavior changed. Evidence: `Legal/2026-06-28-open-agreements-provider-paragraphs.md`; `Direction/reviews/2026-06-28-phase4-16-provider-terms-research.md`; `Direction/reviews/2026-06-28-phase4-16-security-privacy-evidence.md`; `Direction/reviews/2026-06-28-phase4-16-legal-spot-check.md`; `Blueprints/handoffs/2026-06-28-phase4-16-open-agreements-provider-paragraphs.md`; `git diff --check` clean. Done docs: security; content-marketing N/A because no public pages/posts were produced.

### Backend — Behind launch readiness (post-launch, not deferred to 2027)
- [ ] **Tuesday scoring enablement (`OMEN_CRON_SCORING_ENABLED=true`).** Priority: P0. Cost: small (env flip + post-flip verify). Blocked-by: none — Justin approved enabling this 2026-07-12 (see decision log); still requires an approved Supabase dry-run immediately before the flip per the existing gate, not a further founder sign-off. Done-when: dry-run validated against real nflverse scoring data, flag flipped in production env, post-flip `/api/ready`/scoring-cron health reverified, no plaintext secrets/cron-window regressions. Done docs: feature + recommendation + security + release.
- [ ] **Real `GET /api/trade/pulse` endpoint (Trade Analyzer Buy-Low mock refresh).** Priority: P1. Cost: medium. Blocked-by: none — Justin decided 2026-07-12 to build the real endpoint rather than a manual-refresh checklist (see decision log). Replaces `frontend/src/data/tradePulse.js`'s static, hand-authored 5-target list (unchanged since 2026-05-30, commit `4a30556`) with a backend-computed buy-low signal so the existing "updated each preseason" UI copy (`MockBanner` footer) becomes true instead of aspirational. Named as Phase 2 Request 20 in `Blueprints/handoffs/frontend-to-backend.md`; reuse that contract as the starting shape. Done-when: endpoint returns real computed buy-low targets, frontend `tradePulse.js` static list is retired or demoted to an explicit offline fallback, `MockBanner` reflects true data-source status (not hardcoded "Mock"), tests cover empty/error/stale-data paths. Docs affected: `src/routes/` (new route), `frontend/src/data/tradePulse.js`, `frontend/src/pages/Landing.jsx` or wherever `MockBanner` consumes it, `Blueprints/handoffs/frontend-to-backend.md`. Done docs: feature + recommendation.
- [ ] **ESPN mobile-connect onboarding (link/instructions to the browser-extension cookie flow).** Priority: P1. Cost: medium. Blocked-by: none — resolves the ESPN public/mobile posture decision (2026-07-12, see decision log): ESPN stays available on mobile, but routes users to the existing browser-extension cookie flow via an in-app link and/or emailed instructions rather than attempting cookie capture inside the mobile build itself. Scope: onboarding/`ConnectLeague.jsx` ESPN card gets a "Connect ESPN from a browser" explainer with a link to the extension and step-by-step instructions (reuse `Blueprints/playbooks/espn-recovery.md` content where applicable); consider an email-instructions fallback for users who start on mobile. Done-when: mobile-build ESPN card no longer implies in-app cookie capture is possible, extension link is live and tested, copy passes the same attribution/no-affiliation review as Phase 4.20d. Docs affected: `frontend/src/pages/Onboarding.jsx`, `frontend/src/pages/ConnectLeague.jsx`, `Blueprints/playbooks/espn-recovery.md` (or a new mobile-specific playbook). Done docs: feature + design.
- [ ] **FP1 — Public ESPN setup guide with device handoff.** Create public `/espn-connect` using `PublicInfoLayout`; explain why ESPN requires a desktop-browser helper, link `extension/README.md`'s local-load guide, and offer native Share when available plus copy-link, `sms:`, `mailto:`, and QR fallbacks without collecting any phone number or email. Update `ConnectLeague.jsx` and onboarding to point here. Spec: `Blueprints/specs/espn-connect-guide-v1.md`. Done-when: the guide works unauthenticated at desktop and phone widths; no cookie names/values appear in share payloads or URLs; all fallbacks expose the same stable guide URL; tests cover link/copy/share fallback selection; security/privacy evidence confirms the helper only stages credentials locally and does not auto-submit. Priority: P1. Cost: medium. Done docs: feature + page + design + security.
- [ ] **FP2 — ESPN setup mock walkthrough.** Produce a 60–90 second, clearly labeled mock walkthrough for Chrome/Edge: load helper → sign in to ESPN → choose Fill into Omen → review → user clicks Connect. Spec: `Blueprints/specs/espn-connect-guide-v1.md`. Done-when: screenplay/storyboard and rendered mock cut contain no real cookies, account data, or unsupported browser claims; public guide embeds or links the approved hosted video; captions and transcript are present. Blocked by FP1. Priority: P2. Cost: medium. Done docs: content-marketing + design + security.
- [ ] **FP3 — ESPN desktop-handoff verification.** QA the public guide and mock video on phone and desktop, including native-share availability and all copy/QR fallbacks; verify the extension bridge never sends or displays credential values. Spec: `Blueprints/specs/espn-connect-guide-v1.md`. Done-when: documented viewport/browser matrix passes; security evidence and UI audit report no P0/P1; Phase 4.20 integration can cite this evidence. Blocked by FP1 and FP2. Priority: P1. Cost: small. Done docs: feature + page + design + security.
- [ ] ESPN live draft tracking (Lazy Sync, same pattern as Sleeper). Cost: large. Done docs: feature + recommendation + security.
- [ ] Yahoo live draft tracking (Lazy Sync, same pattern as Sleeper). Cost: large. Done docs: feature + recommendation + security.
- [ ] **Win-streak summary contract for post-win rewards.** Add a backend-computed `currentWinStreak`-style field to `GET /api/dashboard/summary` by provider, derived from real matchup history rather than browser-local inference. Spec: `Blueprints/specs/page-system.md` Post-Win Pulse + Streak Ladder. Done-when: Sleeper/Yahoo/ESPN return a safe integer or null, no raw provider ids/credentials leak, tests cover win/loss/tie/unavailable paths, and backend-to-frontend contract documents the shape. Cost: medium. Done docs: feature + security.

### Backend — Bug fixes
- [x] **Rate-limit scope fix for the 2026-07-02 mobile-QA-audit P1 (unhandled 429 raw JSON) — 2026-07-04; merged via PR #78.** `src/server.js`'s general rate limiter was mounted globally (ahead of static assets + SPA catch-all), so page loads/asset requests counted against the same 100/min budget as API calls; a rate-limited hard navigation rendered raw JSON since React never mounted. Scoped the middleware to `/api/*` only. Verified live: 150 requests to a static asset → 150/150 200s (was previously subject to the limiter); 150 requests to `/api/dashboard/summary` → 100 then 429 (limiter still fully functional for actual API traffic). Full `npm test` 402/402; `npm audit --audit-level=moderate` 0; `git diff --check` clean. Evidence: `Blueprints/handoffs/2026-07-04-ratelimit-scope-and-standings-offseason-triage.md`. Done docs: feature.
- [x] **Vault secretId plaintext logging in `vaultDelete()` (`src/routes/platforms.js` lines 200–206) — fixed 2026-07-06; merged and deployed 2026-07-07 (PR #87, `aaf4ca6`; deploy run `28835705215` green; post-deploy health/ready reverified).** `logger.warn("Vault secret deletion failed", { err: error.message, secretId })` logged the raw Vault secret id on RPC failure — violated `Blueprints/hard-prohibitions.md` #9. Fixed to log only `error.message`, matching `src/routes/userPrivacy.js`'s `deleteVaultSecret()` pattern exactly. Evidence: RED test `DELETE /api/platforms/espn never logs the raw Vault secret id when deletion fails` (`test/platforms.test.js`) failed pre-fix (asserted `false` for raw id presence in captured logs, got `true`); GREEN after one-line fix; full `npm test` 422/422; grep confirms `vaultDelete()` no longer passes `secretId` to any logger call. Handoff: `Blueprints/handoffs/2026-07-06-vault-secretid-logging-fix.md`. **Found during this fix, not yet fixed (separate sprint item below, same bug pattern):** `src/omen_gdpr.js` still embeds the raw secret id in both its warn and success log lines; `src/omen_api_v2.js`, `src/services/yahooAuth.js` embed secretId in thrown Vault decrypt/update Error messages (different call sites/pattern, not the one named in this item — flagged in the handoff for a follow-up decision, not fixed here). Priority: P0 (security). Cost: small. Done docs: security.
- [x] **Delete orphaned `src/omen_gdpr.js` (2026-07-13; local commit `26441a1`).** Removed the unmounted legacy module and stale source reference; moved Probo's three privacy controls to the mounted `src/routes/userPrivacy.js`, with a regression assertion against the retired path. Full `npm test` 388/388, audit 0, frontend build clean with pre-existing warnings, and `git diff --check` clean. Evidence: `Direction/reviews/2026-07-13-orphaned-gdpr-module-security-evidence.md`, `Blueprints/audits/2026-07-13-orphaned-gdpr-module-code-review.md`, and `Blueprints/handoffs/2026-07-13-orphaned-gdpr-module-retirement.md`. Done docs: security.
- [x] **Canonical off-season signal for dashboard + league standings (2026-07-04; merged via PR #78).** Added shared `isOffSeason()` in `src/services/nflSchedule.js`; `GET /api/dashboard/summary` now returns `tools.omen_of_the_week.status: "off_season"` with `available: false` for otherwise-ready users during off-season; `GET /api/league/standings` now short-circuits off-season to `200` with `standings: []` before Yahoo/Sleeper/ESPN provider calls. Evidence: `src/services/nflSchedule.js`; `src/routes/dashboard.js`; `src/routes/league.js`; `test/nflSchedule.test.js`; `test/dashboardSummary.test.js`; `test/leagueStandingsRoute.test.js`; `Blueprints/handoffs/backend-to-frontend.md`; `Blueprints/audits/2026-07-04-canonical-offseason-signal-code-review.md`; `Blueprints/handoffs/2026-07-04-canonical-offseason-signal.md`; RED focused tests failed for missing helper/status/empty envelope; GREEN focused tests 28/28; full `npm test` 407/407; `npm audit --audit-level=moderate` 0; frontend build clean with existing warnings; final `git diff --check` clean. Done docs: feature; recommendation/security considered N/A.
### Post-live learning / Justin + Agents (gated)
- [ ] **PL1 — Post-live Omen technology-learning and in-season improvement cycle.** Run only after Release Done, seven stable production days, and a seven-day `slops-product-pulse`. Spec: `Blueprints/playbooks/post-live-technology-learning.md`. Cost: large (when unblocked; six learning sessions). Done-when: all six learning sessions have demonstration evidence, `slops-learning-loop` has a prior-use note, and approved improvements that can land before season end are ordered in this sprint. **Blocked by public Omen baseline + seven stable days.**

### Frontend — Phase 1
- [x] **Phase 1.5d — Post-win pulse animation (2026-06-29).** Built the single-win version from existing `GET /api/dashboard/summary` platform `lastResult` / `lastGameId` fields: compact `<Team> W - bright today` chip on `/football`, one-time 800ms team-accent header-rule wash per stored `lastGameId`, and embedded standings current-user row tint lift from 14% to 22% while the latest connected platform reports a win. Motion honors `prefers-reduced-motion`. Hybrid streak ladder documented but not shipped until a backend-computed `currentWinStreak` field exists. Evidence: `frontend/src/lib/postWinPulse.js`; `frontend/src/pages/Football.jsx`; `frontend/src/components/league/LeagueStandings.jsx`; `frontend/src/index.css`; `test/postWinPulse.test.mjs`; spec `Blueprints/specs/page-system.md`; code review `Blueprints/audits/2026-06-29-phase1-5d-post-win-pulse-code-review.md`; UI audit `Blueprints/audits/2026-06-29-phase1-5d-post-win-pulse-ui-ux-audit.md`; focused 4/4; full `npm test` 394/394; frontend build clean with existing Vite warnings; audit 0; `git diff --check` clean. Visual screenshot/mobile smoke blocked by authenticated `/football` route access in this session. Done docs: feature + page + design.
- [x] **Phase 1.7 — Platform brand color emphasis + button-style consistency (2026-06-30).** New `--color-platform-{sleeper,yahoo,espn}` (+ dark-only `-chip` legibility overrides, + per-theme on-color tokens) in `frontend/src/index.css`; shared `frontend/src/lib/platformChip.js` helper (mirrors `lib/positionChip.js`). Applied to `ConnectLeague.jsx` platform icons + primary Connect buttons, `PlatformConnections.jsx` (`/account` connect-row) Connect buttons, and the `Standings.jsx` + `LeagueStandings.jsx` platform badges. Button shape/size were already unified pre-Phase-1.7 (shared `CTAButton`/`AccentButton`); this phase changed color only. Sourced Yahoo `#410093` and ESPN `#E52534`-derived `#C81E2C` (deepened for safe AA margin as button-fill in both themes) from public brand-color references; Sleeper has no confirmed official hex anywhere found, kept the pre-existing approximation (`#1FA3E8` dark / `#0E6FB3` light) — **flag for Justin if he has Sleeper's actual brand kit.** "Hall of Records badge" and "step-counter cyan→platform" sub-items in `page-system.md` Approved Patterns are N/A (page retired / step covers all 3 platforms at once). Evidence: hand-verified WCAG contrast math for every button-fill/on-color and badge-text/surface pairing (documented in `page-system.md`); browser computed-style check confirmed all 8 new custom properties resolve correctly in both themes; frontend build clean; full `npm test` 398/399 (1 pre-existing, pre-session CRLF line-ending failure in `test/deployHardening.test.js` against `.github/workflows/deploy.yml`, reproduced in isolation, unrelated to this diff — spun off separately, not fixed here); `npm audit --audit-level=moderate` 0; `git diff --check` clean. Could not get a live authenticated screenshot in this sandbox (Supabase `getSession()` hangs, no reachable backend — same root cause as the Phase 1.5d handoff); mitigated via the computed-style check above plus self-administered `slops-ui-ux-audit` and `slops-code-review` (verdict: merge, no P0/P1). `slops-mobile-smoke` is proposal-only (v0.1.0, driver not yet implemented) — substituted a manual `/login` mobile-viewport overflow/console check (clean) since all four touched routes are auth-gated. Done docs: page + design.
- [x] **Phase 1.8 — Confidence gradient endpoints (2026-06-30).** New `--color-confidence-floor` (`#701020`) / `--color-confidence-ceiling` (`#206F3A`) tokens in `frontend/src/index.css` (`:root` + both themes, identical values); `frontend/src/lib/confidenceGradient.js` exports `confidenceBarStyle(score)` using `color-mix(in hsl, ceiling <score>%, floor)` — CSS-native HSL interpolation whose default shorter-arc hue path lands the 50% point on amber automatically, no separate midpoint token needed. Applied to `OmenOfTheWeek.jsx` `ConfidenceBar` (live Omen) and `DraftAssistant.jsx` `ConfidenceBar` (Draft Assistant card), both previously 3-step hardcoded gold fills. `Omen.jsx`'s `ConfidenceMeter` left untouched — confirmed dead code, not routed anywhere. Floor color was brightened mid-build from an initial `#5B1010` draft after contrast math showed 1.24:1 against the dark track (near-invisible); `#701020` improves to 1.45:1, still below WCAG 1.4.11's 3:1 non-text guideline — accepted tradeoff since both bars always print the score as redundant text, documented as a known limitation rather than silently passed. Evidence: `frontend/src/index.css`; `frontend/src/lib/confidenceGradient.js`; `frontend/src/pages/OmenOfTheWeek.jsx`; `frontend/src/pages/DraftAssistant.jsx`; spec `Blueprints/specs/page-system.md` Confidence Gradient (Phase 1.8) — Resolved section (full contrast table + tradeoff writeup); browser `color-mix` computed-RGB verification (floor/mid/ceiling exact values + contrast ratios in both themes) substituted for an authenticated screenshot — same Supabase `getSession()` sandbox limitation as Phase 1.5d/1.7, plus this session's `preview_screenshot` calls additionally timed out; full `npm test` 399 tests, 398 pass / 1 pre-existing CRLF failure (`test/deployHardening.test.js`, unrelated, documented since 2026-06-29); frontend build clean with pre-existing Vite chunk-size warning; `npm audit --audit-level=moderate` 0; `git diff --check` clean. Done docs: design + recommendation.
- [x] **Phase 1.9 — Metallic tier treatment (2026-07-01).** New `--color-tier-{gold,silver,bronze}` + `--color-on-tier-*` tokens in `frontend/src/index.css` (identical across themes — decorative/branded, not surface tokens); gold `#D4AF37` is deliberately brighter/more saturated than `--color-accent`. New `frontend/src/lib/metallicTier.js` exports `metallicTierStyle(rank)`: a `linear-gradient` sheen (light-tint → base → darkened, via `color-mix`) plus an inset bevel (`box-shadow`), returning `null` for rank 4+ so those keep the plain bordered circle. Applied only to the card-header ordinal pill in `DraftAssistant.jsx` `RecommendationCard` (the `rec.rank` circle) — the separate `Omen #N` ADP-footer pill is unchanged, per the locked "ADP / Omen #N footer" structure in Approved Patterns. Appearance-tile add-on out of scope for this pass. Contrast: gold/silver background vs. black text ≈10–11.5:1; bronze background vs. off-white text ≈5.1:1 — all comfortably clear AA, verified by hand WCAG-luminance math (rank is also redundant with the numbered card order and the `Omen #N` footer text, so this is decorative, not the sole signal). Evidence: `frontend/src/lib/metallicTier.js`; `frontend/src/index.css`; `frontend/src/pages/DraftAssistant.jsx`; `test/metallicTier.test.mjs` (2/2); full `npm test` 401/401; frontend build clean; `npm audit --audit-level=moderate` 0; `git diff --check` clean; browser `color-mix`/gradient computed-style check in both themes plus a synthetic rank-1..4 pill screenshot via `preview_eval`/`preview_screenshot` (dev server, unauthenticated — `/draft` itself remains behind the same Supabase `getSession()` sandbox limitation as recent phases); self-administered code review and AAA-framework UI/UX audit: merge, no P0/P1. Done docs: design.
- [x] **Phase 1.12 — Gray contrast pass + Standings refinements (2026-07-02, local — not pushed/merged/deployed).** Raised the `/account/appearance` intro paragraph and `/onboarding` success-step body copy from gray body text to `--color-text-primary`; strengthened `/standings` W-L / PF / PA columns to AA-safe values and upgraded the current-user row to a stronger team-accent surface tint plus a 4px team-accent left edge. The older `/hall-of-records` username target was stale by implementation time — Hall of Records had already been retired to `/ledger`, and no live username column remained to fix, so the closeout documents that as spec debt instead of inventing a replacement surface. Evidence: `frontend/src/pages/Appearance.jsx`; `frontend/src/pages/Onboarding.jsx`; `frontend/src/pages/Standings.jsx`; `Blueprints/specs/page-system.md`; `Blueprints/handoffs/backend-to-frontend.md`; `Blueprints/handoffs/2026-07-02-phase1-12-gray-contrast-standings-refinements.md`; full `npm test` 401/401; `npm --prefix frontend run build` clean with the existing Vite chunk-size warning; `npm audit --audit-level=moderate` 0; `git diff --check` clean. Self-administered `slops-ui-ux-audit` / `slops-code-review` verdict: merge, no P0/P1. Page/Design screenshot gates were not satisfied in this session because the touched routes are auth-gated or flow-gated and the existing Supabase session limitation still blocks routed captures here; substituted token-level contrast verification and route-source review, disclosed in the handoff rather than claimed as done. Done docs: page + design.
- [x] **Phase 1.13 — iOS Safari mobile QA sweep (2026-07-04 — complete).** Closed per Justin correction after PR #75 merged to `main`: public-route touch target / overflow fixes, Draft Assistant radiogroup conversion, cross-browser focus-trap/CSP Safari repairs, discrete team-palette repairs, and Cowork real Safari WebDriver production sweep evidence are accepted as satisfying the phase. The two Cowork P1s are no longer Phase 1.13 blockers: the raw-JSON 429 was fixed by scoping the API rate limiter, and Standings was triaged as a backend off-season contract gap now tracked separately under "Canonical off-season signal for dashboard + league standings." Deferred follow-ups remain separate backlog items, not Phase 1.13 blockers: `ConnectLeague.jsx` ESPN browser-selector tabs pattern, `AppearancePicker.jsx` 32-team grid 2D keyboard nav, Footer safe-area, missing 192×192 PWA icon, and the low-contrast Appearance card. Evidence: PR #75; `Solutions/reports/2026-07-02-mobile-qa-omen.md`; `Blueprints/handoffs/2026-07-02-phase1-13-mobile-qa-sweep-partial.md`; `Blueprints/handoffs/2026-07-03-phase1-13-mobile-qa-real-safari-sweep.md`; `Blueprints/handoffs/2026-07-03-phase1-13-remediation-csp-and-focus-trap.md`; `Blueprints/handoffs/2026-07-03-phase1-13-discrete-fixes-handoff.md`; `Blueprints/handoffs/2026-07-04-ratelimit-scope-and-standings-offseason-triage.md`. Done docs: page + design.

### Frontend — Phase 2
- [x] **Phase 2.9 — Account delete UI (2026-07-04; branch `codex/phase2-9-account-delete-ui`).** Exposed the mounted backend route `DELETE /api/user/delete` from `/account` under a Privacy subsection, with an accessible destructive confirmation dialog requiring the exact phrase `"DELETE MY OMEN DATA"`. Success signs out and redirects to `/login?deleted=true`; the login page shows a short completion notice. Copy says "Omen data" and explicitly excludes fantasy-platform/sign-in-provider deletion. Evidence: `frontend/src/lib/accountDeletion.js`; `frontend/src/pages/Account.jsx`; `frontend/src/pages/Login.jsx`; focused tests 4/4; full `npm test` 403/403; frontend build passes; root audit 0; `git diff --check` clean; security evidence `Direction/reviews/2026-07-04-phase2-9-account-delete-ui-security-privacy-evidence.md`; audits `Blueprints/audits/2026-07-04-phase2-9-account-delete-ui-code-review.md` and `Blueprints/audits/2026-07-04-phase2-9-account-delete-ui-ui-ux-audit.md`; handoff `Blueprints/handoffs/2026-07-04-phase2-9-account-delete-ui.md`. Done docs: feature + page + design + security.
- [x] **Phase 2.10 — Trade share card (2026-07-04; branch `frontend/phase2-10-trade-share-card`; implementation `4db9db0`).** Added a Share result control to Trade Analyzer, a public `/trade/share/:hash` card, server-side crawler meta injection, and `GET /api/trade/share/:hash/og.svg` SVG rendering. Public snapshot copy is recommendation-first and explicitly excludes connected-platform context, ESPN cookies, tokens, and private league data. Evidence: `frontend/src/lib/tradeShare.js`; `frontend/src/pages/TradeShare.jsx`; `frontend/src/pages/TradeAnalyzer.jsx`; `src/services/tradeShareMeta.js`; `src/services/tradeShareOg.js`; route/test updates; screenshots under `output/playwright/phase2-10-trade-share-card/`; focused share tests 9/9; full `npm test` 414/414; frontend build passed with existing warnings; audit 0; `git diff --check` clean; browser QA passed desktop/mobile light/dark + mobile share flow. Done docs: feature + page + design + recommendation + security.
- [x] **Phase 2.11 — FP1 signal-honesty labels (2026-07-05; branch `codex/phase2-11-signal-honesty-labels`).** Added a shared signal-label helper and visible `Input honesty` section to the shared Omen recommendation view so each backend `signals` input renders as Live / Stub / Mock / Unavailable with data-source tokens, plain copy, and a Used/Limited/Preview/Not-used cue. Backend/public demo `status: "demo"` display-normalizes to Mock / preview so sample inputs are never presented as live. Evidence: `frontend/src/lib/omenSignalLabels.js`; `frontend/src/pages/OmenOfTheWeek.jsx`; `test/omenSignalLabels.test.mjs`; audits `Blueprints/audits/2026-07-05-phase2-11-signal-honesty-labels-code-review.md` and `Blueprints/audits/2026-07-05-phase2-11-signal-honesty-labels-ui-ux-audit.md`; handoff `Blueprints/handoffs/2026-07-05-phase2-11-signal-honesty-labels.md`; browser evidence under `output/playwright/phase2-11-signal-honesty-labels/`; focused 2/2; full `npm test` 418/418; frontend build passed with existing warnings; root audit 0; `git diff --check` clean. No backend route, schema, auth, provider, package, env, SQL, deploy, push, merge, or production mutation happened. Done docs: feature + page + design + recommendation.
- [x] **Phase 2.12 — Trade Analyzer form redesign (2026-07-05; branch `codex/phase2-12-trade-form-redesign`).** Replaced player position dropdowns on `/trade` with QB/RB/WR/TE/FLEX/K/DEF button controls, added explicit scoring-format selection (`ppr` / `half_ppr` / `standard`) into the existing `POST /api/trade/compare` payload, added an honest Multi-team deal-shape mode that tells users to enter their net send/receive side from the full deal, added a desktop swap glyph, preserved autocomplete team metadata into payloads, added VORP help text, and moved the sidebar mock buy-low label onto `MockBanner`. No backend route, schema, auth, provider, SQL, package, env, deploy, push, merge, or production mutation happened. Evidence: `frontend/src/lib/tradeForm.js`; `frontend/src/pages/TradeAnalyzer.jsx`; `test/tradeForm.test.mjs`; audits `Blueprints/audits/2026-07-05-phase2-12-trade-form-redesign-code-review.md` and `Blueprints/audits/2026-07-05-phase2-12-trade-form-redesign-ui-ux-audit.md`; handoff `Blueprints/handoffs/2026-07-05-phase2-12-trade-form-redesign.md`; browser evidence under `output/playwright/phase2-12-trade-form-redesign/`; RED focused test failed on missing helper; GREEN focused/helper/share tests 5/5; focused trade contract tests 16/16; full `npm test` 419/419; frontend build passed with existing warnings; root audit 0; `git diff --check` clean. Done docs: page + design + recommendation.
- [x] **Phase 2.13 — Trade Analyzer Strategy + Mock Buy Low content rewrite (2026-07-05; branch `frontend/phase2-13-trade-strategy-copy`).** Justin's "SO SO" verdict addressed via two `slops-ux-copy` passes: (a) all 4 `TRADE_TIPS` bullets rewritten (Option C, headline-style) — Depth bullet no longer ends with "Build roster depth now"; (b) 3 of 5 Mock Buy Low `reason` lines rewritten to lead with the recommendation (Tyjae Spears, Sam LaPorta, Tank Dell), 2 left as-is on review (Zach Charbonnet, Rico Dowdle already cleared the bar). Discovered `tradePulse.js` has no data-refresh mechanism despite its own "updated each preseason" copy — filed separately below rather than fixed in this pass. Evidence: `frontend/src/pages/TradeAnalyzer.jsx`; `frontend/src/data/tradePulse.js`; handoff `Blueprints/handoffs/2026-07-05-phase2-13-trade-strategy-copy-rewrite.md`; browser snapshot verification; full `npm test` 421/421; frontend build clean with pre-existing warnings; `npm audit --audit-level=moderate` 0; `git diff --check` clean. Done docs: page + design; recommendation N/A (static strategy/mock copy, not a generated recommendation output).
- [ ] **Phase 2.14 — Standings team-switching UX.** Easier inter-platform team switching per Justin QA. Pairs with Phase 1.7 platform color emphasis. Cost: medium. Done docs: feature + page + design.
- [ ] **Phase 2.15 — Account subscription card removal (pre-launch hygiene).** Hide the Omen Pro "All features included" card on `/account` while Omen is free this season. Re-show when billing kill-switch flips. Cost: small (one conditional render). Done docs: page + design.
- [ ] **Phase 2.16 — IDP / defensive-player drafting prep.** Position chip palette (Phase 1.6) must already include DEF; this item carries the data + draft flow updates for leagues that draft defensive players. Cost: large (data layer + draft flow). Done docs: feature + page + design + recommendation.
- [x] **Phase 2.18 — Waiver Wire route activation (2026-07-05; branch `frontend/phase2-18-waiver-wire-activation`).** Omen is free indefinitely (facts-of-record 2026-06-15) so the existing `frontend/src/pages/WaiverWire.jsx` Pro gate was doctrine debt. (a) Added `<Route path="/waiver" element={<ProtectedRoute><AppLayout><WaiverWire/></AppLayout></ProtectedRoute>}>` to `routes/index.jsx`. (b) Added a `Waiver Wire` entry to the Header `Tools` nav section. (c) Removed the `<ProGate/>` branch and `proRequired` state — the `/api/optimizer/waiver` 402 path is unreachable while `OMEN_BILLING_ENABLED=false`; kept `TokenExpiredState` and `AuthGate`. (d) Migrated the page's local `PositionBadge` to the shared `positionChipStyle()` from `frontend/src/lib/positionChip.js`. (e) Swept every `slate-*`/`amber-*`/`emerald-*` Tailwind literal onto design-system tokens. (f) Added a `page-system.md` row (Page System Table + Motif/Moment table). Evidence: `frontend/src/routes/index.jsx`; `frontend/src/components/layout/Header.jsx`; `frontend/src/pages/WaiverWire.jsx`; `Blueprints/specs/page-system.md`; handoff `Blueprints/handoffs/2026-07-05-phase2-18-waiver-wire-activation.md`; full `npm test` 414/414; frontend build clean with pre-existing warnings; `npm audit --audit-level=moderate` 0; `git diff --check` clean; grep-confirmed zero remaining Tailwind color literals; unauthenticated browser check confirmed the route redirects correctly and the new nav item's `auth: true` gate hides it from guests. No new dependency, no backend/auth/schema change. Known gaps: no authenticated light/dark screenshot (same sandbox limitation as Phase 1.5d/1.7/1.8/1.12; `preview_screenshot` also timed out this session even for the public `/trade` page); Recommendation Done only partially satisfied since `/api/optimizer/waiver` still has no confidence/risk fields (pre-existing, out of scope). Done docs: feature + page + design; recommendation partial (documented gap).
- [ ] **Phase 2.19 — Win-streak reward ladder UI.** Upgrade Phase 1.5d's single-win pulse into the documented 1/2/3/4/5+ streak ladder (sparkle, flame, crown icon directions; escalating chip/row/header rewards) only after the backend exposes a real `currentWinStreak`-style dashboard field. Spec: `Blueprints/specs/page-system.md` Post-Win Pulse + Streak Ladder. **Blocked by Backend win-streak summary contract.** Cost: medium. Done docs: feature + page + design.
- [ ] **Phase 2.20 — Chant-render implementation (`teamChant.js` + `<ChantEyebrow>` + textures + feature flag).** Build `frontend/src/lib/teamChant.js` (returns `{ string, medium, cssTokens }` for a `(teamId, skin, placement)` triple), new `frontend/src/components/chant/ChantEyebrow.jsx` component, wire suppression rule against `usePlatformStatus()`, extend `frontend/src/lib/postWinPulse.js` for the celebratory-overlay chant, add `--chant-plaque-frame` / `--chant-graffiti-texture` / `--font-graffiti` CSS tokens, lazy-load Permanent Marker (Google) for Color Rush skin, ship behind `VITE_FEATURE_TEAM_CHANTS=true`. Spec: `slops-saloon/Blueprints/specs/chant-and-fan-copy-spec-v1.md` §9. Textures folder: `slops-saloon/omen/logos/textures/` (README ready; assets pending `slops-image-prompt` invocation). **Blocked by:** at least 6 teams' chants verified via `design-md-author` skill runs; initial texture assets authored. Cost: large. Done docs: feature + page + design + recommendation.

### Frontend — Phase 3
- [ ] **Phase 3.14 — Skeleton states for narration zones.** Math + numbers render instantly on KVM1; narration block shows skeleton until KVM2 returns. Cost: small. Done docs: page + design.

### Frontend — Phase 4
- [ ] **Phase 4.18 — Umami snippet** added to `frontend/index.html` once the container is live on KVM1. Cost: small (snippet add; soft-blocked on container). Done docs: feature + security + release when deployed.
- [ ] **Phase 4.19 — FP2 Sleeper/ESPN Omen render polish.** Ensure live envelopes render correctly across all three providers. Cost: medium. Done docs: feature + page + design + recommendation.
- [x] **Phase 1.14 — Deploy verification + prod C/logo audit (2026-07-04).** Confirmed Deploy to Hostinger KVM1 triggered and completed for PR #75 (`642edaf`, run `28672896614`) and PR #76 (`339da00`, run `28674663739`). Current prod is no longer stale by source evidence: `/` loads `/assets/index--4Oa572S.js`, the bundle references `omen-horizontal-lockup-transparent.png` 4 times, contains no baked-black lockup reference and no `[C]` literal, and `/omen-horizontal-lockup-transparent.png` returns `200` at the same byte length as the local public asset. Added a post-health deploy workflow check that fetches the production HTML, extracts the hashed Vite bundle, verifies the transparent lockup reference in that bundle, and checks the asset URL. Evidence: `.github/workflows/deploy.yml`; `test/deployHardening.test.js`; handoff `Blueprints/handoffs/2026-07-04-phase1-14-deploy-logo-verification.md`; RED focused deploy-hardening test failed on the missing workflow check; GREEN focused 3/3; full `npm test` 402/402; `npm audit --audit-level=moderate` 0; `git diff --check` clean. No deploy action, production mutation, package edit, secret access, or Docker/KVM config change happened. Done docs: release + recommendation.
- [x] **Phase 1.15 — Post-deploy visual smoke on prod (2026-07-05; local — not pushed/merged/deployed).** Added a post-health deploy workflow step that fetches `https://slopssaloon.com/`, `/about`, and `/login`, extracts each route's Vite bundle, and fails on `[C]`, missing horizontal lockup PNG, missing transparent lockup, or baked-black fallback references. Browser QA exposed that current prod `/login` was still text-only even though the bundle passed; `Login.jsx` now renders `/omen-horizontal-lockup-transparent.png` with `alt="Omen"`. Evidence: `.github/workflows/deploy.yml`; `frontend/src/pages/Login.jsx`; `test/deployHardening.test.js`; `test/loginBranding.test.js`; audits `Blueprints/audits/2026-07-05-phase1-15-post-deploy-visual-smoke-code-review.md` and `Blueprints/audits/2026-07-05-phase1-15-post-deploy-visual-smoke-ui-ux-audit.md`; handoff `Blueprints/handoffs/2026-07-05-phase1-15-post-deploy-visual-smoke.md`; screenshots under `output/playwright/phase1-15-post-deploy-visual-smoke/`; RED/GREEN focused tests 5/5; full `npm test` 416/416; frontend build passed with existing warnings; client build passed; root audit 0; `git diff --check` clean. No deploy action, production mutation, package edit, secret access, or Docker/KVM config change happened. Done docs: release + page + design.
- [x] **Logo suite swap addressed the `[C]` placeholder (2026-07-03, locally + in `main`, NOT yet in prod).** Replaced with the horizontal lockup PNG in `Header.jsx` main slot + NavDrawer top; also stripped circular emblem wrappers on Landing / OmenLanding. Not an inline SVG (design source unavailable) — used the horizontal lockup PNG at `frontend/public/omen-horizontal-lockup.png`; transparent variant landed on `frontend/transparent-lockup` branch for the light-theme rectangle P1. Doctrine: `slops-saloon/omen/Brand/brand-system.md` §12 Logo Usage; PR handoff: `Blueprints/handoffs/` (Codex handoff dated 2026-07-03). **If the `[C]` is still visible on prod, this indicates a deploy / cache issue, not a code gap — investigate deploy workflow status and browser/CDN cache.**

### Mobile / App Store Readiness
- [x] **Phase 4.20a — Mobile-build kill-switch layer (2026-07-08, local — not pushed/merged/deployed).** Added `VITE_APP_STORE_BUILD` build-time flag, mirroring the existing `VITE_BILLING_ENABLED`/`VITE_ESPN_ENABLED` per-file `import.meta.env` pattern (no shared module introduced). When set: `Account.jsx` suppresses `SubscriptionBanner` and `SubscriptionSection` (root cause of all "Omen Pro" copy on that page); `UpgradeState.jsx` (the app-wide Pro-upsell card) returns `null`; `PlatformConnections.jsx`'s `showEspnRow` and its `activeForm` initial state are short-circuited to false, closing the `espnRecovery` deep-link bypass; `ConnectLeague.jsx`'s `EspnCard` returns `null` before its existing `!ESPN_ENABLED && !connected` check, closing the "already connected" bypass. Yahoo/Sleeper cards untouched — confirmed structurally independent. No backend route/schema change: mirrors `VITE_ESPN_ENABLED`'s existing frontend-render-only posture (backend `/api/platforms/espn/connect` stays technically reachable by direct call, same as today). Evidence: build-output grep comparison — with `VITE_BILLING_ENABLED=true VITE_ESPN_ENABLED=true` and `VITE_APP_STORE_BUILD` unset, the built bundle contains "Connect ESPN", "Omen Pro", `espn/connect`, and Stripe-checkout strings (1 each); with `VITE_APP_STORE_BUILD=true` added, all four are 0 occurrences while "Yahoo" and "Sleeper" remain present (1 each). New regression test `test/appStoreBuildKillSwitch.test.js` (6/6 pass) guards both bypasses source-level via the root `node --test` runner (no frontend test harness exists in this repo). Full `npm test`: 201/271 pass pre-existing-environment failures unchanged (baseline on this branch before the change: 196/271; no new failures introduced — confirmed via `git stash` diff). Docs affected: `.env.example`, `deploy/hostinger/ENV-INVENTORY.md` (also backfilled the pre-existing undocumented `VITE_BILLING_ENABLED` entry in both). CI/deploy build-arg wiring intentionally deferred — not yet needed until actual mobile packaging (e.g. Capacitor) exists; noted as a known follow-up, not a silently-closed gap. Handoff: `Blueprints/handoffs/2026-07-08-app-store-kill-switch.md`. Done docs: design (UI-suppression) + feature.
- [x] **Phase 4.20b — Public legal/support pages (2026-07-13, local — not pushed/merged/deployed).** Added public `/privacy`, `/terms`, `/support`, and `/delete-account` pages with Phase 4.16 provider caveats intact, deletion copy tied to the existing Account flow, corrected no-Stripe wording, and monitored `support@slopssaloon.com`, `privacy@slopssaloon.com`, and `legal@slopssaloon.com` channels. Evidence: `Blueprints/handoffs/2026-07-13-phase4-20b-public-legal-support-pages.md`.
- [x] **Phase 4.20c — Reviewer / demo access documentation (2026-07-08, local — not pushed/merged).** Added `Blueprints/playbooks/app-store-reviewer-access.md`: send reviewers to the public, no-auth `/demo` route, which computes a real optimizer-backed recommendation from a deterministic fixture with no Supabase/platform/LLM calls (verified by direct execution of `buildDemoModeResponse()`, `state: "success"`). Documents `/trade` and `/draft` as secondary populated public surfaces, states plainly that the authenticated dashboard's live recommendation has no reviewer bypass (onboarding's `ConnectStep` has no skip option, no seeded demo backend account exists), and warns against relying on dev-only `?fixture=` params (stripped from production builds). Includes a QA checklist for cold verification. Added index entry to `Blueprints/playbooks/README.md`. Verification note: root `npm ci` fails in this sandbox on a pre-existing lockfile/dependency mismatch (`Missing: gcp-metadata@7.0.1`), so the documented flow was verified via direct source reading plus isolated execution of `buildDemoModeResponse()` rather than a live server click-through — disclosed as a known limitation, not silently skipped. Discovered, not fixed (out of this task's scope): `Blueprints/demo-mode.md`'s status line ("frontend `/demo` implementation pending") is stale — frontend shipped in Phase 2.7. Handoff: `Blueprints/handoffs/2026-07-08-app-store-reviewer-access.md`. Done docs: n/a — doc-only deliverable, no user-visible app UI/feature/security-boundary change.
- [x] **Phase 4.20d — Store metadata, privacy-label, and gambling/DFS copy audit (2026-07-12, local — not pushed/merged).** Grepped + hand-reviewed `Landing.jsx`, `OmenLanding.jsx`, `Onboarding.jsx`, and `README.md`. No betting/gambling/DFS/odds/wagering/guaranteed-winnings language found anywhere. Two high-severity gaps found under risk (b) — named platforms (Yahoo/Sleeper/ESPN) with support/"Live" claims and zero attribution/non-affiliation disclaimer: `Onboarding.jsx`'s ConnectStep "Supported platforms" card, and `README.md`'s MVP Move data-status table + Supported Platforms table. Both fixed by adding the Phase 4.16 packet's already-approved general clause verbatim ("Platform trademarks belong to their respective owners. Omen is not endorsed by or affiliated with those platforms.") directly beneath each. Landing.jsx's waitlist platform picker and "live tool" copy, and OmenLanding.jsx, reviewed and left unchanged — not claims surfaces / accurately describe Omen's own compute per the mock-vs-live labeling rule in `facts-of-record.md`. Findings table + full rationale: `Direction/reviews/2026-07-12-store-metadata-privacy-gambling-copy-audit.md`. App-store description/screenshots/social-preview copy doesn't exist yet — flagged as a follow-up for whoever drafts the Phase 4.20 store listing. `npm run build` clean; `npm test` (root) unchanged from pre-change baseline (183/253 pass — pre-existing `yahooAuthRoute.test.js` MODULE_NOT_FOUND failures, confirmed via `git stash` diff, unrelated to this change). Handoff: `Blueprints/handoffs/2026-07-12-phase4-20d-store-metadata-copy-audit.md`. Done docs: content-marketing (copy-only) + security-privacy cross-cutting gate (affiliation-disclaimer surface).
- [ ] **Phase 4.20 — App-store readiness hardening (integration + verification).** Priority: P0. Cost: large. Blocked-by: 4.20a, 4.20b, 4.20c, 4.20d, and the ESPN-posture + Stripe-mobile-posture Decisions items below. Done-when: mobile build has no external Stripe unlock path; mobile build has no ESPN cookie entry; public legal/support/delete pages render; store-copy review passes; provider affiliation disclaimers are present; privacy/data inventory matches actual backend behavior; no betting/gambling/DFS claims appear in app metadata or public app copy; account deletion copy and sprint guardrails no longer conflict (see Verify item below). Evidence required: closeout doc linking each bullet's evidence back to 4.20a–d, plus a fresh `npm test`/build/audit pass. Docs affected: `Direction/release_readiness.md` (add App-Store Readiness status section), this file, `Blueprints/handoffs/`. Source: `Direction/reviews/2026-07-05-app-store-mobile-readiness-sprint-proposal.md`.
- [ ] **Phase 4.21 — Swap public logo asset to canonical transparent lockup.** Priority: P1. Cost: small. Blocked-by: none. `frontend/public/omen-horizontal-lockup.png` is the flattened black-background export shipped by the 2026-07-03 logo-suite-swap (`Blueprints/handoffs/2026-07-03-logo-suite-swap-handoff.md`); it renders a visible black rectangle around the mark in light-theme app chrome (`/trade` header + drawer). `SLOPS/slops-saloon/omen/logos/omen-horizontal-lockup-transparent.png` already exists as the canonical replacement — same 763x248 dimensions, verified true RGBA with real alpha (0-255 range), no new asset production needed. Done-when: `Header.jsx`, `Landing.jsx`, and `OmenLanding.jsx` reference the transparent asset (copied into `frontend/public/`); light-theme `/trade` header and drawer no longer show a black rectangle around the logo; `npm test`/build clean; screenshot evidence in light and dark mode. Note: this duplicates in effect (but not in landed state) the "transparent-lockup PR" referenced at line 94 above and the Decisions-lane item below — that branch/PR, if it exists, was never merged; verify before starting to avoid redundant work. Docs affected: `frontend/src/components/layout/Header.jsx`, `frontend/src/pages/Landing.jsx`, `frontend/src/pages/OmenLanding.jsx`, `frontend/public/omen-horizontal-lockup.png` (replace or add transparent variant).

### Browser Extension / Tooling
- [ ] **EXT1 — ESPN Cookie Finder browser extension (read-only, local-only).** Small companion Chrome/Firefox extension that reads the user's own `espn_s2`/`SWID` cookie values locally from their browser (matches the existing "ESPN Cookie Finder" prior-art pattern) and lets them copy/autofill into Omen's existing `ConnectLeague.jsx` / `PlatformConnections.jsx` connect form. Reduces onboarding friction for the manual-copy step only. Priority: P2. Cost: medium (new small standalone extension package + store listing prep; not part of the main frontend build). Blocked-by: none — this does not require the ESPN public/mobile posture decision to resolve first, since it makes no calls to ESPN's servers and does not touch Omen's backend ESPN adapter. Done-when: extension reads cookies locally only (no remote server, no analytics/telemetry on cookie values, no league/roster data fetched by the extension itself); values are only ever copied/autofilled into Omen's own connect form, never logged or displayed anywhere else; extension is reviewed for store-listing requirements (privacy disclosure, permissions justification) before any submission. Evidence required: source review confirming zero network calls out of the extension; permissions manifest limited to cookie read + active-tab; a written note in the security tracker confirming no cookie value is ever transmitted by the extension. Docs affected: new extension package (location TBD, likely a sibling folder outside `frontend/`), `frontend/src/pages/ConnectLeague.jsx` / `frontend/src/components/platforms/PlatformConnections.jsx` (copy/UX pointer only, no functional change required), `Blueprints/security-privacy.md` (add a line noting the extension's read-only/local-only scope once built). Research: `Direction/reviews/2026-07-05-espn-community-api-and-extension-research.md`. Do not build the alternate variant (scraping rendered ESPN league data from the tab and forwarding it to Omen) under this item — that shares ESPN's automated-extraction ToS exposure and is gated by the separate "ESPN public/mobile posture" Decisions-lane item.

### iOS Native App (ESPN Relay Sync)

Thin native shell only — the ESPN-connect handoff, not a native reimplementation of Omen. See scope boundary in the spec. Mechanism confirmed live against a real account 2026-07-07: `Direction/reviews/2026-07-07-espn-ios-cookie-sync-research.md`.

- [ ] **Phase 5.1 — Expo project scaffold + EAS Build/Apple Developer credential wiring.** Initialize a new Expo (React Native) project at `ios-app/` (new top-level folder, sibling to `frontend/` and `extension/`) via `npx create-expo-app`; configure `eas.json`/`app.json` for iOS; wire Apple Developer Program credentials through `eas credentials` (interactive — Justin runs `eas login`/`eas credentials` himself, needs his Apple ID + 2FA). Spec: `Blueprints/specs/ios-espn-relay-app-plan-v1.md`. Done-when: `eas build --platform ios --profile development` produces a signed build artifact installable via TestFlight or ad-hoc, with no local Xcode step required. Cost: medium. Blocked-by: none (Apple Developer Program membership already active).
- [ ] **Phase 5.2 — ESPN login webview + injected-fetch relay mechanism.** Build a standalone screen using `react-native-webview` that loads `https://fantasy.espn.com` for login, then on demand runs `injectJavaScript` calling `https://lm-api-reads.fantasy.espn.com/apis/v3/games/ffl/seasons/{year}/segments/0/leagues/{leagueId}?view=mTeam&view=mRoster` with the `x-fantasy-platform: espn-fantasy-web` / `x-fantasy-source: kona` headers confirmed in the research memo, and relays the JSON response back to the app via `onMessage` — never extracts or stores the raw cookie. Spec: same. Done-when: a real ESPN login inside the webview followed by a relay call returns real private league JSON against a real account, with the raw cookie value never appearing in app state, logs, or storage. Cost: medium. Blocked-by: Phase 5.1.
- [ ] **Phase 5.3 — Relay POST integration with Omen's backend.** Wire the Phase 5.2 screen to `POST` the relayed JSON to a new backend relay-ingestion endpoint, which normalizes it via `src/adapters/espn.js`'s existing pure `standingsFromEspnData`/`teamFromEspnData`/`rosterFromEspnData` functions (already built, do not duplicate parsing logic). Spec: same + `Blueprints/api-routes.md` once the endpoint contract is added. Done-when: a successful relay updates the user's Omen-side ESPN connection state end-to-end against a real account, matching the shape the existing cookie-based connection produces. Cost: medium. **Blocked by: `platform_connections.connection_mode` schema addition, drafted on branch `claude/platform-connections-relay-mode` but not applied to the live database — needs Justin's explicit, separate apply-approval before this item is buildable — and the relay-ingestion endpoint itself, which depends on that column.**
- [ ] **Phase 5.4 — Thin-shell handoff back to the web app.** After a successful ESPN connect (Phase 5.3), the app's only other job is getting the user to the existing web product for everything else (Dashboard, Trade Analyzer, Omen of the Week, etc.) — explicitly not a native reimplementation of any existing screen. Spec: same. Done-when: post-connect, the app hands off to the real web app in one tap. Cost: small. Blocked-by: Phase 5.3.

### Verify (confirm done vs open — restate findings, do not assume)
- [ ] **V1 — First company-baseline skill-receipt pilot.** Apply the baseline without changing the selected item's product scope. Spec: `Blueprints/playbooks/omen-company-baseline.md` + `Blueprints/playbooks/skill-activation-runbook.md`. Cost: medium (pilot run + ledger entry, atop whatever item it wraps). Done-when: the task plan names selected skills and N/A reasons, applicable Done gates pass, and `Blueprints/playbooks/skill-usage-ledger.md` links the evidence.
- [ ] Resolve the doc conflict: does Sleeper/ESPN live Omen return `ready` or `pending_live_engine` for paid connected users? Settle to one truth and update docs. Cost: small (read + reconcile + doc update). Done docs: n/a — verification/doc-conflict item; evidence is updated contract path.
- [x] **Reconcile account-deletion guardrail vs. Phase 2.9 (resolved 2026-07-05).** Justin confirmed explicit approval of the Phase 2.9 account-deletion UX copy and confirmation phrase. Guardrails section reworded to reflect the feature is approved and shipped as built; it now only gates future changes to the deletion copy/phrase, not the feature's existence. Logged in `decision_log.md`.
- [ ] **Service-key Supabase route scoping audit.** `src/routes/platforms.js` and `src/routes/userPrivacy.js` both use a service-key client and manually scope every query by `user_id`; verify this holds for every other route using `config.supabaseServiceKey`, and confirm each has an existing cross-user-isolation test or gets one added. Priority: P1. Cost: medium. Done-when: a table (route file → query → scoping column → test reference) covering every service-key route in `src/routes/` is checked into `Direction/reviews/`; any unscoped query found is treated as a P0 bug fix, not closed silently. Research: `Direction/reviews/2026-07-05-app-store-mobile-readiness-sprint-proposal.md` §2.8.

### Decisions (Justin / Agents — not builds)
- [ ] `POST /api/optimizer/mvp-move` (`src/routes/optimizer.js`): merge into Omen as a Pro enrichment layer, or keep separate. Cost: small (recommendation memo).
- [x] **Merge `mvp-move` into Omen; retire the mock helper; ship recovery analytics — decided 2026-07-12 (see decision log).** Justin: merge `POST /api/optimizer/mvp-move` into Omen as a Pro enrichment layer taking the best of both, rather than keeping them separate. Follow-on build tracked as a new item below. `getOmenOfTheWeekMock()` and recovery-analytics timing to be resolved as part of that same merge design, not separately.
- [ ] **Build: merge `mvp-move` optimizer into Omen as a unified recommendation layer.** Priority: P1. Cost: large. Blocked-by: none — decision made 2026-07-12. Design a single Omen recommendation surface that combines `POST /api/optimizer/mvp-move`'s LLM-narrated single highest-value-action output with the existing Omen of the Week contract, rather than shipping two parallel recommendation systems. As part of this, decide and resolve inline: whether `getOmenOfTheWeekMock()` is retired outright or kept as an explicitly-labeled fallback for off-season/no-data states, and whether recovery analytics (tracking reconnect/recovery flows) ships as part of this merge or is deferred past first paid launch. Done-when: one documented recommendation contract in `Blueprints/api-routes.md`, no duplicate/competing recommendation route, `npm test`/build clean, decision log entry for the mock-retirement and recovery-analytics-timing sub-decisions made during the design pass. Docs affected: `src/routes/optimizer.js`, `src/routes/dashboard.js` (or wherever Omen of the Week lives), `Blueprints/api-routes.md`, `Blueprints/handoffs/`. Done docs: feature + recommendation + security.
- [x] **Marketing pillars — leave as provisional for now (decided 2026-07-12, see decision log).** DETECT THE SIGNAL / ANALYZE THE DATA / PREDICT THE OUTCOME / WIN WITH CONFIDENCE stays parked in `Brand/brand-system.md` §10a as provisional; still not to be used on marketing pages, ads, or in-app copy until a future explicit close-out.
- [ ] **Retire baked-black `omen-horizontal-lockup.png` fallback.** Priority: P2. Cost: small (delete file + audit for any remaining code references). Blocked-by: Phase 4.21 (swap public logo asset to the transparent lockup) landing in production first. "Proven in prod for a stable window" means: after Phase 4.21 ships, confirm for ~1-2 weeks of normal production traffic that no error reports, visual regressions, or cached-asset issues surface from the swap, then delete the old black-background PNG and grep for any leftover references. Not yet actionable — Phase 4.21 itself hasn't landed yet; do Phase 4.21 first, then this becomes a dated follow-up.
- [ ] **Audit external references to the old "Corvus" title** on `Omen/Direction/decision_log.md` — corrected 2026-07-03. Cost: small (grep + redirect any stale references).
- [x] **ESPN public/mobile posture — decided 2026-07-12 (see decision log).** ESPN stays available on mobile; users are routed to the existing browser-extension cookie flow via in-app link/instructions, with email-instructions as a fallback, rather than attempting native cookie capture on mobile. Build item tracked above under "Backend — Behind launch readiness" (ESPN mobile-connect onboarding).
- [x] **Stripe mobile posture — decided 2026-07-12, and superseded (see decision log).** Justin: Omen is free indefinitely; Stripe will not be used on this product at all (used on other projects, not Omen). This is broader than the original "web-only for now" framing — Stripe integration in this repo should be scoped for full removal, not just mobile-gating. Tracked as a new Tech Debt item below pending Justin's go-ahead to actually remove the code (payment integration is a Do-Not-Touch boundary; removal needs an explicit separate green light before building).
- [x] **Trade Analyzer Buy-Low mock data refresh — decided 2026-07-12 (see decision log).** Justin: build the real endpoint now. Tracked as a new build item above under "Backend — Behind launch readiness" (`GET /api/trade/pulse`).

### Design docs (Claude authors via `design-md-author` skill)

Every item below invokes `design-md-author` per `Blueprints/skills/design-md-author/SKILL.md`. Inputs common to all: `slops-saloon/Direction/decisions/slops-saloon-fan-experience-doctrine-v1.md`, `slops-saloon/omen/Brand/brand-system.md`, `slops-saloon/omen/Blueprints/specs/omen-ux-ui-design-system-v1.md` (v2 reconciled). Do NOT hand-author `design.md` files without the skill — it enforces template compliance and doctrine inheritance.

- [ ] **Per-team `design.md` — priority tier (Eagles / Cowboys / Chiefs).** Additional inputs: `slops-saloon/Blueprints/specs/team-colorway-system-spec-v1.md` §7 + `chant-and-fan-copy-spec-v1.md` §6 + the existing `-colorway.md` stubs in `slops-saloon/Blueprints/specs/teams/` as reference material. Output: `[team-slug]-design.md` in that folder. Cost: medium (three teams, each full design.md per skill template). Done docs: design. **Blocks retirement of `-colorway.md` stubs.**
- [ ] **Per-team `design.md` — extended roster (29 teams in 8 batches × 4).** Same skill + inputs, batched per `slops-saloon/Blueprints/specs/teams/_batch-tracking.md` regional clustering. Each batch requires fan of at least one team to verify Region Test per colorway spec §3 Test 2 BEFORE the skill is invoked. Cost: large. Done docs: design per team.
- [ ] **Retire the priority-tier `-colorway.md` stubs.** After the corresponding `-design.md` files land, delete the `-colorway.md` files and update `_batch-tracking.md` references. Cost: small.
- [ ] **`design.md` per Omen room mode — Owner Suite (Omen) / GM Suite (Trade Analyzer) / Locker Room (everything else).** Three docs per the fan-experience doctrine three-room mapping. Additional input: whichever room-mode implementation spec exists at author time (currently pending — see Task #15). File at `slops-saloon/omen/Blueprints/specs/design/room-owner-suite-design.md`, `room-gm-suite-design.md`, `room-locker-room-design.md`. Cost: medium. Done docs: design. **Feeds the room-mode implementation spec.**
- [ ] **`design.md` per Omen page-family.** Landing, OmenLanding, Dashboard, Trade Analyzer, Omen (paid), Draft Assistant, Standings, Ledger, Football, Waiver, Account, Sign-In / Connect. Each design.md defines page-level design intent + copy anchors + component states + accessibility notes. File at `slops-saloon/omen/Blueprints/specs/design/page-[name]-design.md`. Batchable at 3 pages per pass. Cost: large. Done docs: design + page per file. **Feeds `slops-saloon/omen/Blueprints/specs/page-system.md` refinements.**
- [ ] **`design.md` for cross-team components with team-theming touch points** — RecommendationCard, ConfidenceMeter, RiskBadge, StatusBadge, DataSourceLabel, MockBanner, appearance picker tile, ChantEyebrow (when built). Each needs a component-level design.md that documents which surfaces accept team color vs. which are data-legibility-invariant (per fan-experience doctrine). File at `slops-saloon/omen/Blueprints/specs/design/component-[name]-design.md`. Cost: medium. Done docs: design + page.

### Specs (fourth downstream spec pending from fan-experience doctrine)

- [ ] **Room-mode implementation spec** — author the fourth downstream spec named in `slops-saloon/Direction/decisions/slops-saloon-fan-experience-doctrine-v1.md` under "What this doctrine does not decide." Encodes how Owner Suite (Omen) / GM Suite (Trade Analyzer) / Locker Room (everything else) render as distinct token + texture modes on top of the reconciled design system. Blocks Codex work of rendering distinct room modes. Inputs: fan-experience doctrine + design-system v2 + room-mode `design.md` files (see Design docs lane). File at `slops-saloon/omen/Blueprints/specs/room-mode-implementation-spec-v1.md`. Cost: medium. Done docs: design + recommendation.

### Tech debt (later, low priority)
- [x] **Remove Stripe billing integration from Omen entirely (2026-07-12, local — not pushed/merged).** Justin confirmed full backend+schema removal scope. Deleted `src/routes/stripe.js`, `src/middleware/subscription.js`, `src/services/subscription.js`, both test files wholly about them (`test/stripeRoute.test.js`, `test/subscriptionMiddleware.test.js`), and `test/config.test.js` (billing-config-only). Removed the `stripe` npm package from root `package.json`/lockfile (root `npm audit` now 0 vulnerabilities, down from 5). Unmounted `/api/stripe/*` from `src/server.js`; removed `config.stripe`/`config.billing` from `src/config/index.js`; removed the `requireSubscription` gate from `/api/optimizer/*` and the `omen_subscription_required` 402 from `POST /api/omen/mvp-move` (both are auth-only now, no paywall). `GET /api/dashboard/summary` no longer returns a `subscription` field; `tools.omen_of_the_week`/`tools.waiver_wire` are `mode: "free"` with no `needs_subscription` status. `src/routes/userPrivacy.js` export/delete no longer touch a `subscriptions` table. Deleted dead-code `UpgradeState.jsx` (zero importers even before this change) and removed the entire `SubscriptionSection`/`PlanPicker`/`ActiveSubscription`/`SubscriptionBanner` UI from `Account.jsx`. Schema: `sql/omen_rls_security.sql` now has explicit `drop table if exists public.subscriptions cascade;` and `alter table public.users drop column if exists is_subscribed;` at the top — **these do not auto-apply to production Supabase; someone must run the updated script against prod separately, with Justin's sign-off**, since this session has no DB-execution capability. Updated 8 test files to match the new free-for-all contract; full `npm test` 388/388 pass, frontend build clean. Also updated living docs (`Blueprints/api-routes.md`, `Direction/facts-of-record.md`, `README.md`, `.env.example`, `deploy/hostinger/ENV-INVENTORY.md`) and appended removal notes to the two append-only handoff logs rather than rewriting history. Left `client/` (a separate legacy duplicate app) untouched per explicit scope decision. Docs affected: see above. Done docs: feature + security + release (release gated on the production SQL drop being run separately).
- [ ] 5 code TODOs: Yahoo per-player projected stats (`adapters/yahoo.js`); `rest_days` / `back_to_back` (`services/agents.js`); weather game-time interval (`services/weatherService.js`). Cost: medium (5 TODOs across 3 files; pick off individually). Done docs: feature + recommendation where recommendation behavior changes.
- [ ] Omen latency optimization: `POST /api/omen/mvp-move` p95 ~5s under repeated local load. Investigate LLM call caching, response streaming, or request coalescing. Not a launch blocker — route is functional. Cost: medium (investigation + targeted fix). Done docs: feature + recommendation.

## Guardrails

- Do not recreate `Omen/`.
- Do not touch `.env`, secrets, DNS, SSL, Nginx, Supabase migrations, Stripe production behavior, or package files without explicit approval.
- Do not deploy unless Justin explicitly approves the deploy action.
- Docs/doctrine/CI-config-only commits must not be treated as app deploys; production deploys stay on `.github/workflows/deploy.yml`, filtered pushes must not restart KVM1, and `workflow_dispatch` remains the manual replay path.
- ESPN must never log or display cookie values.
- Mock data must be clearly labeled and never presented as live fantasy advice.
- Account deletion is approved and ships as built in Phase 2.9 (`/account` → Delete Omen data, confirmation phrase `"DELETE MY OMEN DATA"`) — Justin approved the UX copy 2026-07-05. This guardrail no longer gates the feature; it now just means don't change the deletion confirmation copy or phrase without a fresh approval note here.
