# Omen Skill-Usage Ledger

## Purpose

Prove which skills are doing useful work and expose unused, skipped, or weak procedures. This ledger complements `../done/LEDGER.md`: the Done ledger proves the product gate; this ledger proves the operating method.

## Entry Shape

| Date | Task/milestone | Skill | Result | Evidence | Procedure gap / next correction |
|---|---|---|---|---|---|
| 2026-09-03 | W1-A ESPN in-app connect on iOS + Android, league discovery, Account disconnect, deletion phrase | Native mobile read gate **applied**; repository TDD **applied and load-bearing** (spike-before-port on both platforms; a control test caught a false negative); `pre-build-research` **applied** — ESPN's fan API verified against its own production bundle rather than a third-party repo; `slops-native-sim-drive` **substituted** by real-device driving by the founder plus emulator runs; `engineering:code-review` **not invoked** — founder-directed single sitting with per-change test evidence instead | PARTIAL — flow works end to end on a real iPhone and at parity on Android; **W1-A acceptance NOT met** (zero-emitted-bytes proof, and Android never run against a real ESPN account) | Founder device test: *The Titans of Slopsilonia* connected from a phone, no computer. Commits `b2f348a` → `8e9ae4e`. 957 backend / 398 iOS / 238 Android unit, each with one pre-existing failure named in the handoff. `HttpOnlyCookieSpikeTests.swift` and `HttpOnlyCookieSpikeTest.kt`. `Blueprints/handoffs/2026-09-03-espn-in-app-connect-both-platforms.md` | **TDD earned its place twice and the same lesson repeated four times.** (1) The iOS cookie spike's first run reported "not readable" and was **wrong** — only its control test caught that the store had silently dropped every write; without it this session files a confident false negative confirming what everyone believed. (2) Three ESPN entry URLs were guessed from client-rendered markup and all three failed on a real device; the fix was to stop depending on landing anywhere, which is what the desktop helper had done since it shipped. **The correction to keep: when a claim rests on someone else's system, read that system's own code — ESPN's production bundle settled in minutes what two months of inference got backwards, and exposed two live defects in our parser on the way.** (3) Two CI gates were red or absent with nothing reporting them (`nativeMobileScaffold` on an unpushed commit; **no workflow runs Android unit tests at all** — now `W1-ANDROID-CI`), the same "a check nobody runs does not exist" defect this ledger flagged on 2026-09-02 and which has now recurred. |
| 2026-09-02 | Skills wave (9 authored), sprint reconciliation, agent-docs/kickoff contract, and an unblocked `main` | `slops-skill-author` conventions and `_template/SKILL.md` **applied** — every new skill carries full frontmatter mapping 1:1 to its routing row; `slops-intent-capture` **authored, not yet run**; `pre-build-research` **N/A** — no new external data source; `slops-tdd` **substituted** by writing `test/issueStateConflicts.test.js` alongside the checker fix rather than ahead of it, because the fix was to an existing script whose failure mode was already known from real CI output; `slops-mobile-smoke` and `mobile-first-qa-playbook` **deliberately not invoked** and instead re-scoped web-only | PASS — 6 PRs merged; no product code path changed | omen `main` `c620ed4`, `93e3a85`, `c177df0`, `17806cf`; Slops-OS PRs #18/#19/#20; backend 941/941 on the merged head; `npm audit` 0 vulnerabilities; `check-kickoff-drift.js` passes **and fails correctly on injected drift**; `check-valor-brain.mjs` 2/2; `Blueprints/handoffs/2026-09-02-skills-wave-sprint-reconciliation-and-docs-contract.md` | **The ledger's own premise was proven twice over.** Nine skills existed only as a proposal for most of this session because the backlog file had no route into work — the same defect that left the ESPN webview research unqueued for eight weeks. Second: `main`'s quality gate had been red for over two hours and nothing reported it; it surfaced only because a docs PR incidentally touched `test/**`. **Two procedure gaps to close: (1) research and proposals need a required terminal state — now `slops-intent-capture`, unproven until its first real run; (2) nothing watches `main`'s own gate between merges, and a scheduled or post-merge signal would have caught the audit failure in minutes.** Also: two claims this session were wrong for one shared reason — tool output read, conclusion drawn, source unchecked — so the rule that a negative search result is unproven now lives in the skill and in `scripts/checks/README.md`, not only in a handoff. |
| 2026-08-31 | The bar audit — device testing, map + strategy, 17 founder rulings, failed Tier 0 attempt | `design` (canvas); `artifact-capabilities`; native mobile read gate; repository git-flow substitutes; manual debug procedure | PARTIAL — audit, map, strategy and rulings complete; Tier 0 fix NOT achieved and reverted | `Direction/reviews/2026-08-30-omen-bar-audit-and-strategy.md` (34 findings, §12–§19 rulings, §20–§21 self-corrections); `Blueprints/handoffs/2026-08-31-the-bar-audit-and-tier-0-attempt.md`; `design/command-center/`; both apps built and hand-driven (iPhone 16 / iOS 26.5, API 36 emulator); live API probes including a verified 30-then-429 rate limit | **The method failure is the correction worth keeping: six hypotheses were tested inside a large SwiftUI view when a minimal reproduction (TabView → ScrollView → two TextFields) would have bisected it in one.** A debug procedure should mandate a minimal repro before the second hypothesis, not the seventh. Also: device testing found 8 defects six prior source-only passes missed, and the founder's own bug report was right about the experience and wrong about the cause — reproduce the experience, never the explanation. |
| 2026-08-26 | A7B Phase 4 production infrastructure, backup/recovery, readiness, and activation | repository TDD/quality/git-flow; `engineering:code-review`; `supabase:supabase`; security/alerting procedure | PASS | Founder-approved controlled correction fixture added and exercised on KVM1: real capture/acceptance/correction machinery produced one changed subject with `supersedes`, 0 database writes, and permanent non-upstream labeling. Post-fixture A4 read one real pending row with zero writes and all formats passing; evaluator zero blockers. Exact-witness publication receipt emitted and cron scoring flag enabled after preflight. | The fixture is explicitly rehearsal evidence, not a claim that nflverse corrected data. The minimal A6 DDL added only cron-read fields; no row rewrites. No new dependency or source was added. |
| 2026-08-25 | A7B Phase 3 — local staging shadow and recovery drills | `operations:runbook`; `engineering:testing-strategy`; `engineering:code-review`; repository TDD/quality/git-flow substitutes | PASS for the bounded Phase 3 slice; A7B remains READY before A4/production readiness | `Blueprints/playbooks/football-data-staging-shadow-runbook.md`; `Direction/reviews/2026-08-25-a7b-phase3-code-review.md`; `Blueprints/handoffs/2026-08-25-a7b-phase3-staging-shadow.md`; focused 9/9; full 688/688; exact live artifact staged/recovered; seven synthetic failure scenarios pass | Testing strategy forced explicit infrastructure-failure and recovery cases; code review corrected exact-byte hashing and pre-write root overlap. Runbook makes remote-host non-proof explicit. Live KVM1/Pi and alert delivery require separate approval/evidence. |
| 2026-08-25 | A7B Phase 2 — canonical normalization and scoring acceptance | `data-analytics:validate-data`; `engineering:code-review`; repository TDD/quality/git-flow substitutes | PASS for the bounded Phase 2 slice; A7B overall remains READY and founder-gated before Phase 3 | `Blueprints/handoffs/2026-08-25-a7b-phase2-scoring-acceptance.md`; `Direction/reviews/2026-08-25-a7b-phase2-code-review.md`; exact four-week artifact under `/private/tmp/omen-a7b-live.psGF8Z`; focused 21/21; full backend 679/679; independent recomputation 0/0/0 mismatches; audit 0; diff clean | Data validation made the common-lineage confidence limit explicit and added an independent artifact validator. UI runner and client/design skills were N/A. Fresh-worktree dependency-runtime guidance remains the same Phase 1 procedure gap; no install/package change was made. |
| 2026-08-24 | A7B Phase 1 — local raw vault and exact-manifest replay | `run-slops-saloon` (kickoff/runtime routing only); `engineering:code-review`; substantive repository substitutes for unavailable `slops-repo-inspector`, `slops-tdd`, `slops-data-ingest-plan`, `slops-git-flow`, `slops-quality-baseline`, `slops-code-review`, and `security-privacy-evidence` | PASS for the bounded Phase 1 slice; A7B overall remains READY and founder-gated before Phase 2 | `Blueprints/handoffs/2026-08-24-a7b-phase1-raw-vault.md`; `Direction/reviews/2026-08-24-a7b-phase1-code-review.md`; real temporary capture/replay under `/private/tmp/omen-a7b-live.psGF8Z`; focused 10/10; full backend 668/668; audit 0; diff clean | The UI driver was N/A after routing because there is no UI. Fresh worktrees do not carry the original checkout's untracked `node_modules`; the valid full test used the existing dependency runtime through `NODE_PATH`, with no install/package change. Add that pattern to the worktree procedure. |
| 2026-08-24 | A6/A7 full-league scoring contract foundation | `engineering:architecture`; `anthropic-skills:pre-build-research`; `engineering:code-review` | PASS with deliberate non-merge boundary — ADR, source-rights decision, pure fail-closed engine, and review-only SQL are complete; provider capture/reconciliation is not claimed. | `Direction/reviews/2026-08-24-full-league-scoring-contract-adr.md`; `Direction/reviews/2026-08-24-a7-source-rights-research.md`; `Blueprints/handoffs/2026-08-24-a6-a7-full-league-scoring-contract.md`; `npm test` 658/658. | The research skill’s legacy Corvus naming is superseded by the Omen repo context. A full provider integration remains blocked by rights/entitlement and requires a separate A7B implementation approval; no collector, credential, dependency, SQL application, or deploy was introduced. |
| 2026-08-24 | ESPN full-scoring capability disclosure | native mobile read gate; `engineering:documentation` | PASS — server-owned disclosure reaches existing web, iOS, and Android SignalList without a new provider integration or UI pattern | Focused Node 63/63; Android `:app:testDebugUnitTest --tests '*OmenDecisionTest*'`; iOS mapping evidence recorded in the handoff. | The existing ESPN adapter requests `mSettings`, but its current normalizer extracts only the reception rule. The disclosure therefore explains the complete-rules/final-reconciliation limitation; it does not imply that receptions represent football scoring. No deployment or provider-access expansion occurred. |
| 2026-08-20 | Valor Brain metadata v1 cross-layer ratification and Omen adoption | `slops-repo-inspector`, `slops-context-markdown`, `slops-tdd`, `graphify` | PASS — local contract and real O2 adoption verified | `Blueprints/handoffs/2026-08-20-valor-brain-metadata-v1.md`; local profile 1/1, focused 2/2, full backend 572/572; schema and validator hashes match L0. | Graphify proved its checked-in outputs are stale, so v1 does not claim graph ingestion. No new skill was created: one schema, resolver, and validator are sufficient until a second domain proves reusable modeling or wayfinding behavior. |
| 2026-08-17 | O1b — GlitchTip (Sentry-class) error tracking, deployed on Command Center | `slops-repo-inspector`, `pre-build-research`, `security-privacy-evidence`, `slops-context-markdown` | PASS — closed with direct evidence, not inference | Deployed stack + deliberate-error proof (stored/grouped/queryable with full stack trace in <1s, checked directly in Postgres) in `Blueprints/handoffs/2026-08-17-o1b-glitchtip-error-tracking.md`; `Direction/current_sprint.md` (O1b closed, O6 partially unblocked), `Direction/sprints_completed.md`, `Direction/decision_log.md`. | `self-hosted-observability-runbook` is named in O1b's own `Skills:` line and in the routing matrix, but no canonical skill file for it exists anywhere in this repo (`find` across the tree returned nothing) — followed its stated intent (health/config evidence, deploy gated) with nothing to check against. Worth actually authoring one; this was exactly the kind of task it's meant to cover. `slops-git-flow`/`slops-tdd` N/A: no repo code changed — the deployment lives entirely on Command Center's own filesystem, outside this git tree. |
| 2026-08-13 | Native Command Center UI parity: full-screen iOS, contextual account, Ledger Preview, League Pulse | `figma:figma-use`, `figma:figma-swiftui`, `figma:figma-design-to-code`, `run-slops-saloon`, `computer-use:computer-use` | PASS | Read-only Figma inventory and design context confirmed nodes `31:2`, `34:2`, `72:2`, and `74:2`; no Figma write was needed. Xcode 26.6 passed 123/123 tests and a signed physical-iPhone build/install/launch. Android API 36 passed 4/4 targeted instrumentation, primitive enforcement, and assembly. Live captures proved modern full-screen iOS, displayed account controls, a distinct League glyph, and the approved lower compositions. Evidence: `Blueprints/handoffs/2026-08-13-native-ui-parity-command-center.md`. | The web-oriented run skill was used only for its repository QA discipline; native evidence came from Simulator, ADB, Compose tests, and the physical iPhone. Scripted Simulator scrolling exposed the lower SwiftUI nodes in accessibility but did not move the nested shell visually; Android supplied durable framebuffer review of both lower compositions. No correction to Figma was justified. |
| 2026-08-12 | R3-BUILD-iOS local Mac and physical-device bring-up reconciliation | `engineering:code-review` | PASS with shared-scheme change separated | Classified the complete dirty tree; verified Automatic Signing/team/bundle settings, unsigned CI overrides, Xcode 26 URL parsing, narrow ignores, one physical-device development build, and 108/108 simulator tests. Evidence: `Blueprints/handoffs/2026-08-12-r3-build-ios-local-device-bring-up.md`. | The shared scheme rewrite predates the intentional bring-up edits and remains separate. No entitlement, capability, auth-flow, archive, upload, TestFlight, production, or secret action was reviewed as complete. |
| 2026-08-12 | R3-BUILD-iOS local Mac and physical-device bring-up reconciliation | `engineering:documentation` | PASS; R3 remains READY | Appended dated status/evidence without rewriting history; updated build-environment/auth context and release readiness; recorded branch `main` with no commit/PR/deploy claim. Evidence: `Direction/current_sprint.md`, `Direction/decision_log.md`, `Direction/release_readiness.md`, and the dated handoff. | The local DoD command runs on Xcode 26.6 while its exact-equivalence rule pins Xcode 16.2. The records preserve that mismatch rather than weakening the gate. UI/Figma/provider/Supabase/ship/canary skills were N/A because those scopes did not change. |
| 2026-08-02 | M4-CC-WaiverWatch native composition + wiring | `planning-pass`, `slops-tdd`, `slops-quality-baseline`, `slops-code-review`, `slops-git-flow`, `figma-swiftui`, `figma:figma-design-to-code`, `run-slops-saloon`, `slops-mobile-smoke`, `slops-ui-ux-audit`, `slops-ux-copy`, `slops-design-system-pack` | PASS with iOS and visual-capture gaps recorded | Read the approved Figma node and native gates; implemented only approved Omen primitives on both platforms. RED: Android connected test failed while the placeholder remained. GREEN: 2 connected tests on `omen-api36`, `:app:assembleDebug`, and primitive enforcement passed. Baseline: 506/506 backend tests, moderate audit 0, frontend build, diff check. `slops-code-review` verdict: no release-blocking finding; the not-connected message deliberately does not invent a provider/onboarding action. Evidence: `Blueprints/handoffs/2026-08-02-m4-cc-waiver-watch.md`. | `run-slops-saloon` and `slops-mobile-smoke` are web-driver procedures, so native Android emulator evidence substituted. `slops-ui-ux-audit` was conducted against Figma tokens and semantics, but the emulator framebuffer produced a blank PNG; no pixel-perfect screenshot claim. iOS Xcode/CI unavailable on Windows. `slops-ux-copy` confirmed the exact Figma state copy. Skill improvement: no correction needed; record a blank-framebuffer condition separately from hierarchy/device-test evidence. |
| 2026-08-02 | B3 nflverse Tuesday scoring and production no-write verification | `slops-data-ingest-plan`, `slops-sports-math-intelligence`, `slops-tdd`, `supabase`, `github`, `slops-git-flow`, `slops-context-markdown` | VERIFIED with deferred pre-season behavior | PRs #260/#261/#262; deploy run 30754635716; focused cron 10/10; full `npm test` 494/494; moderate audit 0; process-only production dry run made no Supabase or Redis writes. | The source has not published `player_stats_2026.csv`; it produced one expected unavailable-stat failure. Issue #263 owns an explicit deferred state before persistent scoring enablement is reconsidered. No correction needed to the no-write guard; improve pre-season source handling. |
| 2026-07-28 | M0-BE-3 Yahoo native-return evidence landing | `slops-repo-inspector`, `slops-context-markdown`, `pre-build-research`, `slops-quality-baseline`, `slops-code-review`, `slops-git-flow`, `security-privacy-evidence`, `rbac-risk-review` | PASS (local; PR-ready) | Existing implementation `dbafc66`; focused Yahoo route 9/9; full backend suite 422/422; `Blueprints/handoffs/2026-07-25-m0-be-3-yahoo-return-reconciliation.md`. | No native UI/provider implementation, Figma, device, or release skill applies. Yahoo’s official guide was rate-limited during this reconciliation; RFC 8252 supports the browser and private-scheme boundary, while provider-console/device proof remains human-gated. |
| 2026-07-28 | Security hardening landing | `slops-repo-inspector`, `slops-tdd`, `slops-quality-baseline`, `slops-code-review`, `slops-git-flow`, `security-privacy-evidence`, `rbac-risk-review`, `supabase` | PASS (merged PR #232) | `5fdb2f3`; `Blueprints/handoffs/2026-07-28-security-hardening-landing.md`; `Direction/reviews/2026-07-28-security-hardening-landing.md`; `npm test` 422/422, build, moderate audit 0, diff check clean. | UI/native/legal/release skills N/A. The original RED evidence was retained from the isolated branch; current branch verification is GREEN-only. RLS source is deliberately not a production policy mutation. |
| 2026-07-27 | M4 Help + Support approval recording | `figma-use`, `slops-git-flow` | PASS | Justin approval reflected on Figma nodes `61:2`, `63:2`, and `63:26`; rendered verification of node `61:2`; `Blueprints/handoffs/2026-07-27-m4-help-support-approval.md`. | Native implementation, app/device QA, security/provider, and release skills remain a separate upcoming task; no executable or production behavior changed here. |
| 2026-07-27 | M4 Help + Support contract and proposal | `planning-pass`, `figma-use`, `figma-generate-design`, `figma-generate-library`, `slops-git-flow` | PASS as proposal | Contract plus Figma nodes `61:2`, `63:2`, `63:26`; `Blueprints/handoffs/2026-07-27-m4-help-support-contract.md`. | TDD, quality baseline, code review, UI/mobile app smoke, security/provider, and release skills N/A: no code, API, data flow, rendered app UI, provider, credential, or release change. Figma inspection and visual review are the appropriate evidence; implementation remains approval-gated. |
| 2026-07-13 | Orphaned GDPR module retirement | `slops-repo-inspector`, `slops-git-flow`, `security-privacy-evidence`, `slops-code-review`, `slops-quality-baseline`, `slops-context-markdown` | PASS | `26441a1`; dated security evidence, review, and handoff | TDD/UI/provider/release skills N/A: unmounted deletion with no user-facing or contract change |

Use one row per invoked skill. Record a skipped required skill as `SKIPPED` with the reason. Do not add rows for conditional skills that were genuinely unrelated; list those in the task's skill receipt instead.

## Active Log

| 2026-08-02 | B2-D-E3 ESPN drafted-league provider proof | `run-slops-saloon`, `supabase`, `security-privacy-evidence`, `slops-git-flow` | PASS (read-only provider evidence; local docs branch) | Founder-authorized uniquely recent connection; roster HTTP 200 with 10 populated teams and 160 held players; pool HTTP 200 with 500 entries, 0 roster leaks, 0 non-zero `onTeamId`, and 0 disagreements. Evidence: `Blueprints/handoffs/2026-08-02-b2d-e3-espn-live-proof.md`. | Chrome control was unavailable, so the browser-origin protocol was substituted by equivalent server-side ESPN reads using the existing Vault session in memory. No app code, SQL, dependencies, transaction, deployment, or production mutation. |
| 2026-08-02 | B2-D-E2 ESPN canonical waiver wiring | `slops-repo-inspector`, `slops-tdd`, `security-privacy-evidence`, `slops-quality-baseline`, `slops-code-review`, `slops-git-flow` | PASS (local stacked branch) | Commit `0ad2cc6`; three intended REDs; focused service/route 37/37; full `npm test` 500/500; moderate audit 0; diff check clean; security evidence and handoff recorded. | `planning-pass` N/A because E2 was already ratified. Data-ingest, UI/mobile, and release skills N/A: existing on-demand provider path, unchanged schema/client, no release. Review caught provider popularity order and required projection ranking before commit. |
| 2026-08-02 | B2-D-E1 ESPN waiver-pool adapter | `slops-repo-inspector`, `planning-pass`, `slops-tdd`, `security-privacy-evidence`, `slops-quality-baseline`, `slops-code-review`, `slops-git-flow` | PASS (local adapter boundary) | Commit `2748a5c`; RED missing-normalizer proof; focused adapter 21/21; full `npm test` 496/496; moderate audit 0; diff check clean; security review and handoff recorded. | Data-ingest planning N/A: on-demand existing ESPN read with no storage. UI/mobile/release N/A: no caller, UI, provider proof, or deployment. No correction needed: a fixture-only seam protected credential and recommendation boundaries. |
| 2026-07-23 | P0 Supabase Vault permission remediation | `slops-repo-inspector`, `supabase`, `security-privacy-evidence`, `slops-code-review` | PASS | Live migration `20260723235513`; `Direction/reviews/2026-07-23-p0-supabase-vault-permission-remediation.md`; `Blueprints/handoffs/2026-07-23-p0-supabase-vault-permissions-remediation.md`; focused security SQL tests 9/9 | `rbac-risk-review` N/A: no agent or tool authority changed. `slops-tdd` substituted by direct production privilege verification plus a new source-regression test. No deploy occurred, so ship/canary N/A. |

| Date | Task/milestone | Skill | Result | Evidence | Procedure gap / next correction |
|---|---|---|---|---|---|
| 2026-07-25 | M0-BE-3 Yahoo mobile-aware return reconciliation | `pre-build-research`, `workflow-tree-spec`, `security-privacy-evidence`, `slops-tdd`, `slops-quality-baseline`, `slops-code-review`, `slops-git-flow` | PASS; no runtime change required | Existing `main` commit `dbafc66`; focused `test/yahooAuthRoute.test.js` 9/9; research, evidence, and handoff dated 2026-07-25 | The implementation merged before its required source/evidence closeout. Provider-console callback registration and real-device return remain authorized-human gates; no PKCE/client-flow change is implied because the code exchange stays server-held. |
| 2026-07-23 | M4 Command Center v1.1 corrective — resolves Codex P1/P2 findings + ships approved §3.2 Context Strip and Matchup Spine + reusable visual-evidence CI | `slops-repo-inspector`, `planning-pass`, `slops-tdd`, `slops-quality-baseline`, `slops-code-review`, `slops-git-flow`, `slops-context-markdown`, `slops-ux-copy` | PASS (iOS CI + visual-evidence CI pending PR push; Android device instrumentation now produced by the visual-evidence workflow rather than local shell) | `Blueprints/handoffs/2026-07-23-m4-command-center-v1.1-corrective.md`; branch `claude/m4-command-center-v1.1`; enforcement scanner still green; new reusable CI workflow at `.github/workflows/native-visual-evidence.yml` | `slops-mobile-smoke`/`slops-ui-ux-audit` substituted with Gradle + iOS CI + visual-evidence CI. `security-privacy-evidence`/`rbac-risk-review`/`slops-legal-spot-check` N/A. Procedure gap surfaced by the corrective pass: v1 shipped without reading mobile-visual-briefs §1.1 in full — codify a "read the approved visual brief for the section being built" step in the planning-pass playbook for every M4 screen assembly. Also: this corrective pass added two DS-layer compositions (Context Strip + Matchup Spine) that were §3.2-approved-but-not-built for months; the DS registry should surface an "approved but unimplemented" column so future planning gates catch this earlier. |
| 2026-07-22 | M4 Command Center v1 — first feature screen | `slops-repo-inspector`, `planning-pass`, `slops-tdd`, `slops-quality-baseline`, `slops-code-review`, `slops-git-flow`, `slops-context-markdown`, `slops-ux-copy` | PASS (iOS pending CI; Android instrumentation pending real device / Studio AVD) | `Blueprints/handoffs/2026-07-22-m4-command-center-v1.md`; branch `claude/m4-command-center`; enforcement scanner green with `OmenAndroidApp.kt` OFF allowlist; five local Material Symbols vector drawables recorded with official Google source filenames | `slops-mobile-smoke`/`slops-ui-ux-audit` substituted with Gradle + iOS CI. `security-privacy-evidence`/`rbac-risk-review`/`slops-legal-spot-check` N/A (no trust boundary or credential path — auth code untouched, only extraction). Procedure gap: `:app:` androidTest not wired — small `build.gradle.kts` addition (gated) would let feature-screen Compose UI tests land alongside the feature. Also worth codifying: when a Google-recommended library changes (Icons.Filled deprecated for new work), preserve the old exemption reasoning trail so a later reviewer sees why option (d) beat (a)-(c). |
| 2026-07-22 | M1-P P3 Batch 3 DecisionBrief shell — closes P3 | `slops-repo-inspector`, `planning-pass`, `slops-tdd`, `slops-quality-baseline`, `slops-code-review`, `slops-git-flow`, `slops-context-markdown` | PASS (iOS pending CI) | `Blueprints/handoffs/2026-07-22-m1p-p3-decision-brief-shell.md`; branch `claude/m1p-p3-batch-3` (stacked); Android compile + JVM tests + module assembly + app assembly + enforcement scanner all green | `slops-mobile-smoke`/`slops-ui-ux-audit` substituted with Gradle + iOS CI. `security-privacy-evidence`/`rbac-risk-review`/`slops-legal-spot-check`/`slops-ux-copy`/`design-md-author`/`demo-mode-pre-empty-state`/`slops-taste` N/A. Procedure win: the prepare-next-slice pattern from Batch 2 → Batch 3 halved kickoff time; codify as a compositions-batching playbook. |
| 2026-07-22 | M1-P P3 Batch 2 connection primitives + Batch 3 preparation brief | `slops-repo-inspector`, `planning-pass`, `slops-tdd`, `slops-quality-baseline`, `slops-code-review`, `slops-git-flow`, `slops-context-markdown` | PASS (iOS pending CI) | `Blueprints/handoffs/2026-07-22-m1p-p3-connection-primitives.md`; branch `claude/m1p-p3-batch-2` (stacked); Android compile + JVM tests + module assembly + app assembly + enforcement scanner all green; DecisionBrief shell contract at `Blueprints/specs/mobile/m1-p-p3-decision-brief-shell-brief-v1.md` | `slops-mobile-smoke`/`slops-ui-ux-audit` substituted with Gradle + iOS CI. `security-privacy-evidence`/`rbac-risk-review`/`slops-legal-spot-check`/`slops-ux-copy`/`design-md-author`/`demo-mode-pre-empty-state` N/A: primitive layer, no trust boundary or user-facing product copy (gallery demo strings only). Procedure win: authoring the Batch 3 contract inside this PR (rather than waiting for the next kickoff) is a repeatable "prepare next slice" pattern worth codifying in the compositions playbook. |
| 2026-07-22 | M1-P P3 Batch 1 metric primitives | `slops-repo-inspector`, `planning-pass`, `slops-tdd`, `slops-quality-baseline`, `slops-code-review`, `slops-git-flow`, `slops-context-markdown` | PASS (iOS pending CI) | `Blueprints/handoffs/2026-07-22-m1p-p3-metric-primitives.md`; branch `claude/m1p-p3-compositions`; Android compile + JVM tests + module assembly + app assembly + enforcement scanner all green | `slops-mobile-smoke`/`slops-ui-ux-audit` substituted with Gradle + iOS CI (native substitute). `security-privacy-evidence`/`rbac-risk-review`/`slops-legal-spot-check`/`slops-ux-copy`/`design-md-author`/`demo-mode-pre-empty-state` N/A: pure primitive layer, no trust boundary, provider claim, or new user-facing product copy. Procedure gap: hand-editing pbxproj for multiple sources at once is error-prone — a scripted registration helper with an ID-uniqueness pre-check would harden future iOS batches. |
| 2026-07-22 | F2 status-truth doc reconciliation | `slops-repo-inspector`, `planning-pass`, `slops-context-markdown`, `slops-git-flow` | PASS | Branch `claude/f2-status-truth`; `Blueprints/handoffs/2026-07-22-f2-status-truth.md`; 6 doc lines corrected; runtime unchanged | `slops-tdd`/`slops-quality-baseline`/`slops-code-review` N/A (docs-only). `slops-mobile-smoke`/`slops-ui-ux-audit` N/A. `security-privacy-evidence`/`rbac-risk-review`/`slops-legal-spot-check`/`slops-ux-copy` N/A: no trust boundary, provider claim, or user-facing copy. Procedure gap: when a runtime resolution lands (F2 was runtime-closed 2026-07-19), open a linked doc-reconciliation ticket in the same handoff so the pinned Verify item doesn't stay hot for 3 days. |
| 2026-07-21 | M3A-iOS native auth implementation (GitHub issue #159) | `slops-repo-inspector`, `slops-tdd`, `security-privacy-evidence`, `rbac-risk-review`, `slops-git-flow` | PASS (pending CI) | `Blueprints/handoffs/2026-07-21-m3a-ios-auth-implementation.md`; 37 XCTest cases (exact Android parity count) | No local macOS/Xcode toolchain in this session to compile/run against — `slops-ui-ux-audit`/`slops-mobile-smoke` skipped (web-driver only, no device/simulator here); real verification is `ios-ci.yml` on the PR. Worth a `native-ios-ci-watch` procedure formalizing "push, subscribe to PR CI, iterate on real failures" as the expected loop for compiler-less sessions. |
| 2026-07-21 | M1-P P2 Card/Badge/Chip/Modal/State-surfaces reconciliation | `slops-context-markdown` | PASS | Sprint/inbox/decision/Done-ledger corrections and four dated handoffs; merged evidence PRs #166–#168 | Closeout must happen in the same slice before the next primitive begins; the older sprint wording had drifted after merges. |
| 2026-07-20 | M1-P full Figma screen-contract pass (`01` + 3 `03` proposals + 8 `04`/`05` low-fi flows + 3 golden pairs + `06` QA board) | `figma-use` (Figma plugin-API skill) | PASS | `Blueprints/handoffs/2026-07-20-m1p-figma-reference-and-proposals.md`; 44 new Figma nodes across pages `01`/`03`/`04`/`05`/`06` in `mWjrAKPi4JSIP5lAmGAtB3` | None on the skill itself. Two own-process gaps found and fixed, both self-corrected via the skill's atomic-retry/screenshot-verify discipline before propagating: (1) an initial `get_metadata` call with no `nodeId` returned only the first page and was briefly misread as "foundation boards missing" — enumerate pages via `use_figma`/`figma.root.children`, not `get_metadata` alone; (2) `createAutoLayout()` frames left without an explicit `fills` assignment defaulted to white against the dark theme, and a `resize(w, 10)` call on a fixed-width column left a stale 10px height lock even after `counterAxisSizingMode='AUTO'` — fix is to always set `fills` explicitly and to set `layoutSizingVertical='HUG'` explicitly on any child resized to a fixed width. |
| 2026-07-13 | Promo video retro (pre-reshoot) | `slops-retro` | PARTIAL | Retro produced real findings (3 prior cut generations, superseded-abstract verdict, unlogged-status gap) logged into `Direction/decision_log.md` 2026-07-13 entry, but only because Justin supplied the exact path `Brand/promos/omen-coming-soon/`; the skill's own "Required Inputs" (sprint items, decision log, canary/investigate findings) surfaced zero hits on first pass | Skill has no instruction to search outside `Direction/` for asset-producing work (Brand/, promos, renders); add a step to check adjacent non-Direction folders (Brand/, Blueprints/handoffs/) before concluding "no prior work found" |
| 2026-07-13 | Promo video script draft | `marketing:draft-content` | PARTIAL (substitute) | Produced a usable beat sheet + full VO script grounded in `Brand/brand-system.md` (approved copy anchors only, product-ladder order, platform-agnostic framing, multi-segment structure per Justin's ask) | Skill's own "Content Generation by Type" list has no video-script format (blog/social/email/landing/press-release/case-study only) — output was hand-adapted rather than skill-templated; also has no built-in brand-voice file lookup, so `Brand/brand-system.md` had to be read and applied manually rather than auto-loaded. Consider whether `slops-screenplay-loop` (built for beat-sheet/script handoffs) is the better-fitted skill for future video-script asks, or file a gap for a video-script content type here |

| 2026-06-21 | Convert skill catalog into Omen procedures | `slops-repo-inspector` | PASS | Root/L2 repo, branch, dirty state, current sources, and stale L2 context path inspected | L2 context still carried OneDrive paths; corrected in this procedure pass |
| 2026-06-21 | Convert skill catalog into Omen procedures | `slops-graphify` / `graphify` query | PARTIAL | Root graph query returned skill/sprint/done nodes but only shallow implicit links | Encode explicit links in playbooks, kickoff, and Done gates; refresh graph after merge |
| 2026-06-21 | Convert skill catalog into Omen procedures | `planning-pass` | PASS | Post-live learning gate and procedure adoption routed into the Omen sprint/loop | Future items must use cold-start shape consistently |
| 2026-06-21 | Convert skill catalog into Omen procedures | `slops-context-markdown` | PASS | `Blueprints/playbooks/` baseline, activation runbook, ledger, and post-live runbook | Review monthly for duplicated/stale procedure text |
| 2026-06-21 | Park future skills | `slops-skill-author` | PASS | Canonical statuses, triggers, versions, routing, lifecycle, backup, and runtime removal | Reactivation requires same-pass routing/lifecycle/runtime distribution |
| 2026-06-21 | Refresh stale Omen skill-procedure graph | `slops-graphify` | PASS | `../../References/graphify/REFRESH_REPORT_2026-06-21.md`; canonical graph 228 nodes / 224 edges / 23 communities with 27 L0↔L2 edges | Canonical curated graph retains 52 legacy edge-evidence warnings for a later curation pass |
| 2026-06-21 | Refresh stale Omen skill-procedure graph | `graphify` | PASS | Deterministic Markdown merge, archive-node pruning, recluster, and broad-query verification in canonical and Omen-local graphs | Full semantic rebuild intentionally deferred; no approved local semantic backend used during Claude's active work |
| 2026-06-22 | Phase 1.5g.0 motif grammar spec authoring (Path A) | `Workflow` (3-proposal × 3-lens synthesis + completeness critic) | PASS | `Blueprints/specs/team-motif-grammar.md` v1; category-axis winner over Maximum-flexibility / Tier-Based (avg 5.67 vs 4.67); critic verdict patch-then-ship, 10 patches applied | Workflow ate two rate-limit resets before completing; cache-resume worked as designed (research and most-verify hits returned instantly on resume) |
| 2026-06-22 | Phase 1.5g.0 motif grammar spec authoring (Path A) | `slops-context-markdown` (doctrine) | PASS | Spec frontmatter + house-style markdown discipline applied across `Blueprints/specs/team-motif-grammar.md`, page-system addendum, decision log entry, sprint split, inbox update | n/a |
| 2026-06-22 | Phase 1.5g.0 motif grammar spec authoring (Path A) | `design-md-author` (doctrine) | PASS | Per-team grammar shape (motif / typeFlourish / culturalMoment), AAA gate table, sprint split with done-when, cross-references | n/a |
| 2026-06-22 | Phase 1.5g.0 motif grammar spec authoring (Path A) | `slops-tdd` | N/A | Doc-only deliverable; no behavior change; no fast deterministic test command applies | Pickup at 1.5g.1 — motif resolver + `excludesOmenCard` enforcement + accent-fallthrough Vitest pins are the natural RED targets |
| 2026-06-22 | Phase 1.5g.0 motif grammar spec authoring (Path A) | `slops-ui-ux-audit` | N/A | No UI surface change in 1.5g.0; v1 grammar gates downstream rendering | Pickup at 1.5g.1 — motif color × surface ≥ 3.0 sweep extension |
| 2026-06-22 | Phase 1.5g.0 motif grammar spec authoring (Path A) | `ui-ux-pro-max` | N/A | Doc-only; no styling decisions | Pickup at 1.5g.2 — Alegreya OpenType feature spike (`"smcp" 1` retention vs `text-transform: uppercase` fallback) |
| 2026-06-22 | Phase 1.5g.0 motif grammar spec authoring (Path A) | `slops-code-review` | N/A | No code merging; doctrine fixes applied via 3-lens adversarial verify inside the workflow | Pickup at 1.5g.1+ as the per-PR review skill |
| 2026-06-22 | Phase 1.5g.0 motif grammar spec authoring (Path A) | `slops-ux-copy` | N/A | No production copy ships in 1.5g.0; eyebrow strings drafted in spec as proposals | Pickup at 1.5g.3 — every moment eyebrow string must pass copy review before render |
| 2026-06-22 | Corvus → Omen active rename pass | `graphify` | PASS | Queried L2 and L0 graphs before edits; graph surfaced source/import/router/deploy clusters for rename inventory | Graph itself was not rebuilt; refresh after merge so graph labels stop saying Corvus |
| 2026-06-22 | Corvus → Omen active rename pass | `slops-repo-inspector` | PASS | Confirmed L0/L1/L2 routing, nested Git roots, dirty files, and authoritative vs historical paths before editing | External GitHub/GHCR/Oracle/container identifiers remain a later cutover, not a silent rename |
| 2026-06-23 | Omen operational rename cutover plan | `graphify` | PARTIAL | Queried the existing L2 graph before planning; graph confirmed stale/shallow Corvus ops nodes, then direct deploy-file inspection filled the inventory | Refresh graph after the operational rename lands so deploy identities and labels are current |
| 2026-06-23 | Omen operational rename cutover plan | `slops-repo-inspector` | PASS | Confirmed root/app repo state, dirty worktrees, L2 sprint, and active operational files before writing plan-only artifacts | Worktree remains dirty from the broader rename; future commits must stage explicit paths |
| 2026-06-23 | Omen operational rename cutover plan | `planning-pass` | PASS | Added gated OP1-OP5 and V2 items to `Direction/current_sprint.md`; created `Blueprints/specs/omen-operational-rename-cutover.md` | Implementation must wait for Justin approval; plan intentionally avoids app code and production mutation |
| 2026-06-23 | Omen operational rename cutover plan | `slops-deploy-guard` | PASS | Inspected deploy workflow, Dockerfile, compose files, Hostinger deploy notes/env inventory, and rollback path | KVM1 mutation, runner-label changes, GHCR cutover, and repo rename all require explicit approval |
| 2026-06-23 | Omen operational rename OP2 repo-side seed patch | `graphify` | PARTIAL | Queried existing L2 graph before editing; graph was shallow/stale for deploy ops, so workflow and deploy files were inspected directly | Refresh graph after the operational cutover lands |
| 2026-06-23 | Omen operational rename OP2 repo-side seed patch | `slops-repo-inspector` | PASS | Reconfirmed root/app repo, dirty state, branch, and L2 target before editing workflow/deploy docs | Worktree remains dirty; stage explicit files only if committing |
| 2026-06-23 | Omen operational rename OP2 repo-side seed patch | `slops-deploy-guard` | PASS | Added Omen GHCR tags while preserving Corvus live pull path and rollback tags; no KVM1, DNS, SSL, secret, database, or production action | OP3 KVM1 prep still requires explicit approval before mutation |
| 2026-06-23 | Omen operational rename OP3 repo-side compose prep | `graphify` | PARTIAL | Queried existing L2 graph before editing; graph stayed too shallow for compose ops, so direct file inspection carried the inventory | Refresh graph after compose/deploy identity lands |
| 2026-06-23 | Omen operational rename OP3 repo-side compose prep | `slops-repo-inspector` | PASS | Reconfirmed dirty state and active L2 deploy files before changing compose/docs | Existing rename work remains uncommitted; stage explicit paths only |
| 2026-06-23 | Omen operational rename OP3 repo-side compose prep | `slops-deploy-guard` | PASS | Prepared Omen compose/Nginx sample files and docs without touching KVM1, runner labels, env files, DNS/SSL, secrets, database, or containers | Live OP3 server actions still require a KVM1 command window and approval |
| 2026-06-23 | Omen operational rename execution prep | `graphify` | PARTIAL | Rechecked the stale graph report against direct deploy/workflow searches; graph still maps the app as Corvus at L2, which confirms the operational rename boundary | Refresh graph after the deployment and repo identity are stable |
| 2026-06-23 | Omen operational rename execution prep | `slops-repo-inspector` | PASS | Confirmed branch, dirty state, runner status, runner labels, and latest deploy workflow runs before preparing the cutover workflow | Repo still needs explicit-path staging; do not stage `graphify-out/` or logo source scratch files |
| 2026-06-23 | Omen operational rename execution prep | `slops-deploy-guard` | PASS | Added the `omen-deploy` runner label while retaining `corvus-deploy`, and added a manual-only rollback-guarded cutover workflow for `/opt/omen` | Execute in sequence: seed images through the old path, run cutover, then flip regular deploy workflows to Omen-only |
| 2026-06-23 | Omen operational rename execution prep | `slops-git-flow` | PASS | Prepared to stage explicit paths only; rollback/artifacts and generated graph/cache outputs are excluded from commit scope | If direct main push is blocked, use a branch/PR without broad staging |
| 2026-06-23 | Omen cutover run 27995403847 | `slops-deploy-guard` | HOLD SAFE | Cutover stopped at `/opt/omen` prep because the KVM1 runner lacks passwordless sudo; health/readiness stayed green and live containers were not stopped | Workflow patched to use a short-lived Docker helper for `/opt/omen` prep through existing Docker access |
| 2026-06-23 | Omen operational rename completion | `slops-deploy-guard` | PASS | Cutover `27995488318`, primary deploy `27995567629`, and Tailscale fallback `27996194346` all passed from `/opt/omen`; public health/ready stayed green | Corvus rollback artifacts retained until explicit cleanup approval |
| 2026-06-23 | Omen operational rename completion | `slops-git-flow` | PASS | Commits `42a52a2`, `26a5771`, and `1ff2210` pushed; repo renamed to `justinduverge-design/omen`; local origin updated | Final docs commit still needed after path/remote evidence update |
| 2026-06-23 | Omen operational rename completion | `graphify` | PARTIAL | Existing graph confirmed stale Corvus L2 routing before and during rename; direct file scans verified the live operational residue classification | Refresh graph output in a separate graph maintenance pass after local folder rename settles |
| 2026-06-23 | Phase 1.5g.1 motif schema + hairlines | `slops-repo-inspector` | PASS | Read L2 kickoff files, sprint, inbox, facts, specs, handoffs, branch/dirty state before selecting Phase 1.5g.1 | Existing unrelated dirty prompt files, `graphify-out/`, and `logos/` stayed unstaged |
| 2026-06-23 | Phase 1.5g.1 motif schema + hairlines | `slops-tdd` | PASS | RED `node --test test/teamMotifs.test.mjs` failed on missing motif data/tokens; GREEN passed 3/3 after implementation | Keep focused node:test pins even though sprint wording said Vitest |
| 2026-06-23 | Phase 1.5g.1 motif schema + hairlines | `slops-code-review` | PASS | `Blueprints/audits/2026-06-23-phase1-5g-code-review.md`; verdict merge, no P0/P1/P2 | Existing Vite bundle-size warning remains unrelated |
| 2026-06-23 | Phase 1.5g.1 motif schema + hairlines | `slops-ui-ux-audit` | PASS | `Blueprints/audits/2026-06-23-phase1-5g-ui-ux-audit.md`; screenshots and DOM checks for PIT/MIA/NO/GB | Full iOS Safari sweep remains Phase 1.13 |
| 2026-06-23 | Phase 1.5g.1 motif schema + hairlines | `rbac-risk-review` | PASS | `Blueprints/audits/2026-06-23-phase1-5g-rbac-risk-review.md`; no auth, data, secret, package, or deploy escalation | n/a |
| 2026-06-23 | Phase 1.5g.1 motif schema + hairlines | `slops-quality-baseline` | PASS | `Blueprints/quality/baseline.json` ratcheted from 312/312 to 356/356 with audits/build/diff green | Baseline was old relative to current sprint; refreshed during closeout |
| 2026-06-23 | Phase 1.5g.1 motif schema + hairlines | `run-slops-saloon` | PARTIAL | Local Vite server used and screenshots captured under `Solutions/reports/_screenshots/phase1-5g1/` | Bundled driver assertions are stale for renamed Omen landing copy; direct Playwright was used |
| 2026-06-23 | Phase 1.5g.1 motif schema + hairlines | `slops-git-flow` | PASS | Implementation commit `e66e9d7` staged explicit source/test paths only; closeout commit stages docs/evidence explicitly | No push or deploy from this task |
| 2026-06-23 | Phase 1.5g.1 merge + deploy | `slops-ship` | PASS | Scoped commits cherry-picked to `main` as `4efa338` + `8e5d56b`, pushed, and deployed in run `28062382244` | Branch commit `86480b4` was intentionally excluded because it contained generated graph/logo assets outside Phase 1.5g.1 scope |
| 2026-06-23 | Phase 1.5g.1 merge + deploy | `slops-deploy-guard` | PASS | Read `.github/workflows/deploy.yml`, `Dockerfile`, `docker-compose.yml`, and `scripts/oracle-https-setup.sh`; deploy target KVM1 `/opt/omen`; rollback is revert of `4efa338` + `8e5d56b` | `scripts/oracle-https-setup.sh` remains historical Oracle material, not the active KVM1 path |
| 2026-06-23 | Phase 1.5g.1 merge + deploy | `slops-canary` | PASS | Run `28062382244` green; independent checks passed root+www health/ready, homepage 200, Demo 200, expected 401 boundaries, HTTP->HTTPS redirect, and HSTS | Authenticated real-account smoke not run; no auth/data path changed |
| 2026-06-25 | Phase 1.5g.2 TypeFlourish schema + NE small-caps eyebrow | `slops-repo-inspector` | PASS | Read `DBS_INDEX.md` → `Direction/context.md` → `agent_inbox.md` → `current_sprint.md` → `HOW-TO-RUN-THE-LOOP.md` → `definition-of-done.md`; confirmed `agent-modules/`/`kickoff-modules/` paths referenced by `CLAUDE.md` don't exist in this checkout and self-pulled the top of the auto-populated top-5 instead | The stale `agent-modules`/`kickoff-modules` path reference in `CLAUDE.md` should be corrected or the modules added — flagged, not fixed, in this task |
| 2026-06-25 | Phase 1.5g.2 TypeFlourish schema + NE small-caps eyebrow | `slops-tdd` | PASS | RED: `assertCategoryShape()`'s `assertTypeFlourishes()` already existed (built ahead-of-need in 1.5g.1) so the lock test was written first and confirmed it threw on a constructed italic-700 fixture before NE data existed; GREEN after NE data + resolver + plumbing landed — `test/teamTypeFlourishes.test.mjs` 4/4 | Mirrors the 1.5g.1 precedent: keep `node:test`, not Vitest, despite sprint wording |
| 2026-06-25 | Phase 1.5g.2 TypeFlourish schema + NE small-caps eyebrow | `ui-ux-pro-max` | PASS (substitute) | Pre-merge font-feature spike: Playwright/Chromium screenshot of `font-feature-settings: "smcp" 1` vs `text-transform: uppercase` against the live Google Fonts CSS2 Alegreya Sans `@import` — visibly distinct glyphs confirmed, no self-host fallback needed | Spike script + screenshot live only in `/tmp` scratchpad, not committed — not independently re-runnable from the repo alone |
| 2026-06-25 | Phase 1.5g.2 TypeFlourish schema + NE small-caps eyebrow | `slops-code-review` | SKIPPED (substitute) | Not available as an actual Claude Code skill in this session (same gap as 1.5g.1); substituted a manual self-review of the diff against the Motif-system file-for-file pattern | Third consecutive 1.5g.x phase missing this skill — consider registering a Claude Code equivalent for this session type |
| 2026-06-25 | Phase 1.5g.2 TypeFlourish schema + NE small-caps eyebrow | `slops-ui-ux-audit` | SKIPPED (substitute) | Not available in this session; substituted a manual pattern check against `Brand/brand-system.md` plus the font-feature spike screenshot. The `/account/appearance` cultureTag badge itself was **not** screenshotted in light/dark — no dev server or authenticated browser session was available in this sandbox | Real follow-up needed: capture `/account/appearance` light/dark screenshots once a dev server is reachable, then run a true `slops-ui-ux-audit` pass |
| 2026-06-25 | Phase 1.5g.2 TypeFlourish schema + NE small-caps eyebrow | `slops-git-flow` | PASS | Implementation commit `440be1f` staged explicit source/test paths only (`nflTeams.js`, `typeFlourishes.js`, `teamTemplate.js`, `themeMode.js`, `index.css`, `Appearance.jsx`, `contrast-sweep.mjs`, `test/teamTypeFlourishes.test.mjs`); closeout docs staged and committed separately | No merge to `main`, no deploy — draft PR opened on `claude/corvus-kickoff-hre0je` for Justin's review |
| 2026-06-25 | Fresh-clone kickoff + Phase 1.10A UX copy options | `slops-repo-inspector` | PASS | Confirmed `corvus/` is its own clean git root on `main` after a fresh clone; identified `Blueprints/agent-modules/` and `Blueprints/prompts/kickoff-modules/` as non-existent stale kickoff paths and routed to `Direction/current_sprint.md` + `Direction/agent_inbox.md` + `Blueprints/definition-of-done.md` instead | Stale `agent-modules`/`kickoff-modules` paths in `CLAUDE.md` and both kickoff prompt files still need a real fix or the modules need to be authored — flagged again, not fixed, in this task |
| 2026-06-25 | Fresh-clone kickoff + Phase 1.10A UX copy options | `slops-ux-copy` | PASS | `Blueprints/handoffs/2026-06-25-phase1-10a-ux-copy-options.md` — 3 options each for `/omen` offseason empty state, `/onboarding` success step, and the banned hero headline (covering both `Landing.jsx` and `OmenLanding.jsx`, since the same banned line is live in both) | Caught that `current_sprint.md`'s Phase 1.10A line still floats the now-retired `Less guessing. Better moves.` as a candidate — packet flags it as stale against `Brand/brand-system.md`'s 2026-06-22 update |
| 2026-06-25 | Phase 1.10B (partial) — approved copy applied | `slops-quality-baseline` (substitute: direct `npm run build`) | PASS | `npm ci` + `npm run build` in `frontend/` clean (497.08 kB / 139.99 kB gzip) | No backend change in this slice, so full `npm test` wasn't re-run; frontend-only build is sufficient evidence here |
| 2026-06-25 | Phase 1.10B (partial) — approved copy applied | `run-slops-saloon` (substitute: `mcp__Claude_Preview` dev server) | PARTIAL | Live Vite dev server confirmed correct rendered text + sane light/dark `getComputedStyle` contrast on `/` and `/about` via `preview_eval`; `preview_screenshot` itself timed out on every attempt (server was healthy per console logs, so this looks like a tool-level issue, not an app bug) | `/onboarding` success step not independently rendered — it's gated behind auth + an active platform connection; disclosed as unverified rather than assumed correct from the diff alone |
| 2026-06-25 | Phase 1.10B (partial) — approved copy applied | `slops-ui-ux-audit` | SKIPPED | No screenshot capability this session (tool timeout) to run a real audit | Re-run once `preview_screenshot` works or a real browser session is available — same gap class as 1.5g.2/1.5g.3 |
| 2026-06-25 | Phase 1.10B (partial) merge + deploy | `slops-git-flow` | PASS | Already on `main` with no divergence from `origin/main` (`git fetch` confirmed) — plain fast-forward, no actual merge needed; `git push origin main` → `cdae3bc..5fe20c1` | Two commits pushed together (1.10A naming fix + 1.10B copy) since both were explicitly requested bundled |
| 2026-06-25 | Phase 1.10B (partial) merge + deploy | `slops-canary` (substitute: direct `curl` + bundle grep) | PASS | Run `28169065015` green (quality/build/deploy); independently re-checked `/api/health` (ok), `/api/ready` (ready), homepage (200), and grepped the actual served JS bundle for all three new copy strings — all present, zero remaining "Know the move" matches | First phase this session verified against the live production artifact itself, not just a build log or dev server |
| 2026-06-25 | Phase 1.11A Demo Mode frontend fixtures | `demo-mode-pre-empty-state` | PASS | `Blueprints/demo-mode.md` now distinguishes private dev fixtures from public `omen-demo.v1`; fixtures are deterministic, mock-labeled, and never auto-merge with live data | Keep Phase 2.7 public Demo Mode separate from these visual fixtures |
| 2026-06-25 | Phase 1.11A Demo Mode frontend fixtures | `slops-tdd` | PASS | RED `node --test test/phase1_11aPrivateFixtures.test.mjs` failed on missing `privateDemoFixtures.js`; GREEN passed 3/3 after fixture module + gate + page wiring | Static regression test now rejects routed-page static imports of the private payload |
| 2026-06-25 | Phase 1.11A Demo Mode frontend fixtures | `slops-ux-copy` | PASS | Banner copy reviewed for honesty: private fixture label plus "are mock"; no live/provider-backed claims in fixture states | This was a final-copy review, not a 3-option copy packet, because the sprint requirement already dictated the label semantics |
| 2026-06-25 | Phase 1.11A Demo Mode frontend fixtures | `slops-code-review` | PASS | `Blueprints/audits/2026-06-25-phase1-11a-demo-fixtures-code-review.md`; verdict merge, no P0/P1/P2 | Protected `/omen` and `/ledger` fixture render states still need an authenticated visual pass if Justin wants screenshots |
| 2026-06-25 | Phase 1.11A Demo Mode frontend fixtures | `slops-quality-baseline` | PASS | Focused tests 15/15; full `npm test` 374/374; `npm --prefix frontend run build` clean; audit 0; `git diff --check` clean | Existing Vite chunk-size and `NODE_ENV=production` warnings remain unrelated baseline noise |
| 2026-06-25 | Phase 1.11A Demo Mode frontend fixtures | `run-slops-saloon` / `slops-verify` | PASS | Temporary Vite dev + production preview smoke: dev `/draft?fixture=mock-draft` renders fixture; production preview ignores it; `/omen` and `/ledger` fixture URLs remain auth-gated | Backend not started; frontend-only QA was sufficient because no API route changed |
| 2026-06-25 | Phase 1.11A Demo Mode frontend fixtures | `slops-mobile-smoke` | PARTIAL | Scoped Playwright phone-width check on public `/draft?fixture=mock-draft` at 375/390/430px: no JS errors, no horizontal overflow, fixture label visible | Full route/device matrix remains Phase 1.13; no formal mobile-smoke report generated for this narrow fixture slice |
| 2026-06-25 | Phase 1.11A Demo Mode frontend fixtures | `slops-git-flow` | PASS | Branch `frontend/phase1-11a-demo-fixtures`; commits `a2aee8e` (code/test) + `9470b1a` (docs) pushed after Justin's push request; PR #67 squash-merged to `main` as `44f99d2` after checks were green | Branch deleted by GitHub after merge |
| 2026-06-25 | Phase 1.11A merge + deploy | `slops-ship` | PASS | PR #67 merged only after `ui-quality` completed and merge state was clean; rollback path is revert merge commit `44f99d2` through the normal workflow | No direct server mutation beyond the approved deploy workflow |
| 2026-06-25 | Phase 1.11A merge + deploy | `slops-deploy-guard` | PASS | Preflight confirmed `deploy.yml` deploys from `main` through the KVM1 self-hosted runner and restarts `api`/`cron`; docs-only release-evidence update stays inside paths ignored by deploy | `Blueprints/playbooks/app-cutover-playbook.md` was missing in this checkout, so the workflow file was treated as canonical |
| 2026-06-25 | Phase 1.11A merge + deploy | `slops-canary` | PASS | Run `28172865389` green; canary confirmed apex/www homepage 200, `/api/health` ok, `/api/ready` ready, `/api/demo` still `omen-demo.v1`, HTTP->HTTPS redirect, HSTS present, and no private fixture labels/sample names in live bundle `assets/index-CoI_lqSx.js` | The query keys remain visible as allowlist metadata; private payload strings are absent |
| 2026-06-25 | Phase 2.10 Trade share hash routes | `slops-repo-inspector` | PASS | Read L2 startup/kickoff sources, confirmed branch `main`, clean worktree before inbox refresh, then branch `backend/phase2-10-trade-share-routes` | The kickoff pull refreshed `Direction/agent_inbox.md`; no `current_sprint.md` planning pass was needed |
| 2026-06-25 | Phase 2.10 Trade share hash routes | `slops-tdd` | PASS | RED `node --test test/tradeShareRoute.test.js` failed on missing 404 routes; GREEN focused tests 4/4 after implementation; full `npm test` 378/378 | Added one vertical backend API slice; no frontend share page or OG image included |
| 2026-06-25 | Phase 2.10 Trade share hash routes | `security-privacy-evidence` | PASS | `Direction/reviews/2026-06-25-phase2-10-trade-share-security-evidence.md` | Production Redis values were not inspected; production smoke remains deploy-gated |
| 2026-06-25 | Phase 2.10 Trade share hash routes | `slops-code-review` | PASS | `Blueprints/audits/2026-06-25-phase2-10-trade-share-code-review.md`; no P0/P1 findings | Future revoke/delete and OG rendering remain follow-ups, not blockers |
| 2026-06-25 | Phase 2.10 Trade share hash routes | `slops-quality-baseline` | PASS | `npm test` 378/378; `npm audit --audit-level=moderate` 0; `npm audit --omit=dev --audit-level=high` 0; `npm --prefix frontend run build` clean with existing Vite chunk warning; `git diff --check` clean | Baseline file was not ratcheted in this docs-only closeout; existing baseline remains 356/356 from 2026-06-23 |
| 2026-06-25 | Phase 2.10 Trade share hash routes | `slops-git-flow` | PASS | Code/test commit `1d98332` staged explicit implementation paths only; docs/evidence commit staged explicit closeout paths only | No push, PR, merge, or deploy; Justin gate remains |
| 2026-06-25 | Phase 2.10 merge + deploy | `github:yeet` | PASS | Branch `backend/phase2-10-trade-share-routes` pushed; PR #68 opened and merged | Used `gh` CLI because it was authenticated and available in this runtime |
| 2026-06-25 | Phase 2.10 merge + deploy | `slops-deploy-guard` | PASS | Inspected `.github/workflows/deploy.yml`, `Dockerfile`, `docker-compose.yml`, and `scripts/oracle-https-setup.sh`; deployment used existing KVM1 workflow, no config/secrets/DNS/SQL changes | `scripts/oracle-https-setup.sh` remains historical Oracle material; active path is KVM1 `/opt/omen` via deploy workflow |
| 2026-06-25 | Phase 2.10 merge + deploy | `slops-ship` | PASS | PR #68 merged as `bfefbf1`; deploy run `28192331330` passed quality, build, and deploy | Release Done gates for authenticated smoke, Sentry dashboard, and KVM resources were not run in this session; documented in Done ledger |
| 2026-06-25 | Phase 2.10 merge + deploy | `slops-canary` | PASS | Read-only canary passed apex health/ready/homepage/Demo, `www` health, HTTP->HTTPS 301, HSTS, malformed share hash 400, and missing UUID share 404 | Did not POST a production share because canary is read-only; test/dev already covers create/read |
| 2026-06-25 | Root knowledge-graph refresh (L0+L1+L2) | `slops-graphify` | PARTIAL | Full rebuild written to `References/graphify/graphify-out/graph.json` — 8,440 nodes / 9,166 edges / 617 communities. L0=4,404, L1=71, L2=3,965 (all non-zero). Rename verified: 19 active "corvus" labels remain, all in rename-cutover specs, historical prompts marked pre-rename, or decision-log entries — no stray live references. Report: `References/graphify/REFRESH_REPORT_2026-06-25.md`. Prior snapshot preserved at `graph.json.bak-2026-06-21` | Cross-layer edges = 0 (`merge-graphs` cannot synthesize them). Carryover: next refresh should stage L0/L1/L2 under a single junctioned tree and use `graphify update` so the extractor produces real L0↔L2 edges (2026-06-21 had 27). Run uncommitted; Justin to review |
| 2026-06-25 | Teach `slops-graphify` to walk past nested `.git/` at SLOPS root | `slops-graphify` | PASS | New SLOPS-root mode (`Blueprints/skills/slops-graphify/slops_root_scan.py` + `SKILL.md` v0.1.3) auto-detects SLOPS root, in-process patches `_load_graphifyignore` to drop nested-repo exclusion lines, and runs a deterministic markdown-link enrichment pass. Single-pass canonical graph at `References/graphify/graphify-out/graph.json`: **10,140 nodes / 10,743 edges / 792 communities**. Acceptance gate hit — **L0↔L1=39, L0↔L2=145 (≥27 baseline), L1↔L2=28**. Spot-check: L2 `slops-saloon/omen/Blueprints/playbooks/skill-activation-runbook.md` → L0 `Blueprints/skills/SKILL_ROUTING.md` resolved. Report: `References/graphify/REFRESH_REPORT_2026-06-25-pass2.md` | No disk `.gitignore`, workflow, or upstream graphify-package edits; default behaviour unchanged for non-SLOPS invocations. Closes pass-1 carryover. Run uncommitted; Justin to review |
| 2026-06-25 | Kickoff-doctrine pointer-rot fix (3rd-session blocker) | `slops-context-markdown` (doctrine) | PASS | Surgical rewrite of 6 live wrappers — `CLAUDE.md`, `AGENTS.md`, `Blueprints/prompts/kickoff-frontend-claude.md`, `Blueprints/prompts/kickoff-backend-codex.md`, `Blueprints/prompts/HOW-TO-RUN-THE-LOOP.md`, `Blueprints/definition-of-done.md`, `Direction/facts-of-record.md`. All references to non-existent `../../Blueprints/agent-modules/*` and `../../Blueprints/prompts/kickoff-modules/*` replaced with inlined sections (pull-task, plan-approval, build, done-and-close, safety-gates) or pointers to real local files. Stray `RESOURCES_INDEX.md` pointer also removed. Verified: `grep -r "agent-modules\|kickoff-modules"` against live doctrine paths now returns 0 results; only historical handoffs / decision-log entries / prior ledger rows retain the strings (immutable history, left intact) | Option B (rewrite wrappers) chosen over option A (author the missing L0 modules) per Justin's lean-docs framing. `AGENT.md` (singular) already pointed at real local files and needed no edit. No source code or tests touched; no behavior change. Uncommitted; Justin to review |
| 2026-06-25 | Kickoff-doctrine pointer-rot fix (3rd-session blocker) | `slops-repo-inspector` | PASS | Confirmed `Direction/` and `Blueprints/handoffs/{frontend-to-backend,backend-to-frontend,decisions}.md` and all other replacement targets exist before pointing the wrappers at them; sandbox `git status` errored on the Windows index extension (NT-only) but file-level diffs are clean | None — verification step ahead of an edit, not an evidence-bearing pass |
| 2026-06-27 | Phase 2.17 Platform `lastResult` fields | `slops-repo-inspector` | PASS | Confirmed L2 repo root, branch `main` -> `codex/phase2-17-platform-last-result`, dirty files, active sprint/inbox, backend handoff, and platform adapter paths before editing | Existing unrelated dirty doctrine/ledger files remained unstaged; commit scope stayed explicit |
| 2026-06-27 | Phase 2.17 Platform `lastResult` fields | `pre-build-research` | PASS | `Direction/reviews/2026-06-27-phase2-17-platform-last-result-research.md` | ESPN remains private/high-risk; true kickoff timestamp needs a separate source decision |
| 2026-06-27 | Phase 2.17 Platform `lastResult` fields | `slops-tdd` | PASS | RED `node --test test/dashboardSummary.test.js` failed on missing fields; GREEN dashboard 8/8 and adapter parsers 28/28; full `npm test` 385/385 | n/a |
| 2026-06-27 | Phase 2.17 Platform `lastResult` fields | `security-privacy-evidence` | PASS | `Direction/reviews/2026-06-27-phase2-17-platform-last-result-security-evidence.md` | Real ESPN production smoke remains Justin-gated |
| 2026-06-27 | Phase 2.17 Platform `lastResult` fields | `slops-code-review` | PASS | `Blueprints/audits/2026-06-27-phase2-17-platform-last-result-code-review.md`; no P0/P1 findings | Self-review artifact; no separate reviewer in this session |
| 2026-06-27 | Phase 2.17 Platform `lastResult` fields | `slops-quality-baseline` | PASS | `npm test` 385/385; `npm audit --audit-level=moderate` 0; `npm audit --omit=dev --audit-level=high` 0; `npm --prefix frontend run build` clean with existing chunk warning; `git diff --check` clean | Existing Vite chunk-size warning remains unrelated |
| 2026-06-27 | Phase 2.17 Platform `lastResult` fields | `slops-git-flow` | PASS | Branch `codex/phase2-17-platform-last-result`; explicit-path staging only; no push/deploy | Pre-existing dirty docs require careful staging/partial doc handling |
| 2026-06-28 | Phase 3.13 Token-constrained prompts | `slops-repo-inspector` | PASS | Confirmed Omen repo root, branch, dirty state, required startup docs, and active inbox. Found pre-existing conflict markers in sprint/decision docs | Phase 3.12 skipped because Justin said it is about to merge elsewhere |
| 2026-06-28 | Phase 3.13 Token-constrained prompts | `slops-context-markdown` | PASS | Reconciled touched live conflict-marker sections in `Direction/current_sprint.md` and `Direction/decision_log.md`; updated inbox, handoff, ledgers | Preserved both useful sides of the prior conflict |
| 2026-06-28 | Phase 3.13 Token-constrained prompts | `slops-ai-integration-review` | PASS | `Direction/reviews/2026-06-28-phase3-13-token-constrained-prompts-ai-integration.md` | No cloud provider or cost-cap path added |
| 2026-06-28 | Phase 3.13 Token-constrained prompts | `security-privacy-evidence` | PASS | `Direction/reviews/2026-06-28-phase3-13-token-constrained-prompts-security-evidence.md` | No ESPN cookie, auth, provider credential, or secret path changed |
| 2026-06-28 | Phase 3.13 Token-constrained prompts | `slops-tdd` | PASS | RED `node --test test/llmService.test.js`; GREEN same command 2/2; focused Omen/agent 58/58 | RED failed for intended missing prompt/token/parser limits |
| 2026-06-28 | Phase 3.13 Token-constrained prompts | `slops-quality-baseline` | PASS | Full `npm test` 387/387; `npm audit --audit-level=moderate` 0 | Frontend build N/A; no frontend changed |
| 2026-06-28 | Phase 3.13 Token-constrained prompts | `slops-code-review` | PASS | `Blueprints/audits/2026-06-28-phase3-13-token-constrained-prompts-code-review.md`; no P0/P1 | Self-review artifact; no separate reviewer in this session |
| 2026-06-28 | Phase 3.13 Token-constrained prompts | `slops-git-flow` | PASS | Branch `codex/phase3-13-token-constrained-prompts`; no push/deploy | Explicit-path staging still pending |
| 2026-06-27 | Phase 3.12 KVM2 Gemma bridge | `slops-repo-inspector` | PASS | Fresh worktree `omen-phase3-12` on branch `codex/phase3-12-kvm2-gemma-bridge`; read startup docs, inbox, sprint, facts, issues, decisions, API routes, latest handoff, and active backend paths before editing | Original `omen` checkout was dirty/behind and left untouched |
| 2026-06-27 | Phase 3.12 KVM2 Gemma bridge | `pre-build-research` | PASS | `Direction/reviews/2026-06-27-phase3-12-kvm2-gemma-bridge-research.md` | External docs checked: Tailscale 100.x addresses, Tailscale MagicDNS, Ollama OpenAI compatibility |
| 2026-06-27 | Phase 3.12 KVM2 Gemma bridge | `slops-ai-integration-review` | PASS | `Direction/reviews/2026-06-27-phase3-12-kvm2-gemma-bridge-ai-integration-review.md` | Cloud/provider toggle remains blocked by budget-cap decision |
| 2026-06-27 | Phase 3.12 KVM2 Gemma bridge | `security-privacy-evidence` | PASS | `Direction/reviews/2026-06-27-phase3-12-kvm2-gemma-bridge-security-evidence.md` | No secrets, `.env`, KVM/Tailscale mutation, deploy config, package files, Supabase, or Stripe production behavior touched |
| 2026-06-27 | Phase 3.12 KVM2 Gemma bridge | `slops-deploy-guard` | PASS | Safety gates enforced by no deploy/env/KVM/Docker/workflow change; production enablement remains Justin-gated | No live KVM2 smoke run |
| 2026-06-27 | Phase 3.12 KVM2 Gemma bridge | `slops-tdd` | PASS | RED `node --test test/llmService.test.js` failed on missing `getLlmBridgeStatus`; GREEN focused tests 51/51 after implementation | Kept `node:test`; no new runtime dependency |
| 2026-06-27 | Phase 3.12 KVM2 Gemma bridge | `slops-code-review` | PASS | `Blueprints/audits/2026-06-27-phase3-12-kvm2-gemma-bridge-code-review.md`; no P0/P1/P2 findings | Self-review artifact; no separate reviewer in this session |
| 2026-06-27 | Phase 3.12 KVM2 Gemma bridge | `slops-quality-baseline` | PASS | `npm test` 388/388; root audits 0; primary frontend build clean with existing chunk warning; legacy client build clean; `git diff --check` clean | Frontend/client `npm ci` reported existing advisories in those package trees; no package remediation attempted |
| 2026-06-27 | Phase 3.12 KVM2 Gemma bridge | `slops-git-flow` | PASS | Branch `codex/phase3-12-kvm2-gemma-bridge`; implementation commit `fbbf0c5`; explicit-path staging; no push/deploy | Docs/evidence committed separately |
| 2026-06-28 | Phase 3.12 merge + deploy | `github:yeet` | PASS | Pushed branch `codex/phase3-12-kvm2-gemma-bridge`; PR #70 opened against `main`; merged after clean merge state | PR branch had no separate PR checks; push-to-main deploy workflow was the quality gate |
| 2026-06-28 | Phase 3.12 merge + deploy | `slops-ship` | PASS | PR #70 squash-merged as `a13160b`; deploy run `28306784898` passed quality/build/deploy | Rollback path is revert `a13160b` through PR and normal deploy workflow |
| 2026-06-28 | Phase 3.12 merge + deploy | `slops-deploy-guard` | PASS | Inspected `.github/workflows/deploy.yml`, `Dockerfile`, `docker-compose.yml`, and `scripts/oracle-https-setup.sh`; deploy used existing KVM1 workflow and did not edit secrets/env/config | Existing Docker annotation for public Vite anon key repeated; no workflow failure |
| 2026-06-28 | Phase 3.12 merge + deploy | `slops-canary` | PASS | Independent canary passed apex health/ready/platform-status/homepage/Demo, `www` health, HTTP->HTTPS 301, and HSTS; live ready reports LLM `configured_private` without URL | Direct authenticated route smoke not run; no auth/provider route behavior changed |
| 2026-06-28 | Phase 4.16 Open-agreements provider paragraphs | `slops-repo-inspector` | PASS | Confirmed Omen L2 repo root, branch `codex/phase3-13-token-constrained-prompts`, clean starting worktree, required startup docs, API reference, contract bus, latest handoff, and no active pin before pulling Phase 4.16 | Worktree now contains the Phase 4.16 doc changes only |
| 2026-06-28 | Phase 4.16 Open-agreements provider paragraphs | `slops-context-markdown` | PASS | Created/reconciled review-only markdown packet and closeout docs: `Legal/2026-06-28-open-agreements-provider-paragraphs.md`, provider research, legal spot-check, security evidence, sprint/inbox, decision log, handoff, ledgers | No app code or sensitive files touched |
| 2026-06-28 | Phase 4.16 Open-agreements provider paragraphs | `pre-build-research` | PASS | `Direction/reviews/2026-06-28-phase4-16-provider-terms-research.md`; checked official/current Yahoo, Sleeper, Disney/ESPN, open-agreements/Common Paper sources | Web sources can change; counsel review still required |
| 2026-06-28 | Phase 4.16 Open-agreements provider paragraphs | `compliance-by-template` | PASS | Produced custom clauses for future open-agreements ToS / Privacy Policy insertion in `Legal/2026-06-28-open-agreements-provider-paragraphs.md` | Local `References/legal-templates/open-agreements/` checkout missing; no DOCX/template generation |
| 2026-06-28 | Phase 4.16 Open-agreements provider paragraphs | `slops-legal-spot-check` | PASS | `Direction/reviews/2026-06-28-phase4-16-legal-spot-check.md`; P1 flags for ESPN cookie access, Yahoo commercial-use ambiguity, and Sleeper third-party-benefit ambiguity | Not legal advice; publication remains gated |
| 2026-06-28 | Phase 4.16 Open-agreements provider paragraphs | `security-privacy-evidence` | PASS | `Direction/reviews/2026-06-28-phase4-16-security-privacy-evidence.md`; mapped ESPN cookies, Yahoo tokens, Sleeper ids, provider data, AI boundary, retention, and user expectations | Did not inspect secrets, cookies, env files, or production data |
| 2026-06-28 | Phase 4.16 Open-agreements provider paragraphs | `slops-git-flow` | PASS | Stayed on existing branch; explicit doc paths only; `git diff --check` clean; no staging/push/merge/deploy | No commit requested |
| 2026-06-29 | Phase 1.5d Post-win pulse animation | `slops-repo-inspector` | PASS | Confirmed Omen L2 repo root, branch `codex/phase3-13-token-constrained-prompts`, dirty pre-existing Phase 4.16 docs, required startup docs, active inbox/no pin, sprint blockers, API route contract, and latest handoff before editing | Existing dirty Phase 4.16 files were not reverted or staged |
| 2026-06-29 | Phase 1.5d Post-win pulse animation | `planning-pass` | PASS | Added a backend `currentWinStreak` summary-contract follow-up and a frontend Phase 2.19 streak ladder UI follow-up, both pointing to `Blueprints/specs/page-system.md` Post-Win Pulse + Streak Ladder | Planning happened alongside the active build because Justin explicitly asked to document where the streak work fits before completing the single-win slice |
| 2026-06-29 | Phase 1.5d Post-win pulse animation | `slops-tdd` | PARTIAL | Added `test/postWinPulse.test.mjs` for win-signal detection, fail-closed cases, seen-game persistence, and chip label copy; focused test 4/4; full suite 394/394 | No pre-implementation RED was captured because the helper/test were introduced together after plan approval; behavior is still covered by deterministic tests |
| 2026-06-29 | Phase 1.5d Post-win pulse animation | `slops-ux-copy` | PASS | Chose factual chip copy `<Team> W - bright today`; documented future ladder labels but blocked production streak copy on backend truth | No 3-option packet needed; Justin chose hybrid direction in-thread |
| 2026-06-29 | Phase 1.5d Post-win pulse animation | `slops-ui-ux-audit` | PARTIAL | `Blueprints/audits/2026-06-29-phase1-5d-post-win-pulse-ui-ux-audit.md`; no P0/P1 from source/build review; motion honors reduced motion; token usage checked | Authenticated screenshot evidence blocked because `/football` redirected to `/login` without a session |
| 2026-06-29 | Phase 1.5d Post-win pulse animation | `slops-code-review` | PASS | `Blueprints/audits/2026-06-29-phase1-5d-post-win-pulse-code-review.md`; no P0/P1 findings after tightening favorite-team fallback | Self-review artifact; no separate reviewer |
| 2026-06-29 | Phase 1.5d Post-win pulse animation | `slops-quality-baseline` | PASS | `node --test test/postWinPulse.test.mjs` 4/4; `npm test` 394/394; `npm --prefix frontend run build` clean with existing warnings; `npm audit --audit-level=moderate` 0; `git diff --check` clean | Baseline files not ratcheted; existing Vite warnings remain unrelated |
| 2026-06-29 | Phase 1.5d Post-win pulse animation | `slops-mobile-smoke` / `slops-verify` | BLOCKED | Local Vite ran at `http://127.0.0.1:5174`; installed Chrome launched; `/football` redirected to `/login` without authenticated session | Did not spoof Supabase credentials, secrets, or ESPN cookies; follow-up needs sanctioned authenticated browser/session |
| 2026-06-29 | Phase 1.5d Post-win pulse animation | `slops-git-flow` | PARTIAL | Scoped diff prepared on existing branch with explicit paths identifiable; no push/merge/deploy | No branch switch/commit/stage performed because the worktree already had unrelated dirty Phase 4.16 closeout files and no commit was requested |
| 2026-06-30 | Phase 1.7 Platform brand color emphasis | `ui-ux-pro-max` | SKIPPED — no relevant data | Skill's domain searches (product/style/color/typography/etc.) have no third-party brand-color lookup; substituted `WebSearch`/`WebFetch` against public brand-color references plus hand-computed WCAG contrast math | Routing gap: the spec named `ui-ux-pro-max` as the Phase 1.7 guardrail for "exact platform brand hexes," but this skill has no real-world brand-color database — flag for `slops-skill-author` to correct the routing or document the gap in `skill-activation-runbook.md` |
| 2026-06-30 | Phase 1.7 Platform brand color emphasis | `slops-ui-ux-audit` | PARTIAL | Self-administered against the checklist (Accuracy/Accessibility/Aesthetic Integrity); no P0/P1; flagged Sleeper-hex confidence gap and pre-existing 36px `AccentButton` touch target as carry-forward, not new | Authenticated screenshot evidence blocked — same Supabase `getSession()` hang as Phase 1.5d; substituted a browser computed-style check confirming all 8 new CSS custom properties resolve correctly in both themes |
| 2026-06-30 | Phase 1.7 Platform brand color emphasis | `slops-mobile-smoke` | BLOCKED — not yet implemented | Skill's own SKILL.md states v0.1.0, "Not yet implemented; driver extension is the next step" | Substituted a manual `/login` (375x812) overflow + console-error check since all 4 touched routes are auth-gated; real driver implementation is still owed per the skill's own changelog |
| 2026-06-30 | Phase 1.7 Platform brand color emphasis | `slops-code-review` | PASS | Self-administered against all 7 review lenses; verdict merge, no P0/P1; P2s noted (pre-existing 36px touch target, two coexisting hover mechanisms in `PlatformConnections.jsx`) | Self-review artifact; no separate reviewer |
| 2026-06-30 | Phase 1.7 Platform brand color emphasis | `slops-quality-baseline` | PASS (not ratcheted) | `npm --prefix frontend run build` clean; full `npm test` 398/399 (1 pre-existing unrelated CRLF failure, reproduced in isolation); `npm audit --audit-level=moderate` 0; `git diff --check` clean | Did not update `Blueprints/quality/baseline.json` — it's stale (recorded 2026-06-23 at 356/356) and several merged phases since then haven't refreshed it either; refreshing it is its own task, not silently folded into this one |
| 2026-06-30 | Phase 1.7 Platform brand color emphasis | `slops-git-flow` | N/A — no commit yet | Diff is scoped to exactly the 5 files + 1 new file named in the task contract; no unrelated changes | Awaiting Justin's explicit go-ahead to commit per global "never commit unless asked" rule |
| 2026-06-30 | Phase 1.8 Confidence gradient endpoints | `ui-ux-pro-max` | FAILED — script missing | Skill invoked via Skill tool; `~/.claude/skills/ui-ux-pro-max/scripts/search.py` does not exist in this environment (only `SKILL.md` present) | Procedure gap: skill is unusable here without its `scripts/` asset. Park or flag for `slops-skill-author`/`slops-retro` if this recurs across sessions. |
| 2026-06-30 | Phase 1.8 Confidence gradient endpoints | `slops-ui-ux-audit` | PASS — merge, 1 P1 | Self-administered against Phase 1.8 files; verdict: P1 polish (crimson-floor non-text contrast 1.45:1 vs dark track, below WCAG 1.4.11 3:1, mitigated by mandatory text-label redundancy per brand-system), no P0. Full writeup in `Direction/decision_log.md` 2026-06-30 entries and `Blueprints/specs/page-system.md` Confidence Gradient (Phase 1.8) — Resolved section. | None |
| 2026-06-30 | Phase 1.8 Confidence gradient endpoints | `slops-quality-baseline` | PASS (not ratcheted) | `npm --prefix frontend run build` clean; full `npm test` 398/399 (1 pre-existing unrelated CRLF failure); `npm audit --audit-level=moderate` 0; `git diff --check` clean | `Blueprints/quality/baseline.json` still stale from 2026-06-23 — same gap noted in the Phase 1.7 row above, not refreshed here either |
| 2026-06-30 | Phase 1.8 Confidence gradient endpoints | `slops-git-flow` | PASS | Committed directly to `main` (no feature branch — matches this repo's established trunk-based commit history); scope is exactly the 4 files named in the plan-approval gate plus the `page-system.md`/`Direction/` doc updates for close-out | Not pushed; push needs explicit Justin go-ahead per global policy |
| 2026-07-01 | Phase 1.9 Metallic tier treatment | `slops-repo-inspector` | PASS | Orientation confirmed clean `main` at `d5231d6`, no pin, top-5 inbox accurate against `current_sprint.md`/git history before pulling the task | None |
| 2026-07-01 | Phase 1.9 Metallic tier treatment | `ui-ux-pro-max` | FAILED — no usable contrast/brand-color lookup | Invoked as the spec's named guardrail; same missing-`scripts/` gap already logged in the Phase 1.7/1.8 rows — substituted hand-computed WCAG relative-luminance contrast math instead | Repeats a known procedure gap; still worth a `slops-skill-author`/`slops-retro` pass if it recurs again |
| 2026-07-01 | Phase 1.9 Metallic tier treatment | `slops-ui-ux-audit` | PASS — merge, no P0/P1 | Self-administered against the AAA framework: gold/silver/bronze contrast hand-verified (9.99:1 / 11.55:1 / 5.12:1), both themes confirmed identical via browser computed-style check, motion N/A (no animation added), rank redundant with card order + `Omen #N` footer text | None |
| 2026-07-01 | Phase 1.9 Metallic tier treatment | `slops-code-review` | PASS — merge, no P0/P1 | Self-administered: pure-function helper fails closed (`null` for rank 4+), no unused `isTop` var, `Omen #N` footer scope correctly excluded per locked structure, diff limited to the 4 named files | None |
| 2026-07-01 | Phase 1.9 Metallic tier treatment | `slops-quality-baseline` | PASS (not ratcheted) | `npm --prefix frontend run build` clean; full `npm test` 401/401 (includes new `test/metallicTier.test.mjs` 2/2); `npm audit --audit-level=moderate` 0; `git diff --check` clean | `Blueprints/quality/baseline.json` still stale from 2026-06-23 — same gap noted in the Phase 1.7/1.8 rows, still not refreshed |
| 2026-07-01 | Phase 1.9 Metallic tier treatment | `slops-context-markdown` | PASS | Corrected a scope-creep note in `page-system.md`'s Metallic Tier Resolved section (an intermediate working-tree edit had wrongly claimed the `Omen #N` footer pill was also restyled); `current_sprint.md`, `agent_inbox.md`, `decision_log.md` updated to match actual shipped scope | None |
| 2026-07-01 | Phase 1.9 Metallic tier treatment | `slops-git-flow` | N/A — no commit yet | Diff scoped to exactly `frontend/src/lib/metallicTier.js` (new), `frontend/src/index.css`, `frontend/src/pages/DraftAssistant.jsx`, `test/metallicTier.test.mjs` (new), plus the doc close-out set; no unrelated files | Awaiting Justin's explicit go-ahead to commit per global "never commit unless asked" rule |
| 2026-07-02 | Phase 1.12 Gray contrast pass + Standings refinements | `slops-repo-inspector` | PASS | Kickoff orientation confirmed clean `main`, authoritative Omen sources read in order, and the item was pinned in `Direction/agent_inbox.md` before build | Shared `Blueprints/skills/SKILL_ROUTING.md` path referenced by the runbook is still missing in this workspace, so the installed skill copy remained the practical source |
| 2026-07-02 | Phase 1.12 Gray contrast pass + Standings refinements | `slops-ui-ux-audit` | PASS — merge, no P0/P1 | Self-administered against the touched routes: AA-safe token usage on the corrected body text and standings columns; current-user row now has structural emphasis (accent left edge + stronger tint) instead of the prior too-subtle treatment. Full writeup in `Blueprints/handoffs/2026-07-02-phase1-12-gray-contrast-standings-refinements.md` and `Direction/decision_log.md` 2026-07-02 entries. | Authenticated or flow-gated screenshot evidence remains blocked by the recurring Supabase session limitation in this sandbox |
| 2026-07-02 | Phase 1.12 Gray contrast pass + Standings refinements | `slops-code-review` | PASS — merge, no P0/P1 | Self-administered diff review: scope stayed limited to the three targeted routes plus closeout docs; no API/auth/provider/package surface changed; stale Hall of Records note was handled as doc debt instead of a fake UI fix | None |
| 2026-07-02 | Phase 1.12 Gray contrast pass + Standings refinements | `slops-quality-baseline` | PASS (not ratcheted) | `npm test` 401/401; `npm --prefix frontend run build` clean with the existing Vite chunk-size warning; `npm audit --audit-level=moderate` 0; `git diff --check` clean | `Blueprints/quality/baseline.json` remains stale from 2026-06-23 — same recurring baseline-gap noted in prior rows |
| 2026-07-02 | Phase 1.12 Gray contrast pass + Standings refinements | `slops-git-flow` | N/A — no commit yet | Diff remains scoped to `frontend/src/pages/Appearance.jsx`, `frontend/src/pages/Onboarding.jsx`, `frontend/src/pages/Standings.jsx`, and the explicit closeout docs only | Awaiting Justin's explicit go-ahead to commit per global policy |
| 2026-07-02 | Phase 1.13 iOS Safari mobile QA sweep (partial) | `mobile-first-qa-playbook` | Partial — 6/13 routes swept, real-device axis not run | Scripted 375/390/430px overflow + touch-target sweep results in `Blueprints/handoffs/2026-07-02-phase1-13-mobile-qa-sweep-partial.md` | 7 authenticated routes and the real-device axis remain open; sandbox has no reachable auth session or paired device this session |
| 2026-07-02 | Phase 1.13 iOS Safari mobile QA sweep (partial) | `slops-git-flow` | Applied | Branch `frontend/phase1-13-mobile-qa-sweep` cut from local `main`, scoped commit `f826d2e`, staged-set verified exact | Local `main` was already 2 commits ahead of `origin/main` from Codex's unpushed Phase 1.12 — branched rather than compounding that drift |
| 2026-07-02 | Phase 1.13 iOS Safari mobile QA sweep (partial) | `slops-code-review` (self-administered) | Mergeable | Verdict recorded in the handoff | N/A |
| 2026-07-02 | Phase 1.13 iOS Safari mobile QA sweep (partial) | `slops-ui-ux-audit` (self-administered) | No P0/P1 on routes checked | AAA-framework verdict recorded in the handoff | N/A |
| 2026-07-03 | Phase 1.13 remediation — CSP + focus trap | `mobile-first-qa-playbook` (real-device axis, Safari WebDriver) | PASS — both findings fixed and re-verified | `check3_panels` flipped False→True in `qa_suite.py` re-run; CSP fix confirmed via real Safari against `:3000` production-mode server; full writeup in `Blueprints/handoffs/2026-07-03-phase1-13-remediation-csp-and-focus-trap.md` | None |
| 2026-07-03 | Phase 1.13 remediation — CSP + focus trap | `slops-git-flow` | N/A — no commit yet | Diff scoped to `src/middleware/security.js`, `frontend/src/lib/useFocusTrap.js`, `frontend/src/components/layout/Header.jsx`, `frontend/src/components/ui/HelpButton.jsx` on existing branch `frontend/phase1-13-mobile-qa-sweep` | Awaiting Justin's explicit go-ahead to commit per global policy |
| 2026-07-03 | Phase 1.13 discrete fixes | `slops-repo-inspector` | PASS | Read Omen startup docs, prompt attachment, L1/L2 design/color/chant doctrine, latest handoff, current branch/status, and touched frontend/data paths before editing | Existing unrelated dirty files and untracked prompt/logo work were left untouched |
| 2026-07-03 | Phase 1.13 discrete fixes | `slops-git-flow` | PARTIAL — no commit yet | Work stayed on existing `frontend/phase1-13-mobile-qa-sweep`; no branch switch, stash, revert, stage, push, PR, merge, or deploy | Prompt requested `frontend/phase1-13-discrete-fixes`, but branch switch was skipped to avoid disrupting the dirty in-flight Phase 1.13 tree |
| 2026-07-03 | Phase 1.13 discrete fixes | `slops-quality-baseline` | PASS | `npm --prefix frontend run build` clean with existing warnings; `npm test` 401/401; `npm audit --audit-level=moderate` 0; `git diff --check` clean; targeted grep/script audits passed | Baseline JSON not ratcheted; this pass did not deploy |
| 2026-07-03 | Phase 1.13 discrete fixes | `slops-code-review` | PASS — mergeable, no P0/P1 | Self-review covered the scoped source diff and closeout notes; no auth/API/provider/package/migration surface changed | Separate reviewer not run in this session |
| 2026-07-03 | Phase 1.13 discrete fixes | `slops-ui-ux-audit` | PARTIAL — no P0 from requested changes | Source and screenshot review confirmed the requested typography/order/colorway changes are safe enough to merge; follow-ups recorded for authenticated Appearance visual QA and pre-existing `/demo` light-theme readability | Protected Appearance route redirected to login without a session |
| 2026-07-03 | Phase 1.13 discrete fixes | `slops-mobile-smoke` | BLOCKED — proposal-only | Skill is not a runnable driver yet; substituted local mobile screenshots through the run-slops workflow | Formal phone matrix still needs the future driver or a sanctioned authenticated browser/session |
| 2026-07-03 | Phase 1.13 discrete fixes | `run-slops-saloon` | PARTIAL | Local screenshots written under `.Codex/skills/run-slops-saloon/screenshots/phase1-13-discrete-fixes/` for `/demo`, `/draft`, and the protected Appearance attempt | Browser automation timed out after screenshots; Appearance redirected to login, so alphabetical picker still needs authenticated visual confirmation |
| 2026-07-03 | Phase 1.13 discrete fixes | `slops-tdd` | N/A | Considered and read; no durable test harness was added because the changes are data/UI ordering/typography repairs and were covered by focused scripts, build, and full test suite | A future protected-route visual harness would make this kind of frontend repair easier to test end-to-end |

| 2026-07-03 | Logo suite swap | `slops-repo-inspector` | PASS | L0/L2 doctrine, Omen startup docs, current branch state, dirty state, and source logo assets inspected before work | Original worktree was dirty and on another branch, so work moved to a clean `origin/main` worktree |
| 2026-07-03 | Logo suite swap | `slops-git-flow` | PASS | Branch `frontend/logo-suite-swap`; explicit staging for public assets and touched React files; code/assets commit `bab6b1a`; unrelated dirty files in the original worktree left untouched | None |
| 2026-07-03 | Logo suite swap | `slops-quality-baseline` | PASS (not ratcheted) | `npm --prefix frontend run build` clean with existing Vite chunk-size warning; `npm test` 401/401; `npm audit --audit-level=moderate` 0; `git diff --check` clean | `Blueprints/quality/baseline.json` remains stale from 2026-06-23; not refreshed for this narrow asset/UI swap |
| 2026-07-03 | Logo suite swap | `slops-code-review` | PASS - merge, no P0/P1 code defects | Self-reviewed code/assets diff: no package, dependency, API, auth, team color, or unrelated component changes; manifest addition scoped to app icon | UI audit found a P1 asset-production follow-up, not a code blocker |
| 2026-07-03 | Logo suite swap | `slops-ui-ux-audit` | PASS with P1 asset follow-up | Playwright desktop and mobile checks verified logo image load, `alt="Omen"`, drawer rendering, no horizontal overflow, and route reachability across `/`, `/about`, `/login`, and `/trade` | New transparent/light-safe horizontal lockup is needed because the current PNG has a baked black background visible in light-theme app chrome |
| 2026-07-03 | Logo suite swap | `slops-mobile-smoke` | SUBSTITUTED | Installed skill is proposal-only; one-off Playwright sweep covered 375, 390, and 430 mobile widths plus desktop in dark/light modes | Build the official mobile smoke runner before treating this as a reusable automated gate |
| 2026-07-03 | Logo suite swap | `ui-ux-pro-max` | SKIPPED | Searched installed skills and callable tools; no `ui-ux-pro-max` skill/tool was available in this runtime | Add or install the procedure if future prompts require it as a hard gate |
| 2026-07-03 | Transparent horizontal lockup | `slops-repo-inspector` | PASS | Read Omen startup docs, latest handoff, branch/dirty state, logo-suite prerequisite state, and brand/design logo-usage rules before editing | `frontend/logo-suite-swap` is not in `origin/main`; transparent work is stacked on it |
| 2026-07-03 | Transparent horizontal lockup | `slops-git-flow` | PASS | Created branch `frontend/transparent-lockup` from `frontend/logo-suite-swap`; diff scoped to transparent PNGs, three source swaps, and closeout docs | No commit, push, merge, PR, or deploy performed without Justin's explicit gate |
| 2026-07-03 | Transparent horizontal lockup | `slops-quality-baseline` | PASS (not ratcheted) | `npm --prefix frontend run build` clean with existing warnings; `npm test` 401/401; `npm audit --audit-level=moderate` 0; `git diff --check` clean | Baseline files not ratcheted for this narrow asset/UI swap |
| 2026-07-03 | Transparent horizontal lockup | `slops-code-review` | PASS - merge, no P0/P1 | Self-review in `Blueprints/handoffs/2026-07-03-transparent-lockup-handoff.md`; no package, dependency, API, auth, provider, SQL, env, or deploy surface changed | Self-review artifact; no separate reviewer |
| 2026-07-03 | Transparent horizontal lockup | `slops-ui-ux-audit` | PASS - no P0 | Browser screenshots verified no visible black rectangle in light/dark header slots; shield frame rule preserved; wordmark counters transparent | P2: consider a dedicated light-theme lockup variant if pale wordmark contrast needs more punch |
| 2026-07-03 | Transparent horizontal lockup | `playwright` | PASS | Screenshot evidence under `.Codex/skills/run-slops-saloon/screenshots/transparent-lockup/` for `/trade` light/dark header, light drawer, `/`, and `/about` | First server-spawn attempt hit a Windows `npm.cmd` quirk; shell-spawn path worked, but the script timed out after writing screenshots and required manual port cleanup |
| 2026-07-03 | Transparent horizontal lockup | `run-slops-saloon` | PASS | Frontend build plus local Vite/Chromium screenshot pass; backend not required for public header/logo verification | Temporary Vite process on port 5174 was stopped after timeout; screenshots kept local and ignored from git status |
| 2026-07-04 | Phase 1.14 deploy logo verification | `slops-repo-inspector` | PASS | Confirmed Omen L2 repo, branch, dirty state, authoritative startup docs, and pinned Phase 1.14 task before editing | `Direction/agent_inbox.md` was already dirty from the prior Claude refresh; worked with it and did not revert |
| 2026-07-04 | Phase 1.14 deploy logo verification | `slops-deploy-guard` | PASS | Inspected deploy workflow, Dockerfile, compose file, HTTPS setup script, deploy target, and approval boundary; no deploy or production mutation performed | `scripts/oracle-https-setup.sh` remains historical Oracle material, not active KVM1 deploy procedure |
| 2026-07-04 | Phase 1.14 deploy logo verification | `slops-investigate` | PASS | GitHub Actions runs and live prod probes isolated the stale-logo report to timing/open-tab/cache likelihood, not current KVM/image drift | `/api/version` lacks deployed commit metadata, making proof indirect |
| 2026-07-04 | Phase 1.14 deploy logo verification | `slops-tdd` | PASS | RED focused deploy-hardening test failed on missing post-health logo verification; GREEN focused test passed 3/3 after workflow change | Exact bash replay unavailable locally because WSL/bash is missing in this Windows environment; PowerShell-equivalent production probe passed |
| 2026-07-04 | Phase 1.14 deploy logo verification | `slops-git-flow` | PARTIAL - branch only | Created `codex/phase1-14-deploy-logo-verification`; no stage, commit, push, merge, or deploy | Awaiting Justin's explicit commit/push/PR gate |
| 2026-07-04 | Phase 1.14 deploy logo verification | `slops-quality-baseline` | PASS | Full `npm test` 402/402; `npm audit --audit-level=moderate` 0; `git diff --check` clean; focused test 3/3 | Frontend build not run because no frontend source/package file changed |
| 2026-07-04 | Phase 1.14 deploy logo verification | `slops-code-review` | PASS - mergeable, no P0/P1 | Self-review in `Blueprints/handoffs/2026-07-04-phase1-14-deploy-logo-verification.md`; diff limited to workflow/test plus closeout docs | Separate reviewer not run in this session |
| 2026-07-04 | Phase 1.14 deploy logo verification | `slops-context-markdown` | PASS | Updated `Direction/current_sprint.md`, `Direction/agent_inbox.md`, `Direction/decision_log.md`, Done ledger, skill ledger, and dated handoff | No cross-layer promotion; this remains Omen L2 operational evidence |
| 2026-07-04 | Phase 2.9 Account delete UI | `slops-repo-inspector` | PASS | Kickoff docs, sprint queue, facts, known issues, decisions, loop prompt, DoD files, company baseline, skill activation runbook, and latest handoff read before pulling Phase 2.9 | Main-base branch intentionally avoided unrelated off-season branch commits |
| 2026-07-04 | Phase 2.9 Account delete UI | `slops-git-flow` | PASS | Created `codex/phase2-9-account-delete-ui` from `origin/main`; scoped diff kept off the existing `codex/canonical-offseason-signal` branch | One stash remains from the first inbox claim on the prior branch: `phase2.9 inbox claim before branch` |
| 2026-07-04 | Phase 2.9 Account delete UI | `slops-tdd` | PASS | RED `node --test test/accountDeletion.test.mjs` failed on missing helper; GREEN focused helper + backend privacy tests passed 4/4 | UI behavior verified through build/source/browser because no React component test harness exists |
| 2026-07-04 | Phase 2.9 Account delete UI | `slops-ux-copy` | PASS | Destructive copy says "Omen data", names affected Omen-stored categories, and excludes fantasy-platform/sign-in-provider deletion | Not legal advice; public policy language still needs counsel/founder review |
| 2026-07-04 | Phase 2.9 Account delete UI | `security-privacy-evidence` | PASS | `Direction/reviews/2026-07-04-phase2-9-account-delete-ui-security-privacy-evidence.md` documents data boundary, phrase, copy boundary, and residual risks | Legacy `src/omen_gdpr.js` has old wording but mounted route is `src/routes/userPrivacy.js` |
| 2026-07-04 | Phase 2.9 Account delete UI | `slops-ui-ux-audit` | PASS | `Blueprints/audits/2026-07-04-phase2-9-account-delete-ui-ui-ux-audit.md`; no P0/P1; public `/login?deleted=true` Chrome 390px check passed; protected modal screenshots captured under `output/playwright/phase2-9-account-delete-ui/` | Protected route used a throwaway browser-local session plus stubbed local `/api` responses |
| 2026-07-04 | Phase 2.9 Account delete UI | `slops-code-review` | PASS | `Blueprints/audits/2026-07-04-phase2-9-account-delete-ui-code-review.md`; no P0/P1 | Self-review artifact; no separate reviewer |
| 2026-07-04 | Phase 2.9 Account delete UI | `slops-quality-baseline` | PASS (not ratcheted) | Focused 4/4; full `npm test` 403/403; frontend build passed with existing warnings; `npm audit --audit-level=moderate` 0; `git diff --check` clean | Baseline JSON not updated; no deploy |
| 2026-07-04 | Phase 2.9 Account delete UI | `run-slops-saloon` / `playwright` | PASS | Local Vite server + system Chrome verified `/login?deleted=true` notice and protected `/account` delete modal desktop/mobile screenshots with no horizontal overflow | Playwright package was present but bundled browser missing; used installed Chrome executable and a throwaway browser-local auth session |
| 2026-08-24 | M1-Screen-League + M1-Screen-Trade — revised contracts after the 2026-08-22 rejections | **native mobile read gate applied** (all seven sources) and it is what set the shape of both revisions — `omen-mobile-visual-briefs-v1.md` §1.2/§2.4/§14 is where "league activity is three signal families" came from, and §8.1/§9.2/§9.4 is what stopped the Trade revision from deleting neutral-by-default; `figma:figma-use` applied for every canvas write; `slops-repo-inspector` applied — reading `tradeValue.js` before designing is what found `compareTrade()`'s unused third argument; `slops-tdd` **applied in substance but not RED-first** — the flip case was found by an empirical probe over a grid of offers, then frozen into assertions, because the honest question was "does any league shape change this verdict" and inventing the expected value first would have been fiction; `design:design-critique` applied to both rejected contracts; `security-privacy-evidence` applied narrowly (`league_context` is a *request*, never data — no client-supplied roster or scoring is trusted; no token or provider value logged); `rbac-risk-review` **considered and SKIPPED** — no authority boundary moved, personalization reads the caller's own connection only; `slops-ship`/`slops-canary`/`slops-quality-baseline` **UNAVAILABLE** in a standalone Omen checkout, recorded as unavailable rather than skipped-by-choice, with `npm test` run as the local substitute | PASS — both `Done when:` sets met; both items VERIFIED, neither ratified | `npm test` **651/651** (baseline 618 established first, + 33 new), 0 failures. Backend: `src/services/tradeLeagueContext.js`, `src/routes/trade.js`, `test/tradePersonalization.test.js`, `Blueprints/api-routes.md`. Design: `Blueprints/specs/mobile/m1-league-screen-data-plan-v1.md`; Figma `mWjrAKPi4JSIP5lAmGAtB3` — 12 new frames, 8 superseded annotations, QA records `87:2`/`88:2` extended. Handoff: `Blueprints/handoffs/2026-08-24-m1-league-trade-contract-revision.md`. | **The Figma page listing lied, and probing a single node is what caught it.** `get_metadata` with no `nodeId` returned exactly one page — `00 — Start Here` — which would have supported a confident, wrong finding that the 2026-08-16 pass never wrote the frames it claimed. Querying node `86:2` directly returned a fully-populated references board. **Pages load lazily and an empty listing is not evidence of absence**; the same trap sits in `p.children.length`, which reported 0 children for pages holding 27 frames until `setCurrentPageAsync` loaded them. This is the same shape as the 403-without-a-body lesson from 2026-08-21: a cheap probe beats a plausible inference, and the inference here was *especially* plausible because this repo has a documented history of contracts claimed but not written. **Second, and the reason both rejections were answerable at all: neither needed new data.** The League matchup and the Trade personalization inputs were both already in the codebase, discarded or unpassed. The 2026-08-16 pass asked "is there a feed for this?", got a true "no", and stopped — where the answerable question was "what does the code we already run actually return?". **Codify: when a contract is rejected for missing a capability, grep the adapters before designing around the gap.** **Third, a procedure gap that is now urgent rather than academic:** another agent was working in this same checkout during this session, and one commit captured its A6/A7 work interleaved with this work under a message questioning where it belonged. The fix was an isolated `git worktree`. Nothing in the loop docs tells an agent to check whether it is alone in the tree, and `git status` showing unfamiliar modified files is currently the only signal — it should be a kickoff step. |
| 2026-08-24 | Workspace/Figma/Done-gate doctrine pass (the three flags from the M1 revision) | `slops-repo-inspector` applied — reading `check-sprint-staleness.js` first is what set the new checker's shape (findings + an explicit blind-spot block + never edit), and reading `scripts/README.md` first is what stopped a duplicate being written, which is the stated reason that index exists; `planning-pass` applied to mint `M11`; `slops-context-markdown` applied to the Done-file authoring; `slops-tdd` **N/A and stated** — the new script is a read-only reporter with no behaviour to assert beyond its own output, and it was proven by running it against the real four-worktree collision it was written for; `security-privacy-evidence` **considered and SKIPPED** — no auth, credential, provider, or telemetry surface touched; native mobile read gate **N/A** — no native file, though the Figma caveat lands in the native workflow doc; `slops-ship`/`slops-canary` **UNAVAILABLE** in a standalone checkout | PASS — three flags closed, one unplanned false gate corrected, one founder-gated task minted | `scripts/check-workspace-solo.js` run against the live 4-worktree state and correctly reported the other session's tree; `node scripts/check-sprint-staleness.js` no findings; `npm test` 618/618 unchanged (no source touched). Files: `scripts/check-workspace-solo.js`, `scripts/README.md`, `Blueprints/prompts/kickoff-l2.md`, `Blueprints/prompts/HOW-TO-RUN-THE-LOOP.md`, `Blueprints/playbooks/native-mobile-design-delivery-workflow-v1.md`, `Blueprints/done/design-contract-done.md`, `Blueprints/definition-of-done.md`, `Blueprints/done/feature-done.md`, `Direction/current_sprint.md`. | **The most useful thing here was writing the tool against a failure that had already happened rather than an imagined one.** `check-workspace-solo.js` was run in the exact tree that broke, with the other session still live, and its first real output named that session's worktree and branch — so it is verified against the incident, not against a hypothetical. A checker written before the incident would probably have flagged only "dirty tree", which is too noisy to act on; the incident is what identified *registered worktrees* as the signal that actually distinguishes "my leftovers" from "someone else is typing". **Second, the genuinely important find was one nobody asked for.** Reading `definition-of-done.md` in order to add one pointer row surfaced that it still hard-blocked Release Done on a premise retracted 23 days earlier — the fourth instance of the same correction-not-propagated pattern. **The generalisable point is about tooling scope, not diligence:** `check-sprint-staleness.js` prints "anything in a file outside `Direction/` and `Blueprints/handoffs/`" as a blind spot on every single run, and the stale gate sat squarely in that blind spot. **A tool that names its blind spot every run and is never extended to cover it is a documented hole, not a covered one.** The next correction of this class should be a checker over the *active gate* files, not another handwritten sweep. |
| 2026-08-24 | Post-merge follow-up — `scripts/**` CI gate + record correction | `slops-repo-inspector` applied — reading `pr-quality.yml`'s own header comment is what made the gap legible (it exists *because* `src/**` once had no PR CI, and it reproduced that exact omission for `scripts/**`); `slops-tdd` **applied as mutation testing**, which is the only honest way to prove a CI gate — a gate is a claim about what it would catch, and reading it proves nothing; `slops-quality-baseline` applied (`npm test` 651/651 on merged `main`); `security-privacy-evidence` **applied narrowly** — the decisive design constraint was that six scripts touch network/Supabase/Redis/prod and must never auto-execute in CI, so they are syntax-checked only; `slops-ship`/`slops-canary` **UNAVAILABLE** in a standalone checkout | PASS — gate added and proven by mutation; stale merge/deploy records corrected | `pr-quality.yml` `scripts-gate` job; syntax step green across all 17 scripts and red on an injected syntax error; validator red when the blind-spot list is emptied, green when restored; live production verification of `trade-compare.v2` on four behaviours. `npm test` 651/651. | **Two lessons, and the second is about my own test rather than the subject.** First: the gap was found by asking why #366 reported *no checks* instead of accepting "it's only docs". **"No checks ran" and "checks passed" render almost identically in a PR view and are completely different claims** — the same shape as `DEFERRED-CI` vs a pass, recorded here on 2026-08-20. Second: an ad-hoc mutation run reported the *restored* script as still failing, which looked like a broken gate. It was a broken harness — the reproduction omitted the `set +e` guard the real step has, so `set -e` aborted on the checker's legitimate exit 1. **I nearly drew a wrong conclusion in both directions from a test that did not match the thing it claimed to test.** The fix was to extract the step verbatim from the YAML and run *that*, which is the only version of the test whose result means anything. Generalisable: when reproducing a CI step by hand, extract it from the workflow rather than retyping it. |

## Monthly Review

On the first of each month and after every release:

1. Compare this ledger with `../done/LEDGER.md`.
2. Find required skills skipped, conditional skills never triggered, and repeated procedure gaps.
3. Use `slops-retro` to decide whether to fix the playbook, skill, prompt, done gate, or task scope.
4. Do not keep a skill active solely because it exists; propose parking or retirement when it has no valid trigger.
| 2026-07-04 | Rate-limit scope fix + Standings off-season triage | `slops-investigate` | PASS | Traced the 429 raw-JSON leak to `src/server.js`'s globally-mounted rate limiter (not a frontend gap) and the Standings load failure to missing off-season detection in `src/routes/league.js`, by reading every relevant call site before forming a hypothesis | None — root causes confirmed via code reading + live request-burst verification |
| 2026-07-04 | Rate-limit scope fix + Standings off-season triage | `slops-quality-baseline` | PASS | Full `npm test` 402/402; `npm audit --audit-level=moderate` 0; `git diff --check` clean; live 150-request burst verification against a locally-booted production-mode server | Frontend build not run — no frontend source changed |
| 2026-07-04 | Rate-limit scope fix + Standings off-season triage | `slops-context-markdown` | PASS | Updated `Direction/current_sprint.md`, `Direction/decision_log.md`, `Blueprints/handoffs/frontend-to-backend.md`, Done ledger, skill ledger, and dated handoff | None |
| 2026-07-04 | Canonical off-season signal | `slops-repo-inspector` | PASS | Confirmed Omen L2 repo, branch/dirty state, startup docs, active inbox, sprint item, frontend-to-backend request, and latest handoff before continuing the build | Existing local docs from the Phase 1.13 correction were preserved and worked with |
| 2026-07-04 | Canonical off-season signal | `slops-tdd` | PASS | Intended RED focused tests failed on missing `isOffSeason`, dashboard `ready` instead of `off_season`, and standings `502` instead of empty success; GREEN focused tests passed 28/28 | None |
| 2026-07-04 | Canonical off-season signal | `slops-git-flow` | PARTIAL - branch only | Created and worked on `codex/canonical-offseason-signal`; scoped diff limited to backend route/service logic, tests, and closeout docs | No stage, commit, push, PR, merge, or deploy without Justin's explicit gate |
| 2026-07-04 | Canonical off-season signal | `slops-quality-baseline` | PASS (not ratcheted) | Full `npm test` 407/407; `npm audit --audit-level=moderate` 0; `npm --prefix frontend run build` clean with existing warnings; final `git diff --check` clean; baseline comparison passes because current test count exceeds stale 356/356 baseline | `Blueprints/quality/baseline.json` remains stale from 2026-06-23 and was not ratcheted inside this feature branch |
| 2026-07-04 | Canonical off-season signal | `slops-code-review` | PASS - mergeable, no P0/P1 | Self-review in `Blueprints/audits/2026-07-04-canonical-offseason-signal-code-review.md`; reviewed route precedence, provider-call avoidance, credential/logging surface, tests, and scope | Separate reviewer not run in this session |
| 2026-07-04 | Canonical off-season signal | `slops-context-markdown` | PASS | Updated `Blueprints/api-routes.md`, backend/frontend handoff bus, `Direction/current_sprint.md`, `Direction/agent_inbox.md`, `Direction/decision_log.md`, Done ledger, skill ledger, code review, and dated handoff | No cross-layer promotion; this remains Omen L2 contract evidence |
| 2026-07-04 | Canonical off-season signal | `security-privacy-evidence` | N/A | Considered because standings touches provider-backed routes, but no changed credential flow, auth boundary, data classification, consent, retention, telemetry, external sharing, SQL, secrets, dependencies, or logging surface; off-season path avoids provider calls | No separate security evidence note created |
| 2026-07-04 | Phase 2.10 Trade share card | `slops-repo-inspector` | PASS | Read startup docs, sprint queue, facts, known issues, decision log, loop prompt, DoD, company baseline, skill routing, API contracts, and latest handoff before pulling Phase 2.10 | Worked from `frontend/phase2-10-trade-share-card`; no deploy or production mutation |
| 2026-07-04 | Phase 2.10 Trade share card | `slops-tdd` | PASS | RED focused tests failed on missing frontend helper/meta service/OG route; GREEN focused share/meta/frontend tests passed 9/9 after implementation | React behavior verified through browser QA because no component test harness exists |
| 2026-07-04 | Phase 2.10 Trade share card | `slops-ux-copy` | PASS | Share copy is recommendation-first, plain, no emoji, and explicitly labels the public snapshot/privacy boundary | Strategy sidebar rewrite remains separate Phase 2.13 |
| 2026-07-04 | Phase 2.10 Trade share card | `security-privacy-evidence` | PASS | `Direction/reviews/2026-07-04-phase2-10-trade-share-card-security-privacy-evidence.md` documents public snapshot boundary, credential exclusion, escaped OG/meta output, and residual production-smoke risk | No Supabase, SQL, secret, provider credential, package, or env change |
| 2026-07-04 | Phase 2.10 Trade share card | `slops-ui-ux-audit` | PASS | `Blueprints/audits/2026-07-04-phase2-10-trade-share-card-ui-ux-audit.md`; no P0/P1; browser screenshots under `output/playwright/phase2-10-trade-share-card/` | Local Vite API calls were mocked; shared app chrome `/api/dashboard/summary` was also mocked to avoid unrelated dev proxy noise |
| 2026-07-04 | Phase 2.10 Trade share card | `slops-code-review` | PASS | `Blueprints/audits/2026-07-04-phase2-10-trade-share-card-code-review.md`; no P0/P1 | Self-review artifact; no separate reviewer |
| 2026-07-04 | Phase 2.10 Trade share card | `run-slops-saloon` / `playwright` | PASS | Local Vite + Playwright verified public card desktop/mobile light, desktop dark, and mobile compare/share flow; no console errors, no overflow, minimum visible target 44px | First two QA attempts exposed selector/port-cleanup issues; final run used strict port and process-tree cleanup |
| 2026-07-04 | Phase 2.10 Trade share card | `slops-quality-baseline` | PASS (not ratcheted) | Focused 9/9; full `npm test` 414/414; frontend build passed with existing warnings; `npm audit --audit-level=moderate` 0; `git diff --check` clean; browser QA green | Baseline JSON not ratcheted; no deploy |
| 2026-07-04 | Phase 2.10 Trade share card | `slops-git-flow` | PASS - local commits only | Feature branch `frontend/phase2-10-trade-share-card`; code/test commit `4db9db0`; docs/evidence commit staged separately after closeout docs | No push, PR, merge, deploy, or production write-smoke without Justin gate |
| 2026-07-05 | Phase 2.18 Waiver Wire route activation | `slops-repo-inspector` | PASS | Verified PR #80 (Phase 2.10) merged to `origin/main` at `6cd554b` before pulling the next task; fast-forwarded local `main`; read startup docs, sprint queue, inbox, page-system, Header/route/ProtectedRoute conventions before scoping | Discovered mid-session that a previously-assumed-local branch had already been merged by another agent session; confirmed via `gh pr view` rather than assuming |
| 2026-07-05 | Phase 2.18 Waiver Wire route activation | `slops-tdd` | N/A | No new pure/testable behavior introduced — route registration, nav-item addition, and a Tailwind-to-token color sweep, not new logic | Reason recorded rather than gate silently skipped |
| 2026-07-05 | Phase 2.18 Waiver Wire route activation | `run-slops-saloon` (preview tools) | PASS (partial) | Confirmed `/waiver` redirects unauthenticated visitors to `/login` with no console errors; confirmed the new nav item is correctly hidden for guests via the existing per-item `auth` filter | `preview_screenshot` timed out repeatedly even on the public `/trade` page (environment/tooling issue); no authenticated screenshot captured — same sandbox limitation as Phase 1.5d/1.7/1.8/1.12 |
| 2026-07-05 | Phase 2.18 Waiver Wire route activation | `slops-ui-ux-audit` | PASS (self-administered, partial) | Grep-confirmed zero remaining raw Tailwind color literals; touch targets/focus rings unchanged from pre-existing implementation; token usage matches already-audited `TradeAnalyzer.jsx`/`TradeShare.jsx` patterns | No live authenticated screenshot; documented as a gap rather than claimed |
| 2026-07-05 | Phase 2.18 Waiver Wire route activation | `slops-code-review` | PASS (self-administered) | Reviewed full diff for correctness/security/scope; no P0/P1; no auth/data/secret boundary touched | No separate reviewer in this session |
| 2026-07-05 | Phase 2.18 Waiver Wire route activation | `slops-quality-baseline` | PASS (not ratcheted) | Full `npm test` 414/414 (unchanged); frontend build clean with pre-existing warnings; `npm audit --audit-level=moderate` 0; `git diff --check` clean | Baseline JSON not ratcheted; no deploy |
| 2026-07-05 | Phase 2.18 Waiver Wire route activation | `slops-git-flow` | PASS - local commits only | Feature branch `frontend/phase2-18-waiver-wire-activation` | No push, PR, merge, or deploy without Justin's explicit gate |
| 2026-07-05 | Phase 2.18 Waiver Wire route activation | `slops-context-markdown` | PASS | Updated `Direction/current_sprint.md`, `Direction/decision_log.md`, `Blueprints/specs/page-system.md`, Done ledger, skill ledger, and dated handoff | None |
| 2026-07-05 | Phase 1.15 Post-deploy visual smoke | `slops-repo-inspector` | PASS | Read Omen startup docs, sprint queue, facts, known issues, decision log, loop prompt, DoD, company baseline, skill routing, API contracts, and latest handoff before pulling Phase 1.15 | Existing Phase 2.18 work in the main checkout was isolated by creating a separate worktree |
| 2026-07-05 | Phase 1.15 Post-deploy visual smoke | `slops-deploy-guard` | PASS | Workflow edit stayed read-only and post-health; no deploy, KVM mutation, secrets, package files, Docker config, DNS/SSL, database, or production config changed | The workflow step will only execute on a future approved/normal deploy run |
| 2026-07-05 | Phase 1.15 Post-deploy visual smoke | `slops-tdd` | PASS | RED deploy-hardening test failed before the workflow smoke existed; RED login-branding test failed before `/login` used the lockup image; GREEN focused tests passed 5/5 | React component behavior remains source/browser verified because no component test harness exists |
| 2026-07-05 | Phase 1.15 Post-deploy visual smoke | `slops-canary` | PASS (pattern implemented) | Added public-route logo smoke to `.github/workflows/deploy.yml`; read-only production route/bundle probe passed for `/`, `/about`, and `/login` | Actual post-deploy workflow canary not run because no deploy was performed |
| 2026-07-05 | Phase 1.15 Post-deploy visual smoke | `run-slops-saloon` / `playwright` | PASS | Local Vite + system Chrome captured `/`, `/about`, and `/login` light mobile screenshots and `qa-summary.json`; all three rendered the transparent lockup with `darkOpaqueCorners: 0` | QA command timed out after evidence because the Vite child stayed alive; targeted process tree was stopped afterward |
| 2026-07-05 | Phase 1.15 Post-deploy visual smoke | `slops-ui-ux-audit` | PASS | `Blueprints/audits/2026-07-05-phase1-15-post-deploy-visual-smoke-ui-ux-audit.md`; no P0/P1; login brand now matches public logo treatment | Dark-mode screenshots skipped for this narrow light-theme logo regression; documented in the handoff/Done ledger |
| 2026-07-05 | Phase 1.15 Post-deploy visual smoke | `slops-code-review` | PASS | `Blueprints/audits/2026-07-05-phase1-15-post-deploy-visual-smoke-code-review.md`; no P0/P1 | Self-review artifact; no separate reviewer |
| 2026-07-05 | Phase 1.15 Post-deploy visual smoke | `slops-quality-baseline` | PASS (not ratcheted) | Focused 5/5; full `npm test` 416/416; frontend build passed with existing warnings; client build passed; root audit 0; `git diff --check` clean | Baseline JSON not ratcheted; no deploy |
| 2026-07-05 | Phase 1.15 Post-deploy visual smoke | `slops-git-flow` | PARTIAL - branch/worktree only | Created `codex/phase1-15-post-deploy-visual-smoke` in `C:\Users\JDuve\dev\SLOPS\.codex-worktrees\omen-phase1-15-post-deploy-smoke` from `origin/main` | No stage, commit, push, PR, merge, or deploy without Justin's explicit gate |
| 2026-07-05 | Phase 1.15 Post-deploy visual smoke | `slops-context-markdown` | PASS | Updated sprint queue, inbox, decision log, sprints completed, Done ledger, skill ledger, page-system row, audits, and dated handoff | No cross-layer promotion; this remains Omen L2 release/page evidence |
| 2026-07-05 | Phase 2.12 Trade Analyzer form redesign | `slops-repo-inspector` | PASS | Confirmed the original checkout was dirty with completed Phase 2.11 work and created a clean sibling worktree for Phase 2.12 from `origin/main` | Phase 2.12 branch is separate from the local Phase 2.11 branch; no dirty files were swept |
| 2026-07-05 | Phase 2.12 Trade Analyzer form redesign | `slops-git-flow` | PARTIAL - branch/worktree only | Created `codex/phase2-12-trade-form-redesign` in `C:\Users\JDuve\dev\SLOPS\.codex-worktrees\omen-phase2-12-trade-form` | No stage, commit, push, PR, merge, or deploy without Justin's explicit gate |
| 2026-07-05 | Phase 2.12 Trade Analyzer form redesign | `slops-tdd` | PASS | RED `node --test test\tradeForm.test.mjs` failed on missing helper; GREEN helper/share tests 5/5; focused trade contract tests 16/16 | React component behavior verified through browser QA because no component test harness exists |
| 2026-07-05 | Phase 2.12 Trade Analyzer form redesign | `slops-ui-ux-audit` | PASS | `Blueprints/audits/2026-07-05-phase2-12-trade-form-redesign-ui-ux-audit.md`; no P0/P1; buttons meet 44px target, use text labels, and no mobile overflow was observed | Light-mode screenshot not captured; dark mobile public route screenshot is under `output/playwright/phase2-12-trade-form-redesign/` |
| 2026-07-05 | Phase 2.12 Trade Analyzer form redesign | `slops-code-review` | PASS | `Blueprints/audits/2026-07-05-phase2-12-trade-form-redesign-code-review.md`; no P0/P1; no backend route/schema/auth/provider/SQL/env/package boundary changed | Self-review artifact; no separate reviewer |
| 2026-07-05 | Phase 2.12 Trade Analyzer form redesign | `slops-ux-copy` | PASS | Deal-shape copy explicitly says Multi-team mode compares only the user's net send/receive side; no fake three-team optimizer claim | Phase 2.13 strategy-copy rewrite remains open |
| 2026-07-05 | Phase 2.12 Trade Analyzer form redesign | `playwright` | PASS | Local Vite + Playwright verified `/trade` at 390px dark mode with scoring, deal-shape, position buttons, VORP help, mock banner, result render, closed autocomplete after submit, and no horizontal overflow | API compare was stubbed locally; no backend server or production smoke ran |
| 2026-07-05 | Phase 2.12 Trade Analyzer form redesign | `slops-quality-baseline` | PASS (not ratcheted) | Full `npm test` 419/419; frontend build passed with existing warnings; root audit 0; `git diff --check` clean | Dependency junctions to the main checkout's existing `node_modules` were used in the sibling worktree; no install or package-file churn |
| 2026-07-05 | Phase 2.12 Trade Analyzer form redesign | `slops-context-markdown` | PASS | Updated `Direction/current_sprint.md`, `Direction/agent_inbox.md`, `Direction/decision_log.md`, `Direction/sprints_completed.md`, page-system row, Done ledger, skill ledger, audits, and dated handoff | No cross-layer promotion; this remains Omen L2 frontend evidence |
| 2026-07-05 | Phase 2.11 Signal-honesty labels | `slops-repo-inspector` | PASS | Read kickoff docs and inspected branch/dirty state before pulling the active Phase 2.11 inbox item | Worked from `origin/main` on `codex/phase2-11-signal-honesty-labels`; no production mutation |
| 2026-07-05 | Phase 2.11 Signal-honesty labels | `slops-tdd` | PASS | RED focused test failed on missing `frontend/src/lib/omenSignalLabels.js`; GREEN focused helper tests passed 2/2 after implementation | React behavior verified through browser QA because no component test harness exists |
| 2026-07-05 | Phase 2.11 Signal-honesty labels | `slops-git-flow` | PARTIAL - branch only | Created `codex/phase2-11-signal-honesty-labels`; scoped diff to Omen recommendation labels, helper tests, evidence, and closeout docs | No stage, commit, push, PR, merge, or deploy without Justin's explicit gate |
| 2026-07-05 | Phase 2.11 Signal-honesty labels | `slops-quality-baseline` | PASS (not ratcheted) | Focused 2/2; full `npm test` 418/418; frontend build passed with existing warnings; root audit 0; `git diff --check` clean | Baseline JSON not ratcheted; no deploy |
| 2026-07-05 | Phase 2.11 Signal-honesty labels | `slops-ui-ux-audit` | PASS | `Blueprints/audits/2026-07-05-phase2-11-signal-honesty-labels-ui-ux-audit.md`; no P0/P1; labels use text plus data-source token color, so color is not the only cue | Protected `/omen` redirected to login; public `/demo` screenshot used the same recommendation view with stubbed local API response |
| 2026-07-05 | Phase 2.11 Signal-honesty labels | `slops-code-review` | PASS | `Blueprints/audits/2026-07-05-phase2-11-signal-honesty-labels-code-review.md`; no P0/P1; no backend, auth, provider, SQL, env, package, or deploy boundary touched | Self-review artifact; no separate reviewer |
| 2026-07-05 | Phase 2.11 Signal-honesty labels | `slops-ux-copy` | PASS | Copy is short and direct: `Input honesty`, `Live`, `Stub`, `Mock`, `Unavailable`, and Used/Limited/Preview/Not-used states; demo is not presented as live | Broader Trade Analyzer strategy rewrite remains Phase 2.13 |
| 2026-07-05 | Phase 2.11 Signal-honesty labels | `playwright` | PASS | Local Vite + Playwright verified public `/demo` dark mobile render with input-honesty labels and saved screenshot/body/summary under `output/playwright/phase2-11-signal-honesty-labels/` | Shared app chrome logged a dev-only `/api/dashboard/summary` 500 because no backend proxy was running; `/api/demo` was fulfilled locally and the target panel rendered |
| 2026-07-05 | Phase 2.11 Signal-honesty labels | `slops-context-markdown` | PASS | Updated `Direction/current_sprint.md`, `Direction/agent_inbox.md`, `Direction/decision_log.md`, `Direction/sprints_completed.md`, Done ledger, skill ledger, audits, and dated handoff | No cross-layer promotion; this remains Omen L2 frontend evidence |
| 2026-07-05 | Generalized authenticated-route QA driver | `slops-repo-inspector` | PASS | Confirmed PR #81 (Phase 2.18) merged (`1818626`); discovered mid-task that `.claude/skills/` is gitignored and `.agents/skills/run-slops-saloon/` is the real canonical location, correcting an initial misstep | An earlier attempt built the same files under the gitignored `.claude/` mirror before this was caught and corrected |
| 2026-07-05 | Generalized authenticated-route QA driver | `slops-investigate` | PASS | Root-caused two pre-existing driver failures via direct code reading + a debug run (not guessed): `driver_espn_recovery.cjs` lands on `/onboarding` because it never seeds `omen.onboarding.done`; `driver.cjs`'s H1 assertion is stale against the Phase 1.10B copy change | Neither fixed on those files — documented as pre-existing, out of scope |
| 2026-07-05 | Generalized authenticated-route QA driver | `run-slops-saloon` (this skill, extended) | PASS | Full `/waiver` smoke test 4/4 states against the real merged Phase 2.18 route; light+dark screenshots visually spot-checked; run from a fresh `git worktree` with `npm ci` to prove clean-checkout correctness | Regression check on the 2 untouched existing drivers surfaced pre-existing unrelated breakage (see `slops-investigate` row) |
| 2026-07-05 | Generalized authenticated-route QA driver | `slops-git-flow` | PASS - local commits only, isolated worktree | Branch `tooling/authenticated-route-driver`; used a `git worktree` (`.claude-worktrees/omen-tooling-authenticated-route-driver`) rather than the shared main checkout, which was mid-task on a concurrent `codex/phase2-11-signal-honesty-labels` session | No push, PR, merge, or deploy without Justin's explicit gate |
| 2026-07-05 | Generalized authenticated-route QA driver | `slops-context-markdown` | PASS | Updated `Direction/known_issues.md` (new Resolved Gaps section), `Direction/decision_log.md`, `Blueprints/playbooks/omen-company-baseline.md`, rewrote `.agents/skills/run-slops-saloon/SKILL.md`, and wrote the dated handoff | No cross-layer promotion; Omen L2 tooling evidence |
| 2026-07-05 | Phase 2.13 Trade Analyzer Strategy + Buy Low copy rewrite | `slops-ux-copy` | PASS | Two passes: (1) 3 full option sets for the 4 `TRADE_TIPS` bullets, Justin picked Option C; (2) line-by-line pass on the 5 Buy Low `reason` lines, 3 rewritten, 2 judged already on-voice and left unchanged | Discovered `tradePulse.js` has no refresh mechanism despite its own "updated each preseason" copy — filed as a Decisions-lane sprint item, not a copy fix |
| 2026-07-05 | Phase 2.13 Trade Analyzer Strategy + Buy Low copy rewrite | `slops-context-markdown` | PASS | Updated `Direction/current_sprint.md` (Decisions-lane addition + Phase 2.13 closeout), wrote dated handoff | No cross-layer promotion; Omen L2 frontend evidence |
| 2026-07-06 | Vault secretId plaintext logging fix | `slops-tdd` | PASS | RED: new `test/platforms.test.js` test failed pre-fix (raw secret id present in captured logs); GREEN after one-line fix; full `npm test` 422/422 | None |
| 2026-07-06 | Vault secretId plaintext logging fix | `slops-git-flow` | PARTIAL - branch only | Created `claude/fix-vault-secretid-logging`; scoped diff to `src/routes/platforms.js` + `test/platforms.test.js` + closeout docs | No stage, commit, push, PR, merge, or deploy without Justin's explicit gate |
| 2026-07-06 | Vault secretId plaintext logging fix | `slops-context-markdown` | PASS | Updated `Direction/current_sprint.md`, `Direction/agent_inbox.md`, `Direction/decision_log.md`, Done ledger, this ledger, and dated handoff | No cross-layer promotion; Omen L2 backend evidence |
| 2026-07-08 | Phase 4.20a mobile-build kill-switch layer | `slops-git-flow` | PASS | Scoped diff to `Account.jsx`, `UpgradeState.jsx`, `PlatformConnections.jsx`, `ConnectLeague.jsx`, `.env.example`, `ENV-INVENTORY.md`, new `test/appStoreBuildKillSwitch.test.js`; committed, pushed, draft PR opened per this session's explicit harness instructions | — |
| 2026-07-08 | Phase 4.20a mobile-build kill-switch layer | `slops-code-review` | PASS (self-administered) | Verified exact line numbers via direct file reads before editing; closed both `espnRecovery`/`!connected` OR-bypasses identified during planning; no new P0/P1 found | — |
| 2026-07-08 | Phase 4.20a mobile-build kill-switch layer | `slops-ui-ux-audit` | N/A | Pure suppression of already-gated existing UI (mirrors `BILLING_ENABLED`/`ESPN_ENABLED`), not new UI surface | — |
| 2026-07-08 | Phase 4.20a mobile-build kill-switch layer | `slops-ux-copy` | N/A | No new copy introduced; only conditional suppression of existing strings | — |
| 2026-07-08 | Phase 4.20a mobile-build kill-switch layer | `security-privacy-evidence` | N/A | Removes UI-entry-point reachability for existing gated features; no new data flow, consent, or retention boundary introduced (same reasoning as the 2026-07-06 vault-fix precedent) | — |
| 2026-07-08 | Phase 4.20a mobile-build kill-switch layer | `slops-context-markdown` | PASS | Updated `Direction/current_sprint.md`, `Direction/agent_inbox.md`, `Direction/decision_log.md`, `Blueprints/done/LEDGER.md`, this ledger, and dated handoff | No cross-layer promotion; Omen L2 frontend evidence |
| 2026-07-08 | Phase 4.20c reviewer/demo access playbook | `slops-context-markdown` | PASS | New `Blueprints/playbooks/app-store-reviewer-access.md` + README index entry, matching existing `espn-recovery.md` house style | — |
| 2026-07-08 | Phase 4.20c reviewer/demo access playbook | `pre-build-research` | PASS | Verified `/demo`, `/trade`, `/draft` route registration, `dataMode` semantics, and the backend demo fixture by direct source reading; executed `buildDemoModeResponse()` in isolation (`node -e`) since root `npm ci` fails in this sandbox on a pre-existing lockfile mismatch | Root lockfile is out of sync with `package.json` (`Missing: gcp-metadata@7.0.1`) — blocks full backend boot for verification in this sandbox; not fixed here since it's a package-file edit outside this task's scope |
| 2026-07-08 | Phase 4.20c reviewer/demo access playbook | `slops-git-flow` | PASS | Branch restarted from `origin/main` after the 4.20a squash-merge (`9506ea5`), per harness "already-merged branch" instructions; scoped diff to the new playbook file + README index entry + closeout docs; committed, pushed, draft PR opened | — |
| 2026-07-08 | Phase 4.20c reviewer/demo access playbook | `slops-ui-ux-audit` / `slops-ux-copy` | N/A | Doc-only deliverable; no app UI or in-app copy changed | — |
| 2026-07-08 | Phase 4.20c reviewer/demo access playbook | `security-privacy-evidence` | N/A | Documents an existing, already-reviewed no-auth/no-secret-touching route; introduces no new data flow, consent, or retention boundary | — |
| 2026-07-12 | Phase 4.20d store metadata/privacy-label/gambling copy audit | `slops-ux-copy` | PASS | Full-file review (not just grep) of `Landing.jsx`, `OmenLanding.jsx`, `Onboarding.jsx`, `README.md`; found zero gambling/DFS/wagering language; found 2 high-severity unqualified platform-claim gaps and rewrote both by adding the Phase 4.16 packet's already-approved attribution clause verbatim | Findings table + rationale: `Direction/reviews/2026-07-12-store-metadata-privacy-gambling-copy-audit.md` |
| 2026-07-12 | Phase 4.20d store metadata/privacy-label/gambling copy audit | `security-privacy-evidence` | PASS | Confirmed no gambling/wagering claims and closed the affiliation-disclaimer gap on the two surfaces that name Yahoo/Sleeper/ESPN with support-status claims | No new data flow, consent, or retention boundary introduced — copy-only |
| 2026-07-12 | Phase 4.20d store metadata/privacy-label/gambling copy audit | `slops-context-markdown` | PASS | Updated `Direction/current_sprint.md`, `Direction/agent_inbox.md`, `Direction/decision_log.md`, this ledger, new findings doc, and dated handoff | No cross-layer promotion; Omen L2 frontend/docs evidence |
| 2026-07-12 | Full Stripe/billing removal (backend + schema) | `slops-tdd` | PASS | Updated 8 test files to the free-for-all contract rather than deleting coverage; RED confirmed pre-edit (stale subscription assertions failed against new route behavior) then GREEN after rewrite; full `npm test` 388/388 | 2 wholly-Stripe test files and 1 billing-config-only test file deleted outright since their subject no longer exists |
| 2026-07-12 | Full Stripe/billing removal (backend + schema) | `slops-code-review` (self-administered) | PASS | Full-repo grep sweep for `stripe`/`is_subscribed`/`requireSubscription` after the main edit pass caught 3 missed spots (a stale doctrine comment, an nginx proxy block, a docker-compose env passthrough) | `client/` legacy app's Stripe calls intentionally left as dead code per explicit scope decision |
| 2026-07-12 | Full Stripe/billing removal (backend + schema) | `security-privacy-evidence` | PASS | Confirmed no secrets/credentials touched; schema drop statements added to source but not executed against production (out of this session's capability) — flagged as requiring separate Justin-approved production apply | Root `npm audit` improved to 0 vulnerabilities (down from 5) as a side effect of removing the `stripe` package's transitive deps |
| 2026-07-12 | Full Stripe/billing removal (backend + schema) | `slops-context-markdown` | PASS | Updated `Direction/current_sprint.md`, `Direction/decision_log.md`, `Direction/facts-of-record.md`, `Direction/release_readiness.md`, `Blueprints/api-routes.md`, `Blueprints/agent_handoff.md`, both append-only handoff logs, this ledger | No cross-layer promotion; Omen L2 backend/frontend/schema evidence |
| 2026-07-13 | Phase 4.20b public legal/support pages | `slops-repo-inspector` / `slops-tdd` / `slops-git-flow` / `security-privacy-evidence` / `slops-ux-copy` / `slops-ui-ux-audit` / `slops-mobile-smoke` / `slops-code-review` / `slops-quality-baseline` / `slops-legal-spot-check` / `slops-context-markdown` | PASS | Four public pages, footer navigation, approved support/privacy/legal contacts, RED→GREEN focused tests, full quality pass, and browser viewport checks | `compliance-by-template` N/A: no local template checkout and no signable document output; ship/canary N/A: local branch only |
| 2026-07-15 | Phase A Button Primitive implementation | `jules-react-component` | PASS | Built the canonical Button primitive strictly following `jules-01-button.md`. Component supports variants, sizes, tone overrides, and `asChild` composition without external dependencies. Successfully handled custom active states and motion-reduce fallbacks. Passed local Vite build verification. | Component only; no page migrations performed. Focus-ring implementation relies on outline utility fallback since `--color-focus-ring` is absent from design tokens. |
| 2026-07-16 | Phase A Input/Textarea Primitive implementation | `jules-react-component` | PASS | Built canonical Input and Textarea primitives strictly following `jules-02-input-textarea.md`. Components share the same prop surface, support sizes, states (default, error, success), and leading/trailing icons. Used only existing token variables and zero external dependencies. Checked iOS touch sizing class compatibility. Passed local Vite build verification. | Phase A component-build only; no page migrations performed. Did not use raw hex colors. |
| 2026-07-16 | Phase A Badge/Chip Primitive implementation | `jules-react-component` | PASS | Built canonical Badge and Chip primitives per `jules-03-badge-chip.md`. Mapped specific status tones to explicit `--color-data-*` and `--color-risk-*` tokens for Badge, and position/brand tones for Chip. Assured AA contrast ratios via 15% opacity backgrounds and primary foreground color for Badges, and solid backgrounds for Chips. Passed local Vite build verification. | Component only; no page migrations performed. Zero raw hex values used. |
| 2026-07-16 | Phase A PlatformBadge Primitive implementation | `jules-react-component` | PASS | Built canonical PlatformBadge primitive per `12-platform-badge-brief.md`. Implemented text/monogram fallbacks since no icon assets exist. Handled specific `-chip` tokens for contrast where applicable. Added sizes (sm/md), toggleable labels, and correct `aria-label` handling. Verified zero raw hex and zero package lockfile churn. Passed local Vite build verification. | Component only; no page migrations performed. Platform icons intentionally mocked with text fallbacks due to missing assets as instructed. |
| 2026-07-16 | Phase A PlatformConnectionCard Primitive implementation | `jules-react-component` | PASS | Built canonical PlatformConnectionCard composition per `08-platform-connection-card-brief.md`. Implemented as a Level-2 composition of Card, Badge, and PlatformBadge. Properly mapped statuses (`connected`, `disconnected`, `error`, `pending`) to explicit Badge tones (`success`, `neutral`, `risk`). Implemented correct aria-labelledby and aria-live configurations. Verified zero raw hex and zero package lockfile churn. Passed local Vite build verification. | Component only; no page migrations performed. |
| 2026-07-16 | Phase A State Components (Empty/Error/Loading) | `jules-react-component` | PASS | Built/Updated EmptyState, ErrorState, LoadingState, and Spinner per `07-state-components-empty-error-loading-brief.md`. Implemented skeleton and spinner variants for LoadingState. Removed all raw Tailwind color literals (red, slate, amber) from existing components, replacing them with proper tokens and `color-mix`. Configured proper `aria-live` and `role="status"` accessibility attributes and added `motion-reduce:animate-none` for reduced-motion. Handled Discrepancies by ensuring EmptyState matches the dashed/transparent design spec and setting `motion-reduce` to stop animation instead of hiding entirely for the Spinner. Verified zero lockfile churn and zero raw hex values. Passed local Vite build. | Phase A component-build only; no page migrations performed. |
| 2026-07-17 | Phase A Tooltip Primitive implementation | `jules-react-component` | PASS | Built the canonical Tooltip primitive strictly following `jules-05-tooltip.md`. Component supports accessible hover and keyboard focus with `Escape` dismissal and 150ms show delay. Position uses fixed `top`, `bottom`, `left`, `right` (viewport-edge flip avoidance deliberately excluded). Respects `motion-reduce` natively. Used existing token variables only. Zero external dependencies. Passed local Vite build verification. | Component only; no page migrations performed. Known limitation: No viewport collision detection. |
| 2026-07-17 | Phase A SegmentedControl/TabNav/RadioCardGroup implementation | `jules-react-component` | PASS | Built canonical SegmentedControl, TabNav, and RadioCardGroup primitives strictly following `06-segmented-control-tabnav-radio-card-group-brief.md`. Components implement accessible keyboard navigation via arrow keys and native `radiogroup`/`radio` or `tablist`/`tab` ARIA semantics. Used standard tokens only, utilizing `color-mix` for subtle opacity overlays. Zero external dependencies. Passed local Vite build verification. | Component only; no page migrations performed. Known limitation: RadioCardGroup depends on local SVG icon instead of external asset. |
| 2026-07-17 | Phase A PageHero primitive | `jules-react-component` | PASS | Built canonical PageHero component strictly per `jules-04-pagehero.md`. Used existing tokens and font variables (`font-display`/`font-sans`/`font-mono`), maintaining the current `Alegreya Sans` stack over the discrepant `Cinzel`/`Inter` spec. Component only; zero dependencies; no page migrations. Passed local Vite build verification. | Component only; no page migration. |
| 2026-07-17 | Phase A MetricStrip primitive | `jules-react-component` | PASS | Built canonical MetricStrip component strictly per `11-metric-strip-brief.md`. Consumes `confidenceBarStyle` for `confidenceScore`, includes `Tooltip` (05) for explanations, uses explicitly required tokens (`var(--color-risk-low)`, etc), and applies delta icons instead of relying on color alone. Zero dependencies, no raw hex. Passed local Vite build verification. | Component only; no page migrations performed. |
| 2026-07-17 | Phase A PlayerRow/PlayerChip Primitive implementation (Clean) | `jules-react-component` | PASS | Recreated canonical PlayerRow and PlayerChip primitives cleanly. Used existing token variables only. Handled accessible interactive roles correctly. Passed local Vite build verification. | Component only; no page migrations performed. Zero raw hex used. |
| 2026-07-19 | B1 Unified Omen recommendation contract + F3 skill-receipt pilot | `slops-repo-inspector`, `planning-pass`, `product-gap-analysis-session`, `workflow-tree-spec`, `slops-ai-integration-review`, `security-privacy-evidence`, `slops-context-markdown`, `slops-git-flow`, `slops-tdd`, `slops-quality-baseline`, `slops-code-review`, `slops-retro` | PASS with noted caveats | Contract review/docs: `Direction/reviews/2026-07-19-b1-unified-omen-recommendation-contract.md`; AI review; security evidence; code review; handoff. Focused 55/55; full `npm test` 391/391; production audit 0; `git diff --check` clean. | `slops-tdd` partial: RED not rerun before one-line metadata fix. `slops-quality-baseline` caveat: root dev audit has pre-existing promptfoo/adm-zip advisory; production audit clean. Procedure gap fixed: skill-routing path clarified in `Blueprints/playbooks/skill-activation-runbook.md`. |
| 2026-07-19 | B2 unified Omen phase plan + field needs | `slops-repo-inspector`, `planning-pass`, `slops-context-markdown`, `slops-git-flow` | PASS | Added `Blueprints/specs/b2-unified-omen-recommendation-layer.md`; updated sprint/inbox and backend handoff; handoff `Blueprints/handoffs/2026-07-19-b2-unified-omen-phase-plan.md`; `git diff --check` clean. | No app tests run because this is docs/planning only. B2 implementation must run `slops-tdd`, AI/security review, quality baseline, and code review. |
| 2026-07-19 | B2 unified Omen recommendation layer | `slops-repo-inspector`, `planning-pass`, `slops-git-flow`, `slops-tdd`, `slops-ai-integration-review`, `security-privacy-evidence`, `demo-mode-pre-empty-state`, `slops-quality-baseline`, `slops-code-review` | PASS with root-audit caveat | Implemented direct authenticated POST `off_season` guard and Omen MVP envelope `contract_version` field completeness. Evidence: handoff `Blueprints/handoffs/2026-07-19-b2-unified-omen-recommendation-layer.md`; AI/security reviews; code review; RED focused failed for missing fields/off-season; GREEN focused 44/44; cross-contract 68/68; full `npm test` 393/393; production audit 0; frontend build passed; `git diff --check` clean. | Root dev audit still fails on known `promptfoo`/`adm-zip` chain; not fixed because package-file/breaking dependency work is outside B2. No correction needed to selected skill procedures. |
| 2026-07-19 | M0c native app-shell/auth/API contract (approved) | `slops-repo-inspector`, `planning-pass`, `workflow-tree-spec`, `slops-context-markdown`, `security-privacy-evidence` | PASS | Authored `Blueprints/specs/mobile/omen-native-app-shell-auth-api-contract-v1.md` grounded in `api-routes.md` + approved M0a/M0b. Nav/route table, three-mechanism auth + secure storage, `com.slopssaloon.omen://` deep links, provider-state API mapping, idempotency, demo/env/security. Approved by Justin. Surfaced 4 backend reqs to `frontend-to-backend.md`; pinned F2. Reconciled onto current `main` after parallel PR #149 collision (de-duped M1 sprint row + inbox task). Docs only. | `slops-tdd`/`slops-quality-baseline`/`slops-code-review` N/A: contract, no app code. **Skill gap surfaced:** two agents produced parallel M0b-ratification branches (#149, #150) that collided in shared Direction files — recommend a lightweight "claim the sprint item in inbox before starting" convention via `slops-retro`. |
| 2026-07-19 | M0b native design-system registry (approved) | `slops-repo-inspector`, `planning-pass`, `slops-context-markdown`, `design-md-author`, `slops-design-system-pack`, `slops-ui-ux-audit` | PASS | Authored `Blueprints/specs/mobile/omen-native-design-system-registry-v1.md` grounded in `component-lock-v1.md` + live `index.css` (verified values, caught 4 stale light-mode hexes). Token model, component registry, a11y (incl. non-color focus), theme packs, iOS/Android rules. Approved by Justin. Sprint/inbox/decision-log/handoff updated. Docs only. | `slops-tdd`/`slops-quality-baseline`/`slops-code-review`/`slops-mobile-smoke` N/A: contract/registry, no app code or running app. Figma library tools N/A: Design House is a stub; Markdown is source. No correction needed to selected skill procedures. |
| 2026-07-19 | M0a onboarding/connection contract review + sign-in audit (approved) | `slops-repo-inspector`, `planning-pass`, `slops-context-markdown`, `workflow-tree-spec` (partial), `security-privacy-evidence` (light), `pre-build-research` | PASS | Reviewed + revised `omen-mobile-onboarding-connection-contract-v1.md`; approved by Justin. Evidence: `Direction/reviews/2026-07-19-m0a-onboarding-connection-contract-review.md`, `2026-07-19-m0a-signin-flow-audit.md`; handoff `Blueprints/handoffs/2026-07-19-m0a-onboarding-connection-approved.md`. Connected Figma (`whoami` + Design House metadata/screenshot). Sprint/inbox/decision-log updated. Docs only — no code, no push. | `slops-tdd`/`slops-quality-baseline`/`slops-code-review`/`slops-mobile-smoke` N/A: contract review, no app code or running app. `slops-ux-copy` N/A here: R1 tagline decided by founder directly. **Skill improvement:** native read gate (AGENTS.md) omits `Blueprints/api-routes.md`, which was required to ground the provider state machine — recommend adding it to the gate (AGENTS.md is pin-gated, so flagged not edited). |
| 2026-07-19 | Dev-tool audit remediation | `slops-investigate`, `slops-quality-baseline`, `slops-code-review`, `slops-git-flow` | PASS | `promptfoo@0.121.19` plus root `adm-zip@0.6.0` override; clean `npm ci`; production and full audits 0; Promptfoo mock eval 6/6; full `npm test` 393/393; frontend build and diff check passed. | No app code, runtime dependency, auth, provider, schema, deploy, or production mutation. No correction needed to selected procedures. |
| 2026-07-19 | M0-BE F2 Omen readiness truth | `slops-repo-inspector`, `planning-pass`, `slops-context-markdown`, `workflow-tree-spec`, `security-privacy-evidence`, `rbac-risk-review`, `slops-tdd`, `slops-git-flow`, `slops-quality-baseline`, `slops-code-review` | PASS | Shared contract/matrix: `Blueprints/specs/mobile/omen-native-backend-state-contract-v1.md`; security/RBAC review: `Direction/reviews/2026-07-19-m0-be-f2-security-rbac-review.md`; RED `test/omenReadiness.test.js` failed for the missing module, then focused 25/25 and full `npm test` 395/395 passed. | The formerly known `promptfoo` → `adm-zip` development-audit advisory was remediated separately on `main` before this branch was rebased. No correction needed to the selected procedures. |
| 2026-07-23 | M0-BE-0 shared contract and acceptance matrix | `slops-repo-inspector`, `slops-context-markdown`, `workflow-tree-spec`, `security-privacy-evidence`, `rbac-risk-review`, `slops-git-flow` | PASS | Expanded `Blueprints/specs/mobile/omen-native-backend-state-contract-v1.md`; added matrix/handoff `Blueprints/handoffs/2026-07-23-m0-be-shared-contract-matrix.md` and review `Direction/reviews/2026-07-23-m0-be-security-rbac-review.md`; `git diff --check` clean. | `planning-pass` N/A: the existing sprint item was precise; it was updated only to record completion and its already-authorized next slices. `slops-tdd`, quality baseline, and code review N/A: docs/contract only, no runtime behavior. `pre-build-research` deferred to M0-BE-3 because current OAuth guidance is needed before that provider-bound implementation. |
| 2026-07-23 | M0-BE-1 safe provider-state API | `slops-repo-inspector`, `slops-tdd`, `slops-git-flow`, `security-privacy-evidence`, `slops-quality-baseline`, `slops-code-review` | PASS with baseline limitations | RED/GREEN focused `test/platforms.test.js` (new route absent, then 17/17); additive `/api/platforms/state`, backend/frontend handoffs, security evidence, and `git diff --check` clean. | `workflow-tree-spec` N/A: M0a/M0-BE already own the state tree; this slice implements only persisted server-observable states. `pre-build-research` N/A: no external-provider behavior changed. Full `npm test` has one pre-existing native-scaffold assertion failure after M4 integration; root audit has existing advisories; frontend build passed. No correction needed to the selected procedures. |
| 2026-07-23 | M0-BE-2 idempotent native Sleeper connect | `slops-tdd`, `slops-git-flow`, `workflow-tree-spec`, `security-privacy-evidence`, `slops-quality-baseline`, `slops-code-review` | PASS with baseline limitations | RED/GREEN focused `test/platforms.test.js` (request ID behavior absent, then 21/21); added bounded authenticated replay, frontend/backend handoffs, security evidence, and `git diff --check` clean. | `workflow-tree-spec` used to keep replay inside the existing connection transition; Yahoo/ESPN were excluded. Full `npm test` has the pre-existing `nativeMobileScaffold` assertion failure after M4 integration; root audit has pre-existing advisories; frontend build passed. No correction needed to selected procedures. |
| 2026-07-19 | CI lockfile repair | `slops-investigate`, `slops-quality-baseline`, `slops-code-review`, `slops-git-flow` | PASS | Reproduced the merge-triggered Linux `npm ci` failure, regenerated root lock metadata with CI-compatible npm, then passed clean install, full `npm test` 395/395, audit 0, frontend build, Promptfoo mock eval 6/6, and diff check. | No application/runtime/deploy configuration change; merge-triggered workflow is the only deployment mechanism used. |
| 2026-07-19 | M1 first two native foundation build briefs | `slops-repo-inspector`, `planning-pass`, `slops-context-markdown`, `slops-design-system-pack`, `slops-git-flow` | PASS | Added focus-ring and typography build inputs: `Blueprints/specs/mobile/m1-focus-ring-build-brief-v1.md`, `Blueprints/specs/mobile/m1-native-typography-build-brief-v1.md`; handoff `Blueprints/handoffs/2026-07-19-m1-native-foundation-briefs.md`; `git diff --check` clean. | `slops-taste`, `slops-ux-copy`, `workflow-tree-spec`, `pre-build-research`, TDD/quality/review/UI/device skills, and Figma tools N/A: docs-only briefs, no new visual pattern/copy/flow/code/native project/Figma component page. No correction needed. |
| 2026-07-19 | M1-F native Figma foundation library | `slops-repo-inspector`, `planning-pass`, `slops-context-markdown`, `slops-design-system-pack`, `figma-use`, `figma-generate-library`, `slops-git-flow` | PASS | Created Omen primitive/semantic token collections, ten typography styles, and registry documentation on Figma nodes `13:2` / `14:2`; evidence `Blueprints/handoffs/2026-07-19-m1-figma-foundation-library.md`. | Figma file already existed; no screen/code/provider/trust-boundary work. Initial auto-layout screenshot found 1px frame height, repaired before acceptance. No correction needed. |
| 2026-07-19 | M2-F native app-shell screen contracts | `slops-repo-inspector`, `planning-pass`, `slops-context-markdown`, `slops-design-system-pack`, `figma-use`, `figma-generate-library`, `figma-generate-design`, `slops-git-flow` | PASS | Created token-bound iOS/Android app-shell contract boards on Figma nodes `17:12` / `17:13`; added approved spacing variables; evidence `Blueprints/handoffs/2026-07-19-m2-app-shell-contracts.md`. | No compatible Code Connect, existing native screen, or external design-system asset existed; no code/provider/store action. No correction needed. |
| 2026-07-19 | M2-E native build environment | `slops-repo-inspector`, `planning-pass`, `slops-context-markdown`, `slops-git-flow`, `computer-use` | PASS with portability note | Installed Android Studio Quail 2, SDK/ADB/emulator, and booted `Medium_Phone` Android 17/API 37.1; selected no-billed-usage GitHub macOS simulator CI for future iOS validation. Evidence: `Blueprints/specs/mobile/omen-native-build-environment-v1.md`, `Blueprints/handoffs/2026-07-19-m2e-native-build-environment.md`. | No native project/code, CI workflow, provider, credential, store, deploy, or production change. Studio virtualized the SDK under app-local cache; record as local evidence only, never as a committed/global path. |
| 2026-07-19 | M2 native app-shell project scaffold | `slops-repo-inspector`, `planning-pass`, `slops-context-markdown`, `slops-git-flow`, `slops-tdd`, `slops-quality-baseline`, `slops-code-review` | PASS with iOS validation gap | Created iOS SwiftUI and Android Compose shells, safe env seams, local demo entry, session/navigation placeholders, deep-link contract, and project-local ignore rules. RED focused failed for the missing project and missing demo entry; GREEN focused 1/1. Android `:app:assembleDebug` passed and APK installed/launched on `Medium_Phone`; full `npm test` 396/396, frontend build, production audit 0, and diff check passed. Evidence: `Blueprints/handoffs/2026-07-19-m2-native-app-shell-scaffold.md`. | No provider/OAuth, backend behavior, signing, CI, store, credential, deploy, or production work. Local Android evidence does not prove iOS compilation; that future non-signing CI needs separate approval. |
| 2026-07-19 | M3 native local/demo vertical slice | `slops-repo-inspector`, `planning-pass`, `figma-use`, `slops-git-flow`, `slops-tdd`, `slops-quality-baseline`, `slops-code-review` | PASS with auth/iOS gaps | Implemented Welcome → Try Demo or Get started → visibly temporary local sign-in placeholder → Command Center → mock/recovery Omen on both shells. RED focused failed for missing M3 state copy; GREEN focused passed. Android debug build plus emulator install/launch and visual inspection passed after correcting dark-theme content color. Evidence: `Blueprints/handoffs/2026-07-19-m3-native-vertical-slice.md`; Figma nodes `17:12`/`17:13`. | No real authentication, session persistence, Supabase call, provider/OAuth connection, credential, signing, CI, store, deploy, or production work. M3-A native authentication proof is a mandatory pre-release milestone, not optional follow-up. |
| 2026-07-19 | Apple Sign in provisioning record | `slops-repo-inspector`, `slops-context-markdown`, `slops-git-flow` | PASS | Recorded founder-completed Apple App ID, Services ID, and Supabase-provider setup in `Blueprints/handoffs/2026-07-19-apple-sign-in-provisioning.md`; decision log now names the six-month rotation obligation and V2 capability-research boundary. | `pre-build-research`, `security-privacy-evidence`, TDD, quality baseline, code review, UI, device, and release skills N/A: this documents external configuration only; it neither implements code nor stores/changes a secret in Git. |
| 2026-07-19 | M3-A native auth (Android) — scaffolding + live wiring | `slops-repo-inspector`, `planning-pass`, `slops-tdd`, `slops-git-flow`, `security-privacy-evidence`, `rbac-risk-review`, `slops-quality-baseline` | PASS with real-device + iOS gaps | Built `core:session` (expanded SessionState, Session, SecureSessionStore + AndroidKeystore AES/GCM, SessionManager) and `core:auth` (AuthRepository/Fake, opaque AuthOutcome, validators, Google seam, pure AuthFlow reducer, GoTrueTransport seam + live `SupabaseAuthRepository`); app got `OkHttpGoTrueTransport` + `CredentialManagerGoogleIdTokenProvider` (deps: credentials 1.3.0, googleid 1.1.1, okhttp 4.12.0), BuildConfig public config from git-ignored `local.properties`, INTERNET perm. TDD RED→GREEN: **34 unit tests**; `:app:assembleDebug` green; emulator shows Welcome/sign-in/OTP + live label + "Continue with Google"; zero-side-effect Supabase smoke = HTTP 400 (reachable, anon key accepted). `git diff --check` clean; no client-id/anon-key in any tracked file. Evidence: `Blueprints/handoffs/2026-07-19-m3a-native-auth-scaffolding.md`, `mobile/contracts/m3a-auth-provisioning-checklist.md`. | `slops-ui-ux-audit`/`slops-mobile-smoke` are web-driver only — native verification needed ad-hoc adb/gradlew; propose a `native-android-smoke` procedure (SDK lives under the Codex sandbox path, not default). No secrets committed, no schema/deploy/provider/signing/store/Apple/production; not pushed/merged. Interactive Google/OTP proof needs a Play-services device + real inbox; iOS pending macOS CI. |
| 2026-07-20 | M1-P P2 shared native design-system token layer (color/typography/spacing/focus-ring) | `slops-repo-inspector`, `planning-pass`, `slops-git-flow`, `slops-quality-baseline` (Android + iOS CI) | PASS | Built Android `core:designsystem` (`OmenColor`/`OmenTypography`/`OmenSpacing`/`OmenFocusRing`/`OmenTheme`) and iOS `DesignSystem` (`OmenColor`/`OmenTypography`/`OmenSpacing`/`OmenFocusRing` + hand-edited `project.pbxproj`) from `omen-native-design-system-registry-v1.md` §2 and both approved M1 build briefs, now that M1-P is approved and P2/P3 are unblocked. Android: `./gradlew :core:designsystem:testDebugUnitTest` 18/18, `:app:assembleDebug` BUILD SUCCESSFUL. iOS: pushed `claude/m1p-p2-designsystem-tokens` to get the `ios-ci.yml` signal — run `29784250139` `Build OmenIOS (simulator, unsigned)` passed in 33s. `git diff --check` clean. Branch pushed, not merged. Evidence: `Blueprints/handoffs/2026-07-20-m1p-p2-designsystem-tokens.md`. | `slops-tdd` N/A: new token scaffolding locked by 18 direct tests, not a RED/GREEN defect cycle. `slops-ux-copy`/`security-privacy-evidence`/`slops-legal-spot-check` N/A: no user-facing copy, trust boundary, or provider claim. `slops-mobile-smoke`/`slops-ui-ux-audit` N/A: tokens only, no rendered screen yet. Figma tools N/A: registry markdown + build briefs already carried every exact value. No correction needed — the disclosed-gap-then-push-for-CI-signal pattern from the prior entry worked as intended. |
| 2026-07-20 | M1-P P2 Button + IconButton | `slops-repo-inspector`, `slops-git-flow`, `slops-tdd` (existing Android behavior tests), `slops-quality-baseline`, `slops-code-review` | PASS | Completed Compose and SwiftUI controls, required labels/states, debug gallery, and Xcode Sources wiring. Android: 18 unit tests, 7 emulator tests, assembly/install, and keyboard-focus gallery evidence passed; a visual pass caught and corrected an outward focus-ring clipping defect. Unsigned iOS simulator CI run `29788177311` passed in 41s. Evidence: `Blueprints/handoffs/2026-07-20-m1p-p2-button-iconbutton.md`. | `slops-mobile-smoke`/`slops-ui-ux-audit` substituted: both are web-oriented; native evidence came from Gradle, ADB, emulator semantics, and screenshots. No provider, auth, store, or production change. No merge or release occurred. |
| 2026-07-20 | M1-P P2 TextField/FormField/Picker | `slops-repo-inspector`, `planning-pass`, `slops-git-flow`, `slops-tdd`, `slops-quality-baseline`, `slops-code-review` | PASS | RED: Android field-control test failed for absent controls; GREEN: `:core:designsystem:connectedDebugAndroidTest` 10/10, token unit tests 18/18, and `:app:assembleDebug` passed. SwiftUI sources/project wiring compiled in unsigned iOS CI run `29788913948` in 32s. Evidence: `Blueprints/handoffs/2026-07-20-m1p-p2-form-controls.md`. | Figma tools N/A: the approved registry specifies these existing native control mappings, with no visual-pattern gap. `slops-mobile-smoke`/`slops-ui-ux-audit` substituted: they are web-oriented; Android semantics/device tests and the debug gallery are native evidence. Direct ADB gallery screenshot unavailable because its path was not exposed in this shell; no screenshot claim made. No provider/auth/store/production change or merge. |
| 2026-07-20 | M1-P P2 ListRow | `slops-repo-inspector`, `planning-pass`, `slops-git-flow`, `slops-tdd`, `slops-quality-baseline`, `slops-code-review`, `github`, `yeet` | PASS | Built shared Android Compose and iOS SwiftUI ListRow foundations with Android gallery wiring and Xcode project wiring. RED: connected Android test failed for missing `OmenListRow`; GREEN: 17/17 connected tests and `:app:assembleDebug` passed. iOS unsigned simulator CI run `29791872304` passed for `a8aa7e3`. Evidence: PR #169 and `Blueprints/handoffs/2026-07-20-m1p-p2-list-row.md`. | Figma tools N/A: registry already specifies ListRow mapping and no new visual pattern was introduced. `slops-mobile-smoke`/`slops-ui-ux-audit` substituted by native Gradle/CI checks; no direct screenshot claim. No provider/auth/store/production/signing/SQL change. |
| 2026-07-21 | M1-P P2 PlatformBadge | `slops-repo-inspector`, `planning-pass`, `slops-git-flow`, `slops-tdd`, `slops-quality-baseline`, `slops-code-review` | PASS | Built shared Android Compose and iOS SwiftUI `OmenPlatformBadge` foundations with Android gallery wiring and Xcode project wiring. RED: connected Android test failed for missing `OmenPlatformBadge`; GREEN: 20/20 connected tests (3 new) and `:app:assembleDebug` passed on `Medium_Phone` API 37. iOS unsigned CI pending PR push. Evidence: `Blueprints/handoffs/2026-07-21-m1p-p2-platform-badge.md`. | `slops-mobile-smoke`/`slops-ui-ux-audit` substituted by native Gradle/CI checks (same as ListRow). Registry `-chip` + `on-platform-*` tokens still unfilled; tinted-surface fallback used and flagged in handoff. No provider/auth/store/production/signing/SQL change. |
| 2026-07-21 | M1-P P2 ConfirmationDialog | `slops-repo-inspector`, `planning-pass`, `slops-git-flow`, `slops-tdd`, `slops-quality-baseline`, `slops-code-review` | PASS | Built shared Android Compose (`AlertDialog`) and iOS SwiftUI (`.confirmationDialog`) foundations with default + destructive variants, toggleable Android gallery preview, and Xcode project wiring. RED: connected Android tests failed for missing `OmenConfirmationDialog`; GREEN: 25/25 connected tests (5 new) and `:app:assembleDebug` passed on `Medium_Phone` API 37. iOS unsigned CI pending PR push. Closes M1-P P2. Evidence: `Blueprints/handoffs/2026-07-21-m1p-p2-confirmation-dialog.md`. | `slops-mobile-smoke`/`slops-ui-ux-audit` substituted by native Gradle/CI checks. iOS test surface pins enum contract only (variants distinct, modifier attaches) because SwiftUI dialog presentation isn't testable without a snapshot lib — same style as PlatformBadge tests. No provider/auth/store/production/signing/SQL change; account-deletion phrase untouched. |
| 2026-07-26 | M4 Omen destination screen | `slops-repo-inspector`, `planning-pass`, `slops-git-flow`, `slops-quality-baseline`, `slops-code-review` | PASS with iOS CI deferred | Replaced the Omen-tab placeholder with full existing `OmenDecisionBrief` assembly on both platforms. Demo Mode renders explicitly labeled sample advice; real users render only the disconnected recovery state. Registered paired deterministic visual-evidence scenarios. Android `:core:designsystem:testDebugUnitTest` and `:app:assembleDebug` passed; API 36 emulator screenshots for `omen.demo` and `omen.disconnected` were captured and visually inspected; `git diff --check` passed. Evidence: `Blueprints/handoffs/2026-07-26-m4-omen-screen.md`. | `slops-tdd` was weak: the existing DecisionBrief behavior is reused and no new Android app-test seam exists; compilation and scanner checks cover assembly only. `slops-mobile-smoke`/`slops-ui-ux-audit` are web-oriented and substituted by native Gradle/scanner checks plus Android emulator screenshots; unsigned iOS CI remains deferred until GitHub iOS capacity returns in August. No correction needed to selected procedures. |
| 2026-07-21 | M1-P platform legibility tokens | `slops-repo-inspector`, `planning-pass`, `slops-git-flow`, `slops-tdd`, `slops-quality-baseline`, `slops-code-review` | PASS | Filled registry §2.3 `-chip` legibility overrides + `on-platform-*` foreground tokens on both platforms with AA-tuned hexes (Sleeper darkened for AA vs white; Yahoo unchanged; ESPN small darken for margin). Switched `OmenPlatformBadge` to fill-on-platform. RED: PlatformBadge references new fields that did not exist. GREEN: Android JVM tests + 25/25 connected tests + `:app:assembleDebug` on `Medium_Phone` API 37; iOS unsigned CI pending PR push. Evidence: `Blueprints/handoffs/2026-07-21-m1p-platform-legibility-tokens.md`. | Invariant across themes per registry rule; no team-runtime-theming revived. Chip hex values are contrast-driven judgment calls — brand may override without touching consumers. `slops-mobile-smoke`/`slops-ui-ux-audit` substituted by native Gradle/CI. |
| 2026-07-21 | M1-P P4 dual-platform gallery + primitive enforcement | `slops-repo-inspector`, `planning-pass`, `slops-git-flow`, `slops-tdd` (negative-check-then-restore), `slops-quality-baseline`, `slops-code-review` | PASS | Built SwiftUI `DesignSystemGalleryView` (debug-only) + Android `PrimitiveEnforcementTest` + iOS `PrimitiveEnforcementTests`. Registry §3.2 amended to name both tests as the P4 enforcement gate. Android RED: temporarily removed `OmenAndroidApp.kt` from allowlist → test failed on M2 shell as expected. GREEN: allowlist restored → `:core:designsystem:testDebugUnitTest` BUILD SUCCESSFUL. iOS unsigned CI is the iOS gate on push. Closes M1-P. Evidence: `Blueprints/handoffs/2026-07-21-m1p-p4-gallery-enforcement.md`. | `slops-mobile-smoke`/`slops-ui-ux-audit` substituted by native Gradle unit test + unsigned iOS CI (same as prior M1-P PRs). `OmenAndroidApp.kt` allowlisted rather than refactored — scope; retires with first M4 feature screen. `detekt`/`SwiftLint` not adopted — source-scanning tests carry the rule for now. No provider/auth/store/production/signing/SQL change. Skill improvement noted: the negative-check-then-restore pattern for scanner tests belongs in `slops-code-review` (see decision log). |
| 2026-07-23 | M4-Auth primitive-native auth surfaces (retirement) | `slops-repo-inspector`, `planning-pass`, `slops-git-flow`, `slops-tdd` (scanner as GREEN gate), `slops-quality-baseline`, `slops-code-review` | PASS | Refactored `OmenAuthFlow.kt` and `OmenDeleteAccountScreen.kt` to compose only approved Omen primitives (`OmenCard`, `OmenFormField`, `OmenTextField`, `OmenButton`, `OmenStateSurface`); removed both from `PrimitiveEnforcementTest.ALLOWLISTED_FILES` (now empty). Public composable signatures preserved — no caller changes in `OmenAndroidApp.kt`. GREEN: `:core:designsystem:testDebugUnitTest --tests PrimitiveEnforcementTest` BUILD SUCCESSFUL with empty allowlist; `:app:assembleDebug` BUILD SUCCESSFUL. Failed-sign-in copy now renders through `OmenStateSurface(kind=Error)`; delete-screen validation feedback now routes through `OmenFormField.errorMessage` (polite live region); Google button now honestly disabled when `!googleConfigured`. Non-expansion covenant restated in the enforcement test doc block. Branch `claude/m4-auth-primitive-retirement` — waiting on push/merge. Evidence: `Blueprints/handoffs/2026-07-23-m4-auth-primitive-retirement.md`. | `slops-mobile-smoke`/`slops-ui-ux-audit` substituted by scanner + Gradle assemble (same pattern as prior M1-P/M4 PRs — no visual redesign, primitives already audited). `slops-ux-copy` N/A: only new copy is the error-state title "Sign-in didn't complete"; all other strings preserved verbatim. `security-privacy-evidence`/`rbac-risk-review`/`slops-legal-spot-check` N/A: no data classification, permission, provider, or legal surface changed; auth wiring untouched. Skill improvement: no correction needed — the "shrink-the-allowlist" workflow is a natural fit for the existing `slops-code-review` primitive-enforcement pattern from M1-P P4. No provider/auth/store/production/signing/SQL change. |
| 2026-07-24 | M4-Auth-Providers-v1 Step 4 (contract) + Step 5 (Discord platform impls) + ios-ci YAML fix | `slops-repo-inspector`, `planning-pass`, `slops-git-flow`, `slops-tdd` (12 reducer + 8 repository + 5 PKCE tests per platform, RED-first), `slops-quality-baseline`, `slops-code-review`, `supabase` (search_docs to confirm passkey experimental status + PKCE endpoint shape) | PASS with iOS CI billing gap | Shipped provider-agnostic OAuth + WebAuthn contract layer both platforms (Step 4, PR [#198](https://github.com/justinduverge-design/omen/pull/198) `feat(auth): M4-Auth-Providers-v1 Step 4`), then Discord OAuth platform impls both platforms (Step 5, `feat(auth): M4-Auth-Providers-v1 Step 5`), then fixed a pre-existing duplicate-`branches:` YAML bug in `ios-ci.yml` that had silently 0s-failed every push/PR since check-in (`fix(ci): remove duplicate branches key in ios-ci workflow`). Android: `:app:assembleDebug` + `:core:auth:testDebugUnitTest` + `:core:designsystem:testDebugUnitTest` (primitive-enforcement scanner still empty allowlist) all GREEN. iOS: unsigned iOS CI now valid YAML but macOS runner rejected by GitHub billing gate ("recent account payments have failed or your spending limit needs to be increased"). Passkey wiring intentionally deferred to `M4-Auth-Passkeys-Onramp` (P2) because Supabase's passkey feature is `@_spi(Experimental)` with no public REST shape — decision recorded in `Direction/decision_log.md` and handoff. Evidence: `Blueprints/handoffs/2026-07-24-m4-auth-providers-v1-discord-shipped-passkey-deferred.md`. | `slops-mobile-smoke`/`slops-ui-ux-audit` substituted by native Gradle + iOS CI (blocked on billing). `slops-ux-copy` N/A: only new copy is "Continue with Discord" (Discord brand-required) + placeholder passkey failure strings per brief §10 (defers wording to future passkey-onramp). `security-privacy-evidence`/`rbac-risk-review`/`slops-legal-spot-check` N/A: Discord OAuth uses public PKCE with CSRF state entirely covered by M0c contract; no new trust boundary, permission, or legal surface. Skill improvement worth propagating: when a doctrine ("no SDK") and a feature ("wire passkeys") collide, surface all options with weight to the founder BEFORE writing speculative code — this session did that, saving a shipped-then-broken reverse-engineered WebAuthn REST client. The "discovered CI has never actually run" pattern is a smell worth adding to `slops-quality-baseline`'s startup checks. |
| 2026-07-26 | B2-D-S0 Sleeper projection mapping + B2-D-S1 available waiver pool | `slops-repo-inspector`, `pre-build-research`, `slops-tdd` (RED-first both; negative check on S0), `slops-git-flow`, `slops-quality-baseline`, `slops-code-review` | PASS with roster subtraction unverified | **S0 (PR [#214](https://github.com/justinduverge-design/omen/pull/214)):** live Sleeper projections had been null for every player — the endpoint returns an array with points under `stats`, the adapter read an object map with flat `pts_ppr`. Measured null for 400/400 real players; since `optimizer.js:117` does `Number(projected_points) || 0`, every Sleeper player scored 0 in live start/sit. The two defects masked each other (a string id indexes an array by position, hitting a different player's record), so both were fixed in one normalizer. **S1 (PR [#215](https://github.com/justinduverge-design/omen/pull/215), stacked):** `fetchSleeperAvailablePlayers` derives the pool as active fantasy-eligible players minus all rostered players, ranked, nulls last. Adapter 20/20; full backend 425/425; negative check fails 3 guards on pre-fix code; live normalizer maps 533/533; live-scale pool 3293 ranked Josh Allen/Burrow/Hurts/Mahomes. Evidence: `Blueprints/handoffs/2026-07-26-b2d-s0-s1-sleeper-waiver-foundation.md`. | UI/mobile/copy skills N/A (no user-facing surface); `security-privacy-evidence`/`rbac-risk-review`/`slops-legal-spot-check` N/A (public unauthenticated API, no credential or permission surface); `slops-data-ingest-plan` N/A (no new source — existing cached fetchers); `planning-pass` N/A (backlog authored in PR #213). **Roster subtraction is unverified against a real league** — the live check used a synthetic empty roster set, so S1's own primary correctness risk is untested; that is S3 and needs a founder-provided league id. S0 changes live start/sit output, founder-approved before implementation. **Procedure gap:** fixtures standing in for external API responses must be captured from a real payload and cite the date/request, not authored by hand — the projections fixture encoded a shape the live API has never returned and the green suite certified a production bug indefinitely. Both S0 findings and my own S1 fullback defect came from probing live data; none would have surfaced from the suite. |
| 2026-07-26 | Dependency health controls and SLOPS Prompt Guard | `pre-build-research`, `slops-context-markdown`, `slops-git-flow`, `slops-quality-baseline`, `slops-code-review`, `security-privacy-evidence` | PASS with known dev-tool debt | Production audit 0 after `body-parser` lockfile repair; clean CI-style install; deterministic Promptfoo validation and 6/6 fixtures passed; workflow YAML parsed; full tests/build/diff check recorded in `Blueprints/handoffs/2026-07-26-dependency-health-controls.md`. | `slops-tdd` N/A: no app/API behavior changed; workflow/config behavior was directly verified. Native/UI/provider/release skills N/A: no user-facing, provider, credential, or deployment change. Full audit remains 15 development-only Promptfoo advisories; weekly report + Dependabot + PR review make it visible while the dependency-light runner/upstream-fix follow-up is pending. |
| 2026-07-26 | Full dependency debt remediation | `pre-build-research`, `slops-tdd`, `run-slops-saloon`, `slops-context-markdown`, `slops-git-flow`, `slops-quality-baseline`, `slops-code-review`, `security-privacy-evidence` | PASS | RED missing-runner test, then GREEN `test/slopsPromptGuard.test.mjs`; clean root/frontend installs; full `npm test` 417/417; SLOPS Prompt Guard 18/18; frontend build under React Router 8/Vite 7; browser smoke `/`, `/privacy`, `/corvus` → `/about`; root/frontend full and production audits 0; `git diff --check` clean. Evidence: `Blueprints/handoffs/2026-07-26-full-dependency-debt-remediation.md`. | `slops-tdd` applies to the SLOPS runner only; Router migration was package/API compatibility plus browser QA. No native, provider, credential, deploy, or production mutation. The legacy Promptfoo prompt-text folder remains the fixture content source only; YAML configs and package dependency are removed. |
| 2026-07-26 | Quiet Dependency Inbox and Jules delegation | `pre-build-research`, `slops-context-markdown`, `slops-git-flow`, `slops-quality-baseline`, `slops-code-review`, `security-privacy-evidence` | PASS with founder setup gate | Added Android Gradle Dependabot monitoring, a scheduled in-place GitHub Dependency Inbox, and a manually triggered Jules issue template. Prettier YAML check, embedded workflow-script syntax check, and `git diff --check` passed. Evidence: `Blueprints/handoffs/2026-07-26-quiet-dependency-inbox-jules.md`. | No automatic email, `jules` label, GitHub App install, issue creation, push, merge, deploy, credential, provider, or production action occurred. Founder must install/authorize Jules for this repo and manually label one bounded issue before it can consume a Jules task. |
| 2026-07-27 | D2 AI provider zero-cap control | `slops-repo-inspector`, `planning-pass`, `slops-git-flow`, `slops-tdd`, `slops-ai-integration-review`, `security-privacy-evidence`, `slops-quality-baseline`, `slops-code-review`, `slops-investigate` | PASS | Added local-default `AI_PROVIDER`, with cloud and invalid values fail-closed before any LLM network call. RED/GREEN evidence, AI/security review, and handoff: `Blueprints/handoffs/2026-07-27-d2-ai-provider-zero-cap.md`; review: `Direction/reviews/2026-07-27-d2-ai-provider-zero-cap.md`. Full backend tests 418/418, moderate audit 0, and frontend build passed after a clean install restored the declared Router 8 dependency tree. | `slops-investigate` confirmed the initial frontend build failure was stale ignored `node_modules` (`react-router@6.30.3`), not source code; `npm --prefix frontend ci` restored `8.3.0`. No cloud credential, provider, deploy, production environment, or paid spend was added. |
| 2026-07-27 | M4 Help + Support native implementation | `slops-repo-inspector`, `planning-pass`, `slops-git-flow`, `slops-tdd`, `slops-quality-baseline`, `slops-code-review`, `figma-swiftui` | PASS with device/iOS gaps recorded | Added Account entry and approved Help + Support surfaces on SwiftUI and Compose, with deterministic state scenarios, iOS XCTest wiring, and Android `:core:designsystem:testDebugUnitTest` plus `:app:assembleDebug` green. `git diff --check` green. Evidence: `Blueprints/handoffs/2026-07-27-m4-help-support-implementation.md`. | `slops-mobile-smoke` and `slops-ui-ux-audit` were read and substituted: they audit web surfaces, while this is native. Android ADB/emulator and Xcode are unavailable in this workspace, so no screenshot, TalkBack/font-scale, Dynamic Type/VoiceOver, compact/large-device, or iOS build claim is made. `security-privacy-evidence`/`rbac-risk-review` N/A: no auth/session/provider/personal-data/telemetry behavior changed; the view explicitly forbids automatic sensitive attachments. |
| 2026-07-28 | Sprint-history reconciliation | `slops-context-markdown`, `slops-git-flow` | PASS | Reconciled `Direction/sprints_completed.md` with verified merged work and explicitly separated merged code from deploy/provider/device proof. Evidence: `Blueprints/handoffs/2026-07-28-sprint-history-reconciliation.md`; `git diff --check`. | Runtime/security/native/UI skills N/A: documentation-only reconciliation. No correction needed; the degraded-verification rule now prevents CI outages from creating silent record gaps. |
| 2026-07-25 | Edge extension certification testability packet | `run-slops-saloon` | PASS | Prepared `extension/REVIEWER_NOTES.md`, corrected stale store-status wording in the extension manifest/README, and validated the public Demo Mode contract. Manifest JSON valid; `test/demoMode.test.js` 5/5; frontend production build; `git diff --check` all passed. Evidence: `Blueprints/handoffs/2026-07-25-edge-extension-testability-resubmission.md`. | No real ESPN account, cookie, test credential, store setting, package upload, resubmission, deploy, or production action occurred. Partner Center resubmission remains Justin-gated. |
| 2026-07-27 | B2-D2 guarded Yahoo waiver capability slice | `slops-repo-inspector`, `pre-build-research`, `slops-tdd`, `slops-git-flow`, `security-privacy-evidence`, `slops-quality-baseline`, `slops-code-review` | PASS with external gate | RED proved that removing the no-unavailable-starter guard fetches Yahoo's pool; GREEN focused service/route 20/20; full `npm test` 426/426; frontend build; moderate audit 0; `git diff --check` clean. Evidence: `Blueprints/handoffs/2026-07-26-b2d2-yahoo-waiver-slice.md`. | `slops-data-ingest-plan` N/A: on-demand existing Yahoo call, no ingest/store. AI, workflow-tree, native/UI, and release skills N/A: no model/flow/client/release change. GitHub Actions is SUBSTITUTED during the billing hold. Yahoo API reapproval blocks real-account proof; no provider request or credential inspection occurred. |
| 2026-07-29 | Security logging and browser-policy hardening | `slops-repo-inspector`, `supabase`, `security-privacy-evidence`, `slops-tdd`, `slops-git-flow`, `slops-quality-baseline`, `slops-code-review` | PASS with production gate | OAuth codes/state are scrubbed from Sentry; production access logs keep route/status/timing only; explicit `Permissions-Policy` is present; waitlist RLS remains source-only. Current full suite/audit/build evidence is recorded in the merged security landing and its handoff/review. Evidence: `Direction/reviews/2026-07-24-security-hardening-review.md`, `Blueprints/handoffs/2026-07-24-security-log-redaction.md`. | No Supabase policy application, secret inspection, dashboard, deploy, or production mutation occurred. |
| 2026-07-29 | Claude inactive-branch recovery and closeout | `slops-repo-inspector`, `slops-git-flow`, `slops-tdd`, `slops-quality-baseline`, `slops-code-review`, `security-privacy-evidence`, `pre-build-research` | PASS with external gates recorded | Recovered S1/S2/docs/selector/security/store/ESPN work onto current `main` through PRs #238–#243. Focused selector/service/route 55/55, full backend 469/469, frontend build, moderate audit, and diff checks passed where code changed; docs recoveries used diff checks. Evidence: `Blueprints/handoffs/2026-07-29-claude-work-recovery-closeout.md`. | GitHub Actions was unavailable, so no CI-green claim. Public Sleeper proof needs a drafted league ID; ESPN E1 and Yahoo real-account proof remain separate provider gates. No credential, store, deploy, Supabase, or production action occurred. |
| 2026-07-29 | B2-D-S3 Sleeper live roster-subtraction proof | `slops-repo-inspector`, `pre-build-research`, `slops-quality-baseline` | PASS | Ran the existing public/read-only verification script against founder-provided drafted league `1387633793615036416`: 8 teams, 120 rostered players, 3,174 pool entries, 415 projected, nulls sorted last, and zero rostered-player leaks. Evidence: `Blueprints/handoffs/2026-07-29-b2d-s3-sleeper-live-proof.md`. | No credential/session inspection, claim/drop transaction, source modification, deployment, or CI claim. This proves only Sleeper’s public roster subtraction for the observed league; Yahoo and ESPN retain separate gates. |
| 2026-08-02 | B2-D3-S Sleeper live trade capability | `slops-repo-inspector`, `slops-sports-math-intelligence`, `slops-tdd`, `security-privacy-evidence`, `slops-quality-baseline`, `slops-code-review`, `slops-git-flow` | PASS with frontend-build limitation | Built a selected-context Sleeper all-team roster surface, pure optimal-lineup/trade evaluator, VORP fairness guard, sanitized live recommendation mapping, and public drafted-league proof. RED then GREEN: missing lineup module; IR/taxi eligibility test; integration trade candidate test. Focused 52/52; full `npm test` 488/488 using the existing primary-worktree dependency tree through `NODE_PATH`; `npm audit --audit-level=moderate` 0; `git diff --check` clean. Public no-credential proof reported aggregate counts only and verified no manager identity fields in output. | `npm --prefix frontend run build` is N/A to this backend-only diff; the linked worktree lacks frontend `node_modules`, so the untouched frontend build could not be run here without an install. No UI, public Trade Analyzer, Yahoo/ESPN, SQL, package, secret, or deployment change. `security-privacy-evidence` guided the output-data classification; no separate evidence note was needed because credential/consent/retention/telemetry boundaries did not change. |
| 2026-08-02 | B2-D ESPN waiver planning pass | `slops-repo-inspector`, `planning-pass`, `slops-context-markdown` | PASS | Ratified adapter (`B2-D-E1`), canonical wiring (`B2-D-E2`), and drafted-league proof (`B2-D-E3`) with `Blueprints/specs/b2d-espn-e1-waiver-pool-v1.md`. Evidence: `Blueprints/handoffs/2026-08-02-b2d-espn-waiver-planning-pass.md`. | Implementation/research/security/privacy/TDD/quality/review/git skills are N/A to this planning-only pass. The existing E0 verdict and drafted-league protocol supply the source and safety evidence; no provider request, credential, code, SQL, dependency, production, or deployment change occurred. |
| 2026-08-02 | Valor Ventures public operator identity and orphan-worktree recovery | `slops-repo-inspector`, `slops-tdd`, `run-slops-saloon`, `slops-ui-ux-audit`, `slops-legal-spot-check`, `slops-quality-baseline`, `slops-code-review`, `slops-git-flow`, `slops-ship` | PASS pending publication | Audited four locally untracked Omen worktrees, retired three whose work was merged or superseded, recovered the one unpublished legal-footer commit, and aligned public/legal/brand identity. RED then GREEN focused legal regression; 501/501 full tests; frontend build; moderate audits 0; desktop/mobile light/dark rendered QA. Evidence: `Blueprints/handoffs/2026-08-02-valor-ventures-footer.md` and `Direction/reviews/2026-08-02-*valor-footer*.md`. | Canonical browser driver stopped on its known stale landing-H1 assertion, so a focused Playwright pass covered the changed routes. No auth/provider/data/dependency surface changed. Counsel review remains advisable for final legal instruments. Publication/deploy/canary are distinct and recorded only after observed. |
| 2026-08-02 | Omen LEGAL-V1 finalization and release | `slops-repo-inspector`, `compliance-by-template`, `slops-legal-spot-check`, `security-privacy-evidence`, `slops-tdd`, `slops-git-flow`, `slops-quality-baseline`, `slops-code-review`, `slops-ux-copy`, `slops-ui-ux-audit`, `run-slops-saloon`, `slops-deploy-guard`, `slops-ship`, `slops-canary`, `github:yeet` | PASS released | Finalized Privacy/Terms, exact operator/address, 13+ sign-in assent evidence, Auth-inclusive deletion, waitlist unsubscribe, retention/provider disclosures, and regression coverage. RED 6 failures; focused 13/13; full 506/506; build/audits/diff/render gates passed; PR #269 checks green; merge `64305c1`; deploy `30769488793` green; independent read-only canary PASS. Evidence: `Blueprints/handoffs/2026-08-02-omen-legal-final-v1.md`; `Direction/reviews/2026-08-02-omen-legal-final-v1-*.md`. | Open-agreement templates and Pandoc were unavailable, so the template checklist was applied directly to the React surfaces. No counsel-certification or native-store claim. Live email delivery and authenticated production write/delete flows were not exercised. |
| 2026-08-02 | B2-D canonical-engine evidence reconciliation | `slops-repo-inspector`, `slops-quality-baseline`, `slops-code-review`, `slops-git-flow`, `security-privacy-evidence` | PASS with no deployment claim | Reconciled stale local-branch wording against `main`: #265/`171508f`, #266/`623068a`, and #259/`521268b`; focused 84/84 and full 506/506 passed with existing dependencies; audit 0; evidence `Blueprints/handoffs/2026-08-02-b2d-acceptance-reconciliation.md`. | `slops-tdd` considered but N/A: no behavior changed and existing RED/GREEN evidence remains in the merged slice handoffs. `pre-build-research`, workflow, UI/mobile, provider verification, release, and deployment skills N/A: no new provider/data contract, provider request, client change, or release action. |
| 2026-08-12 | M3A iOS Apple authorization + passkey onramp | `run-slops-saloon`, `anthropic-skills:pre-build-research`, `supabase:supabase`, `engineering:code-review`, `design:ux-copy`, `design:accessibility-review`, `computer-use:computer-use`, `github:yeet` | PASS with founder production/device gate | Implemented and locally verified native Sign in with Apple evidence plus iOS passkey authentication, registration, account management, pairing prompt, Associated Domains entitlement, AASA artifact/route, and official Supabase passkey transport. Xcode 26.6 simulator suite 121/121; signed physical-device build/install succeeded; Node AASA tests 7/7; full backend suite 510/510; frontend build succeeded; source-level copy/accessibility review found no P0/P1. Evidence: `Blueprints/handoffs/2026-08-12-m3a-ios-authorization-passkeys.md`. | `run-slops-saloon` applied only to the web-hosted AASA/build boundary, not native UI. `computer-use` was used only for read-only diagnosis of a suspected signing prompt; no credential was entered. Production deploy/merge, full VoiceOver/device matrix, and Face ID ceremonies remain founder-only. M3A is not closed by one Apple happy path, and Android passkeys remain deferred. |
| 2026-08-13 | P1-WaiverGateMultiProvider + P1-YahooConnectButtons + P1-YahooLeagueBinding (Yahoo rewiring) | None invoked — the Skill tool was not called this session | PASS with no in-session test execution | Three independent PRs, in dependency order: (1) `buildWaiverTool` now gates on `isOmenReadyConnection()` across all providers instead of Yahoo-only, unblocking already-shipped ESPN/Sleeper waiver work (PR [#291](https://github.com/justinduverge-design/omen/pull/291)); (2) both dead Yahoo Connect/Reconnect buttons now call the working `startYahooOAuth()`, and the OAuth callback logs the discarded provider error before it's unrecoverable (PR [#292](https://github.com/justinduverge-design/omen/pull/292)); (3) new `GET /api/yahoo/leagues` + `POST /api/yahoo/league` plus a `ConnectLeague.jsx` league picker close the actual root cause of `P1-YahooLeagueBinding` — no caller has ever supplied a real `league_id`, so every Yahoo connection was structurally unusable regardless of token health (branch `claude/p1-yahoo-league-binding`). Evidence: `Blueprints/handoffs/backend-to-frontend.md` "Yahoo league binding — 2026-08-13"; `Direction/decision_log.md` "2026-08-13 — Yahoo league binding". | **This session's sandbox has no node/npm/Docker on `PATH`** — every test (existing and new) was hand-traced line-by-line against the actual implementation rather than executed; the user was asked and explicitly accepted this tradeoff. This is a real procedure gap, not a substitution to wave away: hand-tracing catches logic errors but not runtime/syntax errors, missing dependencies, or test-harness misuse. `npm test` and `npm --prefix frontend run build` must run before any of these three PRs merge — none of the "PASS" above is CI-equivalent. Separately, a Plan-mode design pass (the `Agent` tool, not the `Skill` tool) caught a load-bearing error before it shipped: the sprint doc's own "Done when" text for `P1-YahooLeagueBinding` said to persist `null` for an unbound league, which would have violated the live `NOT NULL` constraint on `platform_connections.league_id` and broken every first-time Yahoo connect — verified directly against `sql/omen_rls_security.sql` before trusting it. No credential, SQL, deploy, or production action occurred; the Yahoo real-account proof (`F7`) remains a separate, founder-gated item. |
| 2026-08-13 | Native auth completion: Discord callback + email OTP | `supabase:supabase`, `browser:control-in-app-browser`, `github:yeet` | PASS with remaining M3A interactive gates | Repaired Android/iOS Discord PKCE callbacks, configured least-privilege Resend SMTP, converted Supabase signup/login templates to six-digit OTP, aligned server OTP length, normalized pasted OTP whitespace, and proved iPhone Discord, email OTP, and session restore. Android auth tests/assembly passed; Xcode 26.6 passed 121/121; signed iPhone build/install passed. Evidence: `Blueprints/handoffs/2026-08-13-native-auth-completion.md`. | Account deletion was deliberately not run on a founder account. Android email OTP/session restore/deletion/log-safety interactive proof remains open, so M3A-QA stays READY. GitHub CLI was unavailable; publication used local Git plus the authenticated GitHub surface. |
| 2026-08-15 | M6-ContextualHelp — native contextual help, both platforms | `design:ux-copy`, `design:accessibility-review` | PASS — done-when discharged, VoiceOver SUBSTITUTED | Built the registry §3.1 `Tooltip/Help` primitive (absent on both platforms despite the spec calling it "existing") and wired a `What is this?` affordance to all four shipped destinations. Mined the web `PAGE_HELP` map for content and rewrote it for native truth; both required corrections (no Draft Assistant, no in-app ESPN) are enforced by tests on both platforms, with Yahoo/ESPN sentences pinned character-for-character against `ConnectProvider.availability`. iOS 183/183 (Xcode 26.6, iPhone 17 Pro sim; baseline 174); Android 50/50 instrumented on an API 36 AVD (baseline 42) plus `:core:designsystem:test` and `:app:assembleDebug` green. Renders captured light/dark at font scale 1.0 and 1.8 on both platforms. Evidence: `Blueprints/handoffs/2026-08-15-m6-contextual-help.md`. | `figma:figma-design-to-code` considered but N/A — the Figma connector was unauthenticated in this session, so the claim that approved nodes `61:2`/`63:2`/`63:26` cover the Tooltip/Help primitive is documentary, not verified. `engineering:testing-strategy` N/A — spec §6 fixes the test seam. Accessibility was completed in a second pass at the founder's direction: **TalkBack genuinely driven** on an API 36 AVD (service confirmed bound, 48dp target, sheet opened by TalkBack double-tap, System Back returned to the originating screen); **iOS VoiceOver is impossible on the Simulator** (`com.apple.VoiceOverTouch` never acquires a PID) and is **SUBSTITUTED** with `performAccessibilityAudit()` under a new `OmenIOSUITests` target, 5/5, iOS total 188. That audit caught a real AA contrast defect I had shipped — `text-secondary` on `surface-2` is 4.43:1 — fixed on both platforms per registry §3.1. Two further findings were classified pre-existing and logged rather than fixed or suppressed. A third — flagging `OmenMatchupHero`'s "connect Sleeper or ESPN" as a false claim — was **wrong and retracted**: ESPN connects on the web and Omen wants those connections; I had carried the sprint item's "ESPN cannot be connected at all" phrasing into a judgement instead of checking `ConnectProvider.availability`, which says `.useWeb`. A real-device VoiceOver pass remains owed. Android font scaling found a real large-text layout defect the green test suite could not see. The 8 new screenshot scenarios have no CI matrix rows (workflow edit outside the approved file list), and the Android content test needs an emulator because adding a `:app` JVM test source set is a founder-gated dependency edit. Nothing pushed, merged, or deployed. |
| 2026-08-16 | Sprint-queue reconciliation + P1-ConnectContinueRoute | None invoked — the Skill tool was not called; `slops-tdd` was followed procedurally (RED before GREEN) and `slops-ui-ux-audit` was substituted by a routing/dead-end review, since nothing visual shipped | PASS — `Done when:` met in full | Closed 22 COMPLETED + 1 DESCOPED items out of `Direction/current_sprint.md` into `Direction/sprints_completed.md` with exact per-item evidence, held four finished-looking items open with stated reasons, and flipped `M4-CC-PlatformsCompact` from `READY` to `VERIFIED` after finding it had merged as `6466a4c` — the fifth instance of this queue advertising shipped work as pullable. Then fixed `P1-ConnectContinueRoute`: Continue now lands on `/football`, a server-side connection counts as onboarding-complete, and a third defect found mid-fix (`/account/connect` stored as the post-login destination, sending Continue back to the page just completed) is handled. RED first; `npm test` 549/549; frontend build clean. **Correction owed to the toolchain:** the repo's frontend test convention — `readFileSync` + regex against JSX — is what let these defects live, because a grep can prove a call site exists but never that it returns the right destination. Extract frontend *decisions* into `lib/` and test them for real; keep source assertions for wiring. |
| 2026-08-16 | P1-DraftAssistantSideline — remove Draft Assistant from the 1.0 surface | None invoked — the Skill tool was not called; `slops-tdd` followed procedurally (RED before GREEN), `slops-repo-inspector` as a manual reference sweep, `slops-ux-copy` + `slops-legal-spot-check` over landing/about/Privacy/Terms, `slops-ui-ux-audit` SUBSTITUTED by a dead-end review since no new UI shipped | PASS — `Done when:` met in full | Executed a founder decision that had been logged on 2026-08-11 and never carried into code: nav, `/draft`, the hardcoded `dashboard.js:266` tool entry, a Football tab, a landing mini-card, an about-page pill, four help entries, and the Privacy/Terms clauses all still shipped. Backend mount moved behind `DRAFT_ASSISTANT_ENABLED` (mirroring `YAHOO_ENABLED`), failing closed to a 404. RED 8/10 surface tests + the dashboard contract assertion; GREEN `npm test` **563/563** (549 baseline, +14), frontend build clean, `git diff --check` clean. Bundle proof: zero occurrences of `Draft Assistant` / `draft-assistant` / `Draft Position` in production JS. Evidence: `Blueprints/handoffs/2026-08-16-p1-draft-assistant-sideline.md`. | **The dead-end review earned its place** — it caught `nextUrl.js` allowlisting `/draft` as a post-login redirect, a fourth surface absent from the item's scope list, where a crafted `?next=/draft` would have dropped a signed-in user on a 404. **Correction to the previous entry's own lesson:** "move the decision into `lib/` and test it for real" is right for *decisions* and wrong for *removals*, where the property is absence everywhere and a grep proves what a behavioral test cannot. Codify: test removals by absence over **comment-stripped** source — the first version of these tests forced a choice between a green suite and the greppable 2027 re-activation trail — and reserve behavioral tests for the genuine decisions, here exactly two (the flag failing closed, the dashboard contract). Not browser-verified; `R7` remains open and is not closed by this. **Founder override worth recording as a judgement miss:** I held `/api/sleeper/draft*` mounted because it is live-draft *tracking* rather than Draft Assistant — technically right, and the wrong altitude. The founder's frame was product-shaped, not feature-shaped: 1.0 ships no draft surface at all. When a decision removes a *theme* rather than a component, scope by the theme and surface the edges as a question rather than resolving them narrowly and calling it a boundary. |
| 2026-08-16 | R7 — scrub Draft Assistant claims from store metadata and in-app copy | None invoked — the Skill tool was not called; `slops-ux-copy` and `slops-legal-spot-check` followed procedurally over store specs, native placeholder copy, and waiver-state copy; `slops-tdd` used in an unusual direction (write the ban, restore the original strings to prove RED, restore the fix) | PASS — `Done when:` met in full, recorded grep captured | Store metadata needed no scrub; the live defects were two false claims in shipped native copy on both platforms, plus an internal sprint-key leak found alongside them. iOS 192/192 (Xcode 26.6 `17F113`); Android 51/51 connected on `medium_phone` API 36 + `:app:assembleDebug`; backend 563/563; frontend build clean. Evidence: `Blueprints/handoffs/2026-08-16-p1-draft-assistant-sideline.md`. | **The ban was written at the wrong altitude first, by someone else, and it cost five days of false claims.** `M6-ContextualHelp` banned the exact product name "Draft Assistant" in help copy — a correct rule that could never have caught "seasonal Draft entry" or "draft and roster opportunities", because those name the *capability*, not the product. Codify: when a feature is cut, ban the noun and its capability language across all user-facing literals, not the marketing name in one surface. **Second correction:** I nearly shipped an iOS test written against an invented `OmenWaiverWatchState.allAuditableCases` API before checking the type — the toolchain was available the whole time. Verify the symbol exists before writing the assertion; on a Mac with Xcode present there is no excuse for a test that compiles only in imagination. |
| 2026-08-16 | M5-Native-API-Client slice D — Omen destination → live engine, both platforms | None invoked — the Skill tool was not called; the native read gate (`omen-native-mobile-foundation-v1`, `omen-native-delivery-governance-v1`, `omen-native-backend-state-contract-v1` §F2, api-routes) was read before any code, and `slops-tdd` was followed with the contract states enumerated from `src/services/omen.js` rather than guessed | PASS — slice `Done when:` met on both platforms | Replaced fixture selection with the live envelope. `realDisconnected` is now unreferenced on both platforms. iOS 208/208 (Xcode 26.6 `17F113`; baseline 192); Android 64/64 connected on `medium_phone` API 36 (baseline 51); backend 563/563 unchanged. Evidence: `Blueprints/handoffs/2026-08-16-m5-slice-d-omen-destination.md`. | **Reading the server before modelling the client was what made this cheap.** Enumerating `state:` literals out of `src/services/omen.js` produced eleven states; the visual brief and the §F2 table between them named four. Had I modelled from the specs alone, seven recovery states would have fallen into a default branch and rendered "couldn't read that" for perfectly healthy backend answers. Codify: for any contract-mapping slice, grep the emitter for its literal state values before trusting a spec's summary table. **Repeat correction, second time today:** I wrote a test against an invented API before checking the symbol existed. The toolchain was available; the check was one grep. This is now a standing rule for me rather than a one-off note. |
| 2026-08-16 | Staleness check + `:app` JVM test source set + M1 screen-contract canvas | None invoked — the Skill tool was not called; `slops-tdd` applied to the script by simulating the real defect (reverted a closed item to `READY`, confirmed the catch, restored), and the native read gate re-applied for the build-config change | PASS — all three delivered and verified | Script catches the real bug and is quiet otherwise; JVM source set cuts pure-logic Android tests from ~50s + emulator to ~1s with coverage unchanged at 64; three items queued with a one-page canvas. | **The best lesson came from the tool testing itself.** The script's first run flagged `S3` and `S7` on docs PRs that only *mentioned* them — false positives that would have trained everyone to ignore it, the precise failure I had written a comment warning about two minutes earlier. Fixing signal quality mattered more than adding coverage. Codify: when building a check, run it against real history **before** declaring it done, and treat its first false positive as a design input rather than noise to suppress. **Second:** the JVM source set was blocked on a non-obvious detail — android.jar ships `org.json` as throwing stubs, so the source set alone was useless and the tests all failed on the first run. The documented escape hatch (`isReturnDefaultValues`) would have made them pass *silently and wrongly*. Worth remembering that a green test after a config change is not evidence the config change was right. |
| 2026-08-16 | M5-Native-API-Client slice E — Ledger → `GET /api/moves`, both platforms | None invoked — the Skill tool was not called; the native read gate was re-applied before code, and `slops-tdd` was followed with the contract's nullability read out of `src/routes/moves.js` rather than inferred from the sprint item's summary | PASS — slice `Done when:` met on both platforms | Ledger now decodes `moves-history.v1`; loading, empty, and error all route to `OmenStateSurface`; demo proven unable to reach the live path. iOS 221/221 unit + 5/5 UI (Xcode 26.6 `17F113`); Android `:app:testDebugUnitTest` 27/27 + `:app:assembleDebug` + `:core:designsystem` 22/22. Evidence: `Blueprints/handoffs/2026-08-16-m5-slice-e-ledger.md`. | **The slice-D rule paid off a second time and needs one refinement.** "Grep the emitter before trusting the spec" caught the state list in slice D; here the emitter's *nullability* was the payload — `normalizeMove()` nulls each field independently, which no spec summary states. Codify the wider form: read the emitter for **states and per-field nullability**, and on Android check which JSON reader is being used, because `org.json`'s `opt*` family silently converts "not graded yet" into `0` / `false` / `""`. **A real cost worth recording:** adding two files to the iOS project meant hand-editing `project.pbxproj`, and my first attempt reused object IDs already belonging to the UITests target, which made Xcode report the project as *damaged* rather than as an ID collision. Diagnosing it took a stash-and-compare. Codify: when hand-editing `project.pbxproj`, grep each new 24-char object ID for uniqueness **before** writing it — the error message will not tell you that is the problem. |
| 2026-08-16 | M1-Screen-Trade + M1-Screen-League — proposal pass | `figma:figma-use` (invoked before every `use_figma` call, per its mandatory-prerequisite rule); native read gate re-applied; `design:design-critique` followed procedurally as an audit of the pre-existing frames rather than a new critique; `figma:figma-generate-design` considered and SKIPPED — no screen needed generating once the inventory showed the frames existed | PASS with scope correction — the items' `Done when:` clauses were already satisfied by pre-existing frames; the acceptance-gate halves that were missing are now written, and the residual pass-level gap is flagged rather than claimed | References board `86:2`, QA records `87:2` / `88:2`, stale Draft destination corrected at `18:7` / `18:20`, three backend gaps routed to the contract bus. Evidence: `Blueprints/handoffs/2026-08-16-m1-screen-contracts.md`. | **Inventory before authoring is the lesson, and it generalises past code.** Slice D said "grep the emitter before trusting a spec"; this session's version is "inventory the artifact store before building the artifact". Ten minutes of read-only `use_figma` turned a day of redrawing into an audit plus the two things that were actually missing. **The mechanical follow-up worth building:** `check-sprint-staleness.js` cannot see design work at all — it matches sprint keys to merged PR titles. Every one of the seven previous incidents was code; this eighth one was Figma, and it slipped through a check written the same day specifically to end the pattern. A design-lane equivalent — assert that each `M1-Screen-*` item's named frames are absent before the item is offered as pullable — would close the other half. **Second, smaller:** the R7 amendment updated the repo contract and left the Figma copy stale, which is the same one-sided-update failure mode as the handoff-after-merge problem. When an approved contract is amended, both copies move in the same pass. |
| 2026-08-18 | S5 — Mobile token storage review | `security-privacy-evidence`, native mobile read gate (all six contracts, via a dedicated investigation pass) | PASS — `Done when:` met in full | Audited both native codebases' credential storage against the onboarding-connection contract §7 and M0c §2.2. No plaintext token storage found on either platform — iOS Keychain, Android AndroidKeyStore-backed AES-256/GCM before `SharedPreferences` both matched the required mechanism already. Closed the actual gap (no direct test of either store) with 5 new tests per platform against the real secure-storage APIs, including a regression guard proving tokens never surface in the underlying plaintext preference store. iOS 229/231 full suite (1 pre-existing pinned failure, 1 confirmed-flaky UI test unrelated to this work); Android new tests 5/5 plus `:app:assembleDebug` and the existing `SessionManagerTest` (6/6) green. Evidence: `Direction/reviews/2026-08-18-s5-mobile-token-storage-review.md`. | `rbac-risk-review` considered, not separately invoked — no agent/tool/workflow authority changed; the relevant access-scope question is answered by the Keychain accessibility attribute and Android's app-private-prefs + Keystore ACL, both documented in the review record. **Two non-obvious toolchain gaps found and fixed, worth carrying forward:** the documented local iOS test substitute (`CODE_SIGNING_ALLOWED=NO`) breaks real Keychain access outright (`errSecMissingEntitlement`) — no prior test had hit this because none touched the real Keychain; and `androidx.test.ext:junit` alone does not carry the `AndroidJUnitRunner` class into a test APK, `espresso-core` does, transitively. Full detail in the review doc and handoff. |
| 2026-08-18 | O6 — Native crash reporting, Android half | native mobile read gate (re-checked, no conflict), `pre-build-research` (verified Sentry's current envelope-vs-store ingestion format before building, since O1b's proof targeted GlitchTip not Sentry SaaS directly), `slops-tdd` | PASS — Android half proven; iOS half's own gate (`R3-BUILD-iOS`) resolved separately the same day, see the row immediately below | Wired a hand-rolled Sentry envelope sender (no SDK dependency, mirroring O8's backend choice) via `Thread.setDefaultUncaughtExceptionHandler`, chaining to the previous handler so OS crash behavior is unaffected. 7/7 unit tests on envelope construction; `:app:assembleDebug` green. Real-device proof: a deliberate `adb shell am crash` reached Sentry in under 1 second, response 200, and the founder independently confirmed the issue in the Sentry dashboard (`CrashedByAdbException`, 2 events, 0 users). Evidence: `Direction/reviews/2026-08-18-o6-android-crash-reporting.md`. | **A real bug only the real device could catch.** The first live attempt threw `NetworkOnMainThreadException` — an uncaught-exception handler runs on the thread that crashed, and StrictMode forbids synchronous networking on the main thread. All 7 unit tests passed both before and after this bug existed, because StrictMode doesn't exist in a plain JVM test. Fixed by moving the network call to a background thread with a bounded `join()`. Codify: for any Android crash/error-reporting path, the real-device pass is not optional evidence alongside the unit suite — it is the only thing that can prove the threading model actually works. |
| 2026-08-18 | R3-BUILD-iOS — App Store bundle validation fixes | `slops-investigate` (real-time, live troubleshooting alongside the founder's own Xcode/App Store Connect session), `security-privacy-evidence` (Apple credential boundary respected throughout — archive/signing/upload actions stayed founder-only) | PASS — `Done when:` met, independently confirmed | Diagnosed and fixed two real project bugs (missing Release-config app-icon name, missing orientation declaration) blocking App Store bundle validation, using `grep`/`security find-identity`/`curl`/a local `xcodebuild archive` test to confirm each hypothesis before proposing a fix rather than guessing from error text alone. Two false leads ruled out the same way (license agreement, "offline" message) instead of chased blindly. Independently verified the actual result in the App Store Connect dashboard rather than trusting Xcode's own success dialog. This also clears `O6`'s iOS-half blocker (row above) without itself proving that half — the distinction is recorded on both rows so neither reads as more finished than it is. Evidence: `Direction/reviews/2026-08-18-r3-ios-appstore-icon-orientation-fix.md`. | **The credential boundary held throughout a genuinely long live-troubleshooting session and stayed useful anyway.** Every actual account action — accepting the license agreement, creating the distribution certificate, archiving, uploading — was the founder's own click, at their own keyboard; my role was reading logs/keychain/network state and editing source, never touching Apple ID sign-in. That boundary didn't slow the debugging down — most of the actual bugs were in the repo, not the account. **A repeatable pattern worth naming:** "the exact same error persists after fixing what looked like the cause" is itself a diagnostic signal (stale in-memory app state), distinct from "a new error appeared" (real progress, wrong layer). Telling the founder to restart Xcode rather than re-diagnose from scratch saved a full troubleshooting cycle. |
| 2026-08-18 | matchupService.js — nflverse URL, season_type, and CSV-quoting fixes | `slops-investigate` (repo-wide sweep for other nflverse consumers; live-file verification before and after the fix, not fixtures alone), `slops-tdd` (RED confirmed against old fixtures before updating them, three new regression tests) | PASS — both bugs fixed and independently verified against real data | Repo-wide sweep for nflverse URL-construction sites found one the 2026-08-15 fix missed: `matchupService.js` still pointed at the dead `player_stats` tag. Fixed to the cron's already-proven `stats_player_week_<season>.csv` path, added `season_type`/REG filtering (previously absent entirely). A live-data check post-fix returned `null` for a real team/position/week — that surfaced a second, unrelated bug: naive `line.split(",")` silently misreads nflverse's real rows because the quoted `headshot_url` column contains its own unescaped comma, shifting every later column. Ported the cron's already-proven quote-aware parser instead of writing a new one. 10/10 focused, full suite 566/566, real live query now returns real data. Evidence: `Direction/reviews/2026-08-18-matchupservice-nflverse-fix.md`. | **The second time in three days a "fixed" nflverse pattern turned out to have a sibling nobody checked.** The 2026-08-15 fix closed as complete without sweeping for other consumers of the same URL pattern; this is now a named carry-forward in `known_issues.md` rather than a one-off note. **A green result against your own fixtures is not the same claim as a green result against the real upstream file** — the URL fix alone looked complete by every test in the suite, and was still silently wrong until tested against the actual live CSV, which is what caught the quoting bug. Codify both: sweep for sibling consumers before closing a "fixed the URL" class of bug, and run at least one real (non-mocked) call against external data before calling a data-source fix verified. |
| 2026-08-19 | O7 — Forced-update / minimum-version gate | native mobile read gate (all six sources read via a dedicated scoping pass before any native code; no conflict found, and no pre-existing version-gate or remote-config concept existed to extend — O7 is greenfield) | PASS — every `Done when:` clause met on both platforms | Server-driven minimum version, honest blocking prompt on iOS and Android, fail-open on network error, tests covering below-minimum / at-minimum / check-unavailable on all three layers. Backend 569/569, iOS 234/234 unit + 5/5 UI, Android 42/42 + `assembleDebug` clean, plus a live-server route smoke. Evidence: `Blueprints/handoffs/2026-08-19-o7-forced-update-gate.md`. | **Both skills named on the sprint item were considered and correctly N/A**, and saying so explicitly matters more than invoking them for form: `security-privacy-evidence` — the feature reads, stores, logs, and transmits no token, credential, or PII; the route is public by design and its only input is a version string. `rbac-risk-review` — no role, permission, or access-control surface is touched. **The improvement this pass earns is a documentation fix, not a new skill:** `definition-of-done.md`'s iOS local substitute (`CODE_SIGNING_ALLOWED=NO CODE_SIGNING_REQUIRED=NO`) cannot pass `KeychainSessionStoreTests` — the Keychain needs a signing-derived entitlement, so four tests fail `errSecMissingEntitlement` while the same command without those flags passes all five. **The 2026-08-18 S5 row already recorded this trap and the doc still says the wrong thing**, which is precisely how it reached a second session; a note in a ledger is not a fix. Amend the iOS row in `definition-of-done.md`. Generalise: when a session discovers the *documented procedure itself* is wrong, correcting the procedure file is part of the task, not a follow-up — otherwise every later agent pays the same cost and may record a false red against unrelated work. **Second, from the visual pass the founder asked for:** a design-token defect is invisible to the type checker and to every unit test — 276 tests were green across both platforms while the iOS gate screen drew the system default background instead of `OmenColor.bg`. The existing `ScreenshotScenarios` mechanism made proving it a ~10-minute job, and sampling the rendered pixel (rather than eyeballing the capture) turned "looks about right" into an exact token match. Codify: for any new full-screen native surface, register a screenshot scenario and render it before claiming the UI is done; `.background(OmenColor.bg)` on the root is a checklist item, not a nicety. |
| 2026-08-19 | R3-BUILD-iOS + M4-Auth-Providers-v1 closures | `code-review` (as the available substitute for `slops-code-review`, run earlier the same day on O7); native mobile read gate not re-applied — neither closure changed native code | PASS — both `Done when:` sets met and recorded | Verified rather than trusted: read both platforms' provider-conditional rendering and callback-exchange call sites, ran Android scanner/unit/connected/assemble and iOS `xcodebuild test`, and read the machine's own hardware report for the cost clause. No implementation was written for either item. Evidence: `Direction/sprints_completed.md` § "Founder-gate clearing — 2026-08-19". | **The tooling found both, which is the point of having widened it that morning** — `M4-Auth-Providers-v1` had been advertised as available P1 work for six days with every cited PR merged, and no human noticed. **Two habits worth keeping:** read the hardware rather than transcribing the founder's verbal figure (the machine and price both differed from the item's assumed option, and the next hardware decision gets read off that line); and state what a re-verification pass did *not* prove — fresh test counts next to an older live-provider proof will be read as a fresh live proof unless the record forbids it. |
| 2026-08-19 | Known-issues ↔ GitHub reconciliation + widening `check-sprint-staleness.js` | no packaged skill applied — this was a record-integrity pass, and the routing table has no entry for it; `security-privacy-evidence` considered for the Sentry-breadcrumb and GDPR entries and SKIPPED, since the pass surfaced and filed them rather than changing any security code | PASS — every entry checked against `main`, merged PRs, and open issues rather than re-read | Sorted every `known_issues.md` entry into already-fixed, still-open-and-known, and still-open-but-buried. Corrected one cross-file contradiction (Yahoo entitlement, `facts-of-record.md` vs `known_issues.md`), one stale entry (`src/omen_gdpr.js`, deleted by #119 as an unmounted orphan — no capability lost), and raised #338–#341 for the four buried issues. Closed `S8` as already-done. Then rebuilt `check-sprint-staleness.js` as an orchestrator over six domain checkers in `scripts/checks/`. Evidence: `Direction/known_issues.md` § "Reconciled against GitHub — 2026-08-19"; PR #342. | **A partial checker that prints "no staleness found" is worse than no checker** — it converts an unknown into a false all-clear. That is why the orchestrator now prints its coverage, including blind spots, on every run, and reports "DID NOT RUN" rather than passing when GitHub is unreachable. **Tuned against real output, 10 findings down to 3:** section-scoped matching produced four wrong findings on one issue; line-scoped matching still cross-contaminated a summary line naming four issues; and an entry describing a file's deletion was flagged as stale for naming it. Every false positive costs more than a missed finding because it teaches people to skim. **And the pass's own miss, caught by the founder:** closing `S8` without a ledger row or `sprints_completed` entry — the exact "correction written where it was discovered, not everywhere it belongs" pattern the reconciliation had just documented, committed within the hour of documenting it. |
| 2026-08-19 | O2 — rollback runbook (documentation half) | `slops-ship` and `slops-canary` are the item's named skills and **neither is available in this session** — both are L0/SLOPS-layer skills and this is a standalone Omen checkout with no L0 present. Recorded as UNAVAILABLE rather than skipped-by-choice. `operations:runbook` was available and considered, but SKIPPED: it generates a runbook from a described procedure, and the value here was reading `deploy.yml` and `docker-compose.prod.yml` to find what the procedure actually *is* — a generated runbook would have documented the rollback everyone assumed existed. | PASS for the documentation half; the exercise and the named owner are founder-only and outstanding | Delivered `Blueprints/playbooks/rollback-runbook.md`, wired into `release-done.md` gates 11 and 16. Backend `npm test` 570/570 (documentation-only change, run to prove no incidental breakage). No source, workflow, or deploy config touched. Evidence: `Blueprints/handoffs/2026-08-19-o2-rollback-runbook.md`. | **Reading the pipeline beat describing it.** The assumed rollback — "redeploy the previous image tag" — does not exist: `deploy.yml` publishes only `:main`, and `docker image prune -f` deletes the image it just replaced. **The pipeline destroys the artifact you would roll back to**, and no amount of well-written procedure would have surfaced that without opening the workflow. **Second lesson: nobody reads a gate they are not currently trying to pass.** `release-done.md` still carried a HARD-BLOCKED banner for a billing hold retracted 18 days earlier; it took someone actually trying to satisfy the release gates to see it. The known-issues reconciliation two hours earlier could not have — it compared repo records against GitHub, and this was a repo record contradicting a retraction inside the repo. **No LEDGER.md row yet, deliberately:** that file records closures, and `O2` is `IN_PROGRESS`. |
| 2026-08-20 | F9 — mock / live labeling sweep: deferred cross-platform verification and closure | native mobile read gate (N/A — no native feature code written; the one code change was a Swift literal-formatting fix to an existing test); `engineering:code-review` (applied to the full `4c4a0fe` diff across all three clients); `slops-ui-ux-audit`, `slops-ux-copy`, `demo-mode-pre-empty-state` are F9's named skills and were **considered and not applied** — the labeling copy and states were authored in the prior session and this pass was verification, not design | PASS — both deferred runs green; F9 advanced to `VERIFIED` | Signed iOS 246/246 + `OmenDecisionTests` 14/14 (Xcode 26.6, iPhone 16, no signing overrides); Android connected 60/60 on `medium_phone` with both label assertions confirmed in the results XML; backend 575, frontend build, 0/0 audits, Valor Brain 2/2, staleness coverage block read. Evidence: `Direction/reviews/2026-08-20-f9-mock-live-labeling-inventory.md`. | **The deferral was recorded honestly and was still hiding a hard failure.** `4c4a0fe`'s iOS test target did not compile — four inline Swift triple-quote literals — so none of F9's iOS assertions had ever executed, while the commit message's true statement about *Android* instrumented-test compilation read as cross-platform coverage. Codify: a `DEFERRED-CI` row should say **which platforms the compile evidence covers**, because "tests compile" written once beside a multi-platform change is read as all of them. **Second, and cheap enough that there is no excuse:** the two red iOS UI tests were attributed to pre-existing causes by *running them at the pre-F9 base commit in a throwaway worktree* (~60s), not by reasoning from the diff. "Unrelated to my change" is the most convenient possible belief at closing time and the base run converts it from hypothesis to fact. **Third, the gap this pass did not close:** F9 ships new user-visible Mock/Demo/Unverified treatments with zero rendered evidence. O7 established, on this same repo, that a token defect survives a fully green suite — the F9 states should have had a screenshot scenario registered, and the honest move was recording that as unsatisfied on the LEDGER row rather than letting a green test count imply a look. |
| 2026-08-21 | Yahoo entitlement confirmation + contractual attribution (web, native, store copy) | native mobile read gate **applied** — `omen-native-delivery-governance-v1.md` read before touching native, and it is what steered the attribution onto the existing Help + Support surface rather than a new Legal screen needing its own screen contract and Figma approval; `engineering:debug` considered and SKIPPED — the diagnosis turned on reading one discarded response body and running two live calls, not on a structured debugging frame; `legal:review-contract` considered and SKIPPED — the agreement is executed, so the task was compliance with a fixed clause, not negotiation | PASS — refusal confirmed from Yahoo's own body; all three attribution placements met | Confirmed `"This application is not authorized to perform this action."` from inside the production container. Eliminated IP-block, token, homepage-URL, and wrong-app hypotheses with live evidence. Found five Yahoo apps where the repo recorded two. Fixed `src/services/yahoo.js:54` to carry status/body/`www-authenticate`. Attribution shipped to `Footer.jsx`, both native Help + Support screens, and the store-listing spec. Backend 575/575; iOS 248/248 + BUILD SUCCEEDED; Android compile clean; frontend build clean; web attribution verified rendered via a temporary flag flip. Evidence: `Blueprints/handoffs/2026-08-21-yahoo-entitlement-confirmed-and-attribution.md`. | **A status code is not a diagnosis.** `Yahoo API error: 403` was thrown for eight days while Yahoo was willing to say why in the body; four different failures share that status and have nothing in common as remedies. The conclusion happened to be right, which is the dangerous case — nothing forces a re-check when the wrong reasoning produces the right answer. **The founder's "two approval letters and it still doesn't work" was the actual signal**, because the standing explanation could not account for it. **Second: `league_id: "yahoo"` was called a bug on sight and retracted the same session** — a documented NOT NULL sentinel with a guard written for it. Same error as the 403: judging a value without reading the code that produces it, made twice within an hour of documenting it. |
| 2026-08-21 | O8 — wire error tracking into Omen's actual error paths | `security-privacy-evidence` **applied**, and it is what caught the pre-existing scrubber gap — the containment tests were written against realistically-shaped provider bodies rather than against the code's own assumptions, and failed on first run; `engineering:code-review` applied to the full diff; native mobile read gate N/A (backend only); `release-done` N/A (nothing deployed) | PASS — all three `Done when:` clauses met, live delivery into GlitchTip included; `O8` VERIFIED | `captureProviderError()` wired at all three provider chokepoints, demo isolation double-guarded, API process handlers added. `npm test` 587/587 (baseline 575, +12). Real provoked ESPN failure asserted against the transmitted bytes via `scripts/verify-provider-error-capture.js`: 0 leaks, 10 frames, correct tags/fingerprint. No dependency added, no production change. Evidence: `Blueprints/handoffs/2026-08-21-o8-glitchtip-error-paths.md`. | **The task's own Scope was written on a false premise, and following it literally would have made things worse.** It said nothing in `src/` sent anything, and therefore to hand-roll an HTTP integration to dodge the dependency gate — but `@sentry/node` had been wired since Phase 1.2, so the gate did not apply and the instruction would have added a second, weaker path beside a working one. The Scope was written from `O1b`'s handoff without grepping `src/`. **Generalise the standing rule explicitly to absence claims:** *`main` is the proof* is applied reflexively to "X is done" and almost never to "X does not exist" — yet the absence claim is the easier one to get wrong, because nothing contradicts it until someone looks. **Second, the fix a green suite would never have surfaced:** `flushSentry()` is `Sentry.close()`, which flushes *and permanently disables the client*. Using it on a survivable path silently ends error reporting for the life of the container — a failure whose only symptom is the monitoring tool going quiet, which reads as good news. Error-handling code has this property generally: its failures look like health. **Third, and the reason to distrust "we have a scrubber":** the shared scrubber had never matched `access_token=…` at all, because `\b` finds no boundary in `_token` — the literal key names our own code uses were exactly the ones it skipped, and JSON-quoted secrets passed through untouched. It had been live for every event ever processed. A scrubber is only as good as the shapes someone actually tested it against, and the shapes worth testing are the ones the codebase itself produces. **Fourth, and the largest finding of the pass — it came from refusing to leave "nobody has confirmed the deployed value" as a question.** Reading KVM1's actual container env took one command and revealed `SENTRY_DSN` = the literal placeholder `paste_the_value_here` glued to a sentry.io DSN, in both containers. Not a legal URL, so no transport was ever built, and `enabled: Boolean(env)` waved it through: **Omen had been reporting errors nowhere, and reporting itself healthy while doing it.** Codify: *for monitoring and alerting specifically, a config mistake is indistinguishable from the all-clear* — silence is the success signal and the failure signal at once, so these systems need a positive liveness proof, never an "is it set?" check. Issue #354. **Fifth, the mistake I made fixing it, which is the same mistake one level up:** my first DSN validator was looser than `@sentry/node`'s own grammar, so it passed GlitchTip's dashed-UUID key that the SDK then rejected — `/api/ready` would have reported error tracking healthy while every event dropped. **A validator that does not match the thing it validates is not a guard, it is a second bug wearing the guard's clothes.** Match the real parser, and prove it against the real endpoint rather than against your own model of it. |
| 2026-08-21 | O9 — route GlitchTip issues into the Layer 5 Discord dispatcher | `security-privacy-evidence` **applied** — read-only Postgres enforcement verified by confirming an `UPDATE` is refused rather than assuming `PGOPTIONS` took effect, `0700` restored on a script that reads the webhook secret path, issue titles sanitized as vendor-controlled input, and the webhook value never read or echoed; `slops-ship` / `slops-canary` are the lane's usual skills and are **UNAVAILABLE** in a standalone Omen checkout (L0 not present) — recorded as unavailable, not skipped-by-choice; `operations:runbook` considered and SKIPPED — the artifact is a 27-line script vendored with its own README, not a procedure | PASS — all three `Done when:` clauses proven live, founder confirming each Discord message | Chose (a) extend the dispatcher over (b) GlitchTip's native webhook, because (b) puts a second path with different semantics into one channel and could only meet the shared-dedup requirement by reimplementing existing noise control. CRITICAL alert → silent repeat → founder resolved → one RECOVERY → silent again. Other collectors verified directly (6 `result=` lines, 4 Kuma monitors green, Pi-hole clean). Artifact vendored and proven byte-identical to the Pi. Evidence: `Blueprints/handoffs/2026-08-21-o9-glitchtip-discord-alerting.md`. | **Adding a fourth signal source produced the dispatcher's first multi-line signature, and it failed with Discord `400`.** The payload had always been string-interpolated into JSON, where raw newlines are invalid. **Every alert this fleet had ever proven carried exactly one failing signal** — so a simultaneous two-signal failure would have delivered *nothing*, the alerting layer failing precisely in the situation it exists for. A second defect kept it silent instead of loud: state was persisted before delivery was confirmed, so the failed send recorded its signature and the next run saw no change. **Two bugs, each hiding the other, both invisible to every check that existed.** **Codify: a test that exercises one item does not test a list.** Layer 5's noise-control semantics were genuinely proven at build time; the delivery mechanism was only ever tested on its easiest possible input, and nobody noticed the difference because the two were proven in the same breath. Acceptance evidence for alerting should require a **multi-signal** simulated failure. **And the through-line with `O8`, run the same day one layer down: monitoring and alerting fail in the shape of good news.** O8 found production reporting errors nowhere while reporting itself `enabled: true`; O9 found the alert path unable to send the alert it exists for. Neither was visible to an "is it configured?" check; both surfaced only by exercising the real path with real content. **For this class of system, absence of noise is not evidence — it is the failure symptom and the success symptom at once.** |
| 2026-08-21 | Staleness close-out — `F9` and `O6` advanced from VERIFIED to CLOSED | no packaged skill applied — a record-integrity pass, which the routing table still has no entry for (same gap recorded on the 2026-08-19 reconciliation row); `security-privacy-evidence` **considered and applied in part** to `O6`, since its open item was a GDPR question about IP storage rather than a bookkeeping one | PASS — both closed, staleness checker down from 2 findings to 0 | `F9`: re-checked before closing, not rubber-stamped — inventory confirmed present, and the two "not proven" items confirmed to sit on `F10`/`F11` rather than on F9's own `Done when:`. `O6`: founder enabled *Prevent Storing of IP Addresses* on the Sentry iOS/Android/React projects, resolving the item the record had deliberately held it open for. Also corrected a disproven factual claim in `decision_log.md`. | **The two findings looked identical to the tool and were not remotely the same.** `F9` was pure bookkeeping — done, evidenced, never advanced. `O6` was **deliberately** held at VERIFIED with its reason written on the record: an iOS project storing a client IP where Android was not, identical code, different per-project settings. **Closing them with the same gesture would have buried a live GDPR question under a tidy-up.** The checker cannot tell these apart and does not claim to — its own coverage block says "whether a `Done when:` clause was genuinely met — always a human call". **Read the item before believing the tool about it**, especially when the tool is right about the symptom. **Second, on evidence honesty at closing time:** O6 closed on a founder-confirmed dashboard toggle, and the record states plainly that "the toggle is on" and "no IP is stored on the next crash" are different claims, only the first evidenced. The empirical re-check was folded into `F10`'s existing device matrix rather than minted as a task — but the boundary is written down, because the cheapest moment to blur an evidence gap is the moment you are closing something. |
| 2026-08-22 | S3 + S4 + O4 — hot-route rate limits, credential containment, and the load rehearsal | `security-privacy-evidence` **applied** to both S3 and S4 — and it is what produced the two real findings, because the containment tests were written against realistically-shaped provider failures rather than against the code's own assumptions and failed on first run; `slops-tdd` **applied in substance** via mutation checks rather than intended-RED-first (three separate mutants run and restored: remove the limiter mount → 8/14 red; remove the log scrubber and the envelope scrub → both backstop tests red; revert the `authorization` key fix → the whole-object log test red); `engineering:code-review` applied to the full diff; `rbac-risk-review` **considered and SKIPPED** — no authority boundary changed, the rate limiter is a throughput control and the scrubber is an emission control; native mobile read gate **N/A** — backend only, no native file touched; `release-done` **N/A** — nothing deployed, PR left open; `slops-ship` / `slops-canary` / `slops-quality-baseline` are **UNAVAILABLE** in a standalone Omen checkout (L0 not present), recorded as unavailable rather than skipped-by-choice, with their local substitutes run instead (`npm test`, `npm audit`, `git diff --check`, `npm run evals:validate`) | PASS — all three items' `Done when:` clauses met; two live scrubber defects found and closed en route | `npm test` 618/618 locally **and in CI** (baseline 593 at `f4a6ae1`, +14 S3, +11 S4); `npm audit` 0 vulnerabilities; `git diff --check` clean; evals 3 prompts / 2 cases; `check-sprint-staleness.js` no findings; all three PR checks green on [#355](https://github.com/justinduverge-design/omen/pull/355). S3 also proven against a booted server (20 × 401 then 429, `Retry-After: 60`). O4 evidence: `Direction/reviews/2026-08-22-o4-hot-route-load-rehearsal.md`. Handoff: `Blueprints/handoffs/2026-08-22-s3-s4-o4-hot-route-hardening.md`. No dependency added; `package.json`/`package-lock.json` byte-identical to `main`. | **The scrubber has now been found holed in three consecutive sessions, and every time by provoking a real failure — never by review.** O8 found `\b` skipping `access_token=` and JSON-quoted secrets passing; S4 found `authorization` missing entirely and, once added, `Bearer <token>` surviving because the key/value rule stops a value at the first space. **That second one is the instructive shape: the rule matched, redacted the word "Bearer", and reported success on the exact string it was failing to protect.** A redaction rule only ever covers the shapes someone fed it, so the acceptance bar for one has to be *provoke the failure and search the emitted bytes for the value you fed in* — reading the pattern and agreeing with it is not evidence. **Second, my own test produced a finding about itself:** the Yahoo `fetch` stub was broad enough to answer the suite's own loopback request for its error envelope, so it reported a credential leak in an envelope it had never reached. A stub that intercepts more than its target manufactures findings; scope it to the host. **Third, the order dependency in this package was the whole point and is worth generalising:** S3 changes what O4 can honestly measure, so "load-test the hot routes" was an underspecified task until the limits existed. A performance number is only meaningful against a stated admission-control policy, and the concurrency has to be *derived* from that policy — 8 requests/client because `mvp-move` allows 10/min/credential, not because 8 is a nice number. **Fourth, a genuinely useful accident:** the saturation run found that the app-wide 100/min/IP limiter binds before `dashboard/summary`'s own 60/min budget for any client using the whole app. Nobody would have looked for that; it fell out of deliberately building the run that measures the limiter instead of the route. **Procedure gap, unchanged from 2026-08-19 and 2026-08-21 and now recorded a third time:** `skill-activation-runbook.md` still has no routing entry for a record-integrity or evidence-only pass, and `slops-*` skills remain unavailable in a standalone Omen checkout while the routing table keeps naming them. |
| 2026-08-22 | M4 render-evidence package — three items closed on captured, reviewed pixels | **native mobile read gate applied** (all seven sources), and it is what kept this a capture pass rather than a build pass — `omen-native-design-house-v1.md` §10 states that an agent cannot claim visual correctness from code alone, which is the premise all three items were held open on; `slops-repo-inspector` applied; `slops-quality-baseline` applied; `slops-git-flow` applied; `slops-ui-ux-audit` / `slops-mobile-smoke` **SUBSTITUTED** — both are web-route audits in this repo, and the native equivalents are the rendered captures plus `performAccessibilityAudit()`; `slops-tdd` **N/A** for behavior (none changed) — the one new test is accessibility evidence written after the behavior it documents; `security-privacy-evidence` applied narrowly (every capture in screenshot mode with no session/auth/network/provider state, plus a credential-shape scan of the TalkBack tree); `slops-ship` / `slops-canary` **UNAVAILABLE** in a standalone Omen checkout, recorded as unavailable rather than skipped-by-choice | PASS — all three `Done when:` sets met; `M4-CC-PlatformsCompact`, `M4-CC-WaiverWatch`, `M4-Help-Support-Implementation` all CLOSED; `B-FREEZE` down two blockers | Backend 593/593. Android `:app:assembleDebug` SUCCESS, scanner 1/1, designsystem 22/22, app unit 45/45, connected 53/53. iOS branch 263/2 vs base 256/2 (+7 passing, 0 new failures), Xcode 26.6 (17F113). Evidence: `Blueprints/handoffs/2026-08-22-m4-render-evidence-package.md`, `References/evidence/2026-08-14-cc-platforms-compact/`, `.../2026-08-22-m4-waiver-watch-ios/`, `.../2026-08-22-m4-help-support-native/`. | **The screenshot registry has no check that a scenario shows the thing it is named for, and the CI matrix is where that bites.** Two findings share this root. The iOS host omitted `onOpenOmen`, so scenarios *named* for Waiver Watch states rendered those states minus an element of the approved composition — nothing complained, because a scenario's only contract is its key. Separately, the new `waiver-watch.*` scenarios **cannot** go in the CI matrix at all: the section they are named for is below the fold on every current iPhone and the workflow captures with no interaction, so a matrix row would upload the top of the Command Center labelled as Waiver Watch evidence. **The registry's own doc-comment — "add one entry here plus one matrix row" — is wrong for any scenario whose subject is not visible at launch, and following it produces confidently mislabelled evidence.** It should say a scenario earns a matrix row only if it is capturable without interaction. **The broader version, now recorded three times on this repo** (`O7`'s background token, `F9`'s unrendered labels, today): a green suite *plus* a registered scenario still does not mean anyone has seen the pixels. **Second lesson, cheap and repeatable:** the two red iOS tests were proven pre-existing by checking out `f4a6ae1` into a throwaway worktree and running it there — 256/2 with the same two failures — rather than by arguing from the diff; and the third red was proven environmental by re-running it alone. **Third:** sampling the rendered pixel proved a suspicious colour was `#64C139`, but only re-running proved it was TalkBack's focus ring rather than a token defect. Measurement and reproduction answer different questions, and stopping after either one alone gives a wrong answer — a false defect report, or contaminated evidence committed as clean. |
| 2026-08-22 | Defect-repair pass on the three findings the M4 render package surfaced | native mobile read gate **already applied** in the same session; `slops-code-review` applied to each fix; `security-privacy-evidence` N/A — no auth, credential, telemetry, or provider surface touched; `slops-tdd` **N/A by honest accounting** — all three fixes are visual/contrast defects with no assertable behavior change, and the proof is a re-render plus an existing accessibility audit, not a new RED; `design-md-author` / `planning-pass` **considered and SKIPPED** — the contrast fix restores the implementation to registry §3.1 rather than changing a design, so there was nothing to author or ratify | PASS — iOS `main` red→green (256/2 → 265/0), Android nav clean at font scale 2.0, connected 53/53 | Contrast: `accent` `#A67C2E` on `surface-2` `#2C2C2E` measured **3.68:1** vs AA 4.5 (dark only; light 5.70:1). Nav: `maxLines`/`softWrap`/`overflow` on both `OmenBottomNav` and `FauxBottomNav`. Every committed Android image recaptured post-fix. Evidence: `Blueprints/handoffs/2026-08-22-m4-render-evidence-package.md`. | **"Pre-existing" is a claim about provenance, not about severity — and it is routinely allowed to mean both.** The base-commit worktree run did its job: it proved the two red iOS tests were not mine. The convenient next move was to stop there and hand them over as inherited, which the earlier version of this handoff did. Exporting the audit's own failing-element attachment took one command and turned a five-day-old red into a two-line, spec-mandated fix. **Codify: after proving a failure is inherited, spend one more step asking whether it is cheap** — the worktree technique is good at closing the blame question and, precisely because it closes it so cleanly, it invites you to stop. **Second, on staying inside authority without hiding behind it:** the same audit exposed two contrast problems with very different ceilings. One was an implementation deviation from a ratified registry row — fixable outright, no approval needed. The other is a token-pair defect that would change a core semantic colour app-wide — a founder call. **Reading the registry row directly rather than trusting the known-issues paraphrase is what made that distinction legible**; without it both look like "a contrast bug" and the honest options collapse to "escalate everything" or "change a brand colour unattended". |
| 2026-08-26 | A7B Phase 4 production-readiness preparation | `slops-repo-inspector`, `slops-data-ingest-plan`, `slops-tdd`, `slops-git-flow`, `slops-quality-baseline`, `security-privacy-evidence`, `self-hosted-observability-runbook`, `slops-code-review` | PASS for local preparation; remote/A4 gates remain | Fresh worktree at merged Phase 3 base; read-only KVM1 and Command Center inspection; fail-closed readiness evaluator, tests, specification, sanitized evidence, eight-part plan, review, and handoff. Phase 4 7/7 and relevant Phase 1–4 37/37; audit 0; eval metadata passed; diff clean. Evidence: `Direction/reviews/2026-08-26-a7b-phase4-production-readiness-plan.md`, `Direction/reviews/2026-08-26-a7b-phase4-code-review.md`, `Blueprints/handoffs/2026-08-26-a7b-phase4-production-readiness-preparation.md`. | `planning-pass` N/A: the canonical item and founder gate already exist and no queue authority changed. Existing A7 source-rights evidence substituted for new pre-build research; no source changed. `slops-verify`, ship, and canary remain deferred because A4/deploy/activation did not occur. Full suite attempted but not claimed because the fresh worktree lacks declared dependencies; no install or dependency change was made. No remote mutation, secret, SQL/database write, deployment, publication, or scoring enablement. |
| 2026-08-26 | A6 safety containment + A7B closure reconciliation | `run-slops-saloon` | PASS — production hold verified, A6 repair green, A7B clauses ledgered | The skill required the Omen authority/read order and evidence-first loop. It led to the clean worktree, production read-only verification, stop-and-tell escalation, founder-authorized cron-only hold, RED→GREEN persistence tests, full backend 712/712, and record/ledger reconciliation. Evidence: `Blueprints/handoffs/2026-08-26-a6-safety-a7b-closure.md`. | No UI was changed, so screenshot/browser QA portions were N/A. No merge, deploy, provider-access expansion, schema change, SQL, credential read, or database write occurred in this closure pass. A6 remains blocked by provider rights/full-contract reconciliation and production deployment/new-row proof. |
| 2026-08-27 | A7B deploy completion + root-owned env workflow repair | `run-slops-saloon` | PASS — merged repair is live; workflow regression fixed and fully tested | PR #372 merged after 3/3 required checks. Its automatic deploy pulled the correct images but failed closed before recreation because `.env.production` is intentionally root-owned `0600`; API and cron were then recreated manually with the already-pulled images, scoring flags remained false, health/readiness passed, and the live container contained the A6 containment code. The follow-up workflow test went RED then GREEN and the full backend suite passed 714/714. | The skill's production evidence loop exposed a least-privilege boundary absent from deploy tests: Docker-group access is sufficient for pull, but Compose requires root to open the protected env during `up`. Only that operation now uses `sudo`; no scoring activation, schema mutation, database write, provider-access expansion, or UI change occurred. |
| 2026-08-26 | M9 backend gap closure — switcher, Waiver Analysis, Start/Sit detail, Ledger detail, plus the A6 agent half and the B2-D3-S2 reconciliation | `slops-repo-inspector` **applied** and it is the whole session — the audit read the live route and the provider adapter for every claim rather than the queue's description of them, and that is what found the two inverted premises below; `slops-tdd` **applied in substance** — the switcher, waiver, start/sit and ledger suites were written alongside their routes and each one caught a real defect on first run (`token_expires_at` omitted; `Number(null)` reaching a provider-score comparison as a real zero); `security-privacy-evidence` **applied** — every provider error path is provoked with a fake cookie value and the emitted response bytes searched for it, per the standing rule that a scrubber claim needs evidence not review; `engineering:code-review` applied to the full diff; `slops-git-flow` applied; native mobile read gate **N/A by instruction** — backend only, no native file touched, and the four screen contracts were read as *contracts* to derive backend shape, not to build against; `planning-pass` **considered and SUBSTITUTED** — four backend delivery items were minted from an audit rather than from a planning pass, and `M9`'s own screen-minting scope was deliberately left untouched; `release-done` **N/A** — nothing merged, nothing deployed, PR left open; `slops-ship` / `slops-canary` / `slops-quality-baseline` **UNAVAILABLE** in a standalone Omen checkout (L0 not present), recorded as unavailable rather than skipped-by-choice, with local substitutes run instead (`npm test`, `npm audit`, `git diff --check`, `check-sprint-staleness.js`) | PASS — all four screen backends built and tested per provider; A6 agent half complete; §"Not Deployed / Not Merged" empty | `npm test` **812/812** from a recorded **712/712** baseline (+100). `npm audit --omit=dev` 0 production vulnerabilities. `git diff --check` clean. `check-sprint-staleness.js` no findings. `package.json`/`package-lock.json` byte-identical to `main`. Audit: `Direction/reviews/2026-08-26-m9-screen-backend-dependency-audit.md`. Deploy note: `Direction/reviews/2026-08-26-b2d3s2-deploy-note.md`. Handoff: `Blueprints/handoffs/2026-08-26-m9-backend-gap-closure.md`. A7B untouched. | **The queue was wrong about existence in both directions in the same session, and the audit is the only reason either was caught.** `A6`'s `What is wrong:` line asserted a defect that `main` had already fixed — `fetchPendingMoves` does select `scoring` — while `B2-D3-S2` asserted six pieces of work were undeployed when one of them was answering live from production and all six had been on `main` since June. The repo's standing lesson says grep `main` before trusting a claim in either direction; **what this session adds is that the two failures look completely different and cost differently.** The stale-deploy claim cost twelve weeks of a blocked `B-FREEZE`. The stale-defect claim was more dangerous: it would have been *satisfying* to fix, and fixing it would have produced a confident, well-tested no-op while the real defect — an orphaned contract engine with no production caller — stayed invisible. **A task description that describes a defect accurately enough to fix is not evidence the defect is still there.** Second: the prompt itself supplied four "already known, do not rediscover" facts, and one was materially incomplete — `GET /api/optimizer/waivers` does exist, and is Yahoo-only, which inverts what it means for the screen. Checking a fact you were told not to check cost one `sed` and changed the shape of the deliverable. **Third, the null-vs-zero trap caught me in my own new code**: `finite(null)` returned 0 because `Number(null)` is 0 and 0 is finite — the exact shape of the B2-D-S0 projection bug the codebase already carries a comment about, reintroduced by someone who had read that comment twenty minutes earlier. It was caught by a test asserting a *state*, not a number. |
| 2026-08-28 | Yahoo entitlement verification, re-enablement, and the league-binding parser fix it exposed | none invoked — session began as a founder question ("Yahoo emailed approval, is it live?") and resolved into live verification + defect repair; the Omen read gate (`AGENTS.md`, `context.md`, `facts-of-record.md`, `known_issues.md`, `decision_log.md`) was applied before touching production | PASS — entitlement confirmed live, both gates opened, three broken parsers fixed, full Yahoo data path verified end-to-end on the deployed image (885/885) | The value came from the **read gate, not a skill**: `facts-of-record.md` #11 carried the exact re-check mechanism (`GET /api/yahoo/access-probe`, any 200 = granted) and the 2026-08-28 escalation date, so the first action was to run the probe rather than draft the escalation email. Access had landed that morning. **The recorded mechanism was worth more than any diagnosis would have been.** Second lesson, logged in `decision_log.md`: the first parser fix asserted an endpoint shape from inference and got it wrong; measuring all three endpoints against live traffic found two more broken parsers. |
| 2026-08-31 | Tier 0 player resolution, fuzzy recovery, and paired native deploy | `engineering:deploy-checklist` applied; `run-slops-saloon` inspected but its drivers are web-only; native mobile read gate completed before code | Backend 922/922; Android 192/192; iOS unit 318/318; `git diff --check` clean; deploy run 33445423517 green; live fuzzy and refusal probes passed. Native Trade interaction remains unverified. | The first fuzzy implementation measured 15-21 ms because it compared all 11.4k players. Candidate prefiltering reduced miss paths to 1.9-2.6 ms. The iOS full UI runner hung; Android ADB failed during final install, so neither is represented as a running Trade proof. |
| 2026-08-31 | App-wide page workshop, Wave 1 contract, direction propagation, design canvas, ESPN terms read | `design` **applied** — the canvas is the deliverable and the skill's "match the existing app pixel-perfectly" step is what produced the founder's "this is almost what the app looks like now"; `artifact-capabilities` loaded for the publish roster; native mobile read gate **applied** before any screen was drawn; `check-sprint-staleness.js` run and triaged; `planning-pass` **SUBSTITUTED** — the queue items were minted directly from a founder workshop rather than a planning pass, with the workshop record as their authority; `slops-ship` / `slops-canary` **N/A** — no code changed and nothing was deployed | PASS — 9-part workshop record, Wave 1 build contract, 6 direction files propagated, 12-artboard canvas published, ESPN gate closed | Docs only; no code, no deploy, no schema, no production touch. Commits `0a59934`, `8e79861`, `6ae4603`, `6dced3e`, `3213b7c`, `eec1b67`, `6f8483a`, `28ce1f2`, `88f8b5f` on `main`. Canvas: "Omen App Rework". Handoff: `Blueprints/handoffs/2026-08-31-app-wide-page-workshop.md`. | **The canvas found things the written workshop could not.** Two affordances drawn from decisions on paper turned out to have no system behind them — a Sleeper deep link that cannot carry a trade, and a vote poll with no endpoint, storage, or realistic sample size — and the founder caught both by asking what the control *does*. A written contract can specify an impossible button; a drawn one invites the question. **Second: propagating the workshop into the direction files surfaced an unrelated live contradiction** — `context.md` still required a "Pro subscription" and listed three Stripe endpoints roughly seven weeks after billing was deleted, in the file agents read third. The correction was written where it was discovered in 2026-07 and never where it was asserted, which is the exact failure `facts-of-record.md` #11 already records about Yahoo. **Third: the ESPN terms read answered a narrower question than it should have been asked** — the gate asked about the *proposed* sheet, and the governing clauses apply equally to the integration already shipping. |
| 2026-09-01 | Reviewer-path verification on device, three submission-blocking fixes, and first-submission readiness | `design` (canvas continued from 2026-08-31); iOS Simulator control **applied** — the app was built, launched, and driven rather than read; native mobile read gate applied; `engineering:deploy-checklist` substituted by the purpose-built submission runbook; `slops-ship` **N/A** — nothing deployed, submission is founder-gated | PASS — iOS 318/318 signed, Android `:app` 106/106, frontend build clean, `ARCHIVE SUCCEEDED` | Commits `20b202d`, `4d101f3` and this closeout on `main`. Runbook: `Blueprints/playbooks/first-app-review-submission-runbook.md`. | **Driving the app found what reading it could not.** Three defects surfaced only by launching and tapping: the store reviewer notes told Apple demo player names were generic while the shipped fixture read Christian McCaffrey and Ken Walker III; the selected tab rendered in iOS system blue while every other element on the same screen used the Omen accent (Android was already correct, so only iOS had drifted); and Demo Mode — cut by decision the previous day — turned out to be the reviewer's entire path into the app, which the store notes themselves document. **The demo-mode collision is the one worth carrying:** a decision that was correct in isolation would have broken the submission it was meant to de-risk, and it surfaced only because the founder asked for the submission and the onboarding check in the same sentence. Also: an unsigned `xcodebuild test` fails 4 `KeychainSessionStore` tests with `errSecMissingEntitlement` (-34018) — that is `CODE_SIGNING_ALLOWED=NO`, not a regression; the signed run is 318/318. Verified honestly as unverified: the web ESPN consent line is proved in source and in the built bundle but was never rendered, because `/account/connect` is auth-gated and local Supabase is unconfigured. |
| 2026-09-02 | Native onboarding screens — sign-in, email code, connect league on iOS and Android | Native mobile read gate applied; `run-slops-saloon` inspected for capture and QA routing; design canvas at `design/app-rework-canvas` treated as visual authority after the first pass drifted from the approved screens; original `/omen` checkout was fast-forwarded to inspect repo skill updates, and no new native skill was added | PASS with focused native verification and screenshot evidence | iOS focused auth/connect suite 43/43 after final visual corrections; Android `:app:testDebugUnitTest :app:assembleDebug` green; `git diff --check` clean; six screenshots captured under `References/evidence/2026-09-02-native-onboarding/`; Android sign-in corrected to Google primary with Discord/email below and no Apple; canvas auth, chevron, and shield icons are wired natively, and Android uses a clean stacked Omen lockup PNG generated from the source SVG. | Full all-iOS suite was not rerun after the final visual correction; focused auth/connect coverage passed. `check-sprint-staleness.js` still reports pre-existing direction cleanup findings for A4, B2-D3-S2, and issue #308. Android emulator capture hit a System UI ANR/focus issue during evidence collection, so app focus was checked before accepting the final screenshots. |
