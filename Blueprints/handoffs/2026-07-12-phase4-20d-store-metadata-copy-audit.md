# Handoff — 2026-07-12 — Phase 4.20d Store Metadata / Gambling-DFS Copy Audit

## Session start state

Ran the omen kickoff loop. `agent_inbox.md`'s listed Active Task ("Team theme removal," branch
`claude/team-theme-removal`) was already merged to `main` via PR #114 (verified via GitHub —
`merged: true`, `merged_at: 2026-07-12T19:02:50Z`) and is present on this branch's base commit
(`616a840`). Inbox text hadn't been refreshed to reflect the merge. With no live pinned task,
pulled the next unstarted item from the Auto-Populated Top 5: **Phase 4.20d — Store metadata,
privacy-label, and gambling/DFS copy audit (P1)**.

## What shipped

Full-file review (not just grep) of `frontend/src/pages/Landing.jsx`, `frontend/src/pages/OmenLanding.jsx`,
`frontend/src/pages/Onboarding.jsx`, and `README.md` for two risk categories: (a) betting/gambling/
DFS/wagering/odds/guaranteed-winnings language, (b) unqualified Yahoo/Sleeper/ESPN "live"/support
claims not paired with the Phase 4.16 packet's Platform Attribution Snippet disclaimer.

- No (a) language found anywhere.
- Two high-severity (b) gaps found and fixed:
  - `Onboarding.jsx` ConnectStep's "Supported platforms" card (names Yahoo/Sleeper/ESPN with
    "full Omen support" claims) — added the attribution/non-affiliation line beneath the list.
  - `README.md`'s MVP Move data-status table and Supported Platforms table (repeated unqualified
    "Live" + named ESPN/Yahoo/Sleeper) — added the same disclaimer beneath the Supported
    Platforms table and clarified "Live" denotes Omen's own connection status.
- Reviewed and left unchanged: `Landing.jsx`'s waitlist platform picker (form options, not a
  claim), `Landing.jsx`'s "live tool" copy (describes Omen's own compute, honest per the
  mock-vs-live rule), `OmenLanding.jsx` (no platform names or live claims present).

Full findings table: `Direction/reviews/2026-07-12-store-metadata-privacy-gambling-copy-audit.md`.

## Verification

- `npm run build` (frontend, after `npm install` to restore `node_modules`) — clean, no new
  warnings introduced (one pre-existing unrelated `Header.jsx` duplicate-`className` esbuild
  warning, confirmed present before this change too).
- `npm test` (root) — 183 pass / 70 fail, identical to the pre-change baseline confirmed via
  `git stash`; all 70 failures are pre-existing `MODULE_NOT_FOUND` issues in
  `test/yahooAuthRoute.test.js` and unrelated suites, not touched by this change.
- `frontend/package-lock.json` was regenerated by `npm install` (only `peer: true` flag
  normalization, no dependency changes) and reverted — package files are out of scope per
  standing Do Not Touch rule.

## Not done / follow-ups

- App-store description/screenshots/social-preview copy doesn't exist yet in this repo — flagged
  in the findings doc for whoever drafts the actual Phase 4.20 store listing to reuse the same
  disclaimer clause.
- The full Phase 4.16 Legal packet remains unpublished (`Legal/2026-06-28-open-agreements-provider-paragraphs.md`
  still marked review-only); this task reused only its already-approved general clause, not the
  whole packet.

## Evidence

- Diff: `README.md`, `frontend/src/pages/Onboarding.jsx`.
- New file: `Direction/reviews/2026-07-12-store-metadata-privacy-gambling-copy-audit.md`.
- Decision log: "Decisions Added 2026-07-12 (Phase 4.20d store metadata/gambling copy audit)".
- Skill ledger: 3 new rows dated 2026-07-12.
- `Direction/current_sprint.md` Phase 4.20d checkbox closed.

## Next step

Not pushed, merged, or deployed — local commit only, per the omen kickoff loop's Justin-gate
posture. Next unstarted item per the (now-stale) Top 5 is item #2, "Delete or archive orphaned
`src/omen_gdpr.js`," but the inbox should be refreshed first since the Active Task section is
stale (references an already-merged PR).
