# M1-P P2 — Modal Foundation — 2026-07-20

## Scope

Added a native sheet primitive: SwiftUI uses its standard sheet presentation and Compose uses
Material 3 `ModalBottomSheet`. Presentation state remains caller-owned; the shared surface only
applies Omen tokens and accessible title/content hierarchy.

## Evidence

- Code commit: `d2950b2`.
- Android: 15 emulator instrumentation tests passed.
- Merge: PR #167 (`9f05a3a`).
- iOS unsigned simulator CI: `29790409480` passed.

## Limitation and next gate

No direct native screenshot is claimed. ConfirmationDialog remains a separate P2 primitive and
must use the platform-native dialog APIs.
