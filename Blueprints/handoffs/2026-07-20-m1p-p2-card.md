# M1-P P2 — Card Foundation — 2026-07-20

## Scope

Added the shared `OmenCard` surface in Android `core:designsystem` and iOS `DesignSystem` only.
It covers solid, outlined, empty, error, and preview variants plus neutral/Omen/risk tones; it
does not introduce a feature screen, provider flow, credential, or new Figma pattern.

## Evidence

- Code commit: `7d400b7`.
- Merge: PR #166 (`6e173d9`).
- iOS unsigned simulator CI: `29789967145` passed.
- The Android design-system test/build evidence ran on the shared P2 branch before merge.

## Limitation and next gate

No direct gallery screenshot is claimed because the Studio-managed ADB executable was not
available to this shell. The next P2 primitive is ListRow; feature modules must not recreate card
containers locally.
