# Done Ledger

**Purpose:** every closure recorded. Review monthly — gates skipped often signal a prompt or skill to revisit.

## How to use

When you close a task, append a row. Format:

```
| Date | Item | Done docs applied | Gates skipped + why | Skill/prompt to revisit |
|---|---|---|---|---|
| 2026-06-15 | Phase 1.3 — page-system spec | feature, design | none | — |
```

If you skip a gate, write why. If the same gate gets skipped 3+ times across different tasks, that's a signal — either the gate is wrong (fix the gate), the agent doesn't know how to satisfy it (fix the prompt or skill), or the work is being mis-scoped (fix the planning).

## Active log

| Date | Item | Done docs applied | Gates skipped + why | Skill/prompt to revisit |
|---|---|---|---|---|
| 2026-06-15 | Phase 1.3 — page-system spec | feature, design | n/a — spec authoring, not feature ship | — |
| 2026-06-15 | RESOURCES_INDEX + facts-of-record at L0/L1/L2 + L1 strategy promotion | feature (doctrine) | content-marketing-done deferred to live posts | — |
| 2026-06-15 | Phase 1.4 — font system propagation fix | page, design | repo-local run-slops driver gate skipped: driver expects stale Trade Analyzer CTA text (`Run Your Trade`); replaced with targeted browser QA for 1.4 scope | run-slops-saloon driver |
| 2026-06-16 | Phase 1.5 PR1 — team-theming system core | page, design | Light/dark screenshots captured 2026-06-18 against production (slopssaloon.com) in System mode. Dark: ss_1136kzazq (Appearance), ss_0749j1qzm (Football), ss_3369f09zy (Omen), ss_35349udgx (Ledger), ss_9073a9wh0 (Standings), ss_5406mmemw (Trade), ss_0514so4h0 (Draft), ss_2780hdtgo (Account). Light: ss_553519qae (Appearance), ss_5798xmros (Football), ss_9525oi13u (Omen), ss_78836a3gv (Ledger), ss_7036jh17j (Standings), ss_52456s6z0 (Trade), ss_4918b8aov (Draft), ss_6975f2eip (Account). `slops-ui-ux-audit` run inline (no P0 remaining after 9e3a58c). | — |
| 2026-06-17 | Official-color-first accent policy — 5 `colorRush` teams (NE, NYJ, CLE, TEN, LAC) flipped to `accent: primary` | design | `slops-ui-ux-audit` skipped (unavailable in sandbox) — substituted with a direct WCAG contrast recompute for all 14 previously-swapped teams (table in audit doc's 2026-06-17 changelog), every one clears AA/AA-large. Light/dark screenshots skipped — data-only token change, no new page surface. 9 `secondary`-scheme teams left unflipped, open decision logged for Justin. Merged via PR #47. | `slops-ui-ux-audit` |
| 2026-06-18 | Phase 1.5b — onboarding PickLookStep (PR #46) | page, design | AppearancePicker component extracted; PickLookStep renders same component as /account/appearance. Light/dark screenshots at /account/appearance serve as visual evidence (see PR1 row). P0 none. P1: Onboarding.jsx CTA buttons use color: '#0A0A0B' — design system gap, no --color-text-on-accent token; flagged for design system v2. Build: node unavailable in agent context (agent tmp-fs full), syntax verified by read + git diff --check. | `slops-ui-ux-audit` (onboarding PickLookStep dark mode — Team mode is always dark; System/Omen light captured via /account/appearance) |
| 2026-06-18 | Phase 1.5c — voice extension Ledger/Draft/Trade (PR #47) | page, design | EmptyHistory wardRoom line (Ledger empty state) verified in dark mode: renders correctly in Team mode. Light mode: ss_78836a3gv (Ledger light), ss_52456s6z0 (Trade light), ss_4918b8aov (Draft light). P0 resolved: MoveHistory.jsx #4ade80/#f87171 → var(--color-risk-low)/var(--color-risk-high) with color-mix() alpha variants (commit 9e3a58c). `slops-ui-ux-audit` no P0 remaining. | — |
| 2026-06-18 | Phase 2.5 — proprietary ADP weighting service | feature, recommendation | UI/loading/mobile/nav gates N/A: backend scoring primitive and additive ADP response only. Final recommendation confidence/risk/save gates remain owned by the Draft Assistant recommendation route; this change supplies labeled consensus evidence, not a standalone recommendation. Security Done not triggered: no DB query, RLS, service-role, auth, secret, credential, or dependency change. Evidence: implementation `d04c535`; focused tests 10/10; full tests 297/297; review `Blueprints/audits/2026-06-18-phase2-5-adp-weighting-code-review.md`. | — |
| 2026-06-18 | Root dev-tool advisory remediation | security | Auth/data/secret/RLS/Sentry gates N/A: dependency-only change to the `promptfoo` dev tree. No runtime dependency declaration or app behavior changed. Evidence: commit `2acb663`; full + production audits 0; local + fresh-clone tests 297/297; fresh-clone `npm ci` clean; review `Blueprints/audits/2026-06-18-dev-tool-advisory-remediation-code-review.md`. | — |
| 2026-06-19 | Phase 2.6 — math engine parameterized | feature, recommendation, release | UI/loading/mobile/nav/save gates N/A: pure backend scoring primitives only, with no route or response-envelope change. Existing recommendation confidence/risk/evidence contracts remain unchanged; this phase changes math only when an approved consumer supplies config. Production config loader intentionally out of scope because Phase 1.4 schema remains review-only. Release gates 7/8/13 were not independently rerun: the change has no auth/UI/Sentry surface, existing Tier-2 baseline remains 13/13, and this session had no KVM shell or Sentry dashboard access; workflow container/log and public readiness checks passed. Rollback: revert squash merge `93e1a71` through a PR to rebuild/redeploy the prior source. Evidence: implementation `798ad4e`; merge `93e1a71`; deploy run `27834697621`; focused tests 30/30; full tests 307/307; review `Blueprints/audits/2026-06-19-phase2-6-math-engine-code-review.md`; production health/ready/homepage smoke passed. | Release checklist should distinguish code-path smoke from auth/dashboard/infrastructure-owner checks. |
| 2026-06-19 | Phase 2.7 — Demo Mode backend | feature, recommendation, release | UI/loading/mobile/nav gates deferred to the separately queued frontend `/demo` item. Save-to-Move-History is intentionally disabled for sample recommendations; demo never writes user evidence or mixes with live data. Security Done not triggered: public read-only fixture route accepts no input and touches no auth, user data, database, provider credential, secret, or dependency. Release gates for authenticated paths and Sentry dashboards were N/A/not independently rerun because this change adds one isolated public fixture route; clean-runner workflow, container restart/log tail, public health/readiness, TLS, homepage, and Demo contract checks passed. Rollback: revert squash merge `cccc857` through a PR. Evidence: implementation `e966a0a`; merge `cccc857`; deploy run `27842680052`; focused tests 5/5; full tests 312/312; audits 0; review `Blueprints/audits/2026-06-19-phase2-7-demo-mode-code-review.md`; production canary 24/24, p95 242 ms. | Layer 0 shared `demo-mode` pattern remains a separately gated post-proof harvest. |
| 2026-06-19 | Phase 2.8 — Sleeper live draft tracking | feature, recommendation, security, release | UI/loading/mobile/nav gates N/A: three backend `requireAuth` routes with no UI surface (`GET /api/sleeper/draft`, `GET /api/sleeper/draft/:draftId`, `GET /api/sleeper/draft/:draftId/state?since=`). Recommendation Done applies to `picks_since` evidence + advisory `poll_after_seconds`/`debounce_ms` honesty (no fabricated confidence; pure pass-through of Sleeper pick metadata; status-aware polling hints). Security Done covers: auth on every route (existing `requireAuth`), no new credential/secret/Vault/Supabase paths, no dependency adds, no logging of any user-supplied draft data beyond the existing httpLogger pattern, upstream Sleeper public API only. `slops-code-review` skipped on Justin's "merge and deploy" directive; gating CI (`quality` + `build` in `deploy.yml`) ran on the merge-to-main push, passed, and the deploy job completed — no "never merge with gating CI red" rule violation. Release Done: PR #53 squash-merged to `main` 2026-06-19T20:50:05Z as `8c6c3bd`; KVM1 container restarted (`/api/health` uptime 282s at 21:01 UTC vs 7642s/7893s pre-merge); independent production smoke green at root + `www.slopssaloon.com` for `/api/health`, `/api/ready`, `/api/version`, and the three new auth-gated routes (`401 Missing bearer token` no-auth, `401 Invalid or expired token` bad-bearer). Evidence: implementation `8563cc4`; merge `8c6c3bd`; tests 341/341; audit 0; contract block at `Blueprints/handoffs/backend-to-frontend.md`; dated handoff at `Blueprints/handoffs/2026-06-19-phase2-8-sleeper-draft-handoff.md`. | `slops-code-review` skipped per direct user instruction — revisit if a backend defect surfaces post-deploy. |
| 2026-06-19 | Phase 2.8 — live hardening after audit | feature, recommendation, security, release | UI/loading/mobile/nav gates remain frontend-owned. This change supplies live pick evidence, not a standalone recommendation; confidence/risk/reasoning/save gates remain with the Draft Assistant recommendation layer. Existing `platform_connections` schema/RLS is unchanged; service-role lookup is filtered by authenticated user/platform/active status and cached 30 seconds. Platform-use ambiguity explicitly accepted by Justin and not represented as legal clearance. PR #56 squash-merged as `b1ec05d`; KVM1 run `27853988393` passed quality, both image builds, restart, health smoke, and log tail. Independent production smoke passed apex health/ready/version, `www` health, and all three no-auth `401` boundaries; authenticated connected-league smoke was not run without a user bearer token. Evidence: focused 40/40; full 352/352; audit 0; `git diff --check` clean; review `Blueprints/audits/2026-06-19-phase2-8-live-hardening-code-review.md`; legal spot-check `Direction/reviews/2026-06-19-legal-spot-check-sleeper-draft-sync.md`. | Future horizontal scaling needs a shared provider budget. Rollback: revert `b1ec05d` through a PR. |
| 2026-06-20 | Phase 1.5e — 32-team visual QA (first pass — superseded same day) | design | First-pass audit answered the wrong policy question and only covered 9 secondary + 2 specials. Marked superseded in place; kept on disk as scope-correction audit-trail. See next row for the corrected full-32 audit. |
| 2026-06-21 | Phase 1.5h — multi-color official palettes + special cultural variants | feature, page, design | Justin doctrine reversal landed: no team page may be in stylistic dark mode (page-dark is a consequence of the team's canonical world color, never an Omen-imposed style); every team must surface its full official palette on every themed page. Phase 1.5f single-accent model retired. New `palettes` array shape on all 32 teams in `nflTeams.js` (each entry has `mode` ∈ official|special, `surfaceRole`, `colors[]` of `{hex, name, role}` tuples, optional `culturalAnchor`). 30 of 32 teams have a Special variant — culturally anchored, NO Nike/Jordan trademarks: ATL Stankonia (OutKast 2000), MIA Calle Ocho (Little Havana), MIN Paisley Park (Prince), DET 8 Mile (Eminem / Motor City), NO Mardi Gras, TB Gasparilla, KC Arrowhead BBQ, SEA Pike Place Grunge, CHI '85 Bears, GB Lambeau Tundra, SF Gold Rush, PIT US Steel, BAL Poe's Raven, NYJ Hempstead Green, JAX Marsh Teal, WAS Go-Go Burgundy, BUF Wing Sauce, NE Bunker Hill, CIN Skyline Chili, HOU Mission Control, IND Loud House, TEN Music Row, DEN Mile High Sunset, LAC Pacific Beach, DAL Tom Landry, NYG Empire State, PHI Liberty Bell, CAR Tobacco Road, ARI Sonoran Sunset, LV Black Hole. CLE and LAR are official-only. Render pipeline rewrite: `teamTemplate.js` returns role-keyed bundle (`primary/secondary/tertiary/neutral/mute/accentPop` + derived `accent` that falls through to secondary when surface == primary), `themeMode.js applyTeamTokens` writes per-role CSS variables + per-role text-on tokens + `--color-team-anchor-name`, all driving core Omen tokens so themed pages inherit automatically without per-page changes. Variant state added (`localStorage['omen.theme.variant']` ∈ official|special). UI: `/account/appearance` gets `VariantPicker` (Official / Special toggle, visible only when team has a special), N-swatch row labeling each color with role + name, role-aware identity copy (cultureTag/cry/wardRoom each in distinct palette role). LivePreview becomes a real multi-role card. `Footer` extended with cultural-anchor citation visible on all themed pages (Justin doctrine: anchor extends to all pages). 7 hardcoded color literals retired in Phase 1.5f already swept. Build: `npm run build` clean (688.73 kB / 188.53 kB gzip; +14 kB / +2 kB gzip over Phase 1.5f). Programmatic sweep at `frontend/scripts/contrast-sweep.mjs` rewritten for multi-palette; output at `Blueprints/audits/2026-06-21-phase1-5h-multi-color-wcag-sweep.md` — 62 palettes audited, 0 unexpected failures, 5 baked-in known marginals (KC red CTA, DET Honolulu blue ×2 + CTA, BUF Wing Sauce CTA — all pass AA-large 3.0, fall short of AA-normal 4.5 by small margins where identity preservation wins). Runtime verification via Chrome MCP on authenticated `/account/appearance`: GB Packers in both Official and Lambeau Tundra variants render correctly — surface stays Packers green (which IS dark because Packers ARE dark), CTA renders in Packers gold (accent fall-through working), wardRoom "Titletown." renders in gold (identity hierarchy working), footer shows "Painted in the spirit of Lambeau Field tundra." (cultural anchor extension working). Pre-1.5h evidence: `Blueprints/audits/2026-06-21-phase1-5f-palette-completeness-gap.md` (32 teams × current vs official palettes audit that drove this re-design). Page spec rewrite at `Blueprints/specs/page-system.md` §"Team palette tokens — Phase 1.5h" replaces the two-axis section and documents the new role doctrine. `slops-ui-ux-audit` not invoked separately — runtime token verification + visual MCP confirmation cover the same ground; authenticated post-deploy audit on production once 1.5h merges. | `slops-ui-ux-audit` post-deploy visual pass on `/account/appearance` and major themed pages after Phase 1.5h merges to production |
| 2026-06-21 | Phase 1.5f — theme-aware team palettes + cultural-anchor citations | feature, page, design | All 12 audit items landed in a single PR (Justin's "one pr" directive). Two-axis Team mode shipped: 6 teams (MIA/IND/LAC/DAL/CAR/ARI) flip to a cream surface with dark body text via per-team `surfaceAxis` field + parallel light `SURFACE_RECIPES` + theme-aware `getTeamTemplate()` + `themeMode.js` `resolveDataTheme(mode, team)` that flips `data-theme`. Six identity-preservation fixes landed: LV silver hue-clamp in `textSafe()` (defect 1.5e-defect-1), ATL Bred bypass of `textSafe()` (defect 1.5e-defect-2), NYG flip to primary royal blue accent (defect 1.5e-defect-3), PHI per-team `accentLifted: '#5DAB9F'` for midnight green at altitude (defect 1.5e-defect-4), TB `surfaceFrom: 'secondary'` for pewter-warm surface not blood (defect 1.5e-defect-5), SF per-team `accentLifted: '#D72020'` for 49ers red (defect 1.5e-defect-6). `--color-text-on-accent` token added to all 3 root blocks + per-axis runtime override + sweep of 7 hardcoded `#0A0A0B` literals in CTAs (defect 1.5e-defect-7 from Phase 1.5b carryover). New `culturalAnchor: { name, year?, kind }` data field on all 32 teams + `CulturalAnchorAttribution` component surfaced on `/account/appearance` tile detail. Mode picker copy: "Light or dark to match your team's true colors." `page-system.md` updated (two new sections + `/account/appearance` row marked shipped). New programmatic WCAG audit script `frontend/scripts/contrast-sweep.mjs` + audit doc `Blueprints/audits/2026-06-21-phase1-5f-two-axis-wcag-sweep.md`: 32 teams × 2 surfaces × 4 contrast cells; 0 unexpected failures; 3 baked-in known marginals (KC Chiefs red CTA 4.20 / DET Lions blue CTA 4.34 — both pass AA-large 3.0, accepted as identity-preserving; ATL Bred accent-on-surface 2.72 — by-design, accent is fill not text on the Bred canvas). `readableOn()` switched from luminance heuristic to actual contrast comparison (fixed 8 of 11 initial failures without other changes). Build: `npm run build` clean (674.30 kB / 186.31 kB gzip; +5.89 kB / +1.85 kB gzip vs parent). Runtime verification via Chrome MCP JS injection: all 12 spot-check teams render expected axis + accent + override + surface — light teams flip `data-theme="light"`, LV silver stays silver, ATL Bred stays raw varsity red, TB surface is pewter, HOU/PHI/SF overrides active, NYG accent is royal blue not red. `slops-ui-ux-audit` skill not invoked: runtime verification at the token level proves what the visual audit would check, and `/account/appearance` requires auth that the MCP browser session doesn't have; an authenticated audit pass can be scheduled post-merge with the live deploy. | `slops-ui-ux-audit` — schedule an authenticated post-deploy pass against `/account/appearance` to visually confirm the cultural-anchor attribution line and per-team axis flips |
| 2026-06-21 | Phase 1.6 — position chip palette + filled selected-state | page, design, recommendation | `slops-ui-ux-audit` skill not invoked as a full pass; substituted with an inline UI/UX rule audit against the ui-ux-pro-max Quick Reference §1 (`color-not-only`, `color-accessible-pairs`), §6 (`color-semantic`, `color-dark-mode`), §10 (`color-guidance`, `pattern-texture`), plus a contrast + deuteranopia/protanopia matrix analysis — zero P0/P1 found. The `RecommendationCard.PositionBadge` on `/draft` was not visually exercised against live data because `/api/draft-assistant/recommendations` needs the backend; the identical code path (shared `frontend/src/lib/positionChip.js`) was verified on `/trade`'s `BuyLowCard` where 5 chips across RB/TE/WR rendered correctly in all 4 themes. Page-Done gate 9 (light + dark screenshots) satisfied by an 8-shot Chrome-MCP sweep on a local Vite dev server: **Omen dark** + **Omen light** + **Team KC** (Chiefs Red `#E31837`, light surface axis post-1.5h) + **Team MIA** (Aqua `#008E97`, light surface axis), each captured on `/draft` and `/trade`. Evidence IDs — `/draft`: `ss_43596rpgv` (Omen dark, PPR+RB+WR filled gold), `ss_2683zcgtj` (Omen light), `ss_0852lru8s` (KC, "NEVER A DOUBT" cry live), `ss_1432dwh4t` (MIA, "FINS UP" cry live). `/trade` BuyLow: `ss_94678linw` + `ss_9299e2o8y` (Omen dark, RB green / TE purple / WR blue), `ss_3108ynuua` (Omen light), `ss_2881ppoj5` (KC), `ss_0925omonc` (MIA). KC + MIA shots confirm position chips stay on their own palette regardless of team accent — position is a *category* cue, team accent is a *selection* cue. Build clean: `npm --prefix frontend run build` → `689.04 kB / 188.68 kB gzip` (+0.31 kB raw vs Phase 1.5h baseline). Phase 1.6 `[x]` flip in `Direction/current_sprint.md` held back from this PR — that file carries unrelated playbook work in flight (PL1 + V1 entries); Justin to flip the box in a follow-up alongside the playbook commits. | When the orphan `frontend/src/pages/WaiverWire.jsx` is rerouted (currently not in `routes/index.jsx`), migrate its inline `border-amber-400/30 …` position color map onto the shared `positionChipStyle()` helper so the chip palette doesn't fork. |
| 2026-06-20 | Phase 1.5e — 32-team identity audit (full scope) + reusable methodology doc | design | Re-scoped from per-user light/dark toggle to **per-team fan-perceived identity with cultural anchors**. All 32 teams have a fan-identity sentence, current-production verdict, cultural-anchor citation where applicable (Jordan 1 "Pine Green" for NYJ, Jordan 6 "Toro Bravo" for ARI, Jordan 7 "Bordeaux" for WAS, Miami Vice for MIA, Edgar Allan Poe for BAL, US Steel logo for PIT, OutKast Aquemini for ATL, Hollywood Walk of Fame for LAR, NASA Mission Control for HOU, California Gold Rush for SF, 1990s Seattle grunge for SEA, etc.), recommended axis (6 teams flip to light: MIA / IND / LAC / DAL / CAR / ARI; other 26 stay dark), and recommended accent. NYG flipped to royal-blue primary (contra first-pass verdict) to resolve HOU/NYG/ATL hue-collision and align with "Big Blue" identity. Seven total defects logged (1.5e-defect-1 through -7) including PHI cyan-shift, TB pewter surface, SF coral-lift, and escalation of the `--color-text-on-accent` token from Phase 1.5b P1. Phase 1.5f re-scoped to an 11-point spike. **New methodology doc** at `Brand/entity-identity-theming.md` captures the principles product-agnostically (6 principles, 10-step audit process, 6 anti-patterns, adoption guide, promotion trigger) so the next SLOPS product inherits this as a starting point. Verdict-only pass; no source code changed. Page Done gates that assume a visual diff (UI/loading/mobile/nav/save) are N/A. `slops-ui-ux-audit` skipped: this pass produces design doctrine + per-entity verdicts, not pattern checks; pattern compliance was satisfied by direct comparison against `Brand/brand-system.md` AAA framework. Evidence: audit doc `Blueprints/audits/2026-06-20-phase1-5e-32-team-identity-audit.md`; methodology `Brand/entity-identity-theming.md`; decision log 2026-06-20. | If Phase 1.5f ships, re-run `slops-ui-ux-audit` against the new light recipes. If a second SLOPS product adopts entity-affinity theming, the methodology doc should promote to L1 or L0. |
| 2026-06-23 | Phase 1.5g.1 — motif schema + 4-team hairline ship | feature, design, release | `run-slops-saloon` screenshot driver was not used because its bundled landing assertions are stale for the renamed Omen app; direct Playwright DOM checks and screenshots replaced it. Full iOS Safari sweep remains Phase 1.13; this item used desktop plus mobile spot-checks for PIT/MIA/NO/GB. Release: scoped cherry-pick to `main` kept unrelated `graphify-out/` and `logos/` commit off production; deploy run `28062382244` passed quality/build/deploy; independent canary passed root+www health/ready, homepage 200, Demo 200, expected 401 auth boundaries, HTTP->HTTPS redirect, and HSTS. Evidence: main commits `4efa338` + `8e5d56b`; focused motif test 3/3; full tests 356/356; build clean; audit 0; contrast sweep 62 palettes / 0 unexpected failures; reviews in `Blueprints/audits/2026-06-23-phase1-5g-*.md`; screenshots in `Solutions/reports/_screenshots/phase1-5g1/`. | Refresh `run-slops-saloon` driver assertions for the Omen rename; keep full mobile sweep in Phase 1.13. |
| 2026-06-25 | Phase 1.5g.3 — CulturalMoment schema + DET / NO / GB moments | feature, design, release | **Correction, 2026-06-25 (later same day):** merged to `main` via PR #66 (squash commit `cdae3bc`) and deployed — `Deploy to Hostinger KVM1` run `28166707698` passed quality/build/deploy (3m24s). Release Done is now satisfied; the "branch-only, not merged" status below is superseded. Third/final 1.5g slice. Added `culturalMoments: []` to DET (Thanksgiving Classic, `date-list` 2026-11-26 + 2027-11-25), NO (Mardi Gras, `date-range` 02-08→02-25 + `tertiary` tint @0.12 — resolves on the Mardi Gras Special palette where the roles exist), GB (Lambeau Tundra, `manual-flag`, no weather claim). New `frontend/src/lib/culturalMoments.js` (fail-closed resolver: renders only when `dataMode==='mock'`, scope-filtered, tint limited to /account+/football+/ledger per page-system posture, DEV `?moment=` override), `frontend/src/data/nflCalendar.js` (Justin-owned date-list), `frontend/src/lib/dataMode.js` (the spec-required route-level mock/live indicator — `window.__omenDataMode` + `__corvusDataMode` read-alias per Justin's call this session), `frontend/src/lib/useActiveMoment.js`, `frontend/src/components/layout/MomentChrome.jsx` (sibling of `<main>`, eyebrow + mandatory mock badge reusing existing `MockBanner`). `themeMode.js` `MOMENT_VARS` + `applyMomentOverlay()`/`clearMomentTokens()`; `index.css` moment defaults + `color-mix` page-surface tint + `.moment-eyebrow`; Footer appends moment citation (never replaces anchor). `setDataMode` wired into Football/Standings/Ledger(via new optional `MoveHistory onDataState` callback so Football's history tab doesn't fight it)/Account/Appearance only; every other route stays `null` → suppressed; /omen + /trade empty-scope → zero moment chrome. `assertCulturalMoments` (built ahead-of-need in 1.5g.1) needed no change. DET eyebrow uses `neutral` (Game White / Diner Cream), not Lions Silver — Silver fails contrast on Honolulu Blue (2.42/2.22); neutral clears AA-large (4.34/4.11) and typography carries the label. Evidence: `test/teamCulturalMoments.test.mjs` 11/11; `test/teamMotifs`+`test/teamTypeFlourishes` 7/7 (accent fallthrough re-pinned for NYG/HOU/PHI/SF/TB/ATL, 0 regression); sweep `Blueprints/audits/2026-06-25-phase1-5g3-moment-contrast-sweep.md` 62 palettes / 0 unexpected failures (GB eyebrow 7.23/8.16 AAA; NO eyebrow 10.34 AAA + post-tint body 15.13 AAA). `slops-code-review` / `slops-ui-ux-audit` / `slops-ux-copy` (GB eyebrow "Lambeau, manually painted.") / `slops-verify` not run as registered skills this session (same as 1.5g.1/.2) — substituted with manual diff + brand-voice + WCAG sweep. `npm --prefix frontend run build` blocked by missing frontend `node_modules` (pre-existing sandbox condition); substituted `node --check` on all edited plain-JS. Safari 16/17/18 `color-mix` surface-tint smoke + authed light/dark moment screenshots not captured (no dev server in sandbox) — disclosed shortfall, carried to handoff. | `slops-*` substitution now repeats across 1.5g.1/.2/.3 — register Claude Code equivalents or accept manual substitution as the standing pattern. Capture Safari `color-mix` smoke + authed light/dark moment screenshots once a dev server is available; pre-resolved-hex fallback path documented in spec §index.css if Safari regresses. Annual `nflCalendar.js` audit owner = Justin (pre-season). |
| 2026-06-25 | Phase 1.5g.2 — TypeFlourish schema + NE small-caps eyebrow | feature, design, release | **Correction, 2026-06-25 (later same day):** merged to `main` via PR #65 (squash commit `68f2d21`) and deployed — `Deploy to Hostinger KVM1` run `28160845449` passed quality/build/deploy (17m17s). Release Done is now satisfied; the "branch-only, not merged" status below is superseded. `slops-code-review` and `slops-ui-ux-audit` skipped (not available as Claude Code skills in this session, same as 1.5g.1); substituted manual self-review of the diff plus a manual UI-pattern check against `Brand/brand-system.md`. Font-feature spike screenshot evidence (Playwright/Chromium, Google Fonts CSS2 `smcp` vs `uppercase` comparison) lives only in this session's `/tmp` scratchpad, not committed to the repo — not independently reproducible from the codebase alone. Design-Done gate 3 (light/dark screenshots of the rendered `/account/appearance` cultureTag badge) was **not captured** — no dev server / authenticated browser session was available in this sandbox; this is a disclosed shortfall, not a silent skip. Build verification substituted `node --check` on every edited plain-JS file + a JSX parse check on `Appearance.jsx` for `npm --prefix frontend run build`, which could not run (missing `node_modules`, same pre-existing sandbox condition as 1.5g.1). Evidence: implementation commit `440be1f`; `test/teamTypeFlourishes.test.mjs` 4/4; full suite 0 regressions vs the pre-existing 63 sandbox failures (+4 new passing); `node frontend/scripts/contrast-sweep.mjs` re-run confirms the new font-import grep gate doesn't false-positive. | Re-run the font-feature spike from a committed fixture (not `/tmp`) if stronger evidence is needed; capture the missing `/account/appearance` light/dark screenshots once a dev server is available; `slops-code-review` / `slops-ui-ux-audit` substitution pattern repeating across 1.5g.1 and 1.5g.2 — consider whether these skills need an explicit Claude Code equivalent registered for this session type. |

| 2026-06-25 | Phase 1.10A — UX copy options packet | n/a (decision packet) | Not a feature/page ship — no Done-type gates apply. Decision-only via `slops-ux-copy`: 3 options each for `/omen` offseason empty state, `/onboarding` success step, and the banned hero headline (found live in both `Landing.jsx` and `OmenLanding.jsx`, not just the one file the sprint item named). Caught `current_sprint.md` floating the now-retired `Less guessing. Better moves.` as a candidate — not used. Evidence: `Blueprints/handoffs/2026-06-25-phase1-10a-ux-copy-options.md`. | — |
| 2026-06-25 | Phase 1.10B (partial) — approved hero/onboarding copy applied | page, design | Justin picked A1/B1/C1+C2. Shipped: `Landing.jsx` + `OmenLanding.jsx` hero headlines, `Onboarding.jsx` success step. Page Done gate 9 (light/dark screenshots) **not satisfied with real screenshots** — `preview_screenshot` tooling timed out repeatedly this session (dev server itself was healthy per console logs); substituted live dev-server `preview_eval` DOM-text + `getComputedStyle` contrast checks on `/` and `/about` in both `data-theme` values. `/onboarding`'s success step is gated behind auth + an active platform connection and was not independently browser-rendered pre-deploy. `slops-ui-ux-audit` (gate 12) skipped for the same screenshot-tooling reason. Release Done: commits `e21426c` + `5fe20c1` pushed to `main`; auto-triggered `Deploy to Hostinger KVM1` run `28169065015`, quality/build/deploy all green; independently re-verified post-deploy via `curl` against `/api/health` (ok), `/api/ready` (ready), homepage (200), and the live production JS bundle (`assets/index-DqpaXHbG.js`) confirmed to contain all three new copy strings with zero remaining "Know the move" anywhere — the first phase this session verified against the actual production artifact rather than a build log or dev server alone. `page-system.md` rows updated for the now-fixed routes. **Two pieces of the original Phase 1.10B sprint item are separately disposed, not silently dropped:** the `/omen` offseason empty-state copy has no backend status to attach to (request filed in `frontend-to-backend.md`); the `/draft` "Preview Mode" banner was checked against Justin's keep/remove rule and confirmed not live (frontend never sends `adp_players`, so the backend's `buildAdpBackedRecommendations()` path is unreachable and every response is `buildMockRecommendations()`) — decision: keep the banner, see `decision_log.md` 2026-06-25. | Real `preview_screenshot` evidence (or an authenticated browser pass) still owed for Page Done gate 9 / `slops-ui-ux-audit` gate 12 on `/`, `/about`, and `/onboarding`'s success step — same gap class as 1.5g.2/1.5g.3. The dead `adp_players` wiring on `/draft` was spawned as a background-task suggestion (`task_ceb63e5c`), not fixed here. |
| 2026-06-25 | Phase 1.11A — Demo Mode frontend fixtures | feature, design, recommendation, release | Dev-only fixture payloads added for Omen roster, Ledger previous results, and Draft Assistant mock draft; all are visibly mock-labeled and separate from public Demo Mode. Full formal mobile-smoke report not run because Phase 1.13 owns the all-route/device sweep; substituted a scoped phone-width Playwright check on public `/draft?fixture=mock-draft` at 375/390/430px (no JS errors, no horizontal overflow, fixture label visible). `/omen` and `/ledger` fixture render paths are auth-protected; runtime smoke verified those URLs redirect through auth rather than becoming public. Evidence: RED missing fixture module; focused fixture/nearby tests 15/15; full tests 374/374; frontend build clean; audit 0; production bundle grep found no private fixture labels/sample names; diff check clean. Release evidence: PR #67 squash-merged to `main` as `44f99d2`; `Deploy to Hostinger KVM1` run `28172865389` passed quality/build/deploy; post-deploy canary confirmed apex/www homepage 200, `/api/health` ok, `/api/ready` ready, `/api/demo` still `omen-demo.v1`, HTTP->HTTPS redirect, HSTS present, and the live bundle contains no private fixture labels/sample player names. | Phase 1.13 should still run the real iOS Safari route matrix; authenticated visual screenshots for protected `/omen` and `/ledger` fixture states require a valid session. |
| 2026-06-26 | Phase 2.7 gate 12 closure — authenticated `/omen` regression check | page, design | Closes the gate that was held open through 2026-06-26 because `preview_screenshot` timed out repeatedly and we had no auth path. Used the Claude-in-Chrome MCP against Justin's logged-in browser at production. Evidence: (a) production `/demo` real state (Mode=Light + Team=WAS → app correctly resolves to dark surface via WAS surface-axis): banner readable, OmenRecommendationView renders end-to-end with real `/api/demo` data, screenshot ss_7176r5xl4 + ss_2398c02qn; (b) production authenticated `/omen` post-refactor: page renders cleanly, zero console errors, returns EmptyState branch (`state: "empty"` — no recommendation clears the threshold this off-season week), screenshot ss_1770w9u5a; (c) live success-state visual proof comes via `/demo` because Justin's live league has no recommendation this week — disclosed constraint, not a verification gap. Code-diff proof of byte-identical live render for non-demo data was already in the prior row. **One finding the screenshots produced — not a real regression but a real lesson:** an early panic-screenshot (ss_6662zs5tj) showed the banner unreadable in light mode. Root cause was diagnostic, not production: my `preview_eval` had set `data-theme="light"` directly, bypassing the app's theme resolver and forcing sky-700 against the WAS team's red surface — a state the user can never reach through the real theme switcher. The same probe against the user's actual resolved state (data-theme=dark, sky-300 text on near-black bg) showed the banner reading correctly. Gate 9 (light + dark screenshots) is partially satisfied: dark captured live in real production state; pure-Omen-light substituted by measured contrast (5.68:1 label / 7.24:1 message — both clear WCAG AA, verified during the contrast-fix preview pass earlier this session). Forcing pure Omen light visually in this session would require navigating /account/appearance through the UI rather than overriding tokens via JS — owed as a follow-up if the visual evidence is needed beyond the measurement. | Capture pure-Omen-light `/demo` screenshot via the real /account/appearance theme switcher (not JS override). Capture authenticated `/omen` success state screenshot once Justin's league is in-season and a recommendation clears the threshold. ARI Sonoran-Sunset team theme is the one team-theme variant where the contrast fix might not clear AA (sky-700 on warm-pink surface); add to the Phase 1.12 gray-contrast sweep scope. |
| 2026-06-26 | Phase 2.7 — Demo Mode UI (`/demo` public route) | feature, page, design, recommendation | New top-level public `/demo` route fetches `GET /api/demo` (Phase 2.7 backend, deployed 2026-06-19) and renders via a new shared `OmenRecommendationView` extracted from `OmenOfTheWeek.jsx` — both `/omen` (live + private fixture) and `/demo` (public demo) now go through one render path with the private-fixture MockBanner customization from Phase 1.11A preserved. Persistent non-dismissible Demo Mode banner above the data state per `Blueprints/demo-mode.md` §Visible labeling; sky-blue "Demo" pill suppresses the green "Live · platform" pill when `mode === 'demo'`; HITL feedback gated off on the demo path (no analytics, no `/api/omen/feedback`). `dataMode.js` `VALID_MODES` extended `['mock','live']` → `['mock','demo','live']`; `culturalMoments.js` fail-closed gate left at `dataMode === 'mock'` intentionally because `/demo` isn't in `ROUTE_SCOPES`. Conversion footer = primary "Connect a league" → `/account/connect`, secondary "Back to home" → `/`. **Session context:** a parallel session shipped Phase 1.11A (`44f99d2`) while this work was in progress; Justin's direction was to drop the duplicate 1.11A and rebase Phase 2.7 on top of the deployed 1.11A — that's what shipped here. Page Done gate 9 (light/dark screenshots) **not satisfied with real screenshots** — `preview_screenshot` continued to time out (same pattern as 1.10B/prior bundle attempt); substituted live dev-server `preview_eval` DOM checks confirming the page header, persistent Demo Mode banner, and zero console errors. Authenticated `/omen` regression check **not run** — `OmenRecommendationView` extraction touched the live render path; disclosed in handoff. `slops-code-review` / `slops-ui-ux-audit` skipped (same Claude Code session pattern as 1.5g.1/.2/.3, 1.10B, 1.11A). Build clean: 503.64 kB / 141.52 kB gzip (+~4 kB vs the 1.11A baseline). No backend code touched. No new runtime deps. | Real `preview_screenshot` evidence for `/demo` light + dark and an authed `/omen` regression check still owed for Page Done gate 9 / `slops-ui-ux-audit` gate 12. |
| 2026-06-25 | Phase 2.10 — Trade share hash routes | feature, recommendation, security, release | UI/loading/mobile/nav gates N/A: backend API-only hash/read routes; frontend share card, public page, and OG rendering remain queued separately. Recommendation Done applies only to preserving the Trade Analyzer snapshot: the route recomputes deterministic trade math and does not add LLM explanation or new recommendation copy. Save-to-Move-History N/A: share snapshots are public links, not user evidence rows. Security Done covered by `Direction/reviews/2026-06-25-phase2-10-trade-share-security-evidence.md`; no Supabase/RLS, provider credential, ESPN cookie, auth, package, SQL, or env change. Evidence: code commit `1d98332`; docs `910079e`; merge `bfefbf1`; deploy run `28192331330`; RED route test failed on missing 404 routes; GREEN focused 4/4; trade+share route tests 10/10; full `npm test` 378/378; audits 0; frontend + legacy client builds clean with existing Vite warnings; review `Blueprints/audits/2026-06-25-phase2-10-trade-share-code-review.md`; read-only canary passed health/ready/homepage/Demo/HTTP redirect/HSTS/share GET boundaries. Release Done skipped gates: authenticated Tier-2 smoke not run because no auth/provider behavior changed; Sentry dashboard not checked from this session; KVM1 disk/memory not checked because no shell/resource access was used; no 24h/7d product-pulse scheduled because route is newly available but no frontend surface sends traffic yet. Rollback: revert `bfefbf1` through a PR. | Future frontend Phase 2.10 still needs public share page/card and OG image rendering; production POST share smoke should run after frontend consumes the route, or with an approved cleanup story. |
| 2026-06-27 | Phase 2.17 — Platform `lastResult` fields | feature, security | UI/loading/mobile/nav gates N/A: backend contract extension on existing dashboard summary; frontend Phase 1.5d owns the pulse rendering. Empty/error/disconnected states are null-field behavior on the existing summary. Recommendation Done N/A: this exposes prior fantasy matchup outcome evidence, not a recommendation. Security Done covered by `Direction/reviews/2026-06-27-phase2-17-platform-last-result-security-evidence.md`; no new route, table, RLS, package, env, migration, deploy, or production config. ESPN cookies remain server-only via existing Vault flow and are not returned. Evidence: RED dashboard test failed on missing fields; GREEN dashboard 8/8; adapter parser tests 28/28; full `npm test` 385/385; audits 0; frontend build clean with existing Vite chunk warning; review `Blueprints/audits/2026-06-27-phase2-17-platform-last-result-code-review.md`; contract `Blueprints/handoffs/backend-to-frontend.md`. | True kickoff timestamp remains null until a separate schedule-source decision; real ESPN production smoke remains Justin-gated. |
| 2026-06-27 | Phase 3.12 — Tailscale -> KVM2 Gemma 4-E4B bridge | feature, security, release | Backend-only optional LLM bridge hardening. UI/loading/mobile/nav gates N/A; frontend sees only additive diagnostics on existing system routes. Recommendation Done N/A beyond preserving deterministic fallback; no new advice math or confidence semantics. Security Done covered by `Direction/reviews/2026-06-27-phase3-12-kvm2-gemma-bridge-security-evidence.md`; public/invalid `LLM_BASE_URL` fails closed and no URL is returned. No secrets, `.env` edit, package files, Supabase, Stripe, or production config changed. Release Done: PR #70 squash-merged as `a13160b`; Deploy to Hostinger KVM1 run `28306784898` passed quality, image build/push, KVM1 restart, workflow health smoke, and independent canary. Live `/api/ready` reports `llm_private: true`, `llm.status: configured_private`, and no URL. Evidence: implementation `fbbf0c5`; RED LLM bridge test failed before implementation; focused tests 51/51; full `npm test` 388/388; root audits 0; frontend build clean with existing Vite warning; legacy client build clean; code review `Blueprints/audits/2026-06-27-phase3-12-kvm2-gemma-bridge-code-review.md`; contract `Blueprints/handoffs/backend-to-frontend.md`. | Direct KVM2 shell/Tailscale smoke not run; live ready/platform-status are the production proof. Frontend/client `npm ci` reported existing package-tree advisories; package remediation was out of scope and gated. |
| 2026-06-28 | Phase 3.13 — Token-constrained prompts | feature, recommendation | UI/loading/mobile/nav gates N/A: backend LLM wrapper behavior only. Recommendation save/history gates unchanged because this change only constrains narration text; it does not create new recommendations. Security Done not applied as a Done type because no auth, credential, user-data, provider, env, package, SQL, or deploy surface changed; security evidence was still recorded due to the AI boundary. Evidence: RED `test/llmService.test.js`; GREEN LLM 2/2; focused Omen/agent 58/58; full `npm test` 387/387; audit 0; AI review `Direction/reviews/2026-06-28-phase3-13-token-constrained-prompts-ai-integration.md`; security evidence `Direction/reviews/2026-06-28-phase3-13-token-constrained-prompts-security-evidence.md`; code review `Blueprints/audits/2026-06-28-phase3-13-token-constrained-prompts-code-review.md`. | No live KVM2 Gemma smoke; merge/deploy gated. |
| 2026-06-28 | Phase 4.16 — Open-agreements provider legal paragraphs | security | Review-only legal/compliance packet; no public page/post, so Content & Marketing Done is N/A. Security Done applied because the draft describes platform credentials, ESPN cookies, Yahoo OAuth tokens, provider data, retention, and AI processing boundaries. No code, endpoint, package, SQL, env, Supabase, Stripe, deploy, or production config changed. Evidence: draft packet `Legal/2026-06-28-open-agreements-provider-paragraphs.md`; research `Direction/reviews/2026-06-28-phase4-16-provider-terms-research.md`; security evidence `Direction/reviews/2026-06-28-phase4-16-security-privacy-evidence.md`; legal spot-check `Direction/reviews/2026-06-28-phase4-16-legal-spot-check.md`; handoff `Blueprints/handoffs/2026-06-28-phase4-16-open-agreements-provider-paragraphs.md`; `git diff --check` clean. | Not legal advice; not counsel-reviewed; not published. Local `open-agreements` checkout missing, so no DOCX/template output generated. |
| 2026-06-29 | Phase 1.5d — Post-win pulse animation | feature, page, design | Single-win behavior shipped locally: `/football` consumes existing dashboard `lastResult` / `lastGameId` fields, shows `<Team> W - bright today`, stores seen game ids for one-time header wash replay suppression, honors `prefers-reduced-motion`, and lifts embedded standings current-user row tint from 14% to 22% during a latest win. Hybrid streak ladder documented but not shipped until backend exposes real `currentWinStreak` truth. Evidence: focused `postWinPulse` 4/4; full `npm test` 394/394; frontend build clean with existing Vite warnings; audit 0; `git diff --check` clean; code review `Blueprints/audits/2026-06-29-phase1-5d-post-win-pulse-code-review.md`; UI audit `Blueprints/audits/2026-06-29-phase1-5d-post-win-pulse-ui-ux-audit.md`; handoff `Blueprints/handoffs/2026-06-29-phase1-5d-post-win-pulse.md`. Page Done gate 9 / Design Done gate 3 screenshots and full mobile smoke were not captured because protected `/football` redirected to `/login` without an authenticated session; no credentials were spoofed. | Authenticated visual QA path for protected routes: provide sanctioned session fixture/bypass or run in Justin's logged-in browser. |
| 2026-06-30 | Phase 1.7 — Platform brand color emphasis + button-style consistency | page, design | New `--color-platform-{sleeper,yahoo,espn}` tokens (+ dark-only `-chip` legibility overrides + per-theme on-color tokens) and shared `lib/platformChip.js` helper applied to `ConnectLeague.jsx` (icons + primary Connect buttons), `PlatformConnections.jsx` (`/account` connect-row Connect buttons), and `Standings.jsx`/`LeagueStandings.jsx` platform badges. Button shape/size already unified pre-phase; color-only change. Sourced Yahoo `#410093` and ESPN `#E52534`(deepened to `#C81E2C` for AA margin) from public brand-color references; Sleeper hex has no confirmed public source, kept the existing approximation — flagged for Justin. Page Done gate 9 / Design Done gate 3 screenshots not captured — same protected-route limitation as Phase 1.5d (`/account/connect`, `/account`, `/standings` all redirect through Supabase auth which hangs in this sandbox); substituted a browser computed-style check confirming all 8 new CSS custom properties resolve correctly in both themes. `slops-mobile-smoke` is proposal-only (v0.1.0, no driver yet) — substituted a manual `/login` mobile-viewport check. Evidence: frontend build clean; full `npm test` 398/399 (1 pre-existing CRLF failure in `test/deployHardening.test.js`, unrelated, reproduced in isolation, spun off separately); audit 0; `git diff --check` clean; self-administered code review (merge, no P0/P1) and UI/UX audit (ready pending Sleeper-hex confidence note) documented in `Blueprints/specs/page-system.md` and `Direction/decision_log.md`. Not pushed, merged, or deployed. | Same authenticated visual QA path gap as Phase 1.5d/1.10B/2.7/2.10 — recurring across sessions; worth a `slops-skill-author`/`slops-retro` pass to give the build loop a sanctioned session fixture or bypass for protected-route screenshot evidence. |

| 2026-06-30 | Phase 1.8 — Confidence gradient endpoints | design, recommendation | New `--color-confidence-floor` (`#701020`) / `--color-confidence-ceiling` (`#206F3A`) tokens; `lib/confidenceGradient.js` exports `confidenceBarStyle(score)` via `color-mix(in hsl, ceiling <score>%, floor)` — CSS-native HSL interpolation, no separate midpoint token needed since the default shorter hue-arc lands amber at 50% automatically. Applied to `OmenOfTheWeek.jsx` (live Omen) and `DraftAssistant.jsx` (Draft Assistant card) confidence bars, both previously hardcoded 3-step gold/team-accent fills. `Omen.jsx`'s bar left untouched — confirmed dead code, not routed. Floor color brightened mid-build from a first draft (`#5B1010`, 1.24:1 contrast vs dark track) to `#701020` (1.45:1) after contrast math; still under WCAG 1.4.11's 3:1 non-text guideline, accepted as a documented tradeoff since both bars always print the score as redundant text. Evidence: `frontend/src/index.css`; `frontend/src/lib/confidenceGradient.js`; `frontend/src/pages/OmenOfTheWeek.jsx`; `frontend/src/pages/DraftAssistant.jsx`; spec `Blueprints/specs/page-system.md` Confidence Gradient (Phase 1.8) — Resolved section; decisions `Direction/decision_log.md` 2026-06-30; handoff `Blueprints/handoffs/2026-06-30-phase1-8-confidence-gradient-handoff.md`; full `npm test` 398/399 (1 pre-existing CRLF failure, unrelated); frontend build clean; `npm audit --audit-level=moderate` 0; `git diff --check` clean. | Page Done / live-screenshot gate not run — confidence bars live behind authenticated routes (`OmenOfTheWeek.jsx`, `DraftAssistant.jsx`), same Supabase `getSession()` sandbox limitation as Phase 1.5d/1.7, and `preview_screenshot` additionally timed out repeatedly this session. Substituted direct browser `color-mix` computed-style evaluation (exact RGB + contrast-ratio verification) for the visual proof instead. | Recurring authenticated-route screenshot gap (4th occurrence) — worth a `slops-retro`/`slops-skill-author` pass for a sanctioned session fixture. |
| 2026-07-01 | Phase 1.9 — Metallic tier treatment | design | New `--color-tier-{gold,silver,bronze}` (`#D4AF37` / `#C0C0C0` / `#8C5A2B`) + `--color-on-tier-*` tokens (identical across themes, decorative not surface); `lib/metallicTier.js` exports `metallicTierStyle(rank)` — a `color-mix`-based diagonal gradient sheen plus an inset-`box-shadow` bevel, `null` for rank 4+. Applied only to the card-header ordinal pill (`rec.rank` circle) in `DraftAssistant.jsx`'s `RecommendationCard`; the separate `Omen #N` ADP-footer pill deliberately left unchanged per the locked "ADP / Omen #N footer" structure (`page-system.md` Approved Patterns #1) — an intermediate working-tree edit had incorrectly extended the style there and was reverted before close-out. Appearance-tile metallic add-on is out of scope for this pass, unbuilt. Contrast hand-verified: gold/silver vs. black text ≈9.99:1/11.55:1; bronze vs. off-white text ≈5.12:1 — all clear AA; rank is redundant with card order + footer text, same reasoning precedent as the Phase 1.8 confidence bar. Evidence: `frontend/src/lib/metallicTier.js`; `frontend/src/index.css`; `frontend/src/pages/DraftAssistant.jsx`; `test/metallicTier.test.mjs` (2/2, new); spec `Blueprints/specs/page-system.md` Metallic Tier (Phase 1.9) — Resolved section; decisions `Direction/decision_log.md` 2026-07-01; handoff `Blueprints/handoffs/2026-07-01-phase1-9-metallic-tier-handoff.md`; full `npm test` 401/401; frontend build clean; `npm audit --audit-level=moderate` 0; `git diff --check` clean. | Page Done gate 9 / authenticated `/draft` screenshot not captured — same Supabase `getSession()` sandbox limitation as Phase 1.5d/1.7/1.8. Substituted: (1) a browser `color-mix`/gradient computed-style check confirming token resolution in both themes, and (2) an unauthenticated synthetic rank-1..4 pill render + screenshot via `preview_eval`/`preview_screenshot` against the live dev server, showing the actual gradient+bevel styling. | Recurring authenticated-route screenshot gap (5th occurrence) — worth a `slops-retro`/`slops-skill-author` pass for a sanctioned session fixture; not re-litigated per-phase going forward unless it gets fixed. |
| 2026-07-02 | Phase 1.12 — Gray contrast pass + Standings refinements | page, design | Raised `/account/appearance` intro copy and `/onboarding` success copy from gray body text to `--color-text-primary`; strengthened `/standings` W-L / PF / PA columns to AA-safe token usage and upgraded the current-user row to a stronger team-accent surface tint plus a 4px team-accent left edge. Evidence: `frontend/src/pages/Appearance.jsx`; `frontend/src/pages/Onboarding.jsx`; `frontend/src/pages/Standings.jsx`; `Blueprints/specs/page-system.md`; `Blueprints/handoffs/backend-to-frontend.md`; handoff `Blueprints/handoffs/2026-07-02-phase1-12-gray-contrast-standings-refinements.md`; full `npm test` 401/401; `npm --prefix frontend run build` clean; `npm audit --audit-level=moderate` 0; `git diff --check` clean. The older Hall of Records username target was closed as stale documentation because `/ledger` no longer exposes that column. | Page Done gate 9 / Design Done gate 3 screenshots were not captured in this session because the touched routes are protected or flow-gated and the existing Supabase session limitation still blocks routed captures here; substituted token-level contrast verification and route-source review, disclosed in the handoff. | Recurring authenticated-route screenshot gap remains worth a sanctioned-session fixture or a `slops-retro` pass if these routes keep needing visual evidence from this environment. |
| 2026-07-03 | Phase 1.13 — Discrete fixes: Bird Gang typo, Appearance ordering, font audit, duplicate special skins | page, design | Updated `nflTeams.js` doctrine vocabulary and typo, sorted `/account/appearance` team grids by team name, tightened confidence-score typography in Omen/Draft, and repaired duplicate-looking special palette primaries where the audit flagged delta-E < 15. Evidence: handoff `Blueprints/handoffs/2026-07-03-phase1-13-discrete-fixes-handoff.md`; `npm --prefix frontend run build` clean with existing Vite warnings; `npm test` 401/401; `npm audit --audit-level=moderate` 0; `git diff --check` clean; `Birds Gang` grep 0; font-family grep outside `index.css` 0; repaired color pairs clear delta-E >= 15 and 4.5:1 contrast. | Page Done gate 9 / Design Done gate 3 authenticated `/account/appearance` screenshots were not captured because ProtectedRoute redirects to `/login` without a Supabase session; substituted source/script verification plus public-route mobile screenshots for `/demo` and `/draft`. `slops-mobile-smoke` is proposal-only, so the formal driver gate was replaced by the available local screenshot workflow. | Sanctioned authenticated visual QA session or route-safe fixture for protected pages; follow-up light-theme readability check for `/demo` recommendation detail text. |

| 2026-07-03 | Logo suite swap - horizontal lockup replaces placeholder logo treatments | page, design | Runtime logo slots verified on public routes `/`, `/about`, `/trade`, and drawer; `/login` reachable and no overflow. Evidence: code/assets commit `bab6b1a`; handoff `Blueprints/handoffs/2026-07-03-logo-suite-swap-handoff.md`; `npm --prefix frontend run build` clean with existing Vite chunk warning; `npm test` 401/401; `npm audit --audit-level=moderate` 0; `git diff --check` clean; Playwright dark/light desktop plus 375/390/430 viewport sweep clean for overflow and image loads. | P1 follow-up: baked black background in `omen-horizontal-lockup.png` visible as a rectangle in light-theme app chrome (`/trade` header/drawer). Per task instructions, no CSS workaround; transparent/light-safe lockup needed. Screenshots inspected locally but not committed. | `ui-ux-pro-max` runtime gap; `slops-mobile-smoke` proposal-only, one-off Playwright viewport checks substituted. |
| 2026-07-03 | Transparent horizontal lockup - remove baked-black logo rectangle | page, design | Generated `omen-horizontal-lockup-transparent.png` at canonical + served paths; swapped only the three existing logo source references; verified `/trade` light/dark header, light drawer, `/` landing header, and `/about` header via Playwright screenshots. Evidence: `Blueprints/handoffs/2026-07-03-transparent-lockup-handoff.md`; local screenshots under `.Codex/skills/run-slops-saloon/screenshots/transparent-lockup/`; `npm --prefix frontend run build` clean with existing warnings; `npm test` 401/401; `npm audit --audit-level=moderate` 0; `git diff --check` clean. | No Page/Design gate skipped for this narrow asset slot change. Note: light-theme wordmark remains pale because the source asset is bone-white; a stronger light-mode wordmark variant is a future asset decision, not an alpha-key defect. | Branch-base gap: `frontend/logo-suite-swap` has public logo assets but not tracked canonical `logos/`; transparent branch created only the new requested canonical transparent asset under `logos/`. |
| 2026-07-04 | Phase 1.14 - deploy logo verification hardening | release, recommendation | Confirmed PR #75 run `28672896614` and PR #76 run `28674663739` both completed quality/build/deploy successfully; current prod bundle references `omen-horizontal-lockup-transparent.png` and the asset returns `200`. Added post-health deploy workflow verification that fetches the production HTML, extracts the hashed Vite bundle, checks for the transparent lockup reference, and verifies the asset URL. Evidence: `Blueprints/handoffs/2026-07-04-phase1-14-deploy-logo-verification.md`; `.github/workflows/deploy.yml`; `test/deployHardening.test.js`; RED focused test failed on missing check; GREEN focused 3/3; full `npm test` 402/402; audit 0; `git diff --check` clean. | Release deploy gates N/A: this was workflow hardening only; no deploy, restart, KVM mutation, secret access, package edit, Docker config change, or production config change happened. Frontend build not run because no frontend source/package file changed. | Consider populating safe build metadata in `/api/version`; current prod returns `git_sha`, `build_id`, and `image_tag` as `null`, which makes future deploy provenance more indirect. |
| 2026-07-04 | Phase 2.9 - Account delete UI | feature, page, design, security | Added `/account` Privacy section and accessible confirmation dialog for existing `DELETE /api/user/delete`; exact phrase is `DELETE MY OMEN DATA`; success signs out and lands on `/login?deleted=true` with a completion notice. Contract docs now point to the mounted `src/routes/userPrivacy.js` route and warn that provider/sign-in-provider data is outside this route. Evidence: `frontend/src/lib/accountDeletion.js`; `frontend/src/pages/Account.jsx`; `frontend/src/pages/Login.jsx`; `test/accountDeletion.test.mjs`; security evidence `Direction/reviews/2026-07-04-phase2-9-account-delete-ui-security-privacy-evidence.md`; code review `Blueprints/audits/2026-07-04-phase2-9-account-delete-ui-code-review.md`; UI audit `Blueprints/audits/2026-07-04-phase2-9-account-delete-ui-ui-ux-audit.md`; screenshots `output/playwright/phase2-9-account-delete-ui/`; handoff `Blueprints/handoffs/2026-07-04-phase2-9-account-delete-ui.md`; focused 4/4; full `npm test` 403/403; build passed; audit 0; `git diff --check` clean. | Protected `/account` modal screenshots were captured with a throwaway browser-local session and stubbed local `/api` responses; public `/login?deleted=true` browser check passed at 390px. Existing Vite warnings remain: chunk size, `.env` `NODE_ENV=production`, duplicate `Header.jsx` `className`. | Legacy `src/omen_gdpr.js` still contains historical delete wording but is not the mounted `/api/user` route. Not deployed in this session. |

## Monthly review

On the first of each month, scan the previous month's rows. Look for:

- Same gate skipped repeatedly → fix gate, prompt, or skill
- Same Done doc applied without others that should have applied → fix the routing rule in `definition-of-done.md`
- New patterns of "why skipped" → consider new done-file or merge existing
| 2026-07-04 | Rate-limit scope fix (429 raw-JSON P1) + Standings off-season triage | feature | Scoped `src/server.js`'s general rate limiter to `/api/*`; live-verified 150-request bursts against static assets (150/150 200s) and `/api/dashboard/summary` (100 then 429s); full `npm test` 402/402; `npm audit --audit-level=moderate` 0; `git diff --check` clean. Standings off-season case triaged and confirmed (not fixed inline, routed as a combined backend request). Evidence: `Blueprints/handoffs/2026-07-04-ratelimit-scope-and-standings-offseason-triage.md`. | Page/Design gates N/A — no frontend UI changed. Security-done not run as a full cross-cutting gate since no auth/secrets/credentials were touched; rate-limiter functional-equivalence verified live instead. | Standings off-season fix itself remains open, filed to `frontend-to-backend.md`; whoever builds it should also update `nflSchedule.test.js`/`leagueStandingsRoute.test.js`. |
| 2026-07-04 | Canonical off-season signal for dashboard + league standings | feature | Added shared `isOffSeason()` in `src/services/nflSchedule.js`; `GET /api/dashboard/summary` now returns `tools.omen_of_the_week.status: "off_season"` with `available: false` for otherwise-ready users during off-season; `GET /api/league/standings` returns `200` with `standings: []` before provider calls during off-season. Evidence: `Blueprints/handoffs/2026-07-04-canonical-offseason-signal.md`; `Blueprints/audits/2026-07-04-canonical-offseason-signal-code-review.md`; RED focused tests failed on missing helper/status/empty envelope; GREEN focused tests 28/28; full `npm test` 407/407; `npm audit --audit-level=moderate` 0; frontend build clean with existing warnings; final `git diff --check` clean. | Page/Design gates N/A — no frontend UI changed. Recommendation Done N/A — this change suppresses a live recommendation path during off-season but does not create or alter recommendation copy, confidence, risk, reasoning, data-source labels, or Move History writes. Security Done N/A as a full gate — no auth, credential, data classification, consent, retention, telemetry, external sharing, SQL, secret, dependency, or logging boundary changed; provider credentials remain behind existing server-only adapters and the off-season standings path avoids provider calls. | Frontend still needs to render dashboard `off_season` before calling live Omen; branch is local only and not pushed, merged, or deployed. |
| 2026-07-04 | Phase 2.10 - Trade share card | feature, page, design, recommendation, security | Added the `/trade` Share result panel, public `/trade/share/:hash` card, server-side OG/Twitter meta injection, and `GET /api/trade/share/:hash/og.svg`. Public snapshot copy is recommendation-first and excludes connected-platform context, ESPN cookies, tokens, and private league data. Evidence: implementation `4db9db0`; `frontend/src/lib/tradeShare.js`; `frontend/src/pages/TradeShare.jsx`; `frontend/src/pages/TradeAnalyzer.jsx`; `src/services/tradeShareMeta.js`; `src/services/tradeShareOg.js`; route/test updates; security evidence `Direction/reviews/2026-07-04-phase2-10-trade-share-card-security-privacy-evidence.md`; audits `Blueprints/audits/2026-07-04-phase2-10-trade-share-card-code-review.md` and `Blueprints/audits/2026-07-04-phase2-10-trade-share-card-ui-ux-audit.md`; screenshots `output/playwright/phase2-10-trade-share-card/`; handoff `Blueprints/handoffs/2026-07-04-phase2-10-trade-share-card.md`; focused share tests 9/9; full `npm test` 414/414; frontend build passed; audit 0; `git diff --check` clean; browser QA passed desktop/mobile light/dark + mobile share flow. | Protected-route screenshot gap does not apply: `/trade` and `/trade/share/:hash` are public; browser QA used local Vite with mocked public API responses. Existing Vite warnings remain: chunk size, `.env` `NODE_ENV=production`, and duplicate `Header.jsx` `className`. | Not pushed, merged, deployed, or production-smoked in this session. `POST /api/trade/share` production create smoke should run after merge/deploy if write traffic is acceptable. |
| 2026-07-05 | Phase 2.18 - Waiver Wire route activation | feature, page, design; recommendation partial | Registered `/waiver` behind `ProtectedRoute`, added a `Waiver Wire` Tools-nav entry, removed the dead `ProGate`/`proRequired` Pro-upsell branch (unreachable while `OMEN_BILLING_ENABLED=false`), migrated `PositionBadge` to the shared `positionChipStyle()` helper, and swept every remaining `slate-*`/`amber-*`/`emerald-*` Tailwind literal onto design-system tokens. Evidence: `frontend/src/routes/index.jsx`; `frontend/src/components/layout/Header.jsx`; `frontend/src/pages/WaiverWire.jsx`; `Blueprints/specs/page-system.md`; handoff `Blueprints/handoffs/2026-07-05-phase2-18-waiver-wire-activation.md`; full `npm test` 414/414; frontend build clean with pre-existing warnings; `npm audit --audit-level=moderate` 0; `git diff --check` clean; grep-confirmed zero remaining color literals; unauthenticated browser check confirmed route redirect + nav auth-gating. | No authenticated light/dark screenshot — same sandbox limitation as Phase 1.5d/1.7/1.8/1.12, plus `preview_screenshot` timed out this session even on the public `/trade` page. Recommendation Done only partially satisfied: `/api/optimizer/waiver` still has no confidence/risk fields (pre-existing, out of scope for a route-activation phase — not silently accepted as compliant). | Signed-in mobile/desktop light+dark screenshot pass on `/waiver` once merged/deployed. Adding confidence/risk fields to `/api/optimizer/waiver` is a separate backend item if prioritized. |
| 2026-07-05 | Phase 1.15 - Post-deploy visual smoke on prod | release, page, design | Added the public-route logo smoke to `.github/workflows/deploy.yml`; aligned `/login` to the transparent Omen lockup; captured local light mobile screenshots for `/`, `/about`, and `/login`. Evidence: `Blueprints/handoffs/2026-07-05-phase1-15-post-deploy-visual-smoke.md`; audits `Blueprints/audits/2026-07-05-phase1-15-post-deploy-visual-smoke-code-review.md` and `Blueprints/audits/2026-07-05-phase1-15-post-deploy-visual-smoke-ui-ux-audit.md`; `output/playwright/phase1-15-post-deploy-visual-smoke/`; focused 5/5; full `npm test` 416/416; frontend build passed with existing warnings; client build passed; root audit 0; `git diff --check` clean. | Release deploy gates N/A: no deploy, workflow replay, restart, KVM mutation, secret access, package edit, Docker config change, or production config change happened. Page/Design dark-mode screenshots skipped for this narrow light-theme logo regression; existing transparent-lockup evidence already covered dark/light shared logo slots, and this pass captured the three public target routes in light mobile. | A future browser-capable CI smoke could replace the current curl/bundle-level workflow check if runner dependencies are approved. |
