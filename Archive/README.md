# Corvus Archive

This folder preserves superseded, parked, or historical Corvus material.

Archive is not active truth.

Historical files may mention old `ssffmvp` or nested `Corvus/` paths. Current active routing is:

```text
SLOPS/
  slops-saloon/
    corvus/
```

Do not inspect, move, stage, or edit secret-like files, `.env`, keys, cookies, production material, package files, SQL, Docker/deploy config, `.git`, `node_modules`, or app source from archive cleanup without explicit Justin approval.
