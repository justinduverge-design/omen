# Account Components

Tier 2 landing zone for Account-page UI that is not backend-owned.

Planned first use:
- Shared account panels that should not stay embedded in `frontend/src/pages/Account.jsx`.

Do not put secrets or Supabase migrations here.
